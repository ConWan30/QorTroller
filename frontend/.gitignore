# Build output
dist/
dist-ssr/

# Dependencies
node_modules/

# Local env — contains OPERATOR_API_KEY — never commit
.env.local
.env.*.local

# Editor artifacts
.DS_Store
*.local

# ZKBA compiled artifact output (Phase O3-ZKBA-TRACK1).  Output is
# deterministic from inputs; regenerable via scripts/vsd_ui_compiler.py
# + scripts/zkba_compile_gic_ledger.py (or future per-artifact builders).
# Only the .gitkeep directory marker is tracked.
src/artifacts/*/
!src/artifacts/.gitkeep
