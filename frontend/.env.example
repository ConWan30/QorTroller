# VAPI Bridge API Key — copy this file to .env.local and fill in the value
# The operator API key is the OPERATOR_API_KEY value from bridge/.env
# Never commit .env.local to git

VITE_VAPI_API_KEY=
VITE_VAPI_BRIDGE_URL=http://localhost:8080
