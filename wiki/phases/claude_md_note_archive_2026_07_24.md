# CLAUDE.md NOTE archive — 2026-07-24 (lossless)

**40 completed-arc NOTE entries moved verbatim out of `CLAUDE.md`.**
Nothing summarized, nothing rewritten — each block below is the exact original
line, byte-for-byte. Recover any single NOTE by copying its block back.

## Why

`CLAUDE.md` had grown to **225,351 chars** — 3.8x the repo's own
`target_chars=60_000` and 2.3x the `warn_chars=100_000` threshold that
`mythos_claude_md_curation` enforces (the variant was firing 17 findings:
1 oversize + 16 stale-NOTE). NOTE lines alone were **145,660 chars = 64.6%**
of the file, loaded into every session regardless of relevance.

This follows the repo's own established convention — the same lossless
"archive verbatim to `wiki/phases/`, leave a pointer NOTE" move used on
2026-05-18 (400k -> 139k) and 2026-05-22 (6 arcs + Phase O1-D).

Also aligned with current context-engineering guidance: project history and
session memory belong in auto-memory and archives, not in the always-loaded
context file. Most of these NOTEs already end with a `[[project_*]]` pointer
into `.claude/.../memory/` — the detail layer already exists.

## What stayed in CLAUDE.md

The **5 most-recent** NOTEs (current in-flight arcs, 2026-07-12 -> 07-17),
per the file's own stated "only 5-7 most-recent NOTEs" discipline:

- 🆔 ioID CONTROLLER DID REGISTERED LIVE 2026-07-17 (`91449f41`; arc `c0b61ed8`→`91449f41`)
- 🔗 POEP GP-IDENTITY
- ⚡ POEP L3 FIRE+IMU ADAPTER
- 🔄 POEP SINGLE-HID BRIDGE FIRE+IMU RING 2026-07-17 (`92f6e848`, branch `feat/l9-consistency-adversarial-harness`)
- 🛰️ A2A LOOP ENGINE + QORTROLLER PRODUCT + DEPIN NODE BORN & FIRST ANCHOR 2026-07-12/13 (arc merged to main via PR #92 + **PR #93 `4a80ae52`**)

## Recovering the full original

```bash
git show <commit-before-this-change>:CLAUDE.md
git log --grep='NOTE:' -- CLAUDE.md      # commit-by-commit narrative
```

## Index

 1. [🎮 POSP ARC U1/U2](#note-01)
 2. [🟢 GREEN-MAIN GATE RECONCILE + CCO FINISH + TRIO-RETINA THROUGH PHASE 3 2026-06-21 (`ad8bda6a`)](#note-02)
 3. [📦 DECON-2 STREAM 2 COMPLETE](#note-03)
 4. [🕹️ HWFL-1 CYCLE 17](#note-04)
 5. [📜 HWFL-1 CYCLE 16](#note-05)
 6. [🗂️ HWFL-1 CYCLE 15](#note-06)
 7. [🛰️ HWFL-1 CYCLE 14](#note-07)
 8. [🎯 HWFL-1 CYCLE 13](#note-08)
 9. [🔬 HWFL-1 CYCLE 12](#note-09)
10. [✅ HWFL-1 CYCLE 11](#note-10)
11. [📡 HWFL-1 CYCLE 10](#note-11)
12. [🧹 HWFL-1 CYCLE 9](#note-12)
13. [🧯 HWFL-1 CYCLE 8](#note-13)
14. [🔓 HWFL-1 CYCLE 7](#note-14)
15. [🏷️ HWFL-1 CYCLE 6](#note-15)
16. [📡 HWFL-1 CYCLE 5](#note-16)
17. [🔧 HWFL-1 CYCLE 4](#note-17)
18. [🌐 HWFL-1 CYCLE 3](#note-18)
19. [📊 HWFL-1 CYCLE 2](#note-19)
20. [🛰️ HWFL-1 CYCLE 1](#note-20)
21. [🧩 DECON-1 STREAM 2 PHASE 2.1](#note-21)
22. [🔬 DECON-1 STREAM 1 FINAL](#note-22)
23. [🎯 DECON-1 STREAM 4 FINAL](#note-23)
24. [🤖 DECON-1 STREAM 4 VERIFY PASS](#note-24)
25. [🗺️ DECON-1 STREAM 5](#note-25)
26. [🚨 DECON-1 STREAM 3](#note-26)
27. [📦 DECON-1 STREAM 2 PHASE 2.0.5](#note-27)
28. [🔍 DECON-1 STREAM 1](#note-28)
29. [🧱 W3BSTREAM RUST COMPUTE SANDBOX BUILT 2026-05-31 (`4ddc7917`)](#note-29)
30. [🛡️ DATA ECONOMY ARC 7 (Decoupled Cryptographic Sidecar Pointer + Post-Quantum Commitments) BUILT 2026-05-31 (`65ad5560` Thread-C offload → `4a570e6c` ](#note-30)
31. [🧮 ARC 6 ADDITIVE](#note-31)
32. [⏱️ DATA ECONOMY ARC 6 (PoSR](#note-32)
33. [🎞️ DATA ECONOMY ARC 5 (VHR](#note-33)
34. [🔐 PATH A ARC 1 COMPLETE](#note-34)
35. [🔑 GUARDIAN AUTONOMOUS KMS-HSM SIGNING (Tier-1) + ON-CHAIN ANCHOR (Tier-2) 2026-05-20/21](#note-35)
36. [🎮 L9_PRESENCE ARC (PoEP / L9-PoCP / BCC / GCAP) BUILT + ON-RIG VALIDATED 2026-05-21/22](#note-36)
37. [ARCHIVED 2026-05-22 (lossless)](#note-37)
38. [🚀 O3 CEREMONY FIRED LIVE ON IOTEX 2026-05-17](#note-38)
39. [ARCHIVED 2026-05-22 (lossless)](#note-39)
40. [OLDER NOTES ARCHIVED 2026-05-18](#note-40)

---

<a id="note-01"></a>
### 1. 🎮 POSP ARC U1/U2

NOTE: 🎮 POSP ARC U1/U2 — SYNCHRONIZED PRESENCE PROOF (QORTROLLER-POSP-v0 CANDIDATE) BUILT 2026-07-04 (`9c889f5d`/`df1f466f`/`e9bb31c0`, branch `feat/l9-consistency-adversarial-harness`) — **Three-surface synchronized presence proof: session_id join key + PoSP builder + daemon issuance + SDK reader.** **U1 (`9c889f5d`):** `session_id = SHA-256("{label}_{stamp}")` — join key across all three surfaces (KAS authorship / NQPV co-capture / archive manifest); `l9_presence/session_identity.py` (derive_session_id/session_display/parse_daemon_log_name); KAS record gains session_id in `to_dict()` ONLY (NOT body_dict/commitment preimage — byte-stable); daemon mints session_id at start, threads via PITL meta, writes `manifest.json` per-session in the ring archive (`retina_kf_archive/{label}_{stamp}/manifest.json`, schema `qortroller-session-archive-v1`); `dualshock_integration` + `novel_presence_fusion` passthrough; 7 session-identity tests green. **U2a (`df1f466f`):** `l9_presence/posp.py` — `build_posp()` produces `QORTROLLER-POSP-v0 CANDIDATE` records; three verdicts SYNCHRONIZED / PARTIAL_SURFACES / UNVERIFIABLE; REFERENCE-AND-BIND design (no new FROZEN-v1 family / domain-tag / commitment method — integrity derives entirely from the commitments it references: KAS commitment, PoAC hashes, archive SHA-256s); anti-assertion rail (session_id mismatch across surfaces → UNVERIFIABLE); `events_roots` dict carries `kas_session_root` and `retina_perception_root` as **named parallel roots** (§2.3 — structurally parallel by design, never conflated); `nqpv_cocapture_log` gains `session_id TEXT + session_display TEXT` columns via idempotent ALTERs (PoVCA migration pattern) + index on `(session_id, created_at DESC)`; `insert_nqpv_cocapture` + `get_nqpv_cocapture_rows` updated; `bridge/vapi_bridge/main.py` passes session_id from pitl_meta; 6/6 posp tests green. **U2b (`e9bb31c0`):** `_issue_posp()` helper in `scripts/retina_capture_daemon.py` fires after KAS issuance in `cmd_stop` (fail-open, non-fatal); queries `nqpv_cocapture_log` by session_id from bridge DB (direct sqlite3, fail-open — not the Store to avoid init side-effects); reads archive manifest.json; calls build_posp(); writes `audits/posp_record_{label}_{date}.json`; `retina_perception_root=None` (Phase 3c path is separate — §2.3 rail); `sdk/vapi_sdk.py` gains `VAPIPoSPRecord @dataclass(slots=True)` — `from_dict()`/`from_file()` readers, `is_synchronized()` predicate, `advisory=True` machine-readable flag, both named roots as flat fields; 6/6 U2b tests green including offline corpus_growth `PARTIAL_SURFACES` gate (KAS present, no NQPV fusion rows for pre-U2b session — honest). **ALL STAGED ITEMS COMMITTED (`7d036b2f` Fusion Arc Increment 1):** D-CG-1 OCR stall witness; C1 two-layer finding (`bridge/tests/test_c1_freeze_frame_transition.py`); D-PKG-1 v6 flip (`8d3482cc`); G1' full-archive precision gate (`docs/ocr-bootstrap-precision-2026-07-04.md`); D-CERT-5 unified presence-gameplay proof design (`docs/d-cert5-unified-presence-design-2026-07-04.md`). **U2c COMPLETE — Match 13 (2026-07-06, `5b607a0d`) — AUTHORED_SESSION authored=8, 524 crops, PoSP SYNCHRONIZED (kas_verified=True, fusion_rows=358, archive_verified=True); direct HDMI path (PS5→laptop) resolved WGC fps collapse. QORTROLLER-POSP-v0 CANDIDATE proven live across M11/M12/M13. C-3.2 arc CLOSED.** 0 IOTX; no FROZEN-v1 edit; no chain write; no Solidity edit; PV-CI 182 unchanged. **Architecture detail in** `[[project_retina_authorship_correlation_refuted]]`.

<a id="note-02"></a>
### 2. 🟢 GREEN-MAIN GATE RECONCILE + CCO FINISH + TRIO-RETINA THROUGH PHASE 3 2026-06-21 (`ad8bda6a`)

NOTE: 🟢 GREEN-MAIN GATE RECONCILE + CCO FINISH + TRIO-RETINA THROUGH PHASE 3 2026-06-21 (`ad8bda6a`) — **Closed the red-merge governance breakage and finished the autonomously-completable CCO debt; three PRs landed on `main` (#46 merged earlier, #47+#48 merged this session on green governance gates).** **#46** registered the unregistered crypto tags that PRs #29/#41/#42/#43 merged RED (`VAPI-RETINA-STATE-v1/v2`, `VAPI-RETINA-EVENT-LINE-v1`, `VAPI-COMPOSABLE-CLAIM-v1` → `_KNOWN_CAPABILITY_TAGS` in `mythos_variants.py`; candidate/NOT FROZEN-v1 per POSEIDON-BN254-AS reframe) + reconciled the `INV-W3S-006` allowlist digest via `--generate` refactor (operator-fired seal; the `--confirm-governance` variant stays operator-fired per `[[process_autonomous_governance_seal_boundary]]`). **PV-CI baseline is now 179** (176→179: retina Phase 2 added INV-W3S-006 + INV-RETINA-001/002; live gate verified `PASS — 179`). **#47** greened the CI asc surface: `npm ci scripts/w3bstream` installs the AssemblyScript toolchain (was "npx asc: could not determine executable") + the test writes `dist/package.json {"type":"module"}` so the asc-emitted ESM Poseidon loader runs on CI node 18/20 (a node-24 dev box auto-detects ESM, which masked it). **#47 also ships `docs/branch-protection-make-gates-blocking.md`** — the recommended fix for the recurring red-merge pattern (make PV-CI + Mythos *blocking* on `main`; DRAFT, operator-applied). **#48 — CCO finishing:** closed **F-CCO-001** (`device_registry.py` `_enrich_profile`/`get_tier_eligibility` matched `cp.usb_vid/usb_pid` vs non-existent `DeviceProfile.vid/.pid`; now `hid_vendor_id` + `usb_pid in hid_product_ids`; 52 CCO/CHIA tests pass) + landed the ex-PR#44 L6B `rumble_imu` applicability + PoEP `device_auth_note` honest-gap fields + marked **Phase C BUILT** in `CCO_POEP_FUSION_v4.md` (maturity matrix `PoEP device-auth generalization` DESIGN→BUILT — `ChallengeVerifier`: Edge `adaptive_force` + measured DualSense `rumble_imu`) + `docs/cco-phase-g-stage-a-capture-plan.md` runbook. **CCO×PoEP Fusion status: A→F shipped/wired** (CapabilityOracle, T0 Option-C, L6B T0, ChallengeVerifier, CCO→PoEP `presence.poep`, identity grid, off-chain composability). **Phase G measurement VERIFIED + operator-attested:** PREMIUM_EDGE `sony_dualshock_edge_v1` **N=210 VALIDATED**, MID_TIER `sony_dualsense_v1` **N=130 VALIDATED** (HUMAN N=50 FRR proxy 0.0), MINIMAL_PAD **DEFERRED** (no hardware); `CCO_PHASE_G_VALIDATED_TIERS=MID_TIER,PREMIUM_EDGE`. Remaining Phase G debt (gated, not auto-completable): MINIMAL_PAD capture, Edge reflex_verdict true-latency backfill, Empirical Unknown #1 adaptive-trigger separability — runbook in the Stage A plan. **Trio-Retina is BUILT (advisory, default-OFF) through Phase 3:** embedder/perception/`retina_state_commitment`/`retina_events_root` (Poseidon events_root off-chain ZK-prep)/`retina_pda_attestation` + W3bstream validation + DA bulk & witness sidecar (PRs #29/#37/#38/#40/#41/#45). Wired read-only via `session_adjudicator._enrich_retina_evidence`; `RETINA_PERCEPTION_ENABLED`/`RETINA_DA_WITNESS_ENABLED` default-OFF; **does NOT touch the 228B PoAC wire**. Pilot armed on the bridge (HTTP_PORT **8000**, operator endpoints under `/operator/...`). **Honest caveat — `main` CI matrix is still RED on PRE-EXISTING failures independent of #47/#48** (pytest runs before `npx hardhat compile` → `AgentRegistry ABI not found`; FSCA `CONTRADICTION_RULES==28` assertion; `get_pending_records` order-dependent mock = F-DECON2-5 flake). #47/#48 merged on **green governance gates (PV-CI/Mythos/Path Scope)** — the protocol-integrity bar; full-matrix-green is a separate tracked CI-repair (fix-next: move hardhat compile before pytest in `ci.yml`). `CHAIN_SUBMISSION_PAUSED=true` held; 0 IOTX; no FROZEN edit, no chain write, no Solidity edit, no firmware edit. **Detail in** `[[project_gate_reconcile_cco_finish_2026_06_21]]`.

<a id="note-03"></a>
### 3. 📦 DECON-2 STREAM 2 COMPLETE

NOTE: 📦 DECON-2 STREAM 2 COMPLETE — STORE + OPERATOR_API RESIDUE 21/21 APPLIED 2026-06-20 (`38592819`) — **Mechanical decomposition arc closed.** Store: `store/_core.py` (~8.2k lines, schema centralized) + 13 mixins (12 planned domains + `RetinaMixin`). Operator API: `operator_api/_app.py` thin shell (~800 lines) + 12 `register_*` domain modules (`health_gate` through `agent_misc` PR #25); `_app.py` retains only retina (5 routes), `/player/session-status`, and `/operator/poep-registry/{device_id}`. Queue `docs/_daemon_proposals/decon_residue_queue.json` status=complete, zero pending. Map `audits/decon-store-map.md` updated to COMPLETE. Pattern: register-function split (D-DECON-5), one PR per domain, zero call-site import churn. Open flake F-DECON2-5 (mcp_audit batch import-order) non-blocking. PV-CI 176/176 PASS at closure verify. 0 IOTX; no FROZEN edit, no chain write, no Solidity edit, no firmware edit.

<a id="note-04"></a>
### 4. 🕹️ HWFL-1 CYCLE 17

NOTE: 🕹️ HWFL-1 CYCLE 17 — SENSOR B NARRATIVE PASS #2 (S3/S4/S5/S7); PENDING-OPERATOR-NOTE DRAINED TO 0 FOR FIRST TIME 2026-06-13 (`3574e631`) — **First cycle where every Sensor B source carries narrative content** (PENDING-OPERATOR-NOTE 4→0 since Cycle 5). Second zero-code intel cycle (parallels Cycle 5); fetch-and-propose per D-HWFL-16 pattern (c) — Claude researches via WebSearch+WebFetch, drafts narratives, they land UNVERIFIED-EXTERNAL because they carry no `verified_by`/`sources`/`verified_date` (I am the fetcher, not operator-verifier). **Cumulative watch state** (`audits/hardware-watch-cycle-17-2026-06-13.md`, rendered from merged `ops_notes_cumulative_through_cycle17.json` = Cycle 5 S2/S6 + Cycle 17 S3/S4/S5/S7): **FRESH=1** (S1 IIP-64 PR via gh CLI) + **VERIFIED-EXTERNAL=1** (S2 ATECC608A, operator-verified Cycle 6) + **UNVERIFIED-EXTERNAL=5** (S3/S4/S5/S6/S7) + **PENDING=0**. **Intel highlights:** S3 K-Silver — vendor GUANGDONG K-SILVER makes BOTH a Hall (JH16) AND a TMR (JS16) in one form factor (joint manual), directly relevant to BOM C3/C4 same-family L/R discipline (one vendor lets you A/B Hall-vs-TMR without footprint change); GAP — no electrical spec obtainable (HTTP-only site, anti-bot manual). S4 MIDAS — **PROVENANCE GAP finding**, not intel: no brand-specific authoritative source exists for the BOM "MIDAS 5-pin HE" candidate; flagged as lower-confidence than K-Silver/GuliKit until operator re-identifies the vendor (may be a reseller label or internal codename). S5 Magneto/TMR — TMR physics (quantum tunneling, analog resistance output proportional to field, non-contact zero-drift, higher sensitivity than GMR/AMR/Hall); **dominant commercial TMR vendor is GuliKit, not "Magneto"** (better-provenanced); Hall/TMR NOT confirmed pin-compatible → reinforces same-family discipline. S7 competitive — aftermarket converged on Hall (commodity, ~$15/pair) + TMR (premium; GuliKit/AimControllers/Get A Grip) vs legacy potentiometer (what DualSense Edge ships from ALPS per Sensor Stack v2.1); **per-technology separability (Empirical Unknown #4 >20% rank-1) must be measured PER sensor physics, not assumed transferable** — load-bearing for the L4 stick-fingerprint discriminator. **F-CYCLE17-1 surfaced (validates the Cycle 14 prober end-to-end):** WebFetch force-upgrades HTTP→HTTPS and refused the HTTP-only K-Silver vendor site (ECONNREFUSED) even though the prober's plain-HTTP HEAD reached it (200). **The reachability prober is the honest oracle for HTTP-only vendor sites; WebFetch is not** — exactly the F-HWFL-5-1 class of failure the Cycle 14 prober was built to surface, now demonstrated on a live HTTP-only site. Reachability pre-check: `audits/url-reachability-cycle-17-2026-06-13.md` (K-Silver REACHABLE-to-HEAD, manuals.plus FORBIDDEN-403, all 3 TMR sources REACHABLE). **The 3 stick gates (C3/C4/C5) stay MEASUREMENT-PENDING on Stage A** — narrative intel is a prerequisite for selection, NOT the selection. **Sensor B module byte-identical** (no diff; 16/16 tests green) — pure data injection through the runner boundary, re-validating the Cycle 3 D-HWFL-10 pure-function-module choice (module absorbs operator/agent intel without code change). **Files (5 — all new artifacts, ZERO code change):** `audits/ops_notes_cycle17.json` (4 narratives), `audits/ops_notes_cumulative_through_cycle17.json` (merged 6-topic), `audits/hardware-watch-cycle-17-2026-06-13.md` (watch report), `audits/url-reachability-cycle-17-2026-06-13.md` (prober proof), `audits/cycle17_url_probe_input.json` (input record). **All sensors byte-identical** (A v0.1 / A v0.2 / C v0.1.3 / B v0.1.1). **Sensor C ledger byte-identical to Cycles 8-16.** **BOM v0.1 byte-identical since Cycle 7** (C3/C4/C5 rows unchanged — intel informs but does not yet promote them). **Stream-in-flight slot now FREE for Cycle 18.** **Architecture detail covered in** `[[project_hwfl1_cycle17_sensor_b_narrative_pass_2]]`. PV-CI 174/174 PASS unchanged; 0 IOTX; no FROZEN edit, no chain write, no Solidity edit, no firmware edit. Standing OPERATOR-ACTION box unchanged.

<a id="note-05"></a>
### 5. 📜 HWFL-1 CYCLE 16

NOTE: 📜 HWFL-1 CYCLE 16 — F-HWFL-5-2 CLOSE (PATH A SPEC NRND→FAMILY-LANGUAGE AMENDMENT) 2026-06-13 (`684fe1c4`) — **First operator-authorized canonical-spec amendment in the HWFL-1 loop** (F-HWFL-5-2 opened Cycle 5, sat 11 cycles as operator-fired-only, closed Cycle 16 after explicit operator GO). Closes the last clearly-specified agent-buildable HWFL-1 finding. **Driver:** `docs/path-a-manufacturing-spec.md` hard-named the **NRND** ATECC608A as the canonical reference target — partners reading the spec would design against a part Microchip itself marks Not Recommended for New Designs. The Sensor B S2 narrative (Cycle 5) established the successor chain 608A→608B→608C-TFLXTLS and the load-bearing caveat (polling-based timing forward-compat). **Amendment (doc-only, ZERO code change):** (§Status) Arc 2 integration target → ATECC608B/608C-class with NRND note on the 608A bring-up part; (§1) reference "(ATECC608A or equivalent)" → "(ATECC608B/608C-class CryptoAuthentication device or equivalent)" + sign-latency study reworded to family; (§2) NEW Family-Requirement paragraph (608B/608C-class family target, CryptoAuthLib-compatible, polling-based timing REQUIRED, explicit "608A is NRND, MUST NOT be specified for new designs"); table gains a **Lifecycle column** (608A=NRND bring-up-only / 608B=Active drop-in per AN2237 / 608C-TFLXTLS=Active TrustFLEX pre-provisioned + Rung 3 factory-provisioning convergence target); (§2 Mandatory chip properties) NEW property 5 = **polling-based command timing REQUIRED** (not fixed-delay/hardwired) — 608B/608C are command-level drop-ins for 608A per Microchip AN2237 but can differ in exact execution latency; polling is latency-tolerant + forward-compatible across the family; fixed-delay firmware tuned to one part breaks silently on a successor and is non-conformant; CryptoAuthLib polling mode satisfies by default. **The `atcab_*` CryptoAuthLib calls in §3 (manufacturing ceremony) are already family-generic and unchanged.** On-chain schema, cert format, and verification tooling untouched — hardware-selection-guidance amendment only. **No invariant pins this spec's content** (verified via grep against `vapi_invariant_gate.py`). **Files (1 — doc edit):** `docs/path-a-manufacturing-spec.md` (+34/-14). **All sensors + ledger + watch + BOM byte-identical.** **HWFL-1 agent-buildable surface now PLATEAUED:** remaining open findings are F-CYCLE9-1 (git-history rewrite — NEVER-without-explicit-request) and the hardware-gated Rung 2-4 gates (Stage A measurements, dev-kit fab, partner handoff). Future HWFL-1 cycles fire on operator-brought intel (Sensor B narrative pass #2: S3-S5 sticks + S7) or hardware events, not autonomous build. **Architecture detail covered in** `[[project_hwfl1_cycle16_path_a_spec_family_amendment]]`. PV-CI 174/174 PASS unchanged; 0 IOTX; no FROZEN edit, no chain write, no Solidity edit, no firmware edit. Standing OPERATOR-ACTION box unchanged.

<a id="note-06"></a>
### 6. 🗂️ HWFL-1 CYCLE 15

NOTE: 🗂️ HWFL-1 CYCLE 15 — F-CYCLE10-1 OPTION (b) CLOSE VIA CONTRACT-STATUS AUDITOR (58 ACTIVE / 3 SUPERSEDED / 5 DEPRECATED-INFERRED) 2026-06-13 (`90bccb8d`) — **Completes the F-CYCLE10-1 dual-option arc:** Cycle 11 closed Option (a) blunt (prose 49 → 66 total + anchor bump); Cycle 15 closes the deferred Option (b) active-vs-superseded classification. Standalone pure-function classifier `bridge/vapi_bridge/contract_status_audit.py` (~205 LOC) over `deployed-addresses.json`'s 66 addr-shaped non-meta keys + supersession meta-keys. **4 FROZEN classification rules:** (1) key contains `_superseded` substring → SUPERSEDED (explicit); (2) matching meta-key `_<name>_superseded_note` → SUPERSEDED (explicit); (3) sibling with higher version token (V2/V3/_v2…) exists → DEPRECATED-INFERRED (heuristic, not fact); (4) default → ACTIVE. **Real-data result** (`audits/contract-status-cycle-15-2026-06-13.md`): **58 ACTIVE / 3 SUPERSEDED / 5 DEPRECATED-INFERRED.** The 3 explicit SUPERSEDED: `VAPIProtocolLens_v1_superseded` (key suffix), `SeparationRatioRegistry` (meta-note marks it as replaced 2026-05-24 by `0xc88eDc0a…`), `SeparationRatioRegistry_superseded` (key suffix). The 5 DEPRECATED-INFERRED: `TournamentGate`→V2→V3 (only V3 ACTIVE), `TournamentGateV2`, `PITLSessionRegistry`→V2, `PitlSessionProofVerifier`→V2, `VAPIProtocolLens`→_v2. **Honest read:** the currently-canonical contract count is ~**58 ACTIVE**; 66 total includes 3 explicitly-superseded + 5 likely-deprecated-by-versioning. The original README/CLAUDE.md "49" was never right for *either* semantic — it was simply prose-stale by ~9 cycles of deploys. **Anchor decision:** Sensor A v0.2 CONTRACTS anchor STAYS at 66 (total-deployed is the correct anchor semantic; supersession is a separate classification overlay, not a count change — a superseded contract is still deployed and still callable on-chain). **Honesty rail baked into the artifact:** DEPRECATED-INFERRED is a versioning heuristic, NOT a fact — the classifier never reads chain state or contract source, purely metadata-driven; operator confirms by checking actual wiring (`bridge/.env`, `chain.py` imports, SDK consumers); a v0.2 could cross-check those for higher confidence. **Version-token parser** handles bare names (treated as version 1), `VN` suffixes, and `_vN` suffixes; returns the immediately-next-higher sibling so a V2 in a 3-version family is correctly flagged superseded-by-V3 (the original naïve string-append logic missed this — caught + fixed during the test pass, a P-check catching an implementation bug). **Tests:** 10/10 PASS in 0.25s. T1 explicit suffix / T2 meta-note marker / T3 three-version family chain / T4 underscore-version / T5 meta-keys-not-counted / T6 non-addr-skipped / T7 default-active / T8 count-consistency / T9 markdown-render-with-honesty-rail / T10 pipe-escape. **Files (4 — all new):** `bridge/vapi_bridge/contract_status_audit.py` NEW, `scripts/run_contract_status_audit.py` NEW (runner), `bridge/tests/test_contract_status_audit.py` NEW (10 tests), `audits/contract-status-cycle-15-2026-06-13.md` NEW (proof). **All sensors byte-identical.** **Sensor C ledger + Sensor B watch distributions byte-identical to Cycles 8-14.** **BOM v0.1 byte-identical since Cycle 7.** **Stream-in-flight slot now FREE for Cycle 16.** **Architecture detail covered in** `[[project_hwfl1_cycle15_contract_status_audit]]`. PV-CI 174/174 PASS unchanged; 0 IOTX; no FROZEN edit, no chain write, no Solidity edit, no firmware edit. Standing OPERATOR-ACTION box unchanged.

<a id="note-07"></a>
### 7. 🛰️ HWFL-1 CYCLE 14

NOTE: 🛰️ HWFL-1 CYCLE 14 — F-HWFL-5-1 CLOSE VIA STANDALONE MULTI-SOURCE URL REACHABILITY PROBER 2026-06-12 (`4509db97`) — **First cycle to close a Cycle-5-era finding** (F-HWFL-5-1 opened 2026-06-10, closed 2026-06-12; the lifecycle Sensor B / Sensor C ledger findings have been closed within 1-2 cycles, but F-HWFL-5-1 sat 9 cycles waiting for an architectural call). **Closure interpretation (D-HWFL-35):** Cycle 5 framed F-HWFL-5-1 as "Sensor B v0.2 needs multi-source fallback OR mandate workflow (a)/(b)" — the original ask was ambiguous between "make Sensor B auto-extract summary from arbitrary HTML" (LLM-required, scope creep) and "give operator reachability data to route around dead vendor pages." Cycle 14 picks the second interpretation as the smallest honest closure: ship a standalone pure-function URL reachability prober + runner, leave Sensor B v0.1.1 byte-identical, defer watch-render-path integration to v0.2.1 future-cycle decision. **New module `bridge/vapi_bridge/multi_source_prober.py` (~145 LOC):** `ReachState` enum {REACHABLE, REDIRECTED, FORBIDDEN, NOT-FOUND, SERVER-ERROR, TIMEOUT, NETWORK-ERROR, UNREACHABLE} — **8-state taxonomy** that distinguishes anti-bot 403s (operator can still reach via browser) from genuine 404s, and timeouts from DNS failures. `ProbeResult` boundary dataclass (url + status int + error str + elapsed_ms) — fetcher returns it, module classifies. `ReachabilityLine` + `ReachabilityReport` + `_classify()` pure function + `probe()` entry point + `to_markdown()` renderer with HTML+pipe escape on URL + evidence + label per Sensor B Cycle 3 discipline. **Runner `scripts/probe_vendor_urls.py`:** urllib HEAD requests only (no body parsing — avoids the prompt-injection surface entirely); per-URL elapsed_ms; configurable User-Agent + timeout; catches every exception class (HTTPError → status code preserved, URLError → TIMEOUT vs NETWORK-ERROR classification based on reason text, generic Exception → NETWORK-ERROR with repr). Supports two input modes: positional URLs or `--urls-file` JSON (list-shape or label→URLs dict-shape with `_*` keys reserved for meta). **Empirical finding from Cycle 5 URL replay** (`audits/url-reachability-cycle-14-2026-06-12.md`): Microchip atecc608a/608b/608c product pages **REACHABLE (200)** — the Cycle 5 4/4 403s were WebFetch-tool-UA-specific, NOT page-dead. The prober's clean-UA HEAD requests sail through. Wikipedia `Atmel_CryptoAuthentication` is genuinely 404 (wrong page name; operator should consult Microchip docs directly). Espressif ESP32 pages REACHABLE. IIP-64 PR #72 REACHABLE. **This means F-HWFL-5-1's original 4/4-failure narrative was partly a UA-routing problem (vendor anti-bot triggered on WebFetch tool's UA fingerprint) and partly a wrong-URL problem (Wikipedia page-name miss), NOT a fundamental vendor-page-death** — a more useful framing than the Cycle 5 NOTE suggested. The prober makes this distinction mechanical for every future vendor-catalog research pass. **Tests:** 10/10 PASS in 0.25s. T1-T6 cover all 8 ReachStates individually; **T7 specifically replays the Cycle 5 4/4-failure pattern** with mocked responses (Microchip 403 / Wikipedia 404 / Mouser timeout / DigiKey 404) and asserts the classification distinguishes each correctly; T8 fetcher-misbehavior fail-open rail (status=None + error=None → UNREACHABLE, never spurious REACHABLE); T9 HTML+pipe escape on URL + evidence + label adversarial input; T10 distribution-summary rendering. **D-HWFL-35 = standalone companion module, NOT Sensor B v0.2 amendment.** Reasoning: integrating reachability inline into Sensor B's WatchLine state machine would expand the module's responsibilities AND couple it to a particular operator workflow (do operators want reachability inline next to every MANUAL_NARRATIVE entry, or as a separate pre-research artifact?). Standalone ships immediately useful + leaves the integration question open + Sensor B v0.1.1 stays byte-identical (lower change-risk per the loop's no-regression discipline). **Files (5 — all new, ZERO Sensor B edit):** `bridge/vapi_bridge/multi_source_prober.py` NEW (module), `scripts/probe_vendor_urls.py` NEW (runner), `bridge/tests/test_multi_source_prober.py` NEW (10 tests), `audits/url-reachability-cycle-14-2026-06-12.md` NEW (proof), `audits/cycle14_url_probe_input.json` NEW (input record for reproducibility). **All other sensors byte-identical:** Sensor A v0.1 (Mythos #16) byte-identical since Cycle 1; Sensor A v0.2 byte-identical since Cycle 13; Sensor C v0.1.3 byte-identical since Cycle 8; Sensor B v0.1.1 byte-identical since Cycle 6. **Sensor C ledger byte-identical to Cycles 8-13** (4 LIVE / 1 LIVE-FRAGILE / 1 LIVE-PARTIAL / 3 HW / 12 DORMANT / 1 BLOCKED-EXT). **Sensor B watch byte-identical to Cycle 6**. **BOM v0.1 byte-identical since Cycle 7**. **Stream-in-flight slot now FREE for Cycle 15.** **Architecture detail covered in** `[[project_hwfl1_cycle14_multi_source_prober]]`. PV-CI 174/174 PASS unchanged; 0 IOTX; no FROZEN edit, no chain write, no Solidity edit, no firmware edit. Standing OPERATOR-ACTION box unchanged.

<a id="note-08"></a>
### 8. 🎯 HWFL-1 CYCLE 13

NOTE: 🎯 HWFL-1 CYCLE 13 — F-CYCLE12-1 CLOSED VIA D-HWFL-34 SCHEMA-HONESTY RENAME (`hardhat` → `hardhat_regex_scan`); FIRST ALL-CLEAN PROOF ARTIFACT IN HWFL-1 HISTORY 2026-06-12 (`3a6c05a1`) — **First cycle to choose a third path over a binary fork** (Cycle 12 surfaced two paths: blunt-bump-to-proxy OR full-fetcher-upgrade; Cycle 13 picked Option (a-prime) = schema-level honesty, which is neither). **D-HWFL-34 reasoning:** bumping `hardhat=779` would commit CLAUDE.md to a value we don't fully trust (regex over-counts skipped tests, nested .skip describes, commented-out lines that still match the regex). Improving the fetcher to authoritative `npx hardhat test` invokes Windows hardhat subprocess friction Cycle 10 D-HWFL-32 explicitly rejected (the same friction the test-suite probe was designed to dodge). The schema rename costs ~5 LOC + ~5 test-fixture renames + a single CLAUDE.md anchor edit AND produces a permanently more honest schema. **Schema is now extensible:** future cycles that need authoritative hardhat count add a SEPARATE `hardhat_authoritative` key alongside `hardhat_regex_scan`, rather than overloading one key with values from two precision regimes. Precision claims live in the key name, not in footnotes or hopes. **Same-commit data reconciliation per D-HWFL-33 invariant:** bridge 4330 → **4712** (+382, authoritative pytest --collect-only) / sdk 604 → **608** (+4, authoritative pytest --collect-only) / hardhat_regex_scan 674 → **779** (+105, regex-scan proxy) / as_of 2026-06-10 → 2026-06-12. **Module surface:** `_TEST_SUITES = ("bridge", "sdk", "hardhat_regex_scan")` (Cycle 12 was `("bridge", "sdk", "hardhat")`); 8-line block comment explains the rename rationale inline so future readers see the schema-honesty rail at the verifier site, not just in a NOTE. **Test diff:** 6 `test_counts` fixture renames + 2 inline anchor renames; all 12 existing tests pass byte-identical behavior (the verifier logic is byte-identical; only the iterated key name changed). **Runner diff:** fetcher return dict key renamed from `hardhat` to `hardhat_regex_scan` to match module. **Proof artifact `audits/live-state-drift-cycle-13-2026-06-12.md`:** Distribution **ALIGNED=3 / DRIFTED=0 / UNVERIFIABLE=0** — **first all-clean proof artifact in HWFL-1 history**. P-WALLET ALIGNED (32.078372 IOTX exact, no movement since Cycle 4). P-CONTRACT ALIGNED (66 = 66, byte-identical to Cycle 11). P-TESTS ALIGNED (all 3 suites exact match against the just-bumped anchor). **Cycle 10-13 micro-arc complete:** Sensor A v0.2 has now demonstrated all four operational modes on a single telemetry surface — Cycle 10 build+surface (F-CYCLE10-1), Cycle 11 close-via-reconciliation, Cycle 12 surface-only (F-CYCLE12-1), Cycle 13 close-via-schema-rename. The original Cycle 4 F-HWFL-4-1 driver (manual wallet-discipline-was-brittle) is now fully discharged at the protocol level — drift on any of the 3 anchored claims will be mechanically caught + mechanically resolvable through one of the four demonstrated cycle shapes. **Files (5 — 1 new, 4 edits):** `bridge/vapi_bridge/sensor_a_live_drift.py` EDIT (suite tuple rename + 8-line schema-honesty comment), `scripts/run_sensor_a_live.py` EDIT (fetcher return key rename), `bridge/tests/test_sensor_a_live_drift.py` EDIT (6 fixture renames + 2 anchor renames), `CLAUDE.md` EDIT (anchor rename + bump to live values), `audits/live-state-drift-cycle-13-2026-06-12.md` NEW (proof artifact). **Tests:** 12/12 PASS in 0.24s (faster than Cycle 10's 0.52s — module surface unchanged, no new verifier branches). **All other sensors byte-identical:** Sensor A v0.1 (Mythos #16) byte-identical since Cycle 1; Sensor C v0.1.3 byte-identical since Cycle 8; Sensor B v0.1.1 byte-identical since Cycle 6. **Sensor C ledger byte-identical to Cycles 8-12** (4 LIVE / 1 LIVE-FRAGILE / 1 LIVE-PARTIAL / 3 HW / 12 DORMANT / 1 BLOCKED-EXT). **Sensor B watch byte-identical to Cycle 6**. **BOM v0.1 byte-identical since Cycle 7**. **Stream-in-flight slot now FREE for Cycle 14.** **Pattern signal — Sensor A v0.2 reaching steady-state:** 4 cycles (10-13) to fully build + validate + drive to all-aligned. Next sensor-extension cycle is operator-fired (Sensor A v0.3 hardhat_authoritative would be one candidate; Sensor B v0.2 multi-source fallback is the higher-leverage one for vendor-catalog narrative pass #2). **Architecture detail covered in** `[[project_hwfl1_cycle13_f_cycle12_1_schema_rename_close]]`. PV-CI 174/174 PASS unchanged; 0 IOTX; no FROZEN edit, no chain write, no Solidity edit, no firmware edit. Standing OPERATOR-ACTION box unchanged (OA-1 fragility-only per Cycle 9; OA-4 priority elevated; OA-2/3 unchanged).

<a id="note-09"></a>
### 9. 🔬 HWFL-1 CYCLE 12

NOTE: 🔬 HWFL-1 CYCLE 12 — FIRST FULL-RUNNER PASS SURFACES F-CYCLE12-1 (P-TESTS DRIFTED) + HARDHAT REGEX-SCAN PRECISION CAVEAT 2026-06-12 (`446ab6ba`) — **First cycle where the same sensor surfaces a NEW drift finding on a different probe** (Cycle 10 = P-CONTRACT; Cycle 12 = P-TESTS), validating that Sensor A v0.2 catches drift not just once-by-luck but mechanically across CLAUDE.md's tracked-claim surface. Also **first cycle to produce UNVERIFIABLE=0** in the proof artifact (zero unknowns; full visibility into live-vs-claim picture). **F-CYCLE12-1 OPEN:** P-TESTS DRIFTED with bridge:**+382** (claim 4330 → live 4712) sdk:**+4** (604 → 608) hardhat:**+105** (674 → 779). pytest --collect-only path (bridge + sdk) is **authoritative**; hardhat 779 comes from runner's static `it()|test()` regex scan over `contracts/test/*.{js,ts}` per Cycle 10 D-HWFL-32 design choice (hardhat full-run rejected as too slow + Windows-friction-prone). **Hardhat regex-scan precision caveat:** the +105 hardhat delta is "regex-scan delta," NOT necessarily "actual `npx hardhat test` delta" — the regex matches `it()` and `test()` invocations across all test files but may over-count (skipped tests, commented-out tests, nested describe blocks where the parser counts the inner it() but hardhat's runner doesn't if the parent describe is `.skip`'d). The bridge:+382 / sdk:+4 deltas are mechanically authoritative; the hardhat:+105 is approximate. **Critical scope-discipline rail:** Cycle 12 deliberately does NOT bump the anchor — bumping `count=779` would itself be a finding (committing to a value we don't fully trust). Anchor reconciliation deferred to Cycle 13 with two paths on the table: (a) blunt anchor bump to live regex-scan values + document the "proxy" caveat inline in the anchor body, OR (b) improve the hardhat fetcher to use authoritative count (e.g. `npx mocha --dry-run` via hardhat-vm) THEN bump anchor. Operator picks at Cycle 13 entry. **Same shape as F-CYCLE10-1:** CLAUDE.md prose carried stale numbers, sensor caught it on first honest pass. The bridge=4330 claim in CLAUDE.md dates from approximately the Phase 240 baseline (2026-05-31, `5a685b9d`); since then 382 bridge tests have been added across the Cycle 1-11 ship arc itself (12 from Sensor A v0.2 in Cycle 10 alone, plus the underlying bridge growth from other phases). The sensor is now retroactively documenting the test-count growth that the prose forgot to track. **Proof artifact `audits/live-state-drift-cycle-12-2026-06-12.md`:** Distribution **ALIGNED=2 / DRIFTED=1 / UNVERIFIABLE=0** — first time the runner has zero UNVERIFIABLE in any pass. P-WALLET ALIGNED (32.078372 exact, no movement since Cycle 4). P-CONTRACT ALIGNED (66 = 66, byte-identical to Cycle 11 post-reconciliation). P-TESTS DRIFTED (deltas as above). **Files (1 — proof artifact only, ZERO code change):** `audits/live-state-drift-cycle-12-2026-06-12.md` NEW (cycle proof). **No CLAUDE.md edit this cycle** (scope discipline — anchor reconciliation deferred). **All sensor modules byte-identical:** Sensor A v0.1 (Mythos #16), Sensor A v0.2, Sensor C v0.1.3, Sensor B v0.1.1. Runner (`scripts/run_sensor_a_live.py`) byte-identical from Cycle 10. **Pattern signal — sensor effectiveness empirically demonstrated:** Sensor A v0.2 has now surfaced **two** real drift findings on **two** consecutive honest passes (F-CYCLE10-1 on contracts, F-CYCLE12-1 on tests). The sensor is catching drift faster than session-end discipline could by hand — the original F-HWFL-4-1 driver from Cycle 4 has been institutionally honored. **Sensor C ledger byte-identical to Cycles 8/9/10/11** (4 LIVE / 1 LIVE-FRAGILE / 1 LIVE-PARTIAL / 3 HW / 12 DORMANT / 1 BLOCKED-EXT). **Sensor B watch byte-identical to Cycle 6**. **BOM v0.1 byte-identical since Cycle 7**. **Stream-in-flight slot now FREE for Cycle 13.** **New cycle shape this loop: surface-only cycle** (no code change, no data reconciliation, just full-runner pass that produces a new authoritative proof artifact and opens a finding for future-cycle resolution). Parallels Cycle 5's zero-code-change shape but where Cycle 5 was operator-paste-intel-injection, Cycle 12 is sensor-runner-precision-pass. **Architecture detail covered in** `[[project_hwfl1_cycle12_p_tests_drift_surfaced]]`. PV-CI 174/174 PASS unchanged; 0 IOTX; no FROZEN edit, no chain write, no Solidity edit, no firmware edit. Standing OPERATOR-ACTION box unchanged (OA-1 fragility-only per Cycle 9; OA-4 priority elevated; OA-2/3 unchanged).

<a id="note-10"></a>
### 10. ✅ HWFL-1 CYCLE 11

NOTE: ✅ HWFL-1 CYCLE 11 — F-CYCLE10-1 CLOSED (CLAUDE.md 49→66 RECONCILIATION; FIRST END-TO-END SENSOR-A v0.2 LOOP VALIDATION) 2026-06-12 (`997ce8bf`) — **First cycle to close an agent-surfaced finding in the cycle immediately following its surfacing** (Cycle 10 surfaced F-CYCLE10-1, Cycle 11 closes it — parallels Cycle 5→6 closing F-HWFL-5-3 but with Sensor A v0.2 instead of Sensor B). **Validates the Sensor A v0.2 end-to-end loop:** Cycle 10 shipped the sensor + surfaced DRIFTED on first pass; Cycle 11 reconciles CLAUDE.md prose AND bumps the matching anchor in the SAME commit per the D-HWFL-33 invariant; re-run yields P-CONTRACT ALIGNED. The sensor pattern works as designed — drift is mechanically caught, mechanically resolved, mechanically proven-aligned. **Option (a) blunt reconciliation per cycle-opening operator decision:** prose `"49 contracts ALL LIVE"` → `"66 contracts deployed (some superseded — see deployed-addresses.json supersession metadata e.g. _vapiprotocollens_v1_superseded_note, _separationratioregistry_superseded_note; active-vs-superseded audit deferred per F-CYCLE10-1 Option (a) close)"`. Honest about scope: the count is correct (66 addr-shaped non-meta keys), the active-vs-superseded classification is explicitly deferred (Option (b)) to a future cycle if/when reviewer pressure or partner-handoff needs the precision. The deferred-audit anchor is the supersession metadata keys themselves (`_vapiprotocollens_v1_superseded_note` / `_separationratioregistry_superseded_note` exist in the file already) — future audit cycle can iterate over them mechanically. **Wallet anchor as_of bumped 2026-06-10 → 2026-06-12** after live `eth_getBalance` re-verify returned 32.078372 IOTX (byte-identical to Cycle 4 measurement — no movement). **Bumping as_of without value change is honest re-verification, not drift** — establishes the discipline: a session that intends to update CLAUDE.md prose pertaining to a sensor-tracked claim should re-verify live + bump as_of; sessions that don't touch the claim leave the as_of alone (the sensor's job is to catch staleness; as_of bumps are operator/cycle-fired). **P-TESTS UNVERIFIABLE this pass unchanged from Cycle 10** (`--skip-tests` for ship velocity; runner supports both modes; full test-count probe deferred to a future cycle that explicitly takes on the Windows pytest subprocess friction). **Proof artifact `audits/live-state-drift-cycle-11-2026-06-12.md`:** Distribution ALIGNED=2 / DRIFTED=0 / UNVERIFIABLE=1. P-WALLET ALIGNED (32.078372 exact, |delta|=0.000000 IOTX). P-CONTRACT ALIGNED (live=66 vs claim=66, exact match). P-TESTS UNVERIFIABLE (skipped via --skip-tests). Side-by-side with Cycle 10's artifact, the DRIFTED→ALIGNED transition on P-CONTRACT is the empirical proof of the reconciliation. **Files (2 — 1 edit, 1 new artifact):** `CLAUDE.md` EDIT (prose 49→66 + both anchor as_of dates + contract count + closing-NOTE pointer), `audits/live-state-drift-cycle-11-2026-06-12.md` NEW (proof artifact). **No new tests this cycle** — Sensor A v0.2 module is byte-identical (the data it consumed got reconciled, the verifier was already correct). Cycle 10's 12-test rail covers the regression posture. **F-CYCLE10-1 status: CLOSED.** Now-stable open findings carried into Cycle 12: F-HWFL-5-1 (Sensor B v0.2 multi-source WebFetch fallback), F-HWFL-5-2 (Path A spec amendment — operator-fired), F-CYCLE9-1 (git-history rewrite — operator-decision, defer-shaped). **All sensors byte-identical:** Sensor A v0.1 (Mythos #16) / Sensor A v0.2 / Sensor C v0.1.3 / Sensor B v0.1.1. **Sensor C ledger byte-identical to Cycles 8/9/10** (4 LIVE / 1 LIVE-FRAGILE / 1 LIVE-PARTIAL / 3 HW / 12 DORMANT / 1 BLOCKED-EXT). **Sensor B watch byte-identical to Cycle 6**. **BOM v0.1 byte-identical since Cycle 7**. **Stream-in-flight slot now FREE for Cycle 12.** **Cycle 11 establishes a new shape: finding-closure cycle (reconcile data, sensor module untouched, proof = before/after artifact pair).** **Architecture detail covered in** `[[project_hwfl1_cycle11_f_cycle10_1_close]]`. PV-CI 174/174 PASS unchanged; 0 IOTX; no FROZEN edit, no chain write, no Solidity edit, no firmware edit. Standing OPERATOR-ACTION box unchanged (OA-1 fragility-only per Cycle 9; OA-4 priority remains elevated; OA-2/3 unchanged).

<a id="note-11"></a>
### 11. 📡 HWFL-1 CYCLE 10

NOTE: 📡 HWFL-1 CYCLE 10 — SENSOR A v0.2 LIVE-STATE DRIFT DETECTION + F-CYCLE10-1 SURFACED ON FIRST PASS 2026-06-12 (`70b47ab9`) — **First cycle where a newly-built sensor surfaces a real DRIFTED finding on its first run** (vs Cycles 1/2/3 sensors that shipped clean baselines). Closes the F-HWFL-4-1 driver from Cycle 4 — Sensor A v0.2 mechanically catches drift between CLAUDE.md canonical claims and live runtime state, same discipline as Sensor A v0.1 catches spec/impl drift. **Standalone module per D-HWFL-31** (Sensor C precedent / D-HWFL-7): `bridge/vapi_bridge/sensor_a_live_drift.py` (~320 LOC: `DriftState` enum {ALIGNED, DRIFTED, UNVERIFIABLE} / `LiveFetchResult` boundary dataclass / `DriftLine` + `DriftReport` / `parse_anchors()` regex parser / 3 verifier functions / `assemble_drift_report()` orchestrator / `to_markdown()` HTML+pipe-escaping renderer). Sensor A v0.1 (Mythos variant #16 `mythos_path_a_spec_impl_parity`) stays byte-identical — v0.2 is a different telemetry shape, different chassis, same architectural discipline (pure-function module + injected fetcher; network/subprocess lives in `scripts/run_sensor_a_live.py`). **Three probes per D-HWFL-32:** P-WALLET (`eth_getBalance(0x0Cf36dB5…)` on babel-api.testnet.iotex.io with `User-Agent` header per Cycle 4 precedent vs `<!-- SENSOR-A-LIVE:WALLET balance_iotx=… as_of=… -->`); P-CONTRACT (addr-shaped non-meta keys in `contracts/deployed-addresses.json` — honest count strips `_note`/`_phaseNNN_status`/etc. bookkeeping vs `<!-- SENSOR-A-LIVE:CONTRACTS count=… -->`); P-TESTS (`pytest --collect-only -q` for bridge/sdk + `it()|test()` regex scan over `contracts/test/*.{js,ts}` as static lower-bound proxy vs `<!-- SENSOR-A-LIVE:TESTS bridge=… sdk=… hardhat=… -->`; full hardhat run was rejected as too slow + Windows-friction-prone). **HTML-comment anchor strategy per D-HWFL-33** (recommended; regex-on-natural-prose rejected as brittle): 3 anchors added to CLAUDE.md immediately after wallet line + a NOTE comment establishing the invariant — future cycles updating prose MUST update the matching anchor in the same commit or the sensor surfaces DRIFTED. Self-documenting + cycle-friendly + parse-robust + the anchor IS the canonical claim (not the prose around it). **Tolerance per probe** encoded in verifier (not in fetcher): P-WALLET tolerates 0.5 IOTX sub-deploy noise (gas-cost-bounded; bigger deltas like faucet refills are real drift); P-CONTRACT + P-TESTS demand exact match (any delta = real drift). **Fail-open posture parallel to Sensor B/C:** missing anchor → UNVERIFIABLE with `"not found"` evidence; fetch error → UNVERIFIABLE with explicit error string; malformed anchor body (float/int parse fail) → UNVERIFIABLE with parseable-token diagnostic. T12 explicitly proves the "never spurious DRIFTED" rail (md_no_anchors + fetch_no_live yields 3× UNVERIFIABLE, 0× DRIFTED). **HTML/pipe-escape on every external field** (anchor body + error string) — adversarial anchor injection (e.g. `|` in `as_of=2026|06|10`) is mechanically defanged before rendering, same discipline as Sensor B v0.1 Cycle 3 ship. **First-pass proof artifact `audits/live-state-drift-cycle-10-2026-06-12.md`:** P-WALLET ALIGNED (32.078372 IOTX exact match — wallet has NOT moved since Cycle 4's `eth_getBalance` measurement, confirming Cycle 4 wallet line is still current); **P-CONTRACT DRIFTED** live=66 addr-shaped non-meta keys vs claim=49 in CLAUDE.md prose, delta=**+17 contracts** — first agent-surfaced F-CYCLE10-1 finding; the "49 contracts ALL LIVE" line is stale by ~17 deploys spanning Path A Arc 1 (VMDR + LensV2), Arc 2 (buyer-verifier), Arc 6 (TBR), Phase 238 Step H, Phase 237 ZK-SEPPROOF verifiers, Curator NFT, and others not enumerated in the prose; P-TESTS UNVERIFIABLE this pass (runner invoked with `--skip-tests` for ship velocity; runner supports both modes — a future cycle re-runs without skip for full P-TESTS evidence). **F-CYCLE10-1 OPEN** — Sensor A v0.2 shipping does NOT reconcile the CLAUDE.md prose to live state per scope discipline; reconciliation is the next operator decision and forks into: (a) blunt update "49→66" + bump the anchor in the same commit (simple, mechanical), OR (b) audit which of the 66 are genuinely deployed-and-still-active vs superseded (the SeparationRatioRegistry 2026-05-24 reconciliation pattern) — would require defining what "active" means for replaced contracts that are still callable. Cycle 10 deliberately surfaces the finding as evidence the sensor works; resolving it is a future-cycle scope decision. **Tests:** 12/12 PASS in 0.52s. T1 anchor extraction / T2 all-aligned happy / T3 within-tolerance aligned / T4 beyond-tolerance drifted (5 IOTX delta, F-HWFL-4-1-class) / T5 contract off-by-one drifted / T6 test-suite drift any-suite-off triggers / T7 missing-anchor → UNVERIFIABLE / T8 fetch-error → UNVERIFIABLE / T9 malformed-anchor → UNVERIFIABLE / T10 adversarial HTML+pipe escape rail / T11 distribution summary rendering / T12 fail-open never-spurious-DRIFTED rail. **Files (5 — 4 new, 1 edit):** `bridge/vapi_bridge/sensor_a_live_drift.py` NEW (~320 LOC pure module), `scripts/run_sensor_a_live.py` NEW (urllib stdlib only for eth_getBalance + subprocess pytest + static regex scan for hardhat; 30-second urllib timeout + graceful exception → fetch_error string), `bridge/tests/test_sensor_a_live_drift.py` NEW (12 tests), `audits/live-state-drift-cycle-10-2026-06-12.md` NEW (first-pass proof artifact), `CLAUDE.md` EDIT (+5 lines: 3 anchors + 2-line invariant NOTE comment). **Decisions resolved:** D-HWFL-31 = standalone module (different telemetry shape from Mythos #16 / Sensor C precedent); D-HWFL-32 = 3 probes this cycle (wallet + contract + tests); D-HWFL-33 = HTML-comment anchors over regex-on-prose. **Cumulative sensor state after Cycle 10:** Sensor A v0.1 (static spec/impl parity, Mythos #16) byte-identical since Cycle 1 / Sensor A v0.2 (live-state drift, standalone) NEW; Sensor C v0.1.3 byte-identical since Cycle 8; Sensor B v0.1.1 byte-identical since Cycle 6; BOM v0.1 byte-identical since Cycle 7. **Sensor C ledger byte-identical to Cycles 8/9** (4 LIVE / 1 LIVE-FRAGILE / 1 LIVE-PARTIAL / 3 HW / 12 DORMANT / 1 BLOCKED-EXT). **Sensor B watch byte-identical to Cycle 6** (1 FRESH / 1 VERIFIED-EXT / 1 UNVERIFIED-EXT / 4 PENDING). **Stream-in-flight slot now FREE for Cycle 11.** **Architecture detail covered in** `[[project_hwfl1_cycle10_sensor_a_v02]]`. PV-CI 174/174 PASS unchanged; 0 IOTX; no FROZEN edit, no chain write, no Solidity edit, no firmware edit. Standing OPERATOR-ACTION box unchanged (OA-1 fragility-only per Cycle 9 F-CYCLE9-1; OA-4 priority RISES; OA-2/3 unchanged).

<a id="note-12"></a>
### 12. 🧹 HWFL-1 CYCLE 9

NOTE: 🧹 HWFL-1 CYCLE 9 — F-CYCLE8-1 BROADER CLEANUP (CROSS-SENSOR + PUBLIC-DOC SANITIZATION, CATEGORY A FORWARD-CONTAINMENT) 2026-06-12 (`6a7e78ed`) — **First cross-sensor sanitization cycle + first cycle to edit a public-repo doc outside `audits/` (the public DR runbook) + first multi-file regression-rail.** Closes the forward-containment half of F-CYCLE8-1 (Cycle 8 sanitized only G1.6's evidence string; Cycle 9 sanitizes the hardcoded `OA-1` line in BOTH `sensor_c_rung_ledger.py::to_markdown` AND `sensor_b_supply_watch.py::to_markdown` AND `docs/disaster-recovery-runbook.md` §0 row 1 + finding F-DECON-3.2 paragraph — 3 sites, verbatim-matched sanitized text). **Sanitized OA-1 text (verbatim across all 3 sites):** `"Back up MFG Root CA canonical file (path per docs/disaster-recovery-runbook.private.md). F-DECON-3.2 interim mitigation. Highest-leverage 5-min action."` Removes: CA filename (every site), `(517 B)` size annotation (fingerprint risk per D-HWFL-29), Windows-machine OS reference. Adds: pointer to gitignored private companion runbook for canonical-path lookup, F-CYCLE9-1 context note in the DR runbook's F-DECON-3.2 paragraph framing the disclosure-vs-fragility split. **D-HWFL-30 Option α scope expansion:** during V4 verification I surfaced 2 additional unsanitized sites in the public DR runbook (§0 + F-DECON-3.2 finding paragraph) — operator chose to expand Cycle 9 to include them rather than defer to Cycle 10. **Proof-of-cleanup discipline:** Cycle 9 regenerates `audits/rung-gate-ledger-cycle-9-2026-06-12.md` + `audits/hardware-watch-cycle-9-2026-06-12.md` as the empirical artifacts proving the sanitization works — 0 filename hits across both new artifacts AND the edited public DR runbook. Historical cycle artifacts (cycle-2/4/5/6/7/8 .md) intentionally LEFT UNTOUCHED per D-HWFL-27 (they are read-only historical record; modifying them would itself be a finding). **F-CYCLE9-1 OPENED:** sanitization is forward-containment only. Historical exposure persists in committed cycle artifacts cycle-2/4/5/6/7/8 .md, multiple cycle-close commit messages, prior CLAUDE.md NOTE bodies, all git-history diffs touching the unsanitized lines, and public clone history. **Three load-bearing implications:** (1) OA-1 value shifts from secrecy-protection to operational-fragility-mitigation only (the 5-min action remains correct on operational-risk grounds — losing the single canonical file is still a real failure mode regardless of disclosure); (2) **OA-4 (HSM root migration) priority RISES** — it's the only path that both eliminates the SPOF pattern AND implicitly closes the disclosure (file ceases to exist, path becomes irrelevant); (3) "historical-artifact / git-history rewrite" is its own future-cycle decision — force-push to public main is on the CLAUDE.md hard-rule NEVER-without-explicit-request list, would require explicit authorization, AND would not retroactively clean existing clones/forks. **Tests:** T9 (Sensor C) + T8 (Sensor B) AMENDED with negative filename-exclusion assertions (`"qortroller_foundation_mfg_ca.json"` NOT in rendered markdown) + positive content checks for sanitized OA-1 shape (`"F-DECON-3.2 interim mitigation"` + `"disaster-recovery-runbook.private.md"` references present). Parallels Cycle 8's T12 regression rail — any future cycle that re-introduces the filename token now fails fast in CI on both sensors. **Test counts:** Sensor C 12 PASS / Sensor B 16 PASS / total 28/28 in 1.06s. No new tests this cycle — amendments carry the full regression rail. **Files (8 — 2 new, 6 edits):** `bridge/vapi_bridge/sensor_c_rung_ledger.py` EDIT (OA-1 line sanitized), `bridge/vapi_bridge/sensor_b_supply_watch.py` EDIT (OA-1 line sanitized), `docs/disaster-recovery-runbook.md` EDIT (§0 row 1 + F-DECON-3.2 paragraph), `bridge/tests/test_sensor_c_rung_ledger.py` EDIT (T9 amended), `bridge/tests/test_sensor_b_supply_watch.py` EDIT (T8 amended), `audits/rung-gate-ledger-cycle-9-2026-06-12.md` NEW (proof artifact), `audits/hardware-watch-cycle-9-2026-06-12.md` NEW (proof artifact), `audits/rung-gate-ledger-latest.json` EDIT (rolling artifact refreshed). **Decisions resolved:** D-HWFL-27 = regenerate cycle-9 ledger + hardware-watch as proof (leaving cycle-2/4/5/6/7/8 historical artifacts untouched); D-HWFL-28 = no sanitization of OA-2/3/4 (their paths are operator-canonical reference, not operator-private — different threat model); D-HWFL-29 = strip the size annotation (`517 B` is a fingerprint risk); D-HWFL-30 = Option α (expand Cycle 9 scope to include public DR runbook after V4 surfaced 2 sites of leak there). **Sensor C ledger state distribution byte-identical to Cycle 8** (4 LIVE / 1 LIVE-FRAGILE / 1 LIVE-PARTIAL / 3 HARDWARE-GATED / 12 DORMANT / 1 BLOCKED-ON-EXTERNAL; 22 total). **Sensor B watch state distribution byte-identical to Cycle 6** (1 FRESH / 1 VERIFIED-EXTERNAL / 1 UNVERIFIED-EXTERNAL / 4 PENDING-OPERATOR-NOTE). **Sensor A** byte-identical since Cycle 1. **BOM v0.1** byte-identical since Cycle 7. **Sensor C v0.1.3 / Sensor B v0.1.1 unchanged** — sanitization is non-version-shaped (text-content edit, no taxonomy/schema/verifier change). **Stream-in-flight slot now FREE for Cycle 10.** **Architecture detail covered in** `[[project_hwfl1_cycle9_f_cycle8_1_broader_cleanup]]`. PV-CI 174/174 PASS unchanged; 0 IOTX; no FROZEN edit, no chain write, no Solidity edit, no firmware edit. Standing OPERATOR-ACTION box: OA-1 status per operator `[when true]` — rationale now operational-fragility-mitigation per F-CYCLE9-1 (secrecy already lost to public history); OA-4 priority RISES (the only path that closes both the SPOF pattern AND the disclosure trail).

<a id="note-13"></a>
### 13. 🧯 HWFL-1 CYCLE 8

NOTE: 🧯 HWFL-1 CYCLE 8 — G1.6 LIVE→LIVE-FRAGILE VIA SENSOR C v0.1.3 + F-CYCLE8-1 EVIDENCE SANITIZATION 2026-06-12 (`a6a026ca`) — **First LIVE re-classification (not a DORMANT-class promotion).** G1.6 (MFG Root CA file present) moves from `LIVE` → `LIVE-FRAGILE` as Sensor C v0.1.3 introduces `GateState.LIVE_FRAGILE` as the 9th state. **First operator-supplied finding (F-CYCLE8-1)**, distinct from the agent-surfaced F-HWFL-* family — operator caught that my proposed evidence text would have leaked the CA filename + "plaintext" adjective + OS into a public-repo artifact and required sanitization + a pointer to the gitignored private runbook + a regression test. **LIVE-FRAGILE vs LIVE-PARTIAL — orthogonal axes:** LIVE-PARTIAL (Cycle 7) is the *evidence* axis — gate's question has partial answer; LIVE-FRAGILE (Cycle 8) is the *structural-fragility* axis — full evidence BUT a known single-point-of-failure in the underlying pattern. A future gate could in principle be both (LIVE-PARTIAL-FRAGILE) but that's a future-cycle taxonomy question. **Option A architectural choice (D-HWFL-24):** G1.6 returns `LIVE-FRAGILE` unconditionally while the canonical CA file exists. The structural single-point-of-failure pattern (F-DECON-3.2) persists regardless of operational mitigations — OA-1 (interim backup to password manager + encrypted USB) reduces operational risk but does NOT change the ledger state. Demotion to plain LIVE happens only when F-DECON-3.2 ROOT fix lands (HSM-backed CA, OA-4 long-term track). This respects operator's `[when true]` discipline: the loop does not auto-detect OA-1 completion via filesystem inspection; the structural state is about the SOFTWARE PATTERN, not operator operational state. **Sanitized G1.6 evidence text (D-HWFL-25 + F-CYCLE8-1 rider):** `"MFG Root CA canonical file present (existence-only check; contents not read). Structural fragility per F-DECON-3.2; operational detail in docs/disaster-recovery-runbook.private.md. Demotes to LIVE when F-DECON-3.2 root fix lands (HSM-backed CA, OA-4 long-term track)."` — no CA filename, no `plaintext` adjective, no OS reference. **T12 regression rail:** explicitly asserts evidence string does NOT contain `"qortroller_foundation_mfg_ca.json"` or `"qortroller_foundation_mfg_ca"` substrings AND positive content checks for `"F-DECON-3.2"` and `"disaster-recovery-runbook.private.md"` references. Future regressions are mechanically caught. **Cycle 8 ledger distribution:** **4 LIVE** (G1.4 G1.5 G1.7 G2.1) + **1 LIVE-FRAGILE** (G1.6 — NEW state, NEW assignment) + **1 LIVE-PARTIAL** (G2.7) + 3 HARDWARE-GATED (G1.1-G1.3) + 12 DORMANT (G2.2-G2.6 + G3.1-G3.5 + G4.2-G4.3) + 1 BLOCKED-ON-EXTERNAL (G4.1). Total 22 unchanged. **9-state Sensor C taxonomy now:** LIVE / LIVE-PARTIAL / LIVE-FRAGILE / DORMANT / HARDWARE-GATED / BLOCKED-ON-SENSOR-B / BLOCKED-ON-EXTERNAL / DEFERRED / UNVERIFIABLE. **Tests:** Sensor C 11 → **12 PASS in 1.01s**. T5 amended (G1.6 dropped from all-LIVE list since it's now LIVE-FRAGILE not LIVE). T12 NEW (G1.6 = LIVE-FRAGILE on real repo + sanitization regression assertions). All other tests byte-identical behavior. **Files (4 — 1 new, 3 edits):** `bridge/vapi_bridge/sensor_c_rung_ledger.py` EDIT (+9th enum + amend _verify_g1_6 with sanitized evidence); `bridge/tests/test_sensor_c_rung_ledger.py` EDIT (T5 amended + T12 new); `audits/rung-gate-ledger-cycle-8-2026-06-12.md` NEW (regenerated ledger; G1.6 = LIVE-FRAGILE); `audits/rung-gate-ledger-latest.json` EDIT (rolling artifact refreshed). **F-CYCLE8-1 broader exposure scope explicitly raised + deferred:** OPERATOR-ACTION box in `sensor_c_rung_ledger.py` (line 375) AND in `sensor_b_supply_watch.py to_markdown` both still hardcode the CA filename, AND existing committed cycle ledger artifacts in git history (cycle-2/4/7 .md files) contain the same string. **Cycle 8 does NOT touch those** per scope discipline — "redact public-repo exposures" cycle scope. **Sensor B v0.1.1 byte-identical** this cycle (Cycle 6 ship + Cycle 7 unchanged + Cycle 8 unchanged). **Sensor A byte-identical** since Cycle 1. **BOM v0.1** byte-identical since Cycle 7 (C1 row absorbed S6 finding then). **Decisions resolved:** D-HWFL-24 = Option A (pattern-fragility, no auto-detect of OA-1 backup); D-HWFL-25 = revised sanitized text per F-CYCLE8-1 rider; D-HWFL-26 = no conditional OPERATOR-ACTION rendering (would expand scope to 2+ sites). **Stream-in-flight slot now FREE for Cycle 9.** **Architecture detail covered in** `[[project_hwfl1_cycle8_g1_6_live_fragile]]`. PV-CI 174/174 PASS unchanged; 0 IOTX; no FROZEN edit, no chain write, no Solidity edit, no firmware edit. Standing OPERATOR-ACTION box unchanged (OA-1 status per operator: `[when true]`; structural ledger state of G1.6 is LIVE-FRAGILE — OA-1 reduces operational risk but does NOT change state per Option A discipline).

<a id="note-14"></a>
### 14. 🔓 HWFL-1 CYCLE 7

NOTE: 🔓 HWFL-1 CYCLE 7 — G2.7 BLOCKED→LIVE-PARTIAL VIA SENSOR C v0.1.2 (SECOND CONVERSION + FIRST CROSS-ARTIFACT-SYNC VERIFIER) 2026-06-12 (`d83b0250`) — Second DORMANT-class-to-LIVE conversion since HWFL-1 opened (first was G2.1 in Cycle 4). G2.7 (ESP32-class module cert status known) promoted from intrinsic `BLOCKED-ON-SENSOR-B` to verifier-backed `LIVE-PARTIAL` based on operator judgment from Cycle 7 triage that S6's UNVERIFIED-EXTERNAL narrative carries load-bearing evidence — specifically the negative finding "NO Common Criteria / FIPS on Espressif landing page → ESP32 alone is NOT a substitute for ATECC608A → secure-element pairing required (BOM C2)." Sensor C v0.1.2 amendment adds **GateState.LIVE_PARTIAL** as 7th state (between LIVE and DORMANT semantically), distinct from D-HWFL-9's deferred LIVE-FRAGILE concept (which flags single-point-of-failure on a LIVE gate — G1.6 MFG CA being the obvious candidate; future cycle may absorb into LIVE-PARTIAL or add LIVE-FRAGILE separately, NOT this cycle's scope). **First cross-artifact synchronization verifier** in the loop: `_verify_g2_7_esp32_cert_partial_evidence` is two-part per D-HWFL-22 — BOTH conditions required for LIVE-PARTIAL, either alone → UNVERIFIABLE: (a) `audits/ops_notes_cycle5.json` contains `S6.esp32-cert-status` entry with non-empty summary (Sensor B narrative present), AND (b) `docs/qortroller-devkit-bom-v0_1.md` contains the marker `"secure-element pairing required"` in C1 row (the narrative ABSORBED into the canonical hardware-planning artifact). This 2-part rule prevents planning-artifact-vs-ledger desync — if BOM is later edited to remove the marker OR if S6 summary is emptied, G2.7 demotes immediately. **Operator judgment encoded structurally:** the BOM C1 row update is the load-bearing change. Previous text "Espressif ESP32-S3 is the v0.1 reference candidate; cert status pending Sensor B S6" → new text "Espressif ESP32-S3 is the v0.1 reference candidate. Sensor B S6 (Cycle 5): wireless certs via boilerplate, NO Common Criteria/FIPS on landing page — ESP32 alone NOT a substitute for ATECC608A, secure-element pairing required (BOM C2). Cert status: PARTIAL". The operator's S6 finding is now part of the canon, not just floating in Sensor B's narrative. **Cycle 7 ledger distribution:** 5 LIVE (G1.4 G1.5 G1.6 G1.7 G2.1) + **1 LIVE-PARTIAL** (G2.7 — NEW state, NEW assignment) + 3 HARDWARE-GATED (G1.1-G1.3) + 12 DORMANT (G2.2-G2.6 + G3.1-G3.5 + G4.2-G4.3) + **0 BLOCKED-ON-SENSOR-B** (was 1; G2.7 promoted out — first time this state has been empty since it was defined in Cycle 2) + 1 BLOCKED-ON-EXTERNAL (G4.1 IIP-64 PR #72). Total 22 unchanged. **Rung 2 progress:** 0/7 → 2/7 since loop opened (G2.1 Cycle 4 + G2.7 Cycle 7). Remaining 5 gates (Zephyr firmware, Thread-C isolation, φ residency, stick selection, IMU selection) require deeper hardware/firmware work not single-cycle scope. **Tests:** Sensor C 10 → **11 PASS in 0.53s**. 5 existing tests amended (verifier count 5→6; intrinsic distribution `BLOCKED_ON_SENSOR_B` 1→0; LIVE-on-real-repo list, UNVERIFIABLE-on-tmp list, G2.7 pass-through assertion). 1 new test T11 specifically validates the 2-part verifier honesty rail via tmp_path fixture: BOM-marker-removed → UNVERIFIABLE with `"planning artifact desynced"` evidence; S6-summary-emptied → UNVERIFIABLE with `"empty/missing"` evidence; both together happy-path → LIVE-PARTIAL. Same fail-open posture as every other Sensor C verifier — never spurious LIVE-PARTIAL. **Files (5 — 1 new, 4 edits):** `docs/qortroller-devkit-bom-v0_1.md` EDIT (C1 row absorbs S6 finding); `bridge/vapi_bridge/sensor_c_rung_ledger.py` EDIT (+enum + verifier + GateDef amend + _VERIFIERS register); `bridge/tests/test_sensor_c_rung_ledger.py` EDIT (5 amended + 1 new T11); `audits/rung-gate-ledger-cycle-7-2026-06-12.md` NEW (regenerated ledger); `audits/rung-gate-ledger-latest.json` EDIT (rolling artifact refreshed). **Decisions resolved:** D-HWFL-21 = LIVE_PARTIAL as 7th GateState (yes; distinct from deferred LIVE-FRAGILE per D-HWFL-9); D-HWFL-22 = two-part verifier (both sources required, either alone → UNVERIFIABLE); D-HWFL-23 = BOM C1 row edit phrasing approved as proposed. **Sensor B v0.1.1 byte-identical** this cycle (Cycle 6 just shipped — no further B changes). **Stream-in-flight slot now FREE for Cycle 8.** **Architecture detail covered in** `[[project_hwfl1_cycle7_g2_7_live_partial]]`. PV-CI 174/174 PASS unchanged; 0 IOTX; no FROZEN edit, no chain write, no Solidity edit, no firmware edit. Standing OPERATOR-ACTION box unchanged (OA-1 status per operator: `[when true]`).

<a id="note-15"></a>
### 15. 🏷️ HWFL-1 CYCLE 6

NOTE: 🏷️ HWFL-1 CYCLE 6 — SENSOR B v0.1.1 VERIFIED_EXTERNAL STATE + STRUCTURAL PRECONDITIONS (CLOSES F-HWFL-5-3) 2026-06-12 (`ecc45d60`) — First **taxonomy-amendment cycle**; first cycle to close a self-surfaced finding within 1 cycle of opening (F-HWFL-5-3 from Cycle 5 → resolved Cycle 6). Parallels Cycle 4's Sensor C v0.1.1 atomic point-release pattern. Amendment introduces `WatchState.VERIFIED_EXTERNAL` as 6th state between UNVERIFIED-EXTERNAL and FRESH, gated by 3 structural preconditions on the narrative JSON (`verified_by` non-empty str + `sources` non-empty `list[str]` + `verified_date` ISO-8601 YYYY-MM-DD; URLs NOT required per D-HWFL-18 — any identifier-shaped string accepted). **D-HWFL-19 unified freshness window:** `verified_date > source.freshness_days` ago → STALE state, but `verified_by`/`sources`/`verified_date` RETAINED on the WatchLine for detail rendering (reviewer sees what WAS verified, just that verification is past freshness). **D-HWFL-20 backward-compat-with-warning matrix:** (i) 3/3 fields valid + within freshness → VERIFIED-EXTERNAL; (ii) 3/3 fields valid + aged → STALE (fields retained); (iii) 1-2/3 fields present → UNVERIFIED-EXTERNAL + rendered `VERIFICATION WARNING` (catches operator partial-typo); (iv) 3/3 present but `verified_date` unparseable → same warning treatment; (v) 0/3 fields present (Cycle 3-shape JSONs) → silent downgrade UNVERIFIED-EXTERNAL with NO warning bleed (full backward compat). **Empirical proof discipline applied:** the cycle didn't just SAY the amendment works — it re-rendered Cycle 5's `audits/hardware-watch-cycle-5-2026-06-10.md` against the amended module as the proof artifact. Re-rendered Cycle 5 distribution: **FRESH=1** (S1 IIP-64 PR STRUCTURED, unchanged) + **VERIFIED-EXTERNAL=1** (S2 ATECC608A — operator-curated 6 sources / verified 2026-06-10 / verified_by "operator (Con / ConWan30) via Claude.ai session") + **UNVERIFIED-EXTERNAL=1** (S6 ESP32 — Claude WebFetch draft with 0/3 verified fields → silent backward-compat downgrade) + **PENDING-OPERATOR-NOTE=4** (S3-S5 + S7). Same module distinguishes operator-verified from agent-drafted external narrative — that distinction IS the proof. S2 restructured to schema `vapi-sensor-b-narratives-v1.1` with explicit `verified_by`/`sources[6]`/`verified_date` fields lifted out of `raw_excerpt`. **Architectural refactor:** `_resolve_state` consolidated to single state-decision site returning a fully-formed `WatchLine` (was 5-tuple unpacked by assembler; new 10-rule decision matrix encapsulated). `_meta` JSON top-level key now reserved + ignored by `_load_narratives` for file-level metadata that doesn't map to a Sensor B topic. **Test growth:** Sensor B 10 → **16 PASS in 1.22s**. Existing 10 byte-identical behavior preserved; 6 new (T11 verified happy path + stale-retains-fields unified-window proof; T12 missing verified_by with warning; T13 empty sources with warning; T14 missing verified_date with warning; T15 malformed verified_date with warning; T16 zero-fields silent backward-compat with explicit `VERIFICATION WARNING` markdown-bleed-check). **Files (5 — 2 new, 3 edits, 0 new artifact):** `bridge/vapi_bridge/sensor_b_supply_watch.py` EDIT (~+150 LOC), `scripts/run_sensor_b.py` EDIT (_load_narratives reads new fields + _meta-ignore), `bridge/tests/test_sensor_b_supply_watch.py` EDIT (+6 tests), `audits/ops_notes_cycle5.json` EDIT (S2 restructure to schema v1.1), `audits/hardware-watch-cycle-5-2026-06-10.md` REGENERATED (proof). **Sensor C ledger BYTE-IDENTICAL to Cycle 4/5 state — G2.7 triage explicitly deferred to Cycle 7** per operator directive against the re-rendered states (S6 still UNVERIFIED-EXTERNAL after Cycle 6, so G2.7 evidence question unchanged; triage now happens against amendment-stabilized state, not transitional). **Finding closed this cycle:** F-HWFL-5-3 (operator's VERIFIED-EXTERNAL annotation had no first-class state) → resolved by this amendment. **Still open:** F-HWFL-5-1 (vendor-catalog WebFetch reliability poor — Sensor B v0.2 multi-source fallback strategy candidate), F-HWFL-5-2 (Path A spec hard-names NRND ATECC608A — operator-fired spec amendment candidate). **Stream-in-flight slot now FREE for Cycle 7.** **Architecture detail covered in** `[[project_hwfl1_cycle6_sensor_b_v011_verified_external]]`. PV-CI 174/174 PASS unchanged; 0 IOTX; no FROZEN edit, no chain write, no Solidity edit, no firmware edit. Standing OPERATOR-ACTION box unchanged (OA-1 status per operator: `[when true]`).

<a id="note-16"></a>
### 16. 📡 HWFL-1 CYCLE 5

NOTE: 📡 HWFL-1 CYCLE 5 — SENSOR B NARRATIVE PASS #1 (S2+S6) — ZERO-CODE INTEL CYCLE 2026-06-10 (`d6eace78`) — **First zero-code-change cycle:** pure intel injection through Sensor B's `--narratives` runner boundary. Validates the Cycle 3 architectural choice (D-HWFL-10 pure-function module + injected fetcher) — module didn't need to be touched to absorb operator intel. **S2 ATECC608A operator-curated, source-enumerated** (Microchip product page + nerves-hub/nerves_key issue #60 + datasheets DS40002239A/DS40002513 + AN2237/AN3539): ATECC608A is **NRND since ~2021-03** ("Recommend using ATECC608B"); successor chain 608A→608B→608C; at least one 608B variant (ATECC608B-MAH4I-S) is itself NRND with **ATECC608C-TFLXTLS (TrustFLEX pre-provisioned)** as replacement. Migration cost LOW: AN2237 documents 608B as functional drop-in for 608A — same commands/structure, CryptoAuthLib absorbs differences; **caveat: hardwired-timing firmware needs review** (polling-based timing unaffected; future spec amendment should mandate polling-based timing for forward compat). **S6 ESP32 cert status WebFetch-drafted by Claude, operator-approved as-is** after Microchip vendor page returned 403 forcing workflow-(c)→(a) split: ESP32 family actively listed by Espressif alongside successors (S2/S3/C2/C3/C5/C6/H2/H21/H4/P4/E22/S31/C61); wireless certs FCC/CE/SRRC/KCC/IC/TELEC/WFA/BQB mentioned in support-section boilerplate (not ESP32-specific tied); ISO 9001 referenced; **NO Common Criteria or FIPS cert stated** — load-bearing: ESP32 alone is NOT a substitute for ATECC608A, secure-element pairing required per Path A §2. **Architectural signal worth highlighting (cross-narrative convergence):** S2 + S6 together establish that ATECC608C-TFLXTLS is simultaneously (a) the successor to the NRND ATECC608A AND (b) the TrustFLEX pre-provisioned variant that Path A §3.2 Rung 3 factory provisioning already needs. The successor-and-provisioning-strategy converge on the same SKU family — meaningful Rung 2→3 alignment that wasn't visible before this paste. BOM C2 row now has a clear forward path through this single SKU. **Cycle 5 watch distribution:** 1 FRESH (S1 IIP-64 PR #72 still OPEN, stale 18 days) + **2 UNVERIFIED-EXTERNAL (S2 + S6, NEW)** + 4 PENDING-OPERATOR-NOTE (S3-S5 sticks + S7 competitive). Net **−2 PENDING** vs Cycle 3 baseline (6→4). **Three findings surfaced this cycle, all routed to future cycles (NOT silently amended this cycle):** F-HWFL-5-1 vendor-catalog WebFetch reliability poor (4/4 fallback attempts failed during S2 research: Microchip 403, Wikipedia 404 ×2, Mouser 60s timeout, DigiKey 404 — Sensor B v0.2 needs multi-source fallback strategy OR mandate workflow (a)/(b) for vendor-product-page-class sources); F-HWFL-5-2 `docs/path-a-manufacturing-spec.md` §1.1 hard-names NRND ATECC608A (operator-derived from S2 intel) — spec should move to family language `"ATECC608B/608C-class CryptoAuthentication secure element, CryptoAuthLib-compatible, polling-based timing REQUIRED"` in a future operator-fired spec-amendment cycle; F-HWFL-5-3 operator's `STATUS: VERIFIED-EXTERNAL (operator-proxy fetch)` annotation in S2 has no first-class state in Sensor B v0.1 taxonomy — currently preserved as text-inside-summary while module state renders `UNVERIFIED-EXTERNAL`; Sensor B v0.1.1 amendment candidate to introduce 6th state `VERIFIED_EXTERNAL` between UNVERIFIED-EXTERNAL and FRESH (requires JSON shape addition like `"verified": true`, assembler state-resolution update, test coverage). **Sensor C ledger unchanged this cycle:** G2.7 BLOCKED-ON-SENSOR-B stays — populating S6 ≠ unblocking G2.7 (unblock is a separate operator decision after triaging whether S6 narrative is sufficient evidence for the gate). Same posture for BOM C1/C2 row promotion. **D-HWFL-16=(c) I-fetch-propose** for S6 + **operator-direct paste** for S2 after WebFetch fallbacks exhausted; **D-HWFL-17=S2+S6 scope confirmed** (S3-S5 sticks + S7 competitive deferred to future Sensor B population pass). **Files (2 — both new, ZERO code change):** `audits/ops_notes_cycle5.json` (narrative input, schema `vapi-sensor-b-narratives-v1`, includes `_meta` block with discipline notes); `audits/hardware-watch-cycle-5-2026-06-10.md` (regenerated watch report). **Stream-in-flight slot now FREE for Cycle 6.** **Architecture detail covered in** `[[project_hwfl1_cycle5_sensor_b_population_1]]`. PV-CI 174/174 PASS unchanged; 0 IOTX; no FROZEN edit, no chain write, no Solidity edit, no firmware edit, **no code change of any kind**. Standing OPERATOR-ACTION box unchanged — OA-1 now has thematic peers: BOM §1 honesty stamp + S2 single-source-failure-mode lesson + the loop's accumulating evidence that single-copy critical resources are the recurring failure pattern across the protocol.

<a id="note-17"></a>
### 17. 🔧 HWFL-1 CYCLE 4

NOTE: 🔧 HWFL-1 CYCLE 4 — DEV-KIT BOM v0.1 + G2.1 DORMANT→LIVE (FIRST CONVERSION) 2026-06-10 (`e34a51ff`) — **First DORMANT-to-LIVE conversion since HWFL-1 opened.** Cycle 4 ships `docs/qortroller-devkit-bom-v0_1.md` (~180 LOC, 8 sections: §1 honesty stamp / §2 two-supplier discipline rail / §3 critical parts C1-C8 / §4 advisory A1-A3 / §5 accessories E1-E3 / §6 Sensor B linkage table mapping S2-S6 narratives to BOM rows / §7 promotion criteria UNVERIFIED-EXTERNAL→LIVE-SHORTLIST→LIVE-SUPPLIED / §8 provenance) AND **atomically amends Sensor C to v0.1.1** (G2.1 `intrinsic_state=DORMANT` → `verifier_name="verify_g2_1_devkit_bom_exists"`). The verifier checks file existence + all 8 critical part IDs `C1`..`C8` present + "Two-supplier discipline rail" header marker; fail-open → UNVERIFIABLE if any check fails, never spurious LIVE. **Critical parts (C1-C8) with their canonical anchors:** C1 MCU (ESP32-class, Wi-Fi/BT, USB-OTG ≥1000Hz; ref Espressif ESP32-S3, cert status pending Sensor B S6); C2 secure element (ECDSA-P256, locked extraction; ATECC608A/B reference + YubiKey 5 PIV / STSAFE-A110 alternates per Path A spec §2; Sensor B S2 tracks); C3/C4 L/R analog sticks (Hall-effect or TMR per Sensor Stack v2.1 Surface 6; 3 candidates K-Silver JH16 HE / MIDAS 5-pin HE / Magneto TMR; gated MEASUREMENT-PENDING on Empirical Unknown #4 ≥20% same-batch separability; same-family L/R discipline so BOM cannot mix Hall+TMR which breaks calibration corpus); C5 IMU (6-axis gyro+accel ≥1000Hz; ICM-42688-P / BMI270 / LSM6DSO candidates; Empirical Unknown #1 anchors selection); C6 USB-C connector + cable; C7 adaptive trigger mechanism ×2 (the protocol's STRONGEST L4 discriminator per Sensor Stack v2.1 Surface 1 PRIMARY DISCRIMINATOR — must reproduce Sony-class force-curve at 1 kHz 8-bit; off-shelf candidates rare, custom design likely); C8 touchpad capacitive 12-bit X/Y 2-point. **A2 microphone row explicitly DEFERRED per D-HWFL-14** with anchor to TRACK1-LESSON-002 (DualSense exposes single mono UAC1 post-DSP not multi-mic; literature doesn't transfer) + TRACK1-LESSON-003 (BIPA/GDPR/CIPA attach to capture regardless of downstream use); preserved as visible row so future cycles cannot silently re-introduce. Promotion out of DEFERRED requires documented supersession of both lessons. **Sensor C v0.1.1 point-release (D-HWFL-13):** intentionally touches a prior sensor mid-arc — keeping G2.1 DORMANT-intrinsic while shipping the BOM would make the ledger lie, so atomic amendment is more honest than 2-cycle split. Test count assertions adjusted: `len(_VERIFIERS)==5` (was 4), `none_count==5`, `DORMANT count==12` (was 13), G2.1 = LIVE in real-repo test, G2.1 = UNVERIFIABLE on tmp_path test, intrinsic-pass-through test rewritten to sample G2.2 instead. 10/10 tests pass in 0.34s. **Cycle 4 ledger distribution:** 3 HARDWARE-GATED + **5 LIVE** (G1.4 VMDR, G1.5 first device, G1.6 MFG CA single-copy nag inline, G1.7 SE honesty rail, G2.1 dev-kit BOM scaffold) + **12 DORMANT** (was 13) + 1 BLOCKED-ON-SENSOR-B (G2.7) + 1 BLOCKED-ON-EXTERNAL (G4.1 IIP-64 PR #72 still OPEN+stale). Total 22 unchanged. Cumulative effect: Rung 2's deepest blockage breaks for the first time. The promotion is honest about its scope — BOM exists and has structural integrity; NO supplier is committed and NO part has been Stage A measured. **F-HWFL-4-1 corrected (D-HWFL-15):** operator caught that the wallet line `~14.26 IOTX as of 2026-05-21` was stale and my session was carryforwarding "9.120 IOTX unchanged" from Arc 5/6/7 NOTEs unchecked. Verified live via `eth_getBalance` on `babel-api.testnet.iotex.io` (`User-Agent` header required to bypass 403): **32.078372 IOTX** (raw 32078372000000000000 wei). Trajectory implies ~+22.96 IOTX faucet refill since the Arc 5/6/7 baseline. New discipline added to CLAUDE.md wallet line: **every session-end summary that includes wallet must `eth_getBalance` before stating, NEVER echo prior NOTEs.** Surfaces Sensor A v0.2 candidate (extend spec/impl parity to "live state vs CLAUDE.md claim" so wallet/test-count/contract-count drift surfaces automatically). **Stream-in-flight slot now FREE for Cycle 5.** **Architecture detail covered in** `[[project_hwfl1_cycle4_devkit_bom_v01]]`. PV-CI 174/174 PASS unchanged; 0 IOTX (this cycle did NOT spend); no FROZEN edit, no chain write, no Solidity edit, no firmware edit. Standing OPERATOR-ACTION box unchanged — OA-1 MFG CA backup remains, now also surfaced by analogy in BOM §1 honesty stamp (single-source critical part = same failure mode as single-copy CA).

<a id="note-18"></a>
### 18. 🌐 HWFL-1 CYCLE 3

NOTE: 🌐 HWFL-1 CYCLE 3 — SENSOR B v0.1 SUPPLY/STANDARDS WATCH SHIPPED + SENSOR TRIANGLE COMPLETE 2026-06-10 (`1690b257`) — Third cycle of HWFL-1 closes the **sensor triangle** (A drift-findings + C state-ledger + B external-intel); the persistent loop is now fully equipped with the master prompt's intended sensing surface, exactly per D-HWFL-1 order (A→C→B). Sensor B v0.1 is a standalone pure-function module at `bridge/vapi_bridge/sensor_b_supply_watch.py` (~293 LOC: `FetchKind` enum / `WatchState` enum / `WatchSource` frozen dataclass / 7-source canonical registry `_CANONICAL_SOURCES` / `FetchResult` injection dataclass / `assemble_watch_report()` assembler / `WatchReport.to_markdown()` renderer with HTML-escape + pipe-escape on every external field). **Architectural rail (D-HWFL-10):** module never touches the network; the entire network boundary lives in `scripts/run_sensor_b.py` (gh CLI for STRUCTURED + JSON file for MANUAL_NARRATIVE). This isolates the prompt-injection blast radius to one script and makes tests deterministic via FetchResult fixture injection. **State taxonomy extends Sensor C's:** FRESH / STALE / PENDING-OPERATOR-NOTE / UNVERIFIED-EXTERNAL / FETCH-ERROR. **Critical discipline rail:** MANUAL_NARRATIVE sources NEVER reach FRESH — even fresh timestamps demote to UNVERIFIED-EXTERNAL because the narrative summary IS external content. Only STRUCTURED sources (gh CLI JSON with directly-observable fields, never AI summarization) qualify for FRESH. Adversarial-input test #9 confirms HTML `<script>` tags + pipe-chars in external content are escaped before rendering — a malicious branch name or pasted narrative cannot break the table or inject active markup. **7 canonical sources for v0.1 (D-HWFL-11):** S1 IIP-64 PR #72 (STRUCTURED via `gh pr view`), S2 ATECC608A lifecycle, S3 K-Silver JH16 HE stick, S4 MIDAS 5-pin HE stick, S5 Magneto TMR stick, S6 ESP32 cert status, S7 competitive attested-input landscape (S2-S7 all MANUAL_NARRATIVE). **Cycle 3 first watch (D-HWFL-12 = WebFetch now via gh):** S1 = FRESH, PR #72 state=`OPEN`, isDraft=`false`, mergedAt=`null`, last updated `2026-05-23T15:35:47Z` (~18 days stale — no movement IS intelligence; future cycles will surface activity inflection), author=`xinxin-crypto` (Xinxin Fan, IoTeX core), head branch=`iip-64`, +701/-0 lines across 6 commits. S2-S7 = PENDING-OPERATOR-NOTE awaiting operator intel paste via runner's `--narratives ops_notes.json`. **Cross-sensor linkage:** Sensor B v0.1 ships the pipe, not the payload — Sensor C's G2.7 stays BLOCKED-ON-SENSOR-B until real ESP32 cert intelligence (S6) lands as a populated narrative; that's a future-cycle milestone. **Artifact:** `audits/hardware-watch-cycle-3-2026-06-10.md` (per-cycle hardware-watch intelligence brief; first one ever produced). **Companion files:** `scripts/run_sensor_b.py` runner (gh CLI shell-out with 30s timeout + graceful exit-code/timeout/JSON-parse error handling, all materializing as FETCH-ERROR state never crash), `bridge/tests/test_sensor_b_supply_watch.py` (10/10 PASS in 0.25s — covers canonical count, STRUCTURED/MANUAL_NARRATIVE split, all-pending default, FRESH/STALE/UNVERIFIED-EXTERNAL/FETCH-ERROR state transitions, adversarial HTML+pipe input escape, malformed-timestamp tolerance). **Decisions resolved this session:** D-HWFL-10=pure-function module with injected fetcher (matches Sensor C's verifier-injection pattern; one shared discipline); D-HWFL-11=7 sources confirmed (1 STRUCTURED + 6 MANUAL_NARRATIVE); D-HWFL-12=WebFetch the IIP-64 PR now via gh CLI (gh JSON safer than HTML-via-AI-summarizer for prompt-injection posture). **The sensor triangle's intelligence flow:** Sensor A surfaces spec/impl drift on every cycle scan; Sensor C snapshots rung-gate state machine-checkably; Sensor B brings external supply/standards signal in with explicit UNVERIFIED-EXTERNAL framing. Together they constitute the canonical input set for the eventual partner-handoff package assembler (future Rung 3 work). **Stream-in-flight slot now FREE for Cycle 4.** **Architecture detail covered in** `[[project_hwfl1_cycle3_sensor_b_v01]]`. PV-CI 174/174 PASS unchanged; 0 IOTX; no FROZEN edit, no chain write, no Solidity edit, no firmware edit. Standing OPERATOR-ACTION box unchanged — OA-1 MFG CA backup now nags from **three** places every cycle (Sensor C ledger top, Sensor B watch top, Sensor C G1.6 evidence inline) yet remains unaddressed.

<a id="note-19"></a>
### 19. 📊 HWFL-1 CYCLE 2

NOTE: 📊 HWFL-1 CYCLE 2 — SENSOR C v0.1 RUNG-GATE READINESS LEDGER SHIPPED 2026-06-10 (`c0aac6dc`) — Second cycle of the persistent Hardware Futures Loop. Sensor C v0.1 is a **standalone module** (NOT a Mythos variant per D-HWFL-7 — ledger semantics differ from finding semantics; chassis split is deliberate, not inconsistency) at `bridge/vapi_bridge/sensor_c_rung_ledger.py` (~316 LOC: `GateState` enum / `GateDef` frozen dataclass / 22-gate canonical registry `_CANONICAL_GATES` / 4 verifier functions / `assemble_ledger()` orchestrator / `RungLedger.to_json()` + `to_markdown()` serializers). Produces a machine-checkable snapshot of every gate across Rungs 1-4 of the QorTroller manufacturing staircase, rendered honestly: nothing LIVE-weighted that isn't verifiable now. **State taxonomy (FROZEN at v0.1):** LIVE / DORMANT / HARDWARE-GATED / BLOCKED-ON-SENSOR-B / BLOCKED-ON-EXTERNAL / DEFERRED / UNVERIFIABLE — fail-open: verifier exception ⇒ UNVERIFIABLE, **never** LIVE. **Cycle 2 first ledger distribution (22 gates total):** Rung 1 — 4 LIVE (G1.4 VMDR @ `0x2e5B5FB1…`, G1.5 first reference device tx `0x68f6cf49…` block 44028531, G1.6 MFG CA file present, G1.7 SecureElementBackend NotImplementedError honesty rail intact) + 3 HARDWARE-GATED (G1.1 DualSense Edge, G1.2 ATECC608A breakout, G1.3 CH341A USB-I2C); Rung 2 — 6 DORMANT (G2.1 dev-kit BOM, G2.2 Zephyr QorTroller firmware target, G2.3 Thread-C-equivalent isolation, G2.4 φ device-residency [kept in Rung 2 per D-HWFL-8], G2.5 Hall/TMR stick selection, G2.6 IMU selection) + 1 BLOCKED-ON-SENSOR-B (G2.7 ESP32 cert status — surfaces the cross-sensor dependency edge cleanly); Rung 3 — 5 DORMANT (G3.1 partner-handoff assembler, G3.2 TrustFLEX provisioning amendment, G3.3 MFG CA chained to ref root, G3.4 per-batch slot-config audit checklist, G3.5 two-supplier cost model); Rung 4 — 1 BLOCKED-ON-EXTERNAL (G4.1 IIP-64 PR #72) + 2 DORMANT (G4.2 spec-as-compliance-standard, G4.3 device-identity registry interop spec). **Honesty signal embedded:** G1.6's evidence column inline-flags `SINGLE-COPY per F-DECON-3.2 — see OA-1 in OPERATOR-ACTION box` so the MFG CA fragility nags both as standing action box AND as caveat attached to the LIVE gate it props up. **Artifacts:** `audits/rung-gate-ledger-cycle-2-2026-06-10.md` (per-cycle human audit with OPERATOR-ACTION box at top + state summary + per-rung tables) + `audits/rung-gate-ledger-latest.json` (rolling machine-readable artifact, schema `vapi-rung-gate-ledger-v1`, ready for `vapi_unified_state` MCP read-hook wiring in Sensor C v0.2). **Companion files:** `scripts/run_sensor_c.py` one-shot runner, `bridge/tests/test_sensor_c_rung_ledger.py` (10/10 PASS — covers canonical count, verifier registration, intrinsic state distribution, live-repo run, tmp_path fail-open demotion, intrinsic pass-through, JSON schema validation, OPERATOR-ACTION box marker presence, and synthetic-exception UNVERIFIABLE handling). **Decisions resolved this session:** D-HWFL-7=standalone module (ledger semantics ≠ finding semantics); D-HWFL-8=22 hardcoded gates for v0.1 with G2.4 φ-residency kept in Rung 2 (dev-kit-with-φ-on-device IS the canonical Rung 2 deliverable per master prompt); D-HWFL-9=defer LIVE-FRAGILE state to v0.2 (v0.1's job is to ship ledger shape, not encode fragility metadata; OA-1 nags suffice for the immediate concern). **Stream-in-flight slot now FREE for Cycle 3.** Sensors built: A ✓ / C ✓ / B ☐. **Architecture detail covered in** `[[project_hwfl1_cycle2_sensor_c_v01]]`. PV-CI 174/174 PASS unchanged; 0 IOTX; no FROZEN edit, no chain write, no Solidity edit, no firmware edit. Standing OPERATOR-ACTION box unchanged (OA-1 highest-leverage 5-min action remains the MFG CA backup).

<a id="note-20"></a>
### 20. 🛰️ HWFL-1 CYCLE 1

NOTE: 🛰️ HWFL-1 CYCLE 1 — SENSOR A v0.1 SHIPPED (variant #16) 2026-06-10 (`8d3ccfb8`) — First cycle of the persistent Hardware Futures Loop. Sensor A v0.1 is Mythos variant #16 `mythos_path_a_spec_impl_parity` (~190 LOC appended to `bridge/vapi_bridge/mythos_variants.py`), extending the variant #15 chassis pattern (fail-open, COVERAGE_BOUNDARY first, never raises). Probes `docs/path-a-manufacturing-spec.md` against six spec-cited surfaces — VMDR contract (`registerDevice` + `nonReentrant` markers), `deployed-addresses.json` (`0x2e5B5FB1…` VMDR address), `device_birth_cert.py` (3 §4.1 fields), `manufacturer_root_ca.py`, `verify_device_cert.py`, `vapi_invariant_gate.py` (INV-MFG-001/002 pins) — at file-existence + comment-level only per D-HWFL-6 (byte-level structural diff deferred to v0.2). Cycle 1 first run emits 3 LOW findings: COVERAGE_BOUNDARY + B1 (firmware/ tree exists but is the legacy `pebble_tracker.overlay` board, NOT the Path A QorTroller controller — spec silent, scope-boundary clarification not drift) + B2 (φ sanitization in `replay_proof_pipeline/pre_processor.py` is a Sensor Stack v2.1 concern not a Path A concern). All 6 P-probes pass marker checks silently. Companion files: `scripts/run_sensor_a.py` one-shot runner (renders findings to dated markdown), `bridge/tests/test_sensor_a_parity.py` (8/8 PASS), `audits/sensor-a-cycle-1-2026-06-10.md` (first cycle report). HWFL-1 architecture (immutable sensor layer A+B+C / editable orchestrator / operator-gated execution) covered in `[[project_hwfl1_cycle1_sensor_a_v01]]`. Decisions resolved this session: D-HWFL-1=A→C→B (Sensor A first), D-HWFL-2=extend Mythos #15 chassis, D-HWFL-3=raw 18 distinct `VAPI-*-v1` byte-strings as `frozen_v1_commitment_family_count` canonical rule (R1 unblocked; deferred to Cycle 2), D-HWFL-4=include Pebble-vs-QorTroller B1 finding (yes), D-HWFL-5=`mythos_path_a_spec_impl_parity` (Path-A-scoped name), D-HWFL-6=file+comment level only for v0.1. Findings F-HWFL-0-1 (prompt's "128 invariants" was stale; reality 174 pre-DECON-1 and all growth ceremony-stamped — non-critical) and F-HWFL-0-2 (`doc_consistency_registry::frozen_v1_commitment_family_count` registry=12 / narrative=14 / raw=18 disagreement; R1 will fix) closed in Cycle 0 verification. Standing OPERATOR-ACTION box carries 4 unchanged items (OA-1 MFG CA backup highest-leverage 5-min action, OA-2 private DR runbook ARNs, OA-3 IAM scope-down, OA-4 HSM-rooted MFG Root CA long-term). PV-CI 174/174 PASS unchanged; 0 IOTX; no FROZEN edit, no chain write, no Solidity edit, no firmware edit. Stream-in-flight slot now FREE for Cycle 2.

<a id="note-21"></a>
### 21. 🧩 DECON-1 STREAM 2 PHASE 2.1

NOTE: 🧩 DECON-1 STREAM 2 PHASE 2.1 — FIRST MIXIN EXTRACTED (`ZkbaVpmMixin`) 2026-06-10 — First domain mixin extracted per `audits/decon-store-map.md` partition. `bridge/vapi_bridge/store/zkba_vpm.py` NEW (~595 LOC): `class ZkbaVpmMixin` houses 17 methods spanning 6 tables — `cfss_lane_drift_log` (insert/get), `bt_witness_log` (insert/get_status), `mlga_session_log` (insert/get_status), `zkba_artifact_log` (insert + 3 readers), `vpm_artifact_log` (insert + 3 readers), `physical_data_attestation_log` (insert/get_status/history). `bridge/vapi_bridge/store/_core.py` shrinks 18,794 → 18,123 lines (−672); `class Store(ZkbaVpmMixin):` added; sibling import `from .zkba_vpm import ZkbaVpmMixin` added. **CREATE TABLE statements stay in `_core.py::_init_schema()` per D-DECON-2 sub-decision (4)** — schema definition remains centralized. **F-DECON-2.7 PV-CI path-bookkeeping pattern held** — no `vapi_invariant_gate.py` paths required updates because: (a) the table-CREATE invariants pin `_core.py` and the CREATE statements never moved; (b) the FROZEN primitive compute functions (`compute_zkba_commitment`, `compute_pda_hash`) live in dedicated modules (`zkba_artifact.py`, `physical_data_attestation.py`), not in store. **Verification:** PV-CI 174/174 PASS; 17/17 methods accessible on `Store` via MRO (`hasattr(Store, m)` for all); 94/95 smoke tests pass across 8 test files covering all 6 extracted tables (test_phase26_store + test_migration_runner + test_phase_o3_zkba + test_phase_o4_vpm_bridge_endpoints + test_physical_data_attestation + test_phase_o5_mlga + test_phase242_bt_witness_scaffolding + test_cfss_drift_sweeper_integration); 1 failure (`test_t_zkba_3_domain_tag_unique_in_codebase`) is the pre-existing drift identified during Phase 2.0.5 — `b"VAPI-ZKBA-ARTIFACT-v1"` appears in 5 files in `bridge/vapi_bridge/` (operator_api.py + public_forensic_api.py + mythos_variants.py + _mythos_rename_completeness_DRAFT.py + zkba_artifact.py), test expects only zkba_artifact.py — NOT introduced by Phase 2.1. **Zero call-site churn** (no test file modified). 0 IOTX, no FROZEN edits, no chain writes, no Solidity edits, `CHAIN_SUBMISSION_PAUSED=true` unchanged. Remaining ~12 store-domain extractions + 12 operator_api-domain extractions are mechanically identical to this one — pattern proven, future commits operator-paced. Stream 2 task: COMPLETED-MILESTONE (Phase 2.1 shipped; remaining extractions tracked as separate phases at operator discretion).

<a id="note-22"></a>
### 22. 🔬 DECON-1 STREAM 1 FINAL

NOTE: 🔬 DECON-1 STREAM 1 FINAL — SLITHER BASELINE SCAFFOLD 2026-06-10 — Stream 1 closes with `audits/slither-baseline-2026-06-10.md` triage scaffold + D-DECON-1 provisional default (a) (fail CI on NEW high-severity only; baseline suppressed via `slither.db.json` once populated). **Windows env gate F-DECON-1.X surfaced**: `pip install slither-analyzer==0.10.4 solc-select==1.0.4` failed locally with "Failed to build installable wheels for ckzg" — slither's transitive C-extension dep `ckzg` does not have a pre-built Windows wheel matching the operator machine's Python/MSVC config. CI (Ubuntu) is the authoritative baseline source; operator downloads `slither-report-<sha>` artifact from `.github/workflows/slither.yml` run and pastes triaged rows into the scaffold's triage table per the inline `jq` recipe. Stream 1 deliverables on `main`: `.github/workflows/slither.yml` (workflow), `contracts/slither.config.json` (filter), `audits/slither-baseline-2026-06-10.md` (triage scaffold + D-DECON-1 default posture + reproduction commands). PV-CI 174/174 PASS; 0 IOTX; no FROZEN edits; no Solidity edits. Stream 1 task: COMPLETED (operator triages the CI artifact at their leisure; the actual enforcement-posture switch is a 1-line follow-up commit removing `continue-on-error: true`).

<a id="note-23"></a>
### 23. 🎯 DECON-1 STREAM 4 FINAL

NOTE: 🎯 DECON-1 STREAM 4 FINAL — MYTHOS VARIANT #15 + AUDIT CORRIGENDUM 2026-06-10 — Stream 4 closes with: (a) audit corrigendum F-DECON-4.5 (the verify-pass's "0 dedicated test files = DORMANT-UNDOCUMENTED" signal was too weak — both candidates were actually ACTIVE: `age_weight_analysis_agent` is agent #24 with live `/agent/age-weight-analysis-status` endpoint + config default True; `protocol_intelligence_record_agent` is string-listed in `protocol_coherence_agent.py:99` sweep loop); (b) Mythos variant #15 `mythos_agent_utility_honesty` institutionalized in `bridge/vapi_bridge/mythos_variants.py` (~120 LOC, fail-open, COVERAGE_BOUNDARY pattern matches variant #14). Rubric: an agent flags DORMANT-UNDOCUMENTED only when ALL FOUR signals empty AND CLAUDE.md silent — (1) direct test imports `\b<stem>\b` in `bridge/tests/*.py`; (2) operator_api refs `\b<stem>\b` in `operator_api.py`; (3) peer-module string-roster refs `"<stem>"` in any sibling `*.py`; (4) `<stem>_enabled = True / getattr` in `config.py`. 6 SUPPORT files explicitly excluded (`agent_auth`, `agent_message_bus`, `agent_registration`, `mock_agent_registration`, `agent_commit`, `agent_review_emitter`). **Smoke test against live fleet: 50 candidates audited, 1 COVERAGE_BOUNDARY finding, 0 DORMANT-UNDOCUMENTED findings — fleet is clean.** No agent retirements happen this stream (correction made F-DECON-4.1 + 4.2 obsolete). PV-CI 174/174 PASS; 0 IOTX; no FROZEN edits; no chain writes. Stream 4 task: COMPLETED.

<a id="note-24"></a>
### 24. 🤖 DECON-1 STREAM 4 VERIFY PASS

NOTE: 🤖 DECON-1 STREAM 4 VERIFY PASS — AGENT-UTILITY AUDIT 2026-06-10 — First systematic application of the protocol's own honesty rails to its own agent fleet. `audits/agent-utility-audit-2026-06-10.md` NEW (195 lines, READ-ONLY VERIFY PASS — Mythos variant #15 code institutionalization deferred to follow-up commit per D-DECON-11 recommendation b + recursive-verification-first discipline). 56 `*agent*.py` files enumerated by LOC + last-mod date + test-file count (`grep -lE "\b<stem>\b" bridge/tests/*.py`). **Classification:** 27 ACTIVE-VALUABLE (incl. `bridge_agent` 82 tests / `fleet_signal_coherence_agent` 17 / `ruling_enforcement_agent` 9 / Operator Initiative trio 4-6 each) + 9 ACTIVE-LOW-SIGNAL + 11 DORMANT-DOCUMENTED (each named in CLAUDE.md hard rules with explicit `_enabled=False` flag) + **2 DORMANT-UNDOCUMENTED FINDINGS** (`protocol_intelligence_record_agent` + `age_weight_analysis_agent`, both 0 test files, neither named in CLAUDE.md as live OR dormant — violates `"live": false` discipline) + 7 SUPPORT excluded. **F-DECON-4.1 / F-DECON-4.2 surfaced as per-agent findings**, NOT retired in this stream. **F-DECON-4.3 surfaced**: CLAUDE.md says "29 standalone + 3 stewards = 38-ID roster" but 49 fleet-candidate modules exist; the gap is steward-facade splitting (Sentry/Guardian/Curator each = 3 modules) + absorbed-agents-as-classes (`operator_steward_absorbed_agents.py` houses 9). **F-DECON-4.4**: any retirement must cross-check against the on-chain 38-ID roster in `agent_registration.py::_AGENT_IDS` before delete — on-roster agents are cryptographically irreversible. **Decisions surfaced (deferred to operator):** D-DECON-10 retirement policy (recommend: stream closes here, retirements as follow-on commits at operator discretion); D-DECON-11 Mythos #15 timing (recommend: follow-on commit after operator reviews audit); D-DECON-12 ACTIVE classification test-count floor (recommend: 2 tests; <2 = AT-RISK). PV-CI 174/174 PASS; 0 IOTX; no FROZEN edits; no chain writes; no Solidity edits.

<a id="note-25"></a>
### 25. 🗺️ DECON-1 STREAM 5

NOTE: 🗺️ DECON-1 STREAM 5 — ARCHITECTURE.md (101 LINES, EXTERNAL ON-RAMP) ADDED 2026-06-10 — First external-facing one-page architecture reference at repo root. `ARCHITECTURE.md` NEW (101 lines, under the prompt's ≤120 cap): one ASCII pipeline diagram (controller → bridge → IoTeX contracts → marketplace/consent); one 8-row FROZEN-v1 family table (PoAC wire / GIC / WEC / CORPUS-SNAPSHOT / BIOMETRIC-SNAPSHOT / CONSENT / FRR / TEMPORAL-BEACON) with 6 more families named in a footnote line + `grep` reproduction command; one contract-count table sourced from `contracts/deployed-addresses.json` AT WRITE TIME (NOT memory — 80 .sol sources vs 68 deployed addresses vs ~55 substantive per README headline); 5-numbered honest-status paragraph (testnet-only, N=3 corpus / AIT 1.199 N=37 / all_pairs_p0_ok FAILS, Stage A measurement gates OPEN, no external Solidity audit yet, no fiat/token launch); pointers into deep docs (CLAUDE.md / wiki/methodology / wiki/assessments / wiki/phases / disaster-recovery-runbook.md / decon-store-map.md / whitepaper v4). Verification: 101 lines (≤120 ✓); manual Mythos #14 variant check against `doc_consistency_registry.py` REGISTRY (9 canonical facts) — ARCHITECTURE.md uses ONLY current canonical values (N=37 AIT, 29+3 fleet, Mythos #14), zero superseded values (10/11/12/24/35/36/38/153/217/4330 etc.) appear; no test count cited (avoided the bridge_test_count drift trap entirely); PV-CI 174/174 PASS unchanged. **Pre-existing drift surfaced (NOT fixed in Stream 5)**: `doc_consistency_registry.py::frozen_v1_commitment_family_count.current_value="12"` is stale — CLAUDE.md's Arc 6 NOTE says "FROZEN-v1 #14 VAPI-TEMPORAL-BEACON-v1 (13→14)". Registry needs `current_value="14"` + supersede "12" + "13". Out of scope for Stream 5 (doc-only); future registry-maintenance commit. 0 IOTX, no FROZEN edits, no chain writes, no Solidity edits. Stream 4 (agent-utility audit) pending; Stream 2 Phase 2.1 (ZkbaVpmMixin) paused for fresh-session re-entry.

<a id="note-26"></a>
### 26. 🚨 DECON-1 STREAM 3

NOTE: 🚨 DECON-1 STREAM 3 — DISASTER-RECOVERY RUNBOOK + WAL-SAFE BACKUP 2026-06-10 — First DR runbook for QorTroller; closes the "single-machine, single-operator" concentration named in the DECON-1 thesis. `docs/disaster-recovery-runbook.md` NEW (≤200 lines, public): §0 pre-incident checklist (7 OPERATOR-MUST-CONFIRM items, item 1 backs up the 517 B `qortroller_foundation_mfg_ca.json` to password manager + encrypted USB — minimum reversible interim mitigation for the F-DECON-3.2 single-copy-CA finding); §1 wallet key custody (`0x0Cf36dB57f…` continuity-first, never rotate as shortcut); §2 bridge DB backup/restore (the canonical DB is `~/.vapi/bridge.db` 1.5 GB; the repo-local `bridge/vapi_store.db` is orphaned); §3 AWS KMS recovery (D-DECON-6 chose region+alias public / full ARNs in `disaster-recovery-runbook.private.md` gitignored — reveals AWS account ID otherwise); §4 GitHub App OAuth (Guardian/Sentry git-lane authority); §5 8-check post-restore assertion table (PV-CI gate, wallet derive, DB row counts, contract count, MFG CA round-trip, KMS reachable, GIC chain tip, watchdog chain); §6 MCP re-provisioning; §7 Pinata/IPFS JWT rotation; §8 quarterly drill cadence. `scripts/backup_store.py` NEW (sqlite3 online `.backup()` API — NOT file copy, WAL-safe; `--verify` row-count check on 8 representative tables incl. all FROZEN-v1 primitive logs `corpus_snapshot_log`/`biometric_snapshot_log`/`agent_commit_log`/`watchdog_event_log`; `--retain-days` pruning; default dst `~/.vapi/backups/bridge-<unix-ts>.db`). `.gitignore` extended: `docs/disaster-recovery-runbook.private.md` + `backups/` + `bridge-*.db` (defensive layer; default backup path is already outside the repo). **Two CRITICAL findings surfaced (NOT solved unilaterally per Stream 3 discipline):** (a) F-DECON-3.2 — MFG Root CA single-copy software-backed key on one Windows machine; long-term fix = HSM-backed CA + device cert re-issuance (months out); interim = item 1 above. (b) F-DECON-3.5 — KMS credential IAM-scope hardening finding; file/privilege/impact specifics held in `docs/disaster-recovery-runbook.private.md` (gitignored) per F-EXT-1, NOT in any public artifact. Hardening direction tracked privately. Decisions resolved: D-DECON-6=B (region+alias public, full ARN private), D-DECON-7=A (surface CRITICAL not solve), D-DECON-8=A (daily local + weekly off-site, retain 30d), D-DECON-9=B (operator picks off-site target in private companion). Verification: backup script tested against `bridge/vapi_store.db` (1.9 MB, 0.04 s, 8/8 verify tables match); PV-CI 174/174 PASS unchanged; gitignore patterns verified via `git check-ignore -v` (3/3 expected ignores fire, runbook + script correctly tracked). 0 IOTX, no FROZEN edits, no chain writes, no Solidity edits. Stream 2 Phase 2.1 (ZkbaVpmMixin extraction) remains paused for fresh-session re-entry per `[[recursive_verification_first_pattern]]` discipline.

<a id="note-27"></a>
### 27. 📦 DECON-1 STREAM 2 PHASE 2.0.5

NOTE: 📦 DECON-1 STREAM 2 PHASE 2.0.5 — `vapi_bridge.store` MODULE → PACKAGE 2026-06-09 — Recursive-verify pass for Stream 2 (smallest de-risk slice before any method extraction). `bridge/vapi_bridge/store.py` → `bridge/vapi_bridge/store/_core.py` via `git mv` (history preserved); new `bridge/vapi_bridge/store/__init__.py` re-exports `Store` + `CorpusRegressionError` + `PoACRecord` + `STATUS_*` constants + `log` byte-identically. Two derivative fix-ups: (a) 10 in-method single-dot relative imports inside `_core.py` (`from .codec` / `.migrations.runner` / `.continuity_prover` / `.active_play_occupancy` / `.consent_categories` ×3 / `.grind_chain` / `.watchdog_chain` ×2) promoted to double-dot (parent package); (b) 14 `file="bridge/vapi_bridge/store.py"` strings in `scripts/vapi_invariant_gate.py` retargeted to `store/_core.py` — content-pinned digests stay identical (`_hash_file_region` SHA-256s matched lines, not paths; gate file structure was the only path-bookkeeping touch). **F-DECON-2.7 (gate ceremony NOT required)**: per `vapi_invariant_gate.py:1546-1552` digest = SHA-256(matched-line content), so moving content into `_core.py` byte-identical leaves all 14 store-pinned invariants (`INV-003`, `INV-004`, `INV-022`, `INV-024`, `INV-025`, `INV-OPERATOR-AGENT-002/003/006`, `INV-O3-SUPERSEDE-003`, `INV-SS2-PROBE-TYPE-001`, `INV-MLGA-STORE-TABLE-001`, `INV-VHR-WIRING-003`, plus 2 more) PASS without `--confirm-governance`. Verification: PV-CI 174/174 PASS; 60-test smoke pass (test_phase26_store + test_migration_runner + test_continuity_prover + test_operator_api + test_consent_event_log); full bridge suite confirmation deferred (background run `bc6nuq5n6` outstanding ~25+ min — known Windows pytest friction; smoke + PV-CI sufficient for the mechanical move per loop step 4); **zero call-site churn** (all 318 importing files unchanged). FROZEN-v1 primitives (`corpus_snapshot.py`, `biometric_snapshot.py`, `agent_commit.py`, `grind_chain.py`, `watchdog_chain.py`) DEFERRED per D-DECON-3. Decisions D-DECON-2 (12+12 domain partition) / D-DECON-4 (one commit per domain) / D-DECON-5 (register-function pattern over APIRouter for `operator_api`) APPROVED by operator. Phase 2.1+ extracts one leaf domain per commit (next: `ZkbaVpmMixin`). 0 IOTX, no FROZEN edits, no deploys, no Solidity edits. Map: `audits/decon-store-map.md`.

<a id="note-28"></a>
### 28. 🔍 DECON-1 STREAM 1

NOTE: 🔍 DECON-1 STREAM 1 — SLITHER CI GATE (REPORT-ONLY) ADDED 2026-06-09 — First Solidity static-analysis surface in CI. `.github/workflows/slither.yml` NEW (separate file per existing convention alongside `vapi-invariant-gate.yml` / `vapi-mythos-pr-gate.yml` / `vapi-path-scope-gate.yml`; does NOT mutate `ci.yml`): triggers on PRs touching `contracts/**` + push to main/develop + `workflow_dispatch`; pinned `slither-analyzer==0.10.4` + `solc-select==1.0.4` against the existing `0.8.20` hardhat.config.js pin; `continue-on-error: true` first pass per DECON-1 loop (REPORT-ONLY). `contracts/slither.config.json` NEW: filters `node_modules|Mock|mock|test/|tests/|circuits/` so the baseline reflects production contracts only; detectors-to-skip list intentionally empty pre-triage. CI publishes `slither-latest.json` + `slither-latest.md` as artifacts (90-day retention). NEXT: first CI run on push produces baseline artifact → operator downloads → second commit lands `audits/slither-baseline-2026-06-09.md` with triage columns (TP/FP/AR) → D-DECON-1 selects enforcement posture (recommend high-only). 0 IOTX, no FROZEN edits, no deploys, no Solidity edits — additive-only proof-of-loop for the DECON-1 initiative. PV-CI baseline 174 unchanged (gate locally PASS pre-commit).

<a id="note-29"></a>
### 29. 🧱 W3BSTREAM RUST COMPUTE SANDBOX BUILT 2026-05-31 (`4ddc7917`)

NOTE: 🧱 W3BSTREAM RUST COMPUTE SANDBOX BUILT 2026-05-31 (`4ddc7917`) — First Rust/Wasm surface in the repo. `w3bstream/applet/` (Cargo crate compiling to `wasm32-unknown-unknown`) is a native W3bstream message handler that re-implements the off-chain ingestion validation rules in a sandboxed Wasm applet: parses an `EvmLogPayload` JSON, enforces ANCHOR_CADENCE=64 block-cadence alignment (`block_number % 64 == 0`, INV-W3S-001), blockhash temporal rules, and clean-environment isolation (pops `OPERATOR_PRIVATE_KEY` in the Python ingestion listener, INV-W3S-002). `w3bstream/sandbox_config.json` pins the sandbox manifest with `mechanisms` explicitly asserting `frame_grabbing=false` / `optical_capture=false` / `blockhash_temporal_rules=true` — mechanical input-validation only, no biometric capture inside the sandbox. `scripts/test_w3bstream_ingestion.py` (+56 lines) exercises the ingestion path; `scripts/vapi_invariant_gate.py` registers the new W3S invariants. 0 IOTX, no FROZEN edits, no deploys.

<a id="note-30"></a>
### 30. 🛡️ DATA ECONOMY ARC 7 (Decoupled Cryptographic Sidecar Pointer + Post-Quantum Commitments) BUILT 2026-05-31 (`65ad5560` Thread-C offload → `4a570e6c` 

NOTE: 🛡️ DATA ECONOMY ARC 7 (Decoupled Cryptographic Sidecar Pointer + Post-Quantum Commitments) BUILT 2026-05-31 (`65ad5560` Thread-C offload → `4a570e6c` sidecar+PQ) — Adds a post-quantum signature layer to the VHR replay-proof pipeline **without growing the on-chain footprint**, via the **Decoupled Cryptographic Sidecar Pointer** pattern: the bulky 3,309-byte ML-DSA-65 signature lives off-chain on a DePIN DA node; only a 32-byte commitment hash (the pointer) crosses the on-chain / wire boundary. **Pipeline:** `replay_proof_pipeline/da_layer.py` NEW — `MockDAScheme` / `da_router` mock off-chain DePIN storage keyed by 32-byte commitment, stores/serves the 3,309-byte payload (size-checked). `replay_proof_pipeline/pipeline.py` — `VHRProofPackage` gains `pq_commitment` (hex 32B) surfaced in `to_listing_payload`; `_run_mldsa_signing(matrix)` serializes the SanitizedReplayMatrix to deterministic sorted-key JSON → SHA-256 → 32B commitment, then `quantcrypt.dss.MLDSA_65().keygen()/sign()` produces the 3,309-byte signature uploaded to `da_router`. **Thread-C offload (INV-VHR honesty + event-loop discipline):** the ML-DSA signing runs via `await asyncio.to_thread(_run_mldsa_signing, matrix)` so the PQ crypto never blocks the ingestion event loop (parallels the Arc 5 numbed-hot-path rule). **On-chain reach (NOT deployed — deploy-hold):** `VAPIReplayProofVerifier_v2.sol` `verifyWithRecency`/`verifyWithRecencyView` + `IVAPITemporalBeaconRegistry.verifyBeacon` extended to take a `bytes32 pqCommitment` argument; `chain.verify_temporal_beacon(block, hash, pq_commitment)` + `_TEMPORAL_BEACON_ABI` updated to match. **Wasm enforcement:** `w3bstream/applet/src/lib.rs` `EvmLogPayload` gains `pq_commitment`; `resolve_da_proof()` fail-closes (returns Err) on empty/zero-padded commitments and on malformed (non-64-hex) commitments, mock-resolving a 3,309-byte DA payload only for well-formed pointers. **Invariants (3 new):** INV-ARC7-001 (final serialized PoAC wire frame STRICTLY 228 bytes — `HardForkDisallowedError` if any PQ routine alters the packet footprint; the sidecar pattern's whole point), INV-ARC7-002 (EVM-layer `require` blocks null `bytes32(0)` PQ signatures), INV-W3S-005 (Wasm handler enforces non-zero PQ-commitment validation). **Deltas:** +3 bridge tests (`bridge/tests/test_arc7_pq_sidecar.py`); new dep `quantcrypt` (ML-DSA-65); PV-CI invariants registered into the 174 baseline; 0 IOTX; `CHAIN_SUBMISSION_PAUSED=true` unchanged; 228-byte PoAC + all 14 FROZEN-v1 tags untouched; v2 wrapper signature change is code-side only (v2 was never deployed — still HELD pending operator GO + Arc 5 ceremony).

<a id="note-31"></a>
### 31. 🧮 ARC 6 ADDITIVE

NOTE: 🧮 ARC 6 ADDITIVE — `posr.cadence_aligned_block(N)` helper landed 2026-05-30 — pure function returning largest `≤N` block that's a multiple of `ANCHOR_CADENCE_BLOCKS=64`; consumed by `scripts/anchor_beacon.py` keeper + any off-chain verifier reconciling session boundaries with registry cadence; 6 tests (T-CADENCE-1..6); 0 IOTX, no FROZEN edits, no deploys. First end-to-end demo run of the `spec-to-ship` autonomous-loop agent (`.claude/agents/spec-to-ship.md`); spec at `specs/demo-cadence-helper.md`.

<a id="note-32"></a>
### 32. ⏱️ DATA ECONOMY ARC 6 (PoSR

NOTE: ⏱️ DATA ECONOMY ARC 6 (PoSR — Proof of Session Recency) BUILT 2026-05-30 — Five-commit additive arc closing 3 attack surfaces Arc 5 left open: stale re-listing, pre-computation drip, tournament-window backdating. Binds session open/close to verifiable IoTeX block hashes; the proof can no longer claim recency it doesn't have. **FROZEN-v1 #14** `VAPI-TEMPORAL-BEACON-v1` (13→14 — new PATTERN-017 family; does NOT overload any Arc 5 invariant). **Commit 1** `VAPITemporalBeaconRegistry.sol` (Ownable + keeper-set; BEACON_DOMAIN keccak256-pinned by INV-TBR-001; ANCHOR_CADENCE=64 by INV-TBR-002; anchor/verify/latestBeacon view surface; 10 Hardhat tests T-TBR-1..10) + estimate-only deploy script with triple-gate (VAPI_TBR_DEPLOY_CONFIRM=1, 1.0 IOTX hard-cap, 2× balance guard). **Commit 2** `bridge/vapi_bridge/replay_proof_pipeline/posr.py` — SHA-256 sidecar commitment math per spec §1.2/§1.3 with the INSEPARABILITY claim (close chains to open via INV-POSR-002); `PoSRBeaconBinder` fail-closed honesty rail (chain=None / registry-unset / RPC-error / no-anchor → None → fall back to v1 Arc 5 behavior, no fabrication); `chain.py` + `_TEMPORAL_BEACON_ABI` view path; Config knob `temporal_beacon_registry_address` (env TEMPORAL_BEACON_REGISTRY_ADDRESS, default ""). 15 Python tests. **Commit 3** `contracts/circuits/VAPIReplayProofVerifier_v2.circom` — additive: Arc 5 constraints copied verbatim + PoSR adds Poseidon-4 openCommit + Poseidon-4 closeCommit (chains to openBeaconCommitment) + Num2Bits-32 temporal ordering. Compile P-check: 1185 non-linear constraints (vs 553 v1; pot15 ceiling 32,768 = 28× headroom). 9 public + 5 private + 1 output. Pinned by INV-POSR-CIRCUIT-001. WitnessGenerator.build_inputs_v2 14-field dict with close>open fail-fast guard. **Commit 4** `VAPIReplayProofVerifier_v2.sol` wrapper — PROOF_TYPE keccak256("VAPI-REPLAY-PROOF-v2") (INV-VHR-V2-001, NEW discriminator NOT overload of INV-VHR-003); 10-element publicInputs with INPUT_* index constants (output-first then 9 publics matching circuit); verifyWithRecency forwards Groth16 + double-checks temporal ordering + verifyBeacon against registry × 2 (closes loop between in-circuit Poseidon and on-chain anchored hash); ReplayProofVerifiedV2 event; custom errors; verifyWithRecencyView pure variant; mocks + 7 Hardhat tests T-VHR-V2-1..7 + estimate-only deploy script with VAPI_VHR_V2_GROTH16_ADDR + VAPI_TBR_ADDRESS env required. **Commit 5** keeper `scripts/anchor_beacon.py` (operator-fired cron, idempotent, dry-run by default, 0.005 IOTX/anchor hard-cap, single-shot OR --loop) + `docs/posr-keeper-runbook.md` (operational reality + cost math + health checks + failure modes) + this NOTE + memory `project_arc6_posr_built`. **EMPIRICAL DRIFT D6-1 RESOLVED at runbook level**: spec assumed 5s blocks → 21min BLOCKHASH window → 0.135 IOTX/day keeper cost; empirical IoTeX testnet (2026-05-30 20338-block sample) = **2.616 s/block** → BLOCKHASH window **11.2 min** (NOT 21.3) → 64-block cadence = **2.8 min/anchor** (NOT 5.3) → keeper cost **~0.258 IOTX/day** (~2× spec; ~7.7 IOTX/month). Architectural soundness preserved (registry approach works at any block rate); runbook documents the real numbers. **OPERATOR DECISIONS made** (T1=Option B operator cron from bridge wallet — Curator at O1_SHADOW Cedar bundle does NOT include beacon-anchoring; T2=Option A block-hash registry — IoTeX has no on-chain VRF precompile; T3=COEXIST — v1+v2 both callable, recency opt-in until tournament operator requires). **Cumulative deltas:** PV-CI 161→**167** (+INV-TBR-001/002, INV-POSR-001/002, INV-POSR-CIRCUIT-001, INV-VHR-V2-001 = 6 new); +15 Python posr tests + 17 Hardhat Arc 6 tests (10 registry + 7 v2 wrapper); 0 IOTX spent (all work code-side); CHAIN_SUBMISSION_PAUSED=true unchanged; 228-byte PoAC + all 13 prior FROZEN tags untouched; Arc 5 v1 verifier byte-identical. **Held pending operator GO:** (a) Registry deploy (~0.4 IOTX), (b) v2 ceremony contribution (operator interactive), (c) v2 wrapper deploy with VAPI_VHR_V2_GROTH16_ADDR + VAPI_TBR_ADDRESS + VAPI_VHR_V2_DEPLOY_CONFIRM=1. Memory: `project_arc6_posr_built`.

<a id="note-33"></a>
### 33. 🎞️ DATA ECONOMY ARC 5 (VHR

NOTE: 🎞️ DATA ECONOMY ARC 5 (VHR — Verified Human Replay) BUILT 2026-05-29 — Five-commit arc lands the spec-only Arc 5 end-to-end in code under the deploy-hold (0 IOTX spent, no deploys fired, no FROZEN-v1 surfaces edited, `CHAIN_SUBMISSION_PAUSED=true` unchanged). All commits uncommitted on `feat/gameplay-workflow-layer` pending operator review. **Commit 1** `bridge/vapi_bridge/replay_proof_pipeline/pre_processor.py` (φ = φ_spatial ∘ φ_temporal: 1002 Hz → 60 Hz median window; stick → 4-bit radial sector + neutral; trigger → floor(v/16) 4-bit; IMU → 3-bit gravity-sign octant; `FORBIDDEN_COLUMNS` data-floor frozenset fail-closes on `l4_mahalanobis_distance` / `l5_cv` / `e4_spectral_entropy` / `ait_rms`; 11 tests). **Commit 2** `contracts/circuits/VAPIReplayProofVerifier.circom` (553 non-linear constraints, genuine pot15 reuse) + `witness_generator.py` (compute_h_gap + ×1000 scaling + circuit-input assembly; 35 tests). **Drift D-8 (circom 1.x `signal private input` syntax) + D-9 (in-circuit `Poseidon(NB_TICKS*6)` = `Poseidon(1800)` infeasible — circomlib t≤17, ~540k constraints vs pot15's 32,768 ceiling, would require pot20+ ceremony contradicting "PTAU reuse") RESOLVED at Commit 2**: all signals `signal input` with privacy via `component main {public [...]}`; `sanitizedTraceRoot` is a PUBLIC input (off-circuit matrix commitment — matrix already non-invertible by φ, so re-hashing in-circuit was both impossible AND conceptually wrong; verifier recomputes Poseidon-sponge root off-chain). **Commit 3** `contracts/contracts/VAPIReplayProofVerifier.sol` wrapper (FROZEN `PROOF_TYPE = keccak256("VAPI-REPLAY-PROOF-v1")` per `INV-VHR-003`; 6-element snarkjs publicInputs in output-first order with `INPUT_*` index constants `INV-VHR-005`; `ReplayProofVerified` event emits full tuple) + `MockVAPIReplayProofGroth16Verifier.sol` + `scripts/deploy-vapi-replay-proof-verifier.js` (estimate-only triple-gate: deployer==bridge wallet `0x0Cf36dB57…`, 1.0 IOTX hard-cap, 2× balance guard, Groth16 address env-required no zero-default, `VAPI_REPLAY_PROOF_DEPLOY_CONFIRM=1` required) + 9 Hardhat T-VHR-1..9. **Commit 4** `replay_proof_pipeline/pipeline.py` (`VAPIReplayProofPipeline` 6-step orchestrator with 11 outcome codes + `Prover` Protocol + `DeferredProver` honest no-op default — no fake proofs) + Arc 4 `VAPIConsentManifestRegistry.sol` struct **extended in-place** with 4 Dimension 8 fields (`allowReplayProofs` default false, `replayHumanityThreshold` ×100 default 70, `replayQuantizationBits == REPLAY_QUANTIZATION_BITS_FLOOR = 4` pinned to `INV-VHR-001` `RADIAL_BITS = 4` per `INV-VHR-007`, `replayRequireVerdict` default true), `_computeManifestHash` extended, `chain._CONSENT_MANIFEST_ABI` updated (Dimension 8 indices 15-18 inserted; shifts updatedAt/manifestHash to 19/20); 21 orchestrator tests + 8 new Hardhat T239-CM-D8-1..8. **Drift D-2 (consentPolicyHash binding) + D-3 (Dimension 8 architecture) RESOLVED at Commit 4** — D-2: orchestrator reads `chain.get_consent_manifest(gamer_address)` → Arc 4 `getManifest(address)` (gamer-address-keyed, NOT `VAPIConsentRegistry.manifestHash(deviceId)`); contract wrapper deliberately does NOT bind on-chain (consent enforcement at orchestrator/listing layer). D-3: operator-fired choice 2026-05-29 to extend Arc 4 struct in-place (vs separate v2 contract or versioned slot) since Arc 4 is not yet deployed. **Commit 4.5** Curator hook (`on_session_complete_vhr` parallel session-boundary entry orthogonal to skill-proof path; lazy `VAPIReplayProofPipeline` construction keeps numpy off the skill-proof hot path until `replay_proof_pipeline_enabled` flips; pinned by `INV-VHR-009`) + `store.get_pending_replay_proofs(limit)` outcome-filtered ts_ns-DESC over `curator_packaging_log` + `GET /curator/pending-replay-proofs` HTTP endpoint mirroring `/pending-listings` auth pattern + Config flags `replay_proof_pipeline_enabled` (env `REPLAY_PROOF_PIPELINE_ENABLED` default False) + `replay_proof_verifier_address` (env `REPLAY_PROOF_VERIFIER_ADDRESS` default "") + 11 hook/endpoint tests including dormancy + lazy construction + existing-flow-unaffected regression guard. **Commit 5** updates `docs/data-economy-deploy-hold-and-arc5-readiness.md` (Arc 5 row SPEC ONLY → BUILT, PV-CI 144→153, D-2/D-3/D-8/D-9 marked RESOLVED with resolution noted in each finding, ceremony-gate documented) + this NOTE + memory `project_arc5_replay_proof_pipeline_built` + MEMORY.md index. **Cumulative deltas:** PV-CI **144→153** (+9 `INV-VHR-001..009`); +73 Arc 5 Python tests + 17 Arc 5 Hardhat tests; full Hardhat 743 passing / 13 failing — same 13 pre-existing failures as pre-Arc-5, no regressions; wallet **9.120 IOTX unchanged**; deploy-hold posture preserved (Arc 2/4/5 still HELD pending operator GO + Arc 5 trusted-setup ceremony). **Two sequential gates remain before live VHR operation:** (1) snarkjs `groth16 setup` → contribute → `zkey export solidityverifier` producing the inner `Groth16VerifierVAPIReplayProof.sol` whose address feeds the wrapper constructor (parallels Phase 237 ZK-SEPPROOF ceremony precedent); (2) circomlibjs node helper computing public Poseidon commitments byte-identically to in-wasm Poseidon (parallels `compute_inputs_zksepproof.js` from Phase 237). Until both land, `DeferredProver` returns `vhr_proof_deferred` honestly — no fake proofs. Memory: `project_arc5_replay_proof_pipeline_built`.

<a id="note-34"></a>
### 34. 🔐 PATH A ARC 1 COMPLETE

NOTE: 🔐 PATH A ARC 1 COMPLETE — SILICON-ROOTED IPACT RENEWAL AUTHENTICITY DEPLOYED + DEMONSTRATED 2026-05-26/27 — Five-commit arc lands the complete protocol side of Path A (the silicon-rooted upgrade to Phase 3 Path B host-key signing); Arc 2 (real ATECC608A hardware integration) is the prompt-ready handoff at `docs/path-a-arc2-prompt.md`. **Two on-chain deploys + one demonstrated device ceremony, all on IoTeX testnet chainId 4690:** (1) **VAPIManufacturerDeviceRegistry** `0x2e5B5FB110890f498e289E3045d0f54Cfb0F91b0` (Commit 2, tx `0x07efc58a…` block 44019764, gasUsed 867045, cost 0.867 IOTX, bytecode 5236 hex) — manufacturer-authoritative device birth registry, deliberate trust-model divergence from VAPIPoEPRegistry (gamer-sovereign); FROZEN signingPath enum {1=A silicon, 2=B host} + proofTier enum {1=FULL CFI-ZCP1, 2=STANDARD CFI-ZCT1, 3=BASIC} pinned by INV-MFG-001/002. (2) **VAPIProtocolLensV2** `0x32Bf1A01a0a2629955A3Fd5ce74c0571DAd7C989` (Commit 4, tx `0x13acf23a…` block 44028226, gasUsed 605750, cost 0.606 IOTX, bytecode 3068 hex) — strictly-additive lens v2: adds `isFullyEligible_PathA(bytes32)` + `getDeviceTier(bytes32)` to v1's `isFullyEligible` (byte-for-byte preserved); 5 immutable addresses (4 oracles + MFG); replace-posture mirrors SeparationRatioRegistry 2026-05-24 precedent (v1 `0x1972bf75…` preserved as `VAPIProtocolLens_v1_superseded`, still callable); function surface pinned by INV-LENS-V2-001. (3) **First device registered on-chain** (Commit 4 D-3E demo, tx `0x68f6cf49564ed2b193d00e881e5cc9488111a8bc05951c2f2af55e25050ac9c0` block 44028531, gasUsed 192373, cost 0.192 IOTX) — the real DualShock `581a836c98b3a1b6c0f598bfca88e6a3cc3bd7c34591b506692cb40ddf66a9f8` registered as Path B FULL signed by the QorTroller Foundation reference-implementation ManufacturerRootCA (P-256, software-backed at `~/.vapi/qortroller_foundation_mfg_ca.json`); `verify_device_cert.py` returns VERDICT VALID against the live registry (cert sig OK + on-chain birthCertHash `0x8faf67302a3c01b6d77f44456a7e5dc612d0b1e300bb048e8ce92352c80abe2d` byte-match + isActive=True). **Code surface (15 new files + 10 modifications across 5 commits `fd16e7ea`→`62718567`):** `bridge/vapi_bridge/signing_backends/` package (SigningBackend Protocol + HostKeyBackend wrapping the prior composite_device_identity logic byte-identically + SecureElementBackend stub that raises NotImplementedError until Arc 2 wires real silicon — no phantom Path A proofs); `composite_device_identity.py` refactored to thin shims preserving every public function; `bridge/vapi_bridge/device_birth_cert.py` (DeviceBirthCertificate dataclass + dual canonical-bytes methods + sign_cert + verify_cert); `bridge/vapi_bridge/manufacturer_root_ca.py` (wraps SoftwareIdentityBackend; partner-HSM swap = one line); `bridge/vapi_bridge/controller_models.py` (shared KNOWN_MODELS reverse-lookup); `scripts/provision_device_mfg.py` (3-mode ceremony: dry-run / estimate-only / --execute MFG_REGISTER_CONFIRM=1); `scripts/verify_device_cert.py` (4-exit-code audit tool: VALID/INVALID/NOT_REGISTERED/ERROR); `docs/path-a-manufacturing-spec.md` (8-section partner-facing spec); `docs/path-a-arc2-prompt.md` (Arc 2 hardware-integration prompt, gated on ATECC608A breakout + CH341A USB-I2C being physically connected); chain.py gains 5 view methods (get_device_signing_path, get_proof_tier, is_path_a, is_active_in_mfg_registry, get_device_controller_model, is_fully_eligible_path_a, get_device_tier_from_lens); GET /player/session-status gains 4 new fields (signing_path, proof_tier, controller_model, path_a_eligible) with honest dormant defaults. **Honesty rails preserved throughout:** Path A v1 SCOPE = silicon-rooted iPACT renewal authenticity ONLY (per-PoAC record silicon-root reserved for Path A v2 / Arc 3+, per operator decision D-α 2026-05-26); SoftwareIdentityBackend "INSECURE/DEV ONLY" warning fires loud every cert sign (production partner ceremonies MUST replace the QorTroller self-signed reference root CA with hardware HSM); SecureElementBackend will only construct when Arc 2 wires real ATECC608A — no silent fallback to host key. **P-checks:** Arc 1 added 34 tests across the 5 commits (T-SB-1..6 / T-MFG-1..8 / T-VMDR-CHAIN-1..4 / T-DBC-1..6 / T-LENS-V2-1..6 / T-LENS-V2-CHAIN-1..4); PV-CI invariants 32→35 (+INV-MFG-001 / +INV-MFG-002 / +INV-LENS-V2-001); mythos_crypto_drift 0 throughout; 1 bonus test-isolation fix (PSS test was stubbing web3 too aggressively — caught during Commit 3 regression, fixed). Wallet 10.786 → 9.120 IOTX (1.666 spent across 3 on-chain operations across Arc 1; ~5.0× remaining headroom). Memory: `path_a_arc1_complete` (Arc 1 closing index). Next: Arc 2 hardware-gated; until then operator can register additional devices via `provision_device_mfg.py --execute MFG_REGISTER_CONFIRM=1` (~0.2 IOTX per device).

<a id="note-35"></a>
### 35. 🔑 GUARDIAN AUTONOMOUS KMS-HSM SIGNING (Tier-1) + ON-CHAIN ANCHOR (Tier-2) 2026-05-20/21

NOTE: 🔑 GUARDIAN AUTONOMOUS KMS-HSM SIGNING (Tier-1) + ON-CHAIN ANCHOR (Tier-2) 2026-05-20/21 — First non-human Operator steward to autonomously produce an HSM-rooted signature AND anchor a commitment to it on IoTeX. **Tier-1 (commit `7708d60c` + strictly-once `ccdc9063`)**: the live PATH-B v2 executor autoloop, on a clean bridge restart, autonomously signed Guardian's operator-accepted `kms-sign` draft via real AWS KMS HSM (`ECC_SECG_P256K1`/secp256k1), KMS-verified + independent-local-verified, at **0 IOTX** (off-chain — that's WHY it runs with the chain kill-switch on). Proven **strictly-once** across two independent runs (drafts #155 + #690 each signed exactly once; draft #2's 2-sig is the documented pre-fix double-sign, root-fixed by de-dup spawn + atomic `claim_draft_for_execution`). **Tier-2 (commit `11761759`, operator-fired one-shot — NOT autonomous)**: 32-byte commitment `SHA-256(b"VAPI-GUARDIAN-SIG-ANCHOR-v1" || guardian_pubkey_der || digest || sig_der)` anchored on AdjudicationRegistry `0x44CF981f46a52ADE56476Ce894255954a7776fb4` — tx `0x1e868a80bc56ff9fa8461b31414717dc564c0967a7835e46e3b1db4907e4ddc5`, block 43820170, status 1, gasUsed 143115, `isRecorded=True`, **explorer-verified** at testnet.iotexscan.io (fee 0.28623 IOTX). Fired via `scripts/guardian_sig_anchor_tier2.py` (triple-gate + 0.50 IOTX hard cap + pre-send estimate_gas revert-guard + dynamic estimate_gas*1.25). **Cost honesty**: 0.646 IOTX total, ~0.36 wasted on 2 out-of-gas reverts before gas fix (static-gas wrappers `record_adjudication`=80k / `record_gate_attestation_on_chain`=100k hit IoTeX status 101; estimate_gas*1.25≈179k is correct, as `anchor_corpus_snapshot` already does — see memory gotcha). Success-run preimage not retained (one-shot crashed post-broadcast pre-persist, now fixed) → 862c98 is verified-at-creation but NOT preimage-reproducible. **GUARDIAN OPERATOR-CONTROL POSTURE COMPLETE**: `github_app_oauth_tokens_valid=True` (operator completed GitHub App OAuth setup — gates Guardian's audit-lane write authority; Sentry/Curator do NOT consult it) + `operator_dual_key_present=True` + kms_hsm + `phase_o3_guardian_live_writes_enabled=True` → all 4 VAPI-O3-SUPERSEDE attestation factors true. **CURRENT AUTONOMY POSTURE**: Guardian is the ONLY autonomous agent BY DESIGN — its O3 authority (audit-drafting on `lane://audits/**` + kms-sign) is local/off-chain, budget **0.0 IOTX** (audit-drafting routes through a LOCAL DB handler, NOT git-push — git-push guardrail intact). Sentry (`phase_o3_anchor_sentry_live_writes_enabled=False`) + Curator (`phase_o3_curator_live_writes_enabled=False`) are at O3_ACTING authority but executor-DISABLED; making them autonomous is a deliberate TWO-KEY decision (flip per-agent flag AND lift `CHAIN_SUBMISSION_PAUSED`) because their O3 actions spend real IOTX on-chain (Sentry pda-anchor ~0.0008/op; Curator marketplace-suspend ~0.001/op); per-agent budgets 0.05 IOTX/day each as runaway caps. **POSTURE PRESERVED**: `CHAIN_SUBMISSION_PAUSED=true` held in .env (Tier-2 used process-scoped override only); no FROZEN-v1 primitive added (Provenance-API / ZK-Realm ideas explored this session were SCRATCHED — verify directly against L1 + gamer-held ZKBA cert, no intermediary needed); no deployed contract changed; PV-CI 128 unchanged; wallet 14.903826 → 14.257596 IOTX. Proofs: `docs/qortroller-guardian-kms-autonomous-sign-proof.md` + `docs/qortroller-guardian-sig-onchain-anchor-proof.md`.

<a id="note-36"></a>
### 36. 🎮 L9_PRESENCE ARC (PoEP / L9-PoCP / BCC / GCAP) BUILT + ON-RIG VALIDATED 2026-05-21/22

NOTE: 🎮 L9_PRESENCE ARC (PoEP / L9-PoCP / BCC / GCAP) BUILT + ON-RIG VALIDATED 2026-05-21/22 — Standalone `l9_presence/` sub-project (50 files, 22 test files; commits `be0c4fc6`→`8c5132a5`) touching NO FROZEN-v1 / PoAC / chain / contract / grind-PCC. **Strategic reframe: identity → presence** — proves *a live human on the certified Edge now* (population-/physics-level), sidestepping the sub-grade identity ceiling (EER ~29%). **PoEP** (Proof of Embodied Presence): nonce-bound adaptive-trigger challenge-response; liveness reflex-band validated across 5 players; device-auth ~4× ON/OFF force slope (4/5; P4 resistance-invariant → P4b makes device-auth advisory, not a hard block); **born-PQ** commitment `SHA-256(b"QORTROLLER-POEP-v0"||device_id||nonce||response_features||ts_ns)` + hybrid ECDSA+ML-DSA-65 (IIP-64); v0 candidate, NOT anchored, `poep_enabled=False`, L6B N≥50 gate honored. **L9/PoCP** causal presence VALIDATED+banked (coupling 0.29–0.45 vs ~0.02 shuffle; latency-robust to 400–500ms cloud; Stream B generalizes 90.9/72.2/62.5% 2/3/4-player but not standalone-tournament-grade, fusion did not generalize). **BCC** (Behavioral Capture Chain) genesis `QORTROLLER-BCC-GENESIS-v0` — dormant (`enabled=False`), isolated (mythos_corpus_drift=0), Witness-wired to harvest ONLY PRESENT-certified causal sessions (self-cleaning corpus). **GCAP** lattice = honest negative (catches more adversaries — gap 0.357<0.462 — but human TAR collapses 0.806→0.581). The one open lever = **breadth (more people playing)**; BCC harvests it provenance-clean. Detail: `l9_presence/README.md` + `l9_presence/POEP_SCOPE.md`; memory `l9-presence-arc` + `qortroller-presence-reframe`.

<a id="note-37"></a>
### 37. ARCHIVED 2026-05-22 (lossless)

NOTE: ARCHIVED 2026-05-22 (lossless) -- 6 completed-arc NOTEs moved verbatim to `wiki/phases/phase_archive_2026_05_notes_and_summary.md` to cut CLAUDE.md reload cost: (1) QRESCE-0001 v0.5 brand-layer reframing `ff82ce30` [memory: project_qresce_0001_v05_brand_reframing_shipped]; (2) QRESCE-0001 QorTroller brand LOCK [memory: project_qresce_0001_qortroller_lock]; (3) Phase 2 R0 deep-verification `8bd82b0c`; (4) Phase 1 post-STABILITY-9 cleanup `9b2d8722`; (5) STABILITY-9 empirical closure `cf1e64de`+`756eb36a` [memory: project_stability_9_empirical_closure; wiki/phases/phase_235_stability_9_closure.md]; (6) Phase O1-D-PATH-B v1 [superseded by v2; memory: project_phase_o1_d_path_b_v2_wired]. Full text in the wiki archive + `git log --grep='NOTE:' -- CLAUDE.md`.

<a id="note-38"></a>
### 38. 🚀 O3 CEREMONY FIRED LIVE ON IOTEX 2026-05-17

NOTE: 🚀 O3 CEREMONY FIRED LIVE ON IOTEX 2026-05-17 — **First ≥3-agent Operator Initiative fleet at O3_ACTING in any DePIN gaming protocol.** Operator-authorized `parallel_o3_act_anchor.py --confirm` fired at 15:27:19Z UTC after quadruple-gate verification (CHAIN_SUBMISSION_PAUSED=false process-scope env + OPERATOR_INITIATIVE_O3_AUTHORIZED=true intent env + --confirm CLI flag + Gate 4 watcher veto check returned o3_ready=True/3 via the new VAPI-O3-SUPERSEDE-v1 attestation primitive shipped this session). **All 6 transactions landed cleanly on IoTeX testnet (chain ID 4690)** sequenced operational FIRST per INV-OPERATOR-AGENT-001: Sentry op_tx `d07492fb6fdc4e735c02...` + gov_tx `8ebef76b6fd773116d9c...` (15:27:19Z), Guardian op_tx `3678e71c32b0435e1a51...` + gov_tx `dd4c8154019a4ccbb484...` (15:27:35Z), Curator op_tx `dbd13ca1d100cc320363...` + gov_tx `2644949ffcf6d5e18df0...` (15:27:49Z). **Fleet Readiness Root permanently committed**: `0x54b4b698e9a81415034bfa72d82517f78343447e364f5ee5071f4898ce8bca37` (VAPI-FRR-v1 domain tag, phase_code 0x03 for all three agents, ts_ns 1779031623902994900). Post-anchor FRR computed locally + asserted byte-identical to expected pre-anchor FRR. **advancement_log row id=3** written with fleet-at-O3_ACT state. **Wallet 15.083226 → 14.903826 IOTX (cost 0.179400 IOTX)** — well under 3.0 IOTX budget (16× safety margin). **Live O3_ACTING capabilities now ACTIVE on chain**: Sentry has `pda-attestation-anchor` authority on `lane://provenance/**` (real on-chain anchoring via AdjudicationRegistry/PoAdAnchorAgent at ~0.0008 IOTX per anchor) + Guardian has `audit-drafting` authority on `lane://audits/**` (live writes to audit trail) + **Curator has `marketplace-listing-suspend` authority on `chain://iotex-testnet`** (direct suspension via VAPIDataMarketplaceListings.suspendListing() at 0x78Df84Cc... at ~0.001 IOTX per suspension; reversible via reinstateListing()). **Cryptographic justification chain (the entire architectural innovation this session)**: 504h shadow_age calendar gate was empirically superseded by VAPI-O3-SUPERSEDE-v1 attestations rows 4-6 in `operator_initiative_auto_supersede_log` — Sentry attestation `0e60b3d1ba436df300829bda1b4df9ee741bad00bda19aa989882a3ff12eb2b1` + Guardian `e75191a7c6509379f65fb45a2d7fea68f8c27ea801571af466ff4bba934f001d` + Curator `a854641833c085cdc908091ada2d435e900c9ac82b015a964edb31b011fd068a`. Each attestation cryptographically commits to gate-state (draft_count=50, disagreement_rate=0.0, drift_30d=0/0, all 4 operator flags True, shadow_age, ts_ns) such that any third party with the gate values can recompute + verify byte-identically. The supersession was operator-opt-in via PHASE_O3_AUTO_SUPERSEDE_ENABLED=true cfg flag (default False; conservative); the watcher consulted the primitive on this evaluation cycle and removed the o2_age blocker iff eligibility was attested. **SAFETY POSTURE PRESERVED**: bridge/.env still has `CHAIN_SUBMISSION_PAUSED=true` (script used process-scoped env override only; my subprocess `export` doesn't persist to bridge process or file); kill-switch automatically re-engages on next bridge restart. Bridge supervised by watchdog (PID owns 8080); zero zombie cycles since restart. **OPERATOR INITIATIVE PHASE LADDER COMPLETE**: Phase O0 (2026-05-03 on-chain registration `44c26ce0`) → Phase O1 SHADOW (2026-05-03 Cedar bundle dual-anchor `a02bcdb3`) → Phase O2 SUGGEST (2026-05-09 Curator Session 1 + 2026-05-12 Track 2 C8 v2 ceremony) → **Phase O3 ACTING (2026-05-17 this commit)** — the operator initiative's terminal phase reached in 14 days from O0. The fleet's o3_ready transition was the load-bearing question of the entire arc since Phase O1 D shipped 2026-05-09; this commit closes that question via cryptographic empirical-evidence supersession primitive instead of calendar wait. **NEXT MILESTONES** beyond Operator Initiative completion: (a) FSCA fleet_coherence quiet within 15 min post-ceremony (regression-watch); (b) Operator can now invoke live O3 capabilities — Curator's `tool:marketplace-listing-suspend` is the highest-value newly-active surface since it enables real-time marketplace protection; (c) Token launch sequencing remains gated per CLAUDE.md hard rules on separation_ratio>1.0 (AIT cleared 2026-04-18 ratio=1.199 N=37) + Phase 239 G2 closure (G3 done 2026-05-06 GIC_100 anchored) + Phase 99 deploy (PREP complete 2026-05-07; deploy day after wallet refill). The Operator Initiative was the load-bearing prerequisite blocking mainnet deploys per operator directive 2026-05-13; with O3_ACTING reached, that gate now reads CLEARED.

<a id="note-39"></a>
### 39. ARCHIVED 2026-05-22 (lossless)

NOTE: ARCHIVED 2026-05-22 (lossless) -- Phase O1-D-AUTO-SUPERSEDE (VAPI-O3-SUPERSEDE-v1, 11th FROZEN-v1 primitive, 2026-05-17) moved verbatim to `wiki/phases/phase_archive_2026_05_notes_and_summary.md`. Headline: 92-byte preimage `SHA-256(b"VAPI-O3-SUPERSEDE-v1"||...)` empirically supersedes the 504h shadow_age calendar gate; PV-CI 122->125. Full text: wiki archive + git history.

<a id="note-40"></a>
### 40. OLDER NOTES ARCHIVED 2026-05-18

NOTE: OLDER NOTES ARCHIVED 2026-05-18 -- CLAUDE.md curated from ~400k -> target ~60k chars per Claude Code performance threshold (40k warning). Approximately 120 NOTE entries covering Phases 17-235.x-STABILITY-9 stage 5 + Phase O1 ladder C1-C8 + Phase O2/O3 + Phase 237/238 + Methodology Layer arcs DELETED from this file. Full history preserved in: (a) git log --grep='NOTE:' -- CLAUDE.md (commit-by-commit narrative); (b) Phase Summary table below (canonical); (c) wiki/phases/ files for arcs with formal phase docs; (d) .claude/projects/.../memory/ auto-memory (60 architectural-reasoning entries indexed at MEMORY.md). KEEP discipline going forward: only 5-7 most-recent NOTEs in CLAUDE.md; older arcs migrate to wiki/phases/ + git history. Mythos variant mythos_claude_md_curation (in flight as durable guardrail) will auto-flag future stale NOTEs.
