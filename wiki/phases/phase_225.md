# Phase 225 — InvariantGate Provenance Chain + Governance History API

> Generated by remediation backfill pass (2026-04-18)
> Source: CLAUDE.md post-Phase-225 description + INVARIANTS_ALLOWLIST.json state
> [VAPI:Phase225:CLAUDE.md:MEASURED]

## Summary

Phase 225 added a cryptographic provenance chain to the InvariantGate governance log and
a dedicated governance history endpoint. Each allowlist change event now carries a
`governance_provenance_hash = sha256(prev_prov || new_hash || category || text || ts_ns_8b)`,
creating a tamper-evident chain across governance events.

Post-phase tooling (committed in `8081a2b0`): INV-017 and INV-018 were added to the
invariant gate, freezing the audit script's split regex and block-search logic respectively.

## Deliverables

| Component | Status |
|-----------|--------|
| `governance_provenance_chain` table in store.py | LIVE |
| `_compute_governance_provenance_hash(prev_prov, new_hash, category, text)` in gate script | LIVE |
| `_fetch_latest_provenance_hash()` GET /agent/allowlist-governance-history (fail-open "0"*64) | LIVE |
| POST /agent/allowlist-governance-event extended (+provenance fields) | LIVE |
| GET /agent/allowlist-governance-history (entries/total_entries/chain_intact/timestamp) | LIVE |
| GOVERNANCE_CHAIN_BROKEN CRITICAL — 4th INVERSION rule in FSCA | LIVE |
| `VAPIAllowlistGovernance.previous_changes()` rewritten → history endpoint | LIVE |
| `VAPIAllowlistGovernance.chain_intact()` new SDK method | LIVE |
| INV-017: audit script split regex (4-space indent anchor, prevents 800-char regression) | LIVE |
| INV-018: audit script block-search (full-body scan via `_AUTH_CALLS`, no char-window limit) | LIVE |
| Bridge tests | +8 (2344→2352) |
| SDK tests | +4 (492→496) |
| INVERSION rules | 3→4 |
| Invariants | 16→18 |

## Governance Formula

```
governance_provenance_hash = SHA-256(
    prev_provenance_hash (64 hex) ||
    new_allowlist_hash (64 hex) ||
    reason_category (bytes) ||
    reason_text (bytes) ||
    ts_ns.to_bytes(8, "big")
)
```
[VAPI:Phase225:scripts/vapi_invariant_gate.py:MEASURED]

## Protocol Invariants Preserved

- 228B PoAC wire format: FROZEN [VAPI:Phase225:VAPI_INVARIANTS.md:FROZEN]
- SHA-256(raw[:164]) chain hash: FROZEN
- L4 thresholds 7.009/5.367 (stable EMA, NOMINAL only)
- Poseidon(8), C3, nPublic=5 ZK binding: FROZEN
- GSR_ENABLED=false, L6B_ENABLED=false (hardware gates)
- dry_run=True default — Phase 97 live-mode gate not cleared
- INV-017 / INV-018: audit script patterns frozen as of commit `8081a2b0`

## Links

- [VAPI_INVARIANTS.md](../../VAPI-WORKFLOW.v2/VAPI_INVARIANTS.md)
- [CLAUDE.md](../../CLAUDE.md)
- [.github/INVARIANTS_ALLOWLIST.json](../../.github/INVARIANTS_ALLOWLIST.json)
