---PROPOSAL_START---
[DESCRIPTION]
Add ALL_PAIRS P0 GATE to MODE: PHASE_ADVANCE so operators check all_pairs_above_1=True
(per-pair defensibility gate, Phase 150/197) independently of the global ratio > 1.0 gate,
preventing false tournament clearance when P2vP3=0.401 remains below 1.0 even after global
ratio crosses the 1.0 threshold via 2 additional P1 captures.

[SKILL_MD_DELTA]
ADD to section MODE: PHASE_ADVANCE — after priority bullet list, before "Output exactly 3 phase candidates":

### ALL_PAIRS P0 GATE (Phase 150/197 — Per-Pair Defensibility)

**`ratio > 1.0` is NECESSARY but NOT SUFFICIENT for P0 clearance.**
The Phase 150 defensibility gate requires `all_pairs_above_1=True` in addition to global ratio > 1.0.
Current state (2026-04-11): ratio=0.998, P2vP3 inter-distance=0.401 (BLOCKER — P2/P3 indistinguishable).

**Before authorizing tournament activation after ratio > 1.0:**
1. Run `GET /agent/per-pair-separation-status` (Phase 197, Tool #150)
2. Verify `all_pairs_above_1=True` — if False, identify `blocker_pair` (currently P2vP3=0.401)
3. If `all_pairs_above_1=False`: capture more structured touchpad_corners sessions for the lower-distance pair
   - P3 intra-player variance = 1.165 (very high — centroid still moving)
   - P3 needs more sessions to tighten centroid → P2vP3 inter-distance grows naturally
   - Do NOT re-enable SeparationRatioRegistry.sol commit until all_pairs_above_1=True

**Resolution path for P2vP3=0.401:**
- P3's high intra-player variance (1.165) is the root cause; P2vP3 is 60% below target
- 3-4 additional P3 touchpad_corners sessions predicted to stabilize centroid
- Verify per-pair by rerunning `analyze_interperson_separation.py --session-type touchpad_corners`
- SeparationRatioRegistry.sol commit must wait for all_pairs_above_1=True (Phase 197 Phase 153 gate)

[WHAT_IF]
W1 — Global ratio crosses 1.0 via 2 additional P1 sessions (predicted ~1.02); SeparationRatioMonitorAgent
  fires separation_ratio_breakthrough; TournamentActivationChainAgent reads separation_ok=True;
  POST /agent/run-tournament-preflight returns overall_pass=True. But P2vP3=0.401 means P2 and P3
  are indistinguishable from each other — any tournament enrolling both players simultaneously
  has a 50% false eligibility rate for the P2/P3 pair.
  Implication: VHP credentials issued post-crossing are indefensible in P2 vs P3 dispute arbitration.
  SeparationRatioRegistry.sol commitment hash cannot be revoked; legal audit will show the
  on-chain proof-of-calibration committed at a moment when per-pair separation was unverified.
  Mitigation: Phase 197 per-pair P0 gate adds `all_pairs_above_1` as explicit preflight condition
  (10th condition, alongside biometric_ttl_ok Phase 196). Current defensible=False state
  (Phase 150) naturally blocks SeparationRatioRegistry.sol commit — safety net is already active.
  Phase candidate: Phase 197 MUST land before P1's next hardware capture session (~2 sessions to >1.0).

W2 — P2/P3 biometric proximity (P2vP3=0.401 < P2 intra=0.502) can serve as a Biometric Identity
  Convergence Monitor: when a pair's inter-distance < their mean intra-player variance, the bridge
  auto-flags that these two enrolled players may be the same person using two different session setups.
  Phase 138 already demonstrated this with P3/P4 distance=0.074 → confirmed same person (P4→P3 merge).
  If P2vP3 continues to decrease despite additional capture sessions for P3, the convergence monitor
  should fire and suggest player identity verification (P2 and P3 may be the same person or
  have correlated hardware setups — e.g., same room, same calibration grip style).
  Mechanism: store.monitor_pair_convergence() computes delta(P2vP3) per new snapshot insertion;
  when delta negative for ≥3 consecutive insertions → fire `pair_convergence_warning` bus event;
  operator must manually verify identities before resuming capture.
  Phase candidate: Phase 199 add-on to Phase 197 (~2h: convergence monitor + bus event + GET endpoint).
  Exclusive because: only VAPI has labeled per-player Mahalanobis distance history across sessions
  with identity-verified player roster; no other DePIN anti-cheat can detect player identity
  convergence from biometric session data.
---PROPOSAL_END---
