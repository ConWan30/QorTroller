---PROPOSAL_START---
[DESCRIPTION]
Add FLEET_COHERENCE_CRITICAL mode to SECURITY_REVIEW so that CONTRADICTION/ORPHAN/INVERSION
findings from FleetSignalCoherenceAgent (agent #36) are systematically treated as live attack
surface indicators — RENEWAL_WITHOUT_ATTESTATION specifically bypasses attestation chain dry_run
validation and can silently escalate from advisory to tournament-blocking if not caught within
the 900s poll window.

[SKILL_MD_DELTA]
ADD to section MODE: SECURITY_REVIEW — after the existing adversarial matrix block:

### FLEET COHERENCE CRITICAL PATH (Phase 193 — FleetSignalCoherenceAgent)

FleetSignalCoherenceAgent (agent #36) is the ONLY agent with fleet_coherence_enabled=True DEFAULT.
It monitors the 36-agent fleet for signal contradictions across 3 failure modes:

**CONTRADICTION (7 rules — highest attack surface):**
- RENEWAL_WITHOUT_ATTESTATION: CRITICAL severity — detects attestation chain bypass between
  Phase 185 (ReEnrollmentAttestationAgent) and Phase 186 (AttestationBoundRenewalAgent).
  An adversary who triggers biometric_renewal without a valid HMAC attestation token can
  bypass the Phase 185 gate entirely. This is the separation ratio dry_run equivalent for
  the attestation layer — it will silently pass until FleetSignalCoherenceAgent catches it.
  **HARD STOP: Any RENEWAL_WITHOUT_ATTESTATION CRITICAL finding must be resolved before
  dry_run=False transition (N>=100 live adjudications) is authorized.**
- 6 additional CONTRADICTION rules cover cross-agent verdict conflicts

**ORPHAN (5 rules — stale signal detection):**
- Signals from BiometricRenewalEngine, CeremonyAuditGate, or SeparationRatioMonitor that
  remain unresolved >48h become ORPHAN entries.
- ORPHAN signals in separation_ratio domain indicate the 0.998→1.0 touchpad_corners
  threshold crossing event was detected but not confirmed on-chain (SeparationRatioRegistry)
- Check cmd_coherence_status() after every touchpad_corners session capture

**INVERSION (3 rules — Provenance DAG walk):**
- BiometricPrivacyComplianceAgent (BP-001 TBD decay) and CorpusDataCuratorAgent
  (session_contribution_weight_log) can produce inverted ordering: a recent session
  may score LOWER than an older session due to decay weight and LOO centroid recalculation.
  This is NOT an error — it is expected behavior for N<30 per player.
  Inversion findings are MEDIUM severity when ratio is 0.998 (near-gate) and should NOT
  block tournament activation if all_pairs_above_1=True after the crossing.

**SECURITY REVIEW PROCEDURE for fleet_coherence_critical:**
1. `cmd_coherence_status()` — check open entries (CONTRADICTION first)
2. Any CRITICAL open → resolve before any dry_run toggle
3. Any ORPHAN in separation domain → rerun analyze_interperson_separation.py
4. INVERSION findings at ratio > 1.0 → DOCUMENT (expected, not an attack)
5. N_PROMOTE_THRESHOLD=3 auto-promotes persistent contradictions to VAPI_WHAT_IF.md

**Coherence ID format:** `coherence_id = "coh_" + SHA-256(rule_name+sorted_agents+ts_ns)[:16]`
Idempotent per cycle — same rule firing twice in the same cycle produces same coherence_id,
preventing double-counting.

[WHAT_IF]
W1 — RENEWAL_WITHOUT_ATTESTATION contradiction fires but is classified MEDIUM instead of
  CRITICAL due to severity misconfiguration, allowing the attestation bypass to persist
  through the dry_run=False transition gate without triggering a HARD STOP.
  Implication: An operator migrates to live enforcement (N>=100 adjudications) while a
  biometric renewal pathway lacks HMAC attestation binding. Renewals issued without
  attestation can generate VHP credentials for re-enrolled players without going through
  ReEnrollmentAttestationAgent's HMAC gate — this breaks the separation ratio defensibility
  chain, since the renewed credential is not cryptographically bound to the calibration corpus
  that produced the 0.998 (approaching 1.0) touchpad_corners ratio.
  Mitigation: RENEWAL_WITHOUT_ATTESTATION severity is HARDCODED as CRITICAL in
  fleet_signal_coherence_agent.py (not configurable) — confirmed in Phase 193. The rule
  also fires for ANY renewal event that lacks a matching attestation_hash in
  reauth_attestation_log. Added as Phase 194 audit item: verify severity is not
  overridden by fleet_coherence_log table direct writes. Phase candidate: Phase 194 audit gate.

W2 — FleetSignalCoherenceAgent's coherence_id idempotency enables a VAPI-exclusive
  "contradiction fingerprint" registry: recurring coherence_ids across multiple poll cycles
  identify structurally persistent inter-agent conflicts (not transient race conditions),
  which can be auto-classified as protocol maturity blockers and fed into
  ProtocolMaturityScoringAgent's threat_forecast_accuracy_component.
  Mechanism: Store coherence_id occurrence count per rule. When the same coherence_id appears
  >=3 cycles (N_PROMOTE_THRESHOLD), it is already promoted to VAPI_WHAT_IF.md. Extend this
  to also increment ProtocolMaturityScoringAgent's threat_forecast_accuracy score negatively —
  each persistent contradiction reduces the maturity score proportionally, creating a direct
  feedback loop from fleet coherence to protocol maturity without human intervention.
  Phase candidate: Phase 194 (WIF-032/WIF-033 context) or Phase 195.
  Exclusive because: No other gaming anti-cheat protocol has a 36-agent fleet that
  self-monitors for inter-agent contradictions and auto-degrades its own tournament
  readiness score in response — this is the only system where the protocol can formally
  declare itself not ready based on internal signal consistency, not just external metrics.
---PROPOSAL_END---
