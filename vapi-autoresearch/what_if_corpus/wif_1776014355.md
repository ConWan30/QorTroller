---PROPOSAL_START---
[DESCRIPTION]
Establishes the TouchAC Mobile Biometric Architecture (TMBA) foundation for VAPI mobile
expansion: how mobile touchscreen gaming uses the same 228-byte PoAC wire format,
isFullyEligible() composability, and 36-agent fleet pattern as PC, with mobile-exclusive
biometric features anchored in swipe_velocity_phase_coherence as the neurological tremor
equivalent of PC tremor_peak_hz.

[SKILL_MD_DELTA]
ADD new section after "Critical Gaps" table:

**TouchAC Mobile Integration Architecture (WIF-025 Phase 204+ candidate)**

Prerequisites before mobile expansion:
1. PC separation ratio > 1.0, all_pairs_above_1=True (currently 0.728 BLOCKER)
2. Phase 205 AccelTremorFFT — fix tremor_peak_hz for accel-domain still-hold sessions
3. Phase 204 IOSWARM_ACTIVE_NO_ADJUDICATIONS CONTRADICTION rule (WIF-038)
4. PC dry_run=False validated (N>=100 live adjudications, zero false positives)
5. PC VHP end-to-end on testnet (Tournament Condition 4)
6. Mobile calibration corpus N>=74 sessions/player/platform (current N=0)

TouchAC wire format: 228 bytes (164B body + 64B ECDSA-P256 sig) — same wire format invariant.
platform tag in body byte 12: 0x01=PC_USB, 0x02=MOBILE_IOS, 0x03=MOBILE_ANDROID.
chain_hash = SHA-256(raw[:164]) INVARIANT PRESERVED. deviceId = keccak256(pubkey) PRESERVED.
New contract: TouchACSessionRegistry.sol (same pattern as PITLSessionRegistryV2.sol).

Mobile L4 features (TouchAC-13):
  [0] contact_area_variance — pressure variation (macros cannot fake natural variance)
  [1] swipe_onset_velocity — finger acceleration profile on initiation
  [2] swipe_peak_velocity — maximum velocity in 500ms window
  [3] swipe_velocity_phase_coherence — tremor FFT of detrended swipe velocity (8-12 Hz)
  [4] multi_touch_coordination_entropy — inter-finger timing entropy
  [5] tap_press_duration_variance — contact duration distribution (human: 40-200ms CV)
  [6] touch_lift_jitter — variability in finger lift position
  [7] touchscreen_spatial_entropy — Shannon entropy of touch point distribution (8x8 heatmap)
  [8] reaction_to_event_lag_variance — stimulus-to-touch timing distribution
  [9] swipe_curvature_variance — non-straight-line deviation in arc swipes
  [10] tap_interval_autocorr_lag1 — temporal autocorrelation of inter-tap intervals
  [11] tap_interval_autocorr_lag5 — same at lag 5
  [12] wearable_gsr_sympathetic_index — Apple Watch ECG / Galaxy Watch BioActive (L7)

Mobile L4 thresholds: NOT 7.009/5.367 (USB PC calibration — incompatible transport/rate).
Requires mobile_threshold_calibrator.py against N>=74 mobile sessions/player/platform.
mobile_calibration_enabled=False default. mobile_anomaly_threshold: TBD. mobile_continuity_threshold: TBD.

swipe_velocity_phase_coherence — mobile tremor anchor:
During deliberate long-arc swipe (300-800ms), detrended velocity magnitude reveals
neurological tremor at 8-12 Hz — identical physiological origin to PC tremor_peak_hz.
Solves PC limitation: right_stick_x=128 (neutral) gives tremor=0 in still-hold sessions.
Mobile arc swipes produce non-zero continuous velocity — tremor FFT activates naturally.
Calibration: 30 arc swipes per session (3 directions x 10 reps) via arc_swipe_probe type.
IMPORTANT: swipe_velocity_phase_coherence is mobile_calibration_ONLY — not valid for PC corpus.

Mobile PITL stack (TouchAC-8):
  L0: App presence (MotionEvent stream non-empty)
  L1: Chain integrity (228B PoAC SHA-256 commitment + ECDSA-P256)
  L2: Injection detection (MotionEvent.getSource()==TOUCHSCREEN; velocity_discontinuity check)
  L3: ML behavioral classifier (ClassJ pattern, trained on mobile corpus)
  L4: Mahalanobis biometric fingerprint (TouchAC-13 features)
  L5: Temporal rhythm oracle (tap CV/entropy/quantization)
  L6: Arc swipe challenge-response (no haptic; display arc prompt, verify curvature+timing)
  L7: Wearable GSR (wearable_gsr_enabled=False default; N>=30/player/wearable model required)

Mobile agent fleet (#M1-#M5, isolated from PC agents #1-#36):
  #M1 TouchAC Calibration Agent — enrolls mobile features, establishes mobile separation ratio
  #M2 Mobile W3bstream Relay Agent — offline-buffer + retry for mobile network unreliability
  #M3 Wearable GSR Bridge Agent — BLE from Apple Watch / Galaxy Watch; wearable device keys
  #M4 TouchAC Separation Monitor Agent — monitors mobile ratio independently from PC agent #15
  #M5 Cross-Platform VHP Relay Agent — LayerZero bridge PC VHP to mobile tournament contexts

VAPIProtocolLens.sol mobile extension (Phase 210 candidate):
  isMobileEligible(bytes32 touchacDeviceId) view returns (bool)
  isCrossPlatformEligible(bytes32 pcDeviceId, bytes32 mobileDeviceId) view returns (bool)
  = isFullyEligible(pcDeviceId) AND isMobileEligible(mobileDeviceId)

ioSwarm mobile quorum: same IoSwarmConsensusAggregator (BLOCK_QUORUM=0.67).
Mobile task spec: scripts/vapi-mobile-touchac-swarm-agent.json (Phase 208 candidate).
MINT_QUORUM=0.80 applies to mobile VHP — same as PC, fail-CLOSED.

New inference codes for mobile:
  0x35 TOUCH_INJECT_DETECTED — velocity_discontinuity_score above threshold (L2 mobile)
  0x36 SWIPE_TREMOR_ABSENT — swipe_velocity_phase_coherence=0 during arc swipe probe (L4)
  Both: ADVISORY ONLY until N>=30 mobile calibration per player.

[WHAT_IF]
W1 — Mobile OS touch injection bypasses PC L2 (HID driver injection detection) entirely:
Android MotionEvent.obtain() and iOS UITouch simulation require no special permissions;
a bot generates synthetic MotionEvent streams with gaussian-noise pressure, valid
TOUCHSCREEN source, and plausible coordinates — PC L2 code 0x28 DRIVER_INJECT cannot fire.
  Implication: Without mobile-specific L2, a mobile bot controlling MotionEvent generation
  controls the biometric signal source itself. The L4 separation ratio is computed from
  synthetic inputs that can be engineered to match a target player enrollment.
  The mobile tournament gate is structurally bypassed before calibration begins.
  Mitigation: Phase 207 — 0x35 TOUCH_INJECT_DETECTED inference code.
  velocity_discontinuity_score: injected events have mathematically perfect linear
  interpolation between keyframes; physiological swipes have micro-tremor sinusoidal
  deviation. inter_event_interval_quantization: injected events cluster at 16.67ms=60fps
  boundaries; real touch is non-quantized. Both: ADVISORY ONLY until calibrated.

W2 — swipe_velocity_phase_coherence as the mobile tremor anchor — identical neurological
origin to PC tremor_peak_hz, measured through touch velocity during arc swipes.
Detrended velocity magnitude during a deliberate long arc swipe reveals 8-12 Hz tremor
at the same physiological origin as controller hand tremor (motor system, CNS coupling).
Simultaneously solves: (1) mobile separation anchor at same physiological layer as PC,
(2) PC tremor_seed right_stick_x=128 limitation structurally absent on mobile, and
(3) defines Class K_m (mobile tremor spoofer: programmatic sinusoidal velocity injection)
pre-detectable via gsr_game_event_correlation — same countermeasure as Class K_t.
  Mechanism: #M1 TouchAC Calibration Agent captures 30 arc swipes per session.
  swipe_velocity_fft_analyzer.py detrends velocity, computes FFT, extracts
  [swipe_velocity_phase_coherence, swipe_tremor_peak_hz, swipe_band_power].
  Predicted per-player separation: same inter-player spread as PC tremor_peak_hz
  since neurological origin is identical (individual CNS tremor signature).
  Phase candidate: Phase 205 — swipe_velocity_fft_analyzer.py + arc_swipe_probe session
  type + MockTouchScreen(seed=42) + swipe_tremor_peak_hz as 14th feature.
  +8 bridge tests, +4 SDK tests. No hardware needed (MockTouchScreen pattern).
  Exclusive because: requires VAPI per-session Mahalanobis infrastructure + IoSwarm
  BLOCK_QUORUM=0.67 + TouchAC-13 feature pipeline + WIF-025 mobile agent fleet.
---PROPOSAL_END---
