---PROPOSAL_START---
[DESCRIPTION]
Adds FSCA 8th CONTRADICTION rule gap analysis: when ioSwarm is active (Phase 200) but no
clean adjudications have ever been recorded, VHP MINT_QUORUM=0.80 is permanently blocked
by design — but no coherence signal detects this fleet-level stall, creating an invisible
tournament activation ceiling even after separation ratio > 1.0 is confirmed.

[SKILL_MD_DELTA]
ADD to section "INVERSION Classification Rubric (Phase 193 — FleetSignalCoherenceAgent)":

**Phase 202/203 FSCA state (post-implementation):**
- ORPHAN rules: 6 (5 original Phase 193 + RATIO_VELOCITY_NEGATIVE Phase 202)
- INVERSION rules: 4 (3 original Phase 193 + CONTEXT_HASH_MISMATCH Phase 203)
- CONTRADICTION rules: 7 (Phase 193; no Phase 202/203 additions)

**8th CONTRADICTION rule gap (Phase 204 candidate): IOSWARM_ACTIVE_NO_ADJUDICATIONS**
When ioswarm_enabled=True AND ruling_streaks table has zero consecutive_clean entries
older than 24 hours, VHP MINT_QUORUM=0.80 (Phase 110 IoSwarmVHPMintCoordinator) is
permanently fail-CLOSED because IoSwarmAdjudicationCoordinator has never produced a
quorum-confirmed CERTIFY verdict for the mint coordinator to act on.
This is a fleet-level SEMANTIC CONTRADICTION: ioSwarm reports as active (Phase 200
IOSWARM_ENABLED=true) while the tournament authorization chain is structurally blocked
at its own last step — no adjudication has ever been quorum-confirmed.
Gap: FSCA's 7 CONTRADICTION rules don't monitor the conjunction of ioswarm_active=True
AND consecutive_clean_count=0 over an extended window. Result: the bridge logs no
coherence alert while tournament operators observe that VHP minting never fires.
Phase 204 candidate: 8th CONTRADICTION rule IOSWARM_ACTIVE_NO_ADJUDICATIONS.
Trigger: ioswarm_adjudication_log has no rows OR all rows have quorum_reached=False
AND time_since_first_row > 86400s. Severity: HIGH.
Exclusion: rule is dormant when dry_run=True (expected state; mint is not authorized).

[WHAT_IF]
W1 — FSCA 7 CONTRADICTION rules have no coverage of the ioSwarm adjudication-to-mint
handoff gap: ioSwarm can report fully active while VHP minting is structurally impossible.
  Implication: Tournament operators see ioswarm_enabled=True and assume VHP mint is live.
  In reality, IoSwarmVHPMintCoordinator.request_mint() fails CLOSED on every call because
  IoSwarmAdjudicationCoordinator has zero quorum-confirmed CERTIFY verdicts in its log.
  No coherence alert fires. The separation ratio breaks through 1.0, the tournament
  activation chain completes, dry_run flips to False — and the first real VHP mint attempt
  silently fails with MINT_QUORUM=0.80 not reached. This stall is undetectable from
  existing FSCA rules, agent bus channels, or the operator dashboard.
  Mitigation: Phase 204 — 8th CONTRADICTION rule IOSWARM_ACTIVE_NO_ADJUDICATIONS.
  Checks: SELECT COUNT(*) FROM ioswarm_adjudication_log WHERE quorum_reached=1 → 0
  AND time() - MIN(created_at) > 86400 AND ioswarm_enabled=True. Severity HIGH.
  resolution: "Run at least 1 live adjudication session to prime IoSwarmAdjudicationCoordinator.
  Check ioswarm_adjudication_log for quorum_reached=False patterns. Verify BLOCK_QUORUM=0.67
  is reachable with current emulator seed (Phase 109A 5-node emulator, seed=109/110)."

W2 — IoSwarm Adjudication Primer: one-shot GET /agent/prime-ioswarm-adjudication endpoint
that replays the last N=5 ruling_streaks entries through IoSwarmAdjudicationCoordinator
(BLOCK_QUORUM=0.67) in offline emulation mode to produce the first quorum-confirmed
CERTIFY verdict, unblocking IoSwarmVHPMintCoordinator for subsequent real sessions.
  Mechanism: POST /agent/prime-ioswarm-adjudication runs 5 synthetic adjudication cycles
  against emulator nodes (seed=109) using the most recent consecutive_clean ruling streak
  as input. Records result in ioswarm_adjudication_log with source="primer". This gives
  FSCA's IOSWARM_ACTIVE_NO_ADJUDICATIONS rule a quorum_reached=1 row to check against,
  clearing the contradiction without requiring live hardware sessions. Result: VHP mint
  pathway is unblocked and validated before the first real tournament.
  Phase candidate: Phase 204 — +8 bridge tests, +4 SDK tests, no Hardhat needed.
  primer_enabled=False default (infrastructure-first).
  Exclusive because: Requires simultaneous ioSwarm emulator (Phase 109A) + SessionAdjudicator
  streak data (Phase 66) + IoSwarmVHPMintCoordinator (Phase 110) + FleetSignalCoherenceAgent
  CONTRADICTION monitoring (Phase 193) — a 5-phase stack only VAPI has.
---PROPOSAL_END---
