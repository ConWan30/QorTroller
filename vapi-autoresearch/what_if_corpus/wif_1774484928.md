---PROPOSAL_START---
[DESCRIPTION]
Adds a WHAT_IF Corpus bootstrap section to MODE: WHAT_IF in skill.md, seeding it with the first two validated pairs covering the highest-stakes ioSwarm risk (Phase 110 node-pool homogeneity attack on MINT_QUORUM=0.80) and the most critical open gap risk (separation ratio 0.362 calibration deadline), plus the most novel Phase 111 opportunity (Proof of Adjudication as second composable on-chain primitive).

[SKILL_MD_DELTA]
ADD to section "MODE: WHAT_IF (v2 Enhanced)" — append after the closing ``` template block:

### WHAT_IF Corpus — Validated Pairs (Phase 110+)

These pairs have passed the eval harness (score ≥ 0.70) and represent ground truth for
future cycle priming. Reference before generating new WHAT_IF pairs to avoid duplication.

**Corpus Entry WIF-001 — ioSwarm Node-Pool Homogeneity (Phase 110)**

W1 — Failure mode: ioSwarm MINT_QUORUM=0.80 is trivially satisfied if all 5 nodes are
operated by the same entity, collapsing the quorum guarantee to a 1-party signature.
  Implication: soulbound VHP tokens minted without genuine distributed consensus;
  swarm_fingerprint=SHA-256(node_verdicts_json) audit trail is intact but verifies only
  that one party agreed with itself — fail-CLOSED direction preserved but distributed
  authorization guarantee voided; indistinguishable from legitimate quorum on-chain.
  Mitigation available: yes — VAPISwarmOperatorGate.sol enforces min 3 distinct staker
  addresses in node pool; stake-weight cap at 1.5× per node prevents whale capture;
  Phase 112: isSufficientlyDecentralized() as additive 5th gate on POST /agent/mint-vhp.

W2 — Opportunity: Proof of Adjudication (PoAd) as second composable on-chain primitive.
  Mechanism: PoAd_hash = SHA-256(sorted(node_verdicts_json) + quorum_str + ts_ns_str);
  AdjudicationRegistry.sol stores per-cycle digests; downstream tournament contracts call
  hasCleanAdjudicationHistory(deviceId, lookback_days) alongside isFullyEligible();
  two independent composable primitives for different tournament risk tolerances.
  Phase candidate: Phase 111
  Exclusive because: presupposes ioSwarm node_verdicts on-chain anchored to PoAC chain
  integrity (SHA-256(raw[:164]) + 228 bytes wire format) — no competitor has distributed
  per-device adjudication records; PoAd is only valid because PoAC is already anchored.

**Corpus Entry WIF-002 — Separation Ratio Calibration Deadline Risk (Phase 108)**

W1 — Failure mode: inter-person separation ratio remains 0.362 through Phase 113 if
touchpad recapture hardware session is indefinitely deferred.
  Implication: TOURNAMENT BLOCKER persists; all 4 tournament readiness conditions unmet;
  VHP minting proceeds on testnet but live tournament integration impossible; ratio > 1.0
  is non-negotiable TGE gate — token launch blocked regardless of ioSwarm phase completion.
  Mitigation available: yes — schedule dedicated DualShock Edge + NCAA CFB 26 session now;
  run python scripts/interperson_separation_analyzer.py after post-Phase-17 touchpad
  recapture; no software phase (spectral entropy, jitter variance) can substitute.

W2 — Opportunity: SeparationRatioRegistry.sol — on-chain immutable audit trail of each
separation ratio measurement, anchored to calibration session hash and player count.
  Mechanism: separation_ratio_commitment = SHA-256(ratio_str + N_sessions + player_ids_sorted
  + ts_ns); stored with operator signature on IoTeX L1; provides cryptographic proof that
  ratio > 1.0 confirmed empirically before TGE — legally defensible sequencing compliance.
  Phase candidate: Phase 112
  Exclusive because: VAPI's Mahalanobis inter-person distance metric on DualShock Edge
  biometric features is the only calibrated biometric separation measure in anti-cheat;
  no competitor has inter-person separation ratio as a token launch gate condition.

[WHAT_IF]
W1 — Failure mode: ioSwarm node-pool homogeneity collapses MINT_QUORUM=0.80 to a
1-party signature, voiding distributed VHP authorization on Phase 110 fail-CLOSED gate.
  Implication: soulbound VHP mints authorized without genuine quorum — swarm_fingerprint
  audit trail is cryptographically valid but economically meaningless; fail-CLOSED direction
  preserved but distributed guarantee voided; indistinguishable from legitimate mint on-chain.
  Mitigation available: yes — VAPISwarmOperatorGate.sol min 3 distinct staker addresses;
  Phase 112 isSufficientlyDecentralized() as 5th gate; stake-weight cap 1.5× per node.

W2 — Opportunity: PoAd (Proof of Adjudication) as second composable on-chain primitive
enabling game developers to compose clean adjudication history checks alongside isFullyEligible().
  Mechanism: PoAd_hash = SHA-256(sorted_node_verdicts + quorum + ts_ns); AdjudicationRegistry.sol
  anchors per-cycle ioSwarm digests; hasCleanAdjudicationHistory(deviceId, lookback_days) returns
  bool; two independent VAPI composable primitives for tournament integrators.
  Phase candidate: Phase 111
  Exclusive because: presupposes ioSwarm per-device node_verdicts anchored to PoAC 228-byte
  wire format chain integrity — no competitor protocol has this biometric-to-quorum link.
---PROPOSAL_END---
