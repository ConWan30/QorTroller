I'll verify Inc-1 against the V1–V8 hammer checklist by inspecting the shipped code, grepping for residual hardcodes, and running the OA/sensor tests.Found a potential note-field sanitization issue in the seed JSON. Digging into sensor wiring, serialization, and the test suite next.# A2A Round-31 VERIFY — Inc-1 OA-box single-source refactor

**Charter:** you built, I verify. Findings first, then verdict.

---

## Findings

| Sev | ID | Finding |
|-----|-----|---------|
| **LOW (nit)** | F1 | Seed top-level `note` self-suppresses: `audits/operator_actions.json` `note` contains forbidden substring `~/.vapi` (meta “do not put this here”). Renderer drops the whole note (`if note and _forbidden_token(note) is None`). **No leak**, but the didactic italic note **never appears** in Cycle-18 (or any) render. Optional: rephrase note to avoid the token so it actually ships. |
| **LOW (latent)** | F2 | `machine_hint` scan is `id + text + note + hint_ref` only — not the full hint dict. Probed: secrets in extra keys (`secret: arn:aws:…`) do **not** reach markdown today (only `kind`/`ref` used; `kind` never echoed). Not a current leak. Latent if someone later dumps the whole dict. Optional harden: scan `json.dumps(hint, sort_keys=True)`. |
| — | — | **No HIGH/MEDIUM blockers.** No write path, no legacy revive, no Path serialization leak, no dual-site hardcode residual in the two sensors. |

---

## Hammer V1–V8

### V1 — Identical OA block + repo_root
**PASS.**  
Runtime: `render_operator_actions(None) == render_operator_actions(REPO_ROOT)`; both sensor `to_markdown()` contain that exact block; B with `repo_root=None` (self-locate) equals B with explicit root.  
`_resolve_repo_root(None)` → `Path(__file__).parents[2]` → repo root; matches explicit.  
Cycle-18 artifacts: C OA block **byte-identical** to B OA block.

### V2 — Missing / malformed fail-open, no legacy
**PASS.**  
Missing file, wrong schema, malformed JSON, bare `Exception` → header + `_MISSING_BANNER` only.  
Both sensors’ `to_markdown()` survive empty `repo_root` without crash.  
`"Highest-leverage 5-min action"` **absent** from renderer and sensor fallbacks. No fabricated OA items.

### V3 — Statuses not auto-mutated; no write path
**PASS.**  
Unknown status → forced `"open"` + `[ ]` (never silent `[x]`).  
Module uses only `path.read_text` / `json.loads`; **no** `write_text` / `open(` / `dump` / unlink / rename.  
T-OA-6 pins source bytes unchanged after render.

### V4 — Sanitization
**PASS (with F1/F2 nits).**  
Rendered output: no `qortroller_foundation_mfg_ca`, `arn:aws:`, `~/.vapi`.  
Item-level forbidden token → whole-item **REDACTED** (token never in line).  
Top-level note with forbidden token → **dropped** (not REDACTED string — still safe).  
Shipped JSON items are clean; only meta `note` carries `~/.vapi` (dropped).  
`machine_hint` extra keys: not a current render leak (F2).

### V5 — Dedup / hardcode gone
**PASS.**  
`grep` over `bridge/vapi_bridge/sensor_{b,c}*.py`: **zero** hits for `Highest-leverage`, `F-DECON-3.2 interim mitigation`, legacy OA prose.  
Only comments pointing at `render_operator_actions(...)`.  
(Historical cycle-2..17 artifacts still have old text — intentional, out of Inc-1 scope.)

### V6 — Fallback honesty (r30 ruling)
**PASS.**  
Missing path is honest MISSING banner, **not** compact-legacy hardcode. Matches “never freeze v1 lies.”

### V7 — Serialization / Path leak
**PASS.**  
`RungLedger.to_dict()` / `to_json()` omit `repo_root` (only schema/cycle/gates…).  
`WatchReport` has no `to_dict`/`to_json` — markdown-only surface. No Path in JSON companions.

### V8 — `assemble_watch_report` callers
**PASS.**  
`repo_root` is optional kwarg, default `None` → self-locate (backward-compatible).  
`run_sensor_b.py` passes `repo_root=_REPO_ROOT`; `--narratives` still only fills `fetched`. Existing tests call without kwarg and still get the OA box.

---

## Empirical checks

| Check | Result |
|-------|--------|
| `test_operator_actions` + sensor B/C | **36 passed** in 1.47s |
| PV-CI | **PASS — 184** |
| Statuses in live render | OA-1 moot `[x]`, OA-2 done `[x]`, OA-3 open `[ ]`, OA-4 done `[x]` |
| Cycle-18 C↔B OA blocks | identical; zero forbidden tokens |

---

## Verdict

# **SHIP**

Inc-1 matches the r30 design: single source, dual consumer, operator-attests-only, fail-open MISSING (not legacy), sanitization active defense, no serialization leak, tests decoupled from attestation prose (T9/T8 footgun closed).

**Non-blocking follow-ups** (do not hold staging):
1. **F1** — rephrase seed `note` so it doesn’t contain `~/.vapi` → note actually renders.  
2. **F2** — optionally scan full `machine_hint` via `json.dumps` for defense-in-depth.

No FIX required for staging this increment.
