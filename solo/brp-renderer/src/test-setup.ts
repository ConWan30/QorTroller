// Vitest setup — runs before every test suite.
// Pulls @testing-library/jest-dom matchers (toBeInTheDocument, toHaveAttribute, etc.)
// into Vitest's expect namespace.
import "@testing-library/jest-dom/vitest";
