[pytest]
# importlib mode prevents bridge/tests and sdk/tests from colliding as the
# same "tests" package when pytest collects both suites from the project root.
# testpaths: run all sub-protocol suites from project root.
# Missing directories (pragmajudge/, mobile/) produce "no tests found" — correct.
testpaths = bridge pragmajudge mobile
addopts = --import-mode=importlib -m "not hardware and not bluetooth"
asyncio_mode = strict

[pytest:markers]
hardware: requires physical DualShock Edge (Sony CFI-ZCP1) connected via USB.
          Skip in CI; run with: pytest tests/hardware/ -v -m hardware
bluetooth: requires physical DualShock Edge (Sony CFI-ZCP1) connected via Bluetooth (USB cable disconnected).
           Skip in CI; run with: pytest tests/hardware/ -v -m bluetooth
