# VAPI — Claude Code Project Context

## What This Project Is

VAPI (Verified Autonomous Physical Intelligence) is a cryptographic anti-cheat protocol
for competitive gaming. It produces a 228-byte Proof of Autonomous Cognition (PoAC) record
per cognition cycle, anchored on IoTeX L1. The certified device is a DualShock Edge
(Sony CFI-ZCP1). The primary game corpus is NCAA College Football 26.

## Repository

`C:\Users\Contr\vapi-pebble-prototype`

~270 files, ~4,330 automated tests total (~4,279 CI excluding 37 hardware, 14 E2E).
Bridge: 3487 passing. Autoresearch: 7 passing. Contract: 668. SDK: 562. Hardware: 37. E2E: 14. PV-CI: 86. FSCA: 27. Frontend Vitest: 26.
NOTE: 🧮 PHASE O4-W3B-POSEIDON-AS COMPLETE 2026-05-14 — Closes the POSEIDON_HASH delta by shipping a **protocol-internal AssemblyScript Poseidon(BN254) cryptographic capability** — verified byte-identical to circomlibjs 0.1.7, PV-CI-pinned, and honestly reframed at the W3bstream applet boundary. **POSEIDON-BN254-AS is NOT an 11th PATTERN-017 commitment family** — the commitment-family count stays **10**; it is the hash-function *capability* that supports PATTERN-017 #8 ZK-SEPPROOF + the Phase 62 PitlSessionProof circuit. Seven commits. **I.1a `afa31416`** (BN254 modular arithmetic over 4×u64 limbs — add/sub/mul via 256→512 schoolbook + binary long-division reduction, pow5 S-box) + **I.1b `53aed9c4`** (Poseidon permutation + 3 arity entry points poseidon_t2/t3/t9 = circomlib Poseidon(1)/(2)/(8); C/M constants replicated from circomlibjs via `poseidon_constants_gen.js`) + **I.2 `ad89fb91`** (bytes32 ↔ BN254 field-element byte I/O). Arity set corrected to `{t=2,t=3,t=9}` at P.2 after a Risk #7 V-check found `compute_inputs_pitl.js` was STALE (Poseidon-7 vs circuit's Poseidon-8) — the circuit is the oracle. **V.1 `0b6adc13`** — `bridge/tests/test_w3bstream_poseidon_as.py` T-W3B-POSEIDON-AS-1..10 + `scripts/w3bstream/poseidon_runtime.js` (Node WASM invocation helper): 375 final-output vectors (100 random + 25 boundary per arity) + 3 published circomlib BN254 canonical vectors + per-arity determinism, all byte-identical. **V.2 `a80f3fb4`** — `scripts/w3bstream/poseidon_bn254_debug.ts` (per-round-emitting mirror; imports the same field arithmetic + C/M constants — anti-drift) + T-W3B-POSEIDON-AS-11..16: **48,100 intermediate round-state elements** byte-identical (t2 6400 + t3 9750 + t9 31950, 50 per_round vectors per arity) + 150 final-output. V.3 cross-reference triangulation SKIPPED — **AMBER path** (P.0-confirmed: no second BN254-compatible Poseidon reference available in agent runtime; circomlibjs 0.1.7 is the single reference; V.1 boundary coverage + V.2 per-round differential are the compensating discipline). **PV.1 `de64af4c`** — governance ceremony PV-CI **83 → 86**: INV-POSEIDON-AS-001 (poseidon_t2/t3/t9 entry points, min_matches=3) + INV-POSEIDON-AS-002 (BN254 prime byte-literal) + INV-POSEIDON-AS-003 (vector-corpus SHA-256 pinned in `poseidon_test_vectors.sha256` — V-check drift correction: the plan named a `POSEIDON_VECTORS_SHA256` constant that I.1 correctly never added to the production module). **W.1 V-check found a plan premise error** — W.1/W.2 assumed the W3bstream applet COMPUTES featureCommitment/nullifierHash. It cannot: `scaledFeatures[7]` + `deviceIdHash` are circuit-PRIVATE inputs (`PitlSessionProof.circom`) absent from the FROZEN 164-byte PoAC wire body (`codec.py`: 4 hashes + 13 telemetry scalars); `validate_zk_sepproof.ts` receives `featureCommitment` as a pre-computed input. The applets **RELAY** bridge-computed ZK public inputs — they are not the computation site. Operator authorized **Option A** (reframe honestly). **W.1+W.2+W.3+T.1 `178c375b`** — W.1: `validate_poac_record.ts` comment-only correction of the stale POSEIDON_HASH framing (recompiles byte-identical); W.2: confirmed no-op; W.3: `scripts/w3bstream_applet_audit.py` reframe — POSEIDON_HASH moved CRYPTO_INTEGRATION_DELTAS → CLOSED_DELTAS (closure phase `Phase O4-W3B-POSEIDON-AS`, honest "resolved at capability layer / architecture-N/A for in-applet wiring / relay-not-compute" summary), the "AssemblyScript Poseidon implementation" DEPENDENCY_BLOCKERS entry REMOVED (resolved); `crypto_deltas_closed` 3→4, open 2→1, verdict UNCHANGED at `STUB_DEPS_BLOCKED` (P256_VERIFY is now the sole genuinely-open delta, still dep-blocked by `@assemblyscript/wasm-crypto` 404); T.1: `test_w3bstream_applet_audit.py` aligned (fixed T-W3B-4 + T-W3B-12, enhanced T-W3B-13 + T-W3B-15, added T-W3B-18 reframe-closure + T-W3B-19 blocker-removed; 17→19 tests). **FLAGGED (pre-existing, NOT fixed — separate scope):** `validate_poac_record.ts` offset constants `OFF_DEVICE_ID=0`/`OFF_INF_CODE=104`/`OFF_CHAIN_HASH=32` do not match the FROZEN PoAC wire format (`codec.py`: byte 0=prev_poac_hash, 32=sensor_commitment, 128=inference_result) — a systematic applet-stub offset bug needing its own focused audit pass. **Verification:** V.1/V.2/PV.1 introduce **zero regressions** — proven by a revert test (pre-PV.1 allowlist 83 and post-PV.1 allowlist 86 produce IDENTICAL 11-failed/21-passed across test_phase223/224/225/226; the 11 are pre-existing stale count-assertions + bridge-offline endpoint tests). 35 directly-relevant tests PASS (19 applet-audit + 16 poseidon-as). Bridge +18 (V.1 +10, V.2 +6, T.1 +2). PV-CI 83→86. Hardhat 668 unchanged. Wallet-free; `CHAIN_SUBMISSION_PAUSED=true` held throughout; ZERO mainnet operations. Memory hygiene: `MEMORY.md` compressed 36,989→11,812 bytes (was truncating on load; all 60 index entries + links preserved). Future-vector composition roadmap at `wiki/methodology/poseidon_methodology_composition_roadmap.md` (`10eb3cba`) — ZKBA-ZK variant is the highest-leverage downstream consumer.
NOTE: 🔁 STREAMS CONTINUATION 2026-05-14 — Post-plan-execution agent-dispatched continuation of the v4 §15 fallback streams shipped at HEAD `084b0f17`. Operator authorized "ship #1 and #2 in parallel" — push the deferred B-PARTIAL + A-RED fallbacks to fuller completion via 2 parallel Agent dispatches. **Stream A pushed through SAFELY at `48e177e5`** (3 of 4 planned safe deltas closed in `scripts/w3bstream/validate_poac_record.ts`): ABI_ENCODER CLOSED (real 7-arg `submitPITLProof` ABI v2 encoder + real selector `0x7c4847ed` replacing 0xDEADBEEF placeholder); CONSENT_RETURN_DATA CLOSED (new `chain_call_returndata` host import + `_read_bool_from_returndata` helper reading rightmost byte of 32-byte chain_call return-data buffer); DEVICE_ID_TO_GAMER CLOSED (new `_resolve_device_to_gamer` helper calling `VAPIioIDRegistry.getDeviceWallet(bytes32)` with real selector `0x0ff0779b` before consent check; replaces stub assumption that device_id_hash == gamer_address). POSEIDON_HASH **atomic-stopped per safety rule** — no Python Poseidon reference available in agent runtime to verify a hand-written AS implementation against circomlib BN254 test vectors; deferral documented in audit's DEPENDENCY_BLOCKERS roster + inline `validate_poac_record.ts` deferral block. P256_VERIFY **preserved as stub** per Hard Rule (no W3bstream runtime available for ECDSA-P256 AS-side verification testing; cannot ship unverified crypto). Audit verdict transition: STUB_DEPS_BLOCKED (5 open deltas) → STUB_DEPS_BLOCKED (2 open + 3 closed; verdict tier unchanged because P256_VERIFY + POSEIDON_HASH both remain dep-blocked, but the audit surface now distinctly reports 3 closed deltas under `crypto_deltas_closed` for the first time + a new `getDeviceWallet(bytes32)` entry in AUTHORITATIVE_SIGNATURES). T-W3B test band 14 → 17 (+3 tests: T-W3B-15 closed-delta drift guard + T-W3B-16 getDeviceWallet selector + T-W3B-17 stale-blocker guard). PV-CI 83/83 PASS; Hardhat baseline 668 preserved; Wallet 0 IOTX. **Stream B atomic-stopped** — Agent attempted 3 install paths for `@layerzerolabs/lz-evm-oapp-v2@^3.0.168` (--legacy-peer-deps full install, vendor-minimal, hybrid with Solidity remappings); ALL THREE BLOCKED. Root cause: `@layerzerolabs/lz-evm-messagelib-v2` transitively peer-requires `@eth-optimism/contracts@^0.6.0` which peer-requires `ethers@^5`, but `@nomicfoundation/hardhat-toolbox@^4.0.0` peer-requires `ethers@^6.14`; npm refuses to resolve both. Vendor-minimal path fails the audit's `PRODUCTION_PATTERNS[0]` regex requiring literal `@layerzerolabs/lz-evm-oapp` import string. Agent reverted working tree to `084b0f17` cleanly; no commit; baseline fully preserved. Stream B remains at **PARTIAL_REFACTOR** state from `dfdeaf2b` (defensive prep: `MockLayerZeroEndpoint.sol` test mock + `bridgeMint()` + `setBridgeAddress()` + `BridgeAddressSet`/`VHPBridgeMinted` events on `VAPIVerifiedHumanProof.sol`). **Three forward-resolution paths documented** for the eventual full OApp refactor: (1) wait for LayerZero release that drops the `@eth-optimism/contracts` transitive constraint; (2) migrate hardhat-toolbox to a 5.x line supporting both ethers v5+v6; (3) manually vendor the full OApp + dependency chain (OAppCore + OAppSender + OAppReceiver + MessagingFee + Origin + ILayerZeroEndpointV2) with Solidity remappings via `hardhat.config.js` (~300-500 LOC; 2-3 hours next-session work). Audit verdict at this state remains STUB (4/4 stub indicators present; 0/5 production indicators) — correctly reports the upstream blocker. **OPERATIONAL POSTURE PRESERVED:** CHAIN_SUBMISSION_PAUSED=true held; wallet 0 IOTX impact across the 2-agent dispatch; ZERO mainnet operations; ZERO FROZEN-region modifications; PV-CI 83/83 PASS unchanged; Hardhat 668 unchanged. **Cumulative session test count progression:** Bridge 3463 → 3469 (+3 Stream A); Hardhat 668 unchanged (Stream B stopped). **Auditable claims with visible limits posture STRENGTHENED:** every deferral has an explicit reason (Hard Rule cite OR dep-blocker cite OR forward-resolution path) recorded both in inline source comments + audit script reasoning fields + commit message body + this NOTE entry + memory file `project_streams_continuation_2026_05_14.md`.
NOTE: 🔒 POST-O4 v4 §15 WALLET-FREE BACKLOG CLOSED 2026-05-13 — Single continuous session arc shipped 17 atomic wallet-free commits closing every agent-actionable item in Whitepaper v4 §15 forward-vector roster. Commits in order: `9f8581cd` docs revamp (README + Whitepaper v4 successor canonical) + `9a335c1b` precision tuning (10 framing items per operator review including AGaaS = Anti-cheat as a Service + 49 substantive live contracts standardization + first/only → introduces/reference-implementation) + `2c243f26` G7 Curator Review Readiness observability harness (+10 tests T-G7-1..10; 5-section sqlite-only audit) + `a7b8a025` Reproducibility Receipt external artifact verifier (+12 tests T-REPLAY-1..12; live smoke 6 ZKBA artifacts PASS) + `32917ab5` CFSS Cedar-policy lane authority drift sweeper CLI (+9 tests T-CFSS-1..9; reuses EXPECTED_LANE_MATRIX as single source of truth) + `a13318f3` Layer 7 composition exhaustiveness + VPM lifecycle regression guard (+18 tests; closes T-ZKBA-HW-10 non-collision + composition propagation formal gap) + `523e5c51` VPMAnchorRegistry.sol contract + Hardhat tests + deploy script + runbook (+14 Hardhat tests; extends FROZEN quadruple-bind to quintuple-bind via `anchorVPM(zkbaManifestHash, vpmManifestHash, tsNs)` with `IAdjudicationRegistry.isRecorded()` cross-contract integrity check at write time + Ownable+CEI guards) + `72d7b2f4` Curator graduation readiness consolidated audit (+11 tests; orchestration audit consolidating G7+watcher+CFSS+on-chain into READY/BLOCKED/FAIL/ERROR verdict with priority ERROR > FAIL > BLOCKED > PASS) + `e2da3e6c` W3bstream applet integration audit + real selectors documented inline (+11 tests; computes `0xbabcf9f5` for `isConsentValid` + `0x7c4847ed` for `submitPITLProof` + documents 5 crypto-integration deltas P256_VERIFY/ABI_ENCODER/POSEIDON_HASH/CONSENT_RETURN_DATA/DEVICE_ID_TO_GAMER) + `d74a8961` LayerZero VHP mainnet activation runbook + readiness audit (+8 tests; 4 STUB indicators + 5 PRODUCTION indicators tracked; mainnet activation gated on touchpad_corners closure + VHP production pipeline + third-party audit) + `19e6ba0f` chain.anchor_vpm() bridge-side helper for VPMAnchorRegistry (+11 tests T-VPM-ANCHOR-1..11; mirrors anchor_corpus_snapshot pattern with kill-switch-first + config + account presence + hex norm + zero-hash upfront reject + gas-estimate × 1.25 + never raises; FROZEN `_VPM_ANCHOR_ABI` literal pinned by T-VPM-ANCHOR-11) + `be53cd3c` continuous CFSS sweeper + 27th FSCA rule CFSS_LANE_AUTHORITY_DRIFT CRITICAL (+10 tests T-CFSS-INT-1..10; bridge runtime asyncio task at 60s cadence aligned with cedar_drift_sweeper; new cfss_lane_drift_log table Phase 1210 migration; closes data-layer / policy-layer enforcement asymmetry for Cedar v2 CFSS) + `1bbf163f` PV-CI invariants pinning new constructs 77 → 83 (6 new INV-VPM-ANCHOR-* + INV-CFSS-SWEEPER-* + INV-FSCA-CFSS-RULE-001; allowlist regenerated via governance ceremony with phrase "I understand this changes a frozen protocol invariant" piped to --confirm-governance; reason category invariant_change; bridge governance event POST 404 non-blocking matches Phase 224 + Phase O4 close precedent) + `0f2d10fa` 3 audit HTTP endpoints `GET /operator/g7-curator-readiness` + `GET /operator/cfss-lane-drift-status` + `GET /operator/curator-graduation-readiness` (+8 tests T-AUDIT-EP-1..8; read-key auth + asyncio.to_thread worker dispatch per Phase 235-STAB invariant — never block event loop with sync work) + `1df30de4` parallel_vpm_anchor.py triple-gate ceremony script (+11 tests T-PARALLEL-VPM-1..11; mirrors parallel_o2/o3 triple-gate pattern; DISTINCT env var OPERATOR_VPM_ANCHOR_AUTHORIZED prevents O2/O3 carry-over). **CUMULATIVE DELTAS:** Bridge tests 3344 → 3463 (+119); Hardhat 528 → 542 (+14 VPMAnchorRegistry suite); SDK / Contracts (LIVE 49) unchanged; PV-CI invariants 77 → 83 (+6); FSCA contradiction rules 26 → 27 (+1 CFSS_LANE_AUTHORITY_DRIFT). **OPERATIONAL POSTURE:** CHAIN_SUBMISSION_PAUSED=true held throughout the entire 17-commit envelope; wallet 0 IOTX impact; ZERO mainnet operations; ZERO governance-bypass attempts; ZERO FROZEN-region modifications. **6 wallet-free CLI audit harnesses + 3 audit HTTP endpoints** now ship under one shape (stdlib-only + read-only + JSON mode + exit-code-on-verdict + test bands): g7_curator_review_readiness_audit.py + replay_artifact.py + cfss_lane_drift_sweep.py + curator_graduation_readiness_audit.py + w3bstream_applet_audit.py + layerzero_vhp_bridge_audit.py. **VPMAnchorRegistry consumption path END-TO-END COMPLETE** at the wallet-free agent-actionable surface — contract + Hardhat tests + deploy script + runbook + chain.anchor_vpm() helper + FROZEN ABI literal + PV-CI invariants + triple-gate ceremony script. Operator fires deploy ceremony when wallet ready (~0.1 IOTX) per the runbook three-factor authorization; entire downstream consumption path activates with no further agent work required. **CFSS DATA-LAYER / POLICY-LAYER ENFORCEMENT SYMMETRY ACHIEVED** — FSCA's prior 26 contradiction rules covered the data layer; the new 27th rule CFSS_LANE_AUTHORITY_DRIFT (CRITICAL) covers the Cedar policy layer. **v4 §15 FORWARD-VECTOR BACKLOG AT WALLET-FREE AGENT-ACTIONABLE SURFACE: CLOSED.** Remaining items each require operator three-factor authorization for wallet-gated ceremonies (VPMAnchorRegistry deploy / Curator O2→O3 graduation / W3bstream applet pipeline rewrite + registration / LayerZero VHP refactor) or hardware capture sessions (touchpad_corners corpus / BT calibration / Sensor Stack v2). MAINNET DEPLOYS EXPLICITLY BLOCKED until Operator Initiative (Sentry/Guardian/Curator all at O3_ACT) is totally complete per operator directive 2026-05-13. Whitepaper v4 (`docs/vapi-whitepaper-v4.md` at architecture anchor `e81e04aa` / documentation revamp `9f8581cd` / precision tuning `9a335c1b`) is the canonical successor; v3 Zenodo DOI `10.5281/zenodo.18966169` preserved as historical baseline.
NOTE: 🏛️ PHASE O4-VPM-INTEGRATION COMPLETE 2026-05-13 — **Methodology Layer (Layer 7) reaches FULL OUTPUT SURFACE STATE.** Nine atomic commits + final close ship across the protocol stack: `168256a0` Layer 7 coverage audit + 7-of-7 closure doc → `603c98cb` V-check provenance pin → `524ae1cc` A.0 compiler engine + T-VPM-COMPILER-1..10 → `fd0d6699` A.1 4 internal VPM compilers + Anti-Hype Visual Grammar → `7052144f` A.2 2 consumer-facing + procedural geometric art → `169471bb` A.3+A.4 4 draft manifests + vpm_audit tooling → `1b13618d` B.0-B.3 store + 3 read endpoints → `d5803d47` B.4-B.7 write + validate + audit + stability harness → `0061e6d9` C VPM Registry tab + Layer 3 grammar verifier → this close commit. **Phase O4 turns the seven Phase O3-ZKBA-TRACK1 artifact classes (AIT/GIC/VHP/HARDWARE/CONSENT/TOURNAMENT/MARKET) from filesystem-only HTML into first-class delivered media** with three independent enforcement surfaces (Python compiler-side + Python bridge-side + JavaScript browser-side). **Stream A — Protocol surface (6 atomic commits):** new `compile_vpm_artifact()` entry-point in scripts/vsd_ui_compiler.py with FROZEN static guards (no external URLs / no runtime network / no randomness / no wall-clock / mandatory 9-field Integrity Label DOM) + 6 active VPM compilers (HONESTY-BOARD/AGENT-REVIEW/CDRR-DAG/GIC-LEDGER-BETA Sentry+Guardian internal under VBDIP-0002A §10 umbrella IDs + DISPUTE-PACKET Guardian-lane + MARKET-LISTING Curator-lane consumer-facing) + 4 draft manifests (PROOF-WALLET/QR-ELIGIBILITY/HARDWARE-LINEAGE/CONSENT-CAPSULE at Reserved→Draft Manifest promotion) + scripts/vpm_audit.py 6-section CLI harness + shared scripts/vpm_visual_grammar.py FROZEN 6-state DOM signature matrix (live/dry-run/emulated/frozen-disabled/revoked/unverified) + **procedural geometric art** v1 FROZEN algorithm in MARKET-LISTING-v1 (32 SHA-256 bytes → 8 four-byte chunks → 8 polygon tiles; per-byte sensitivity cryptographically inherits from SHA-256). **Stream B — Bridge HTTP surface (2 atomic commits):** vpm_artifact_log SQLite table (Phase 1200 migration) + 6 new HTTP endpoints (/operator/vpm-list + /vpm-artifact/{commit} + /vpm-manifest/{commit} + /vpm-compile + /vpm-validate-manifest + /vpm-audit-status) under read-key/full-key auth separation + FROZEN CSP headers (default-src 'none' / frame-ancestors 'self' / form-action 'none') + worker-thread compile dispatch (asyncio.to_thread). **Stream C — Frontend VPM Registry tab (1 atomic commit):** 6th top-level view added strictly-additively to App.jsx + ViewSelector.jsx + bridgeApi.js (no existing components rewritten); VpmRegistryView two-pane orchestrator + VpmFilterChips + VpmIframe with FROZEN `sandbox="allow-scripts allow-same-origin"` literal (no forbidden flags) + VpmManifestPanel with client-side Web Crypto SHA-256 hash check + VpmGrammarVerifier as Layer 3 of three-layer Anti-Hype Visual Grammar (reads iframe contentDocument; runs FROZEN signature assertions; surfaces GRAMMAR FAIL red badge on drift); first frontend test infrastructure in the repo (Vitest + jsdom + @testing-library/react; 26 tests across 4 component test files; build chunk 14.26 KB raw / 4.76 KB gzipped well under 60 KB envelope). **PV-CI gate 67 → 77** via governance ceremony (phrase "I understand this changes a frozen protocol invariant" piped to --confirm-governance): 10 new INV-VPM-* invariants pinning compile_vpm_artifact + vapi-vpm-artifact-v1 schema literal + 9-field Integrity Label tuple + 6-state VISUAL_STATES tuple + 5-state _VPM_CAPTURE_MODES + wrapper schema ref + _VPM_HTML_RESPONSE_HEADERS CSP dict + "allow-scripts allow-same-origin" iframe literal + POST /operator/vpm-compile route + section_1_active_compiler_registry audit function. 11 total INV-VPM-* family (INV-VPM-WRAPPER-001 pre-existing 2026-05-12). **FSCA contradiction rules 23 → 26** (+3): VPM_VISUAL_STATE_DOM_MISMATCH HIGH (incomplete-compile detection) + VPM_MANIFEST_HASH_DRIFT HIGH (cross-table referential integrity vs zkba_artifact_log) + VPM_LIFECYCLE_REGRESSION MEDIUM (Compiler Target VPMs stagnant >30d). **VBDIP-0002 Appendix B §B.8 gate sweep: 10 of 11 sub-gates SATISFIED** — G3 PARTIALLY → SATISFIED (compiler harness covers all 7 ZKBA classes + 6 VPM compilers + 4 drafts × 6 visual states × 3-layer grammar enforcement); G8 PENDING → SATISFIED (internal-first sequencing honored: HONESTY-BOARD/AGENT-REVIEW/CDRR-DAG/GIC-LEDGER-BETA shipped Stream A.1 BEFORE consumer-facing DISPUTE-PACKET/MARKET-LISTING Stream A.2); G5c EXTENDED with Layer 3 frontend-side enforcement; G7 only OPEN sub-gate (Curator Review Readiness — operator-runtime observation work, 7-day window with ≥9/10 acceptance gate; not a Phase O4 deliverable). **VBDIP-0002A §10 registry lifecycle ladder**: 8 of 10 IDs at active lifecycle (2 Test Fixture: HONESTY-BOARD-v1 + AGENT-REVIEW-v1; 2 Compiler Target: DISPUTE-PACKET-v1 + MARKET-LISTING-v1; 4 Draft Manifest: PROOF-WALLET-v1 + QR-ELIGIBILITY-v1 + HARDWARE-LINEAGE-v1 + CONSENT-CAPSULE-v1; 2 Reserved: PROOF-TRAILER-v1 + DEV-SANDBOX-v1 — deferred for broadcast partnership / SDK 4.x). Bridge 3160 → 3344 (+184 tests across 21 new test files). Frontend Vitest 0 → 26 (first frontend test infra). SDK / Hardhat / Contracts unchanged at 562 / 528 / 49 LIVE. **CHAIN_SUBMISSION_PAUSED=true held throughout the entire Phase O4 envelope; wallet 0 IOTX impact across all 10 commits.** Provenance pin at `wiki/phases/phase_o4_vpm_integration.md`. **Three-layer Anti-Hype Visual Grammar now LIVE** across compile-time + bridge-time + browser-time. Drift between any compiler's emitted HTML and the canonical 6-state DOM signature surfaces as: VPMComplianceError (Layer 1) / scripts/vpm_audit.py Section 5+6 FAIL (Layer 2) / GRAMMAR FAIL red badge at operator console (Layer 3). This is the protocol's first structural UX defense against demo-as-production / revoked-as-active / unverified-as-verified overclaim attacks — the "DePIN-photoshop-attack" defense per Phase O4 plan §5.5. **VAPI now holds the frozen-primitive ↔ frozen-compiler ↔ frozen-visual-grammar ↔ frozen-iframe-sandbox quadruple-bind** — every cryptographic claim is independently verifiable by anyone with the canonical-JSON algorithm + SHA-256 + the public source of the four enforcement layers.
NOTE: 🛠️ 7TH (FINAL) ZKBA ARTIFACT TARGET — HARDWARE PARTICIPATION CARD SHIPPED 2026-05-13 — **Closes Layer 7 coverage to 7-of-7 ZKBAClass values** (AIT=1 / GIC=2 / VHP=3 / **HARDWARE=4** / CONSENT=5 / TOURNAMENT=6 / MARKET=7). External reviewers comparing the Pass 2C activation matrix against shipped artifacts can no longer flag `class 4 = HARDWARE` as unverified; the §9.29 whitepaper integration claim becomes "all 7 ZKBAClass values empirically composable through the same PATTERN-017 primitive + compiler + wrapper + validator + audit pipeline" without an exception clause. **First MANUFACTURER-bound audience artifact** — prior 6 artifacts targeted gamers (CONSENT), operators (AIT/GIC/VHP/TOURNAMENT), or buyers (MARKET). HARDWARE Participation Card binds the certifying MANUFACTURER's on-chain Ethereum address into the preimage as a publicly-attributable cryptographic surface — first ZKBA artifact whose commitment depends on which manufacturer certified the device, not just on the device or gamer state. Surfaces manufacturer-attestation as a cryptographic surface for partner programs (Sony Partner / GuliKit aftermarket / etc.). Selection: `ZKBAClass.HARDWARE` (= 4). `ProofWeightClass.CHAIN_ONLY` (VAPIHardwareCertRegistry state — Phase 99A LIVE on IoTeX testnet — on-chain anchored at certification time; `chain.isCertified(profile_hash)` is a zero-gas view call consumed offline at compile time). Sentry-lane artifact (writes to `zk_artifacts/` under existing Cedar v2 lane authority; no new lane matrix entries required). New file `scripts/zkba_compile_hardware_card.py` (~480 LOC) ships single-32B-component composition `SHA-256(profile_hash(32) || device_id_hash(32) || cert_level_be(1) || manufacturer_addr(20) || is_certified_byte(1))` = 86B preimage → 32B. **Manufacturer address NOT hashed** — surfaced in clear in the preimage (and in HTML) for public attribution; mirrors the gamer-address-hashing privacy decision in CONSENT v1 but flipped (hardware certification is publicly attributable by design, so no privacy hashing applies). Manufacturer address parsed case-insensitively (lowercased to raw 20B) — EIP-55 checksum case difference produces identical preimages. Deterministic HTML renderer: 3 sections (Hardware Profile / Certifying Manufacturer / ZKBA Commitment) + CERTIFIED green / REVOKED red status badge + cert-tier badge (TIER 1 — CONTROLLER ONLY / TIER 2 — CONTROLLER + GSR per VAPIHardwareCertRegistry §35) + manufacturer-address badge + footer reasserting "completes Layer 7 coverage to 7-of-7 ZKBAClass values". CLI: --profile-hash/--device-id-hash/--cert-level/--manufacturer-address/--is-certified/--output-dir/--ts-ns. Default output `frontend/src/artifacts/hardware_participation_card/` (gitignored per existing pattern). New file `bridge/tests/test_phase_o3_zkba_hardware_card.py` (~470 LOC, 14 tests T-ZKBA-HW-1..10 with parametrized #7×5): byte layout matches FROZEN spec (#1); end-to-end build verifies CERTIFIED status + manufacturer address surfaced in clear (#2); idempotent (#3); byte-stable two-run (#4); VPM wrap (#5); G4 validate (#6); per-field tamper x5 covering all 5 input fields (#7); audit CFSS unchanged (#8); **#9 explicit Sentry-lane authority assertion** — Sentry PERMITs `tool:zk-artifact-anchor` on `draft://zk_artifacts/*`, Guardian + Curator both FORBID; mirrors the MARKET-9 Curator-exclusive + CONSENT-9 Guardian-exclusive assertions; **with this test, the 3-agent CFSS lane authority matrix is empirically verified at the Cedar policy level for ALL three artifact lanes** (zk_artifacts/, zk_verifications/, zk_listings/) — the triangle closes; (#10) **Layer 7 7-of-7 closure invariant** — confirms `ZKBAClass.HARDWARE == 4` (FROZEN-v1 enum position); verifies cross-class commitment collision does NOT occur with otherwise-identical inputs (iterates all 6 OTHER ZKBAClass values — AIT/GIC/VHP/CONSENT/TOURNAMENT/MARKET — and asserts none collide; proves the domain-tag byte in `compute_zkba_commitment` is load-bearing); confirms manufacturer address is independently load-bearing (zero address vs canonical bridge wallet → different commitments). CLI smoke confirmed live: output_hash `e1fe4f28dcc0c46c5c7535a76b08252849b91715527a0757b32e492995f07f57`, input_commitment `678cf62f89bedc6a80d153f17408b65ec581212ff2e52974f03f48f6af103243`. Regression band 175/175 PASS in 31s (HARDWARE 14 + CONSENT 15 + TOURNAMENT 16 + MARKET 14 + VHP 12 + AIT 13 + compiler/GIC 28 + manifest validator 55 + VPM wrapper 24 + Track 2 audit 8). PV-CI 67/67 PASS unchanged (HARDWARE reuses existing INV-ZKBA-001/002/003 — no new invariants required; PATTERN-017 primitive composition + manifest schema + domain tag uniqueness all hold). Bridge 3146→3160 (+14). SDK / Hardhat / Contracts unchanged. CHAIN_SUBMISSION_PAUSED=true held; wallet 0 IOTX impact (filesystem-only ship). No bridge restart required. **Layer 7 pipeline now empirically validated across the FULL DIMENSION CUBE**: 7/7 artifact classes × 3 proof weights × all 3 agent lanes × {1-primitive, 2-primitive, 3-primitive} composition shapes × {gamer, operator, buyer, manufacturer} audiences + GDPR Art. 17 audit surface + manufacturer-attestation surface. **PATTERN-017 + Methodology Layer (Layer 7) is now operationally complete** — no remaining unshipped ZKBAClass values; future artifacts compose existing classes via the same pipeline rather than expanding the class enum. The Phase O3-ZKBA-TRACK1 + Track 2 envelope is now CLOSED for breadth as well as depth.
NOTE: 🔐 6TH ZKBA ARTIFACT TARGET — CONSENT RECEIPT CARD SHIPPED 2026-05-12 (commit `9bfa981e`) — Sixth ZKBA artifact. **Closes the 3-agent CFSS lane coverage gap** with three architectural firsts. (1) **First Guardian-lane artifact** — writes to `zk_verifications/` under Guardian's audit-trail authority. Prior 4 artifacts (GIC/VHP/AIT/TOURNAMENT) all under Sentry's `zk_artifacts/`; MARKET (`269e439c`) under Curator's `zk_listings/`. With this commit, **all three Operator Initiative agents (Sentry/Guardian/Curator) now have empirical lane utilization**, not just policy-level authority. (2) **First gamer-facing artifact audience** — prior artifacts targeted operators (Sentry surfaces) or buyers (MARKET). CONSENT is the gamer's own audit surface for their own consent state. Exercises gamer-self-sovereignty: bridge produces READ-ONLY projection, never grants/revokes consent in this script. (3) **First GDPR Article 17 compliance-bearing artifact** — `revoked_at` field surfaces right-to-be-forgotten semantically; cryptographically distinguishable from active state per T-ZKBA-CONSENT-10. Selection: `ZKBAClass.CONSENT` (= 5). `ProofWeightClass.CHAIN_ONLY` (default; consent state lives in VAPIConsentRegistry on chain). Composes the Phase 237 CONSENT v1 FROZEN primitive output as first preimage component (`consent_hash` = output of `compute_consent_hash()`). New file `scripts/zkba_compile_consent_receipt.py` (~430 LOC) ships single-32B-component composition `SHA-256(consent_hash(32) || category_bitmask_be(4) || revoked_at_be(8) || gamer_address_hash(32) || receipt_id_be(8))` = 84B preimage → 32B. Gamer address is hashed via SHA-256 — pseudonymous identifier; the address itself never appears in the artifact. Deterministic HTML renderer surfaces GDPR Art. 17 notice prominently + status badge (ACTIVE green / REVOKED red) + decoded category list from bitmask (4 categories: TOURNAMENT_GATE bit 0 / ANONYMIZED_RESEARCH bit 1 / MANUFACTURER_CERT bit 2 / MARKETPLACE bit 3) + footer reasserting gamer-self-sovereignty invariant. CLI: --consent-hash/--category-bitmask/--revoked-at/--gamer-address-hash/--receipt-id/--output-dir/--ts-ns. Default output `frontend/src/artifacts/consent_receipt_card/` (gitignored per GIC pattern). New file `bridge/tests/test_phase_o3_zkba_consent_receipt.py` (~400 LOC, 15 tests T-ZKBA-CONSENT-1..11 with parametrized #7×5): byte layout matches FROZEN spec (#1); end-to-end build verifies GDPR notice + gamer-self-sovereignty text + decoded category labels in HTML (#2); idempotent (#3); byte-stable (#4); VPM wrap (#5); G4 validate (#6); per-field tamper x5 (#7); audit CFSS unchanged (#8); **#9 explicit Guardian-lane authority assertion** — Guardian PERMITs `tool:zk-audit-trail` on `draft://zk_verifications/*`, Sentry + Curator both FORBID; mirrors the MARKET-9 Curator assertion; CFSS now empirically verified for all 3 agents at the Cedar policy level; (#10) **GDPR Art. 17 revocation semantic invariant** — active vs revoked MUST produce different commitments; 1-second revocation timing difference also distinguishable; cryptographic audit of GDPR compliance becomes possible; (#11) bitmask category decoding invariant — tests all 16 combinations of 4-bit bitmask; verifies bitmask is in preimage not just renderer. CLI smoke confirmed live: output_hash `dcc8976a4bfac96a9c0209e19bb7c3e1614ebef632998ea18bbe0cbd3f3a3a19`, input_commitment `d45615ff1ffdef9efa7857fc930c43c0dd20ed492076537d85cc96ae537ac97b`. Regression band 182/182 PASS in 91s. PV-CI 67/67 PASS unchanged (CONSENT reuses INV-ZKBA-001/002/003). Bridge 3131→3146 (+15). SDK / Hardhat / Contracts unchanged. CHAIN_SUBMISSION_PAUSED=true held; wallet 0 IOTX. **Layer 7 pipeline now validated across 6 of 7 artifact classes × 3 proof weights × all 3 agent lanes × {1-primitive, 2-primitive, 3-primitive} composition shapes × {operator, buyer, gamer} audiences + GDPR Art. 17 audit surface**. Remaining: HARDWARE (class 4) — final unshipped class.
NOTE: 🏆 5TH ZKBA ARTIFACT TARGET — TOURNAMENT ELIGIBILITY CARD SHIPPED 2026-05-12 (commit `25e7f8f2`) — Fifth ZKBA artifact + deepest composition test to date. **First artifact referencing 3+ FROZEN-v1 primitive surfaces simultaneously**: VHP v1 (Phase 99-PREP soulbound token) + GIC v1 (Phase 235-A continuity primitive) + `VAPIProtocolLens.isFullyEligible()` (the singular on-chain composability gate the entire protocol design hinges on, which itself transitively composes PoAC + L4 + L2B + L2C + L5 over the 9-level PITL stack). Prior MARKET artifact (`269e439c`) composed 2 primitives; this commit pushes to 3, empirically proving Layer 7 composes at depth — the architectural pattern future HARDWARE + CONSENT classes will inherit. Selection: `ZKBAClass.TOURNAMENT` (= 6); `ProofWeightClass.CHAIN_ONLY` (all referenced state on-chain anchored). Same Sentry `zk_artifacts/` lane authority (operator-facing artifact for tournament organizers + adjudicators consulting it for dispute resolution months after a match). Real fixture: VHP tokenId=2 (Session 3 mint `76c92e9b`) + GIC_100 head `0x0e9d453d904220148d632e75802b71bdff74c7197aa8afcbfec5d36a61ab48da` (Phase 239 G3 permanent anchor 2026-05-06) + canonical Sony DualShock Edge CFI-ZCP1 device_id_hash + synthetic `tournament_id=20260601001`. New file `scripts/zkba_compile_tournament_card.py` (~470 LOC) ships flat single-32B-component composition `SHA-256(vhp_token_id_be(32) || vhp_is_valid_byte(1) || gic_chain_head(32) || gic_chain_length_be(8) || is_fully_eligible_byte(1) || device_id_hash(32) || tournament_id_be(8))` = 114B preimage → 32B. **Flat composition (no sub-hash trees)** — preimage is auditable by a verifier with only the seven values + SHA-256 implementation; reproducibility from on-chain state alone with zero extra-protocol machinery. Deterministic HTML renderer: sections per primitive (VHP / GIC / Hardware Binding / Tournament Context / ZKBA Commitment); prominent ELIGIBLE/INELIGIBLE badge; cross-primitive composition surfaced in footer ("transitively anchored to the full 9-level PITL stack"). CLI: --vhp-token-id/--vhp-is-valid/--gic-chain-head/--gic-chain-length/--is-fully-eligible/--device-id-hash/--tournament-id/--output-dir/--ts-ns. Default output `frontend/src/artifacts/tournament_eligibility_card/` (gitignored). New file `bridge/tests/test_phase_o3_zkba_tournament_card.py` (~450 LOC, 16 tests T-ZKBA-TOURN-1..10 with parametrized #7×7 — first artifact with 7-axis tamper coverage): byte layout matches FROZEN spec (#1); end-to-end build verifies all 3 primitive references appear in HTML (#2); idempotent (#3); byte-stable (#4); VPM wrap (#5); G4 validate (#6); per-field tamper x7 (#7) covering all 7 input fields; audit CFSS unchanged (#8); **#9 eligibility semantic invariant** — flipping `is_fully_eligible` MUST change the commitment; verifies vhp_is_valid bit is independently load-bearing; (#10) **cross-primitive composition depth** — explicitly groups fields by source primitive: same VHP+different GIC → different commitments; same GIC+different VHP → different commitments; different device → different commitments. Proves all referenced primitive surfaces participate (not just one), distinct from per-field tamper coverage. CLI smoke confirmed live: output_hash `5b09a65e64f13026461ef5ea7aff701f8840f1c1e5202f60bb8f88a7474da5cb`, input_commitment `b0092146da552426a93f42d25e0748655b16627b02dc4c1be4f9dc037a48b7a5`. Regression band 167/167 PASS in 71s. PV-CI 67/67 PASS unchanged (TOURNAMENT reuses INV-ZKBA-001/002/003). Bridge 3115→3131 (+16). SDK / Hardhat / Contracts unchanged. CHAIN_SUBMISSION_PAUSED=true held; wallet 0 IOTX. **Layer 7 now empirically proven to compose primitives at depth** — pipeline validated across 5 classes × 3 proof weights × 2 agent lanes × composition shapes {1-primitive, 2-primitive, 3-primitive}. Remaining classes: HARDWARE (4), CONSENT (5).
NOTE: 🏪 4TH ZKBA ARTIFACT TARGET — MARKETPLACE LISTING CARD SHIPPED 2026-05-12 (commit `269e439c`) — Fourth ZKBA artifact target. **Three architectural firsts in a single ship**: (1) **First Curator-lane artifact** — writes to `zk_listings/` under Curator's Cedar v2 lane authority (vs Sentry's `zk_artifacts/` for GIC/VHP/AIT); empirically tests CFSS holds across agents, not just in the audit harness matrix. (2) **First MARKETPLACE_DERIVED proof weight** — third distinct ProofWeightClass through the pipeline (CHAIN_ONLY → CALIBRATION_PLUS_CONTEXT → MARKETPLACE_DERIVED). (3) **First cross-primitive composition** — component preimage references TWO FROZEN-v1 primitives: LISTING-v1 (Phase 238 `listing_commitment`) + CONSENT v1 (Phase 237 MARKETPLACE-category `consent_hash`). Prior artifacts each composed at most one primitive — this commit empirically validates that PATTERN-017 composes primitives compositionally, the pattern future TOURNAMENT + HARDWARE classes will inherit. Selection: `ZKBAClass.MARKET` (= 7). New file `scripts/zkba_compile_marketplace_listing.py` (~395 LOC) ships single-32B-component composition `SHA-256(listing_commitment(32) || tier_multiplier_milli_be(8) || ipfs_cid_root(32) || consent_hash(32) || suspended_byte(1))` = 105B preimage → 32B. IPFS CID hashed via SHA-256(UTF-8 bytes) for fixed-length preimage (v0+v1 CID encoding-agnostic; no CID-internal normalization). Tier encoded as uint64 milli (tier×1000; e.g. 2000 for tier 2.0×). Deterministic HTML renderer: ACTIVE/SUSPENDED status badge color-coded; tier badge amber; cross-primitive composition surfaced in footer. CLI: --listing-commitment/--tier-multiplier-milli/--ipfs-cid/--consent-hash/--suspended/--output-dir/--ts-ns. Default output dir `frontend/src/artifacts/marketplace_listing_card/` (gitignored per GIC pattern). New file `bridge/tests/test_phase_o3_zkba_marketplace_listing.py` (~390 LOC, 14 tests T-ZKBA-MARKET-1..10 with parametrized #7×5): byte layout matches FROZEN spec + suspended flip (#1); end-to-end build asserts proof_weight is neither CHAIN_ONLY nor CALIBRATION_PLUS_CONTEXT first MARKETPLACE_DERIVED artifact (#2); rebuild idempotent (#3); byte-stable two-run determinism (#4); VPM wrapper first MARKETPLACE_DERIVED round-trip (#5); G4 validator first MARKETPLACE_DERIVED manifest accepted (#6); per-field tamper detection on listing/tier/cid/consent/suspended (#7); post-audit CFSS unchanged after MARKET row inserted alongside GIC/VHP/AIT (#8); **#9 explicit cross-agent lane authority** — Curator PERMITs `tool:zk-marketplace-listing` on `draft://zk_listings/*`, Sentry + Guardian both FORBID; verifies CFSS at Cedar policy level not just at audit matrix level; sanity-checks EXPECTED_LANE_MATRIX encodes 1-permit/2-forbid invariant; (#10) IPFS CID encoding invariant — different CIDs (and CID case difference) produce different commitments; locks "no CID-internal normalization" claim. **End-to-end Layer 7 pipeline now validated across 4 dimensions**: 4 artifact classes {AIT=1, GIC=2, VHP=3, MARKET=7} × 3 proof weights × 2 agent lanes × 2 composition shapes (single-primitive AND cross-primitive). CLI smoke confirmed live: output_hash `1649f2803e0e3207f93fb1daac25d71d579ba3150d9d15317b97fe0e65a70d5f`, input_commitment `32c466da6f3db5c7b3f7fc1d2214ed4cd4c1d7ad90a42b29b4f2a51cfcf73e44`. Regression band 151/151 PASS in 48s. PV-CI invariant gate 67/67 PASS unchanged (MARKET reuses existing INV-ZKBA-001/002/003). Bridge 3101→3115 (+14). SDK / Hardhat / Contracts unchanged. CHAIN_SUBMISSION_PAUSED=true held; wallet 0 IOTX impact. No bridge restart required. **Layer 7 now empirically proven to compose primitives, agents, and proof weights compositionally** — the Methodology Layer is multi-dimensional, not a single-axis surface.
NOTE: 📈 3RD ZKBA ARTIFACT TARGET — AIT SEPARATION SNAPSHOT SHIPPED 2026-05-12 (commit `bdbcf67f`) — Third concrete ZKBA artifact target after GIC Continuity Ledger Alpha (`3b3081d3`) and VHP Verification Card (`4f399282`). **First non-CHAIN_ONLY proof weight class through the pipeline**: `ProofWeightClass.CALIBRATION_PLUS_CONTEXT` (matches DEFAULT_PROOF_WEIGHT_BY_CLASS[AIT] in G4 manifest validator). Empirically proves Layer 7 composability holds across multiple ProofWeightClass values, not just the single CHAIN_ONLY profile exercised by the first two artifacts. Selection: `ZKBAClass.AIT` (= 1) completes the contiguous `{AIT, GIC, VHP} = {1, 2, 3}` enum triplet. Real fixture is the headline empirical anchor — Phase 229 + Phase 231 AIT corpus close: `separation_ratio = 1.199` (ratio_milli=1199000) at `N=37` (P1=13/P2=10/P3=14, all >= 10), `all_pairs_above_1 = True` (first probe to clear the tournament gate), `pair_distances = {P1vP2:1.850, P1vP3:1.846, P2vP3:1.349}`, `analysis_date = 1745539200` (2026-04-20 UTC). New file `scripts/zkba_compile_ait_snapshot.py` (~340 LOC) ships single-32B-component composition `SHA-256(ratio_milli_be(8) || n_sessions_be(8) || analysis_date_be(8) || pair_distances_root(32))` where `pair_distances_root = SHA-256(canonical_sorted_key_JSON(pair_distances))` — sort_keys=True discipline matches `scripts/vsd_ui_compiler.canonical_json` verbatim. Ratio encoded as uint64 milliratio per same OS-deterministic byte-encoding convention as CORPUS-SNAPSHOT v1. Deterministic HTML renderer: gate badge color-coded (green when all_pairs_above_1=True, red otherwise); pair_distances rendered as canonical-JSON pre block; self-contained no CDN/network/wall-clock. CLI: --ratio-milli/--n-sessions/--analysis-date/--pair-distances (JSON)/--all-pairs-above-1/--output-dir/--ts-ns. Default output dir `frontend/src/artifacts/ait_separation_snapshot/` (gitignored per GIC pattern). New file `bridge/tests/test_phase_o3_zkba_ait_snapshot.py` (~340 LOC, 13 tests T-ZKBA-AIT-1..9 with parametrized #7×5): byte layout matches FROZEN spec (#1); end-to-end build manifest+HTML+DB row asserts proof_weight!=CHAIN_ONLY first non-CHAIN_ONLY artifact in pipeline (#2); rebuild idempotent UNIQUE(commitment_hex) (#3); byte-stable two-run determinism (#4); VPM wrapper consumes manifest cleanly first CALIBRATION_PLUS_CONTEXT round-trip (#5); G4 validator returns valid=True first non-CHAIN_ONLY manifest through validator's enum range check (#6); per-field tamper detection ratio_milli/n_sessions/analysis_date/pair_distances_value/pair_distances_extra_key (#7); post-audit CFSS lane authority unchanged after AIT row inserted alongside VHP card (#8); **pair_distances canonical ordering invariant** — different dict insertion orders MUST produce identical component hash (#9) — locks sort_keys=True discipline against future serialization drift. **End-to-end Layer 7 pipeline now validated across 3 artifact classes + 2 proof weights**: GIC (class 2, CHAIN_ONLY) + VHP (class 3, CHAIN_ONLY) + AIT (class 1, CALIBRATION_PLUS_CONTEXT). CLI smoke confirmed live: output_hash `23da742e84a1b7f43fa44673f0820f66c639cee3c94be739b9be16ba3dad554a`, input_commitment `9b61c3ffd95e557bb5e978cd1b8a69073661a30ec2c4fef1fd2359e119105739`. Regression band 137/137 PASS in 34s. PV-CI invariant gate 67/67 PASS unchanged (AIT snapshot reuses existing PATTERN-017 INV-ZKBA-001/002/003 — no new invariants required). Bridge 3088→3101 (+13). SDK / Hardhat / Contracts unchanged. CHAIN_SUBMISSION_PAUSED=true held; wallet 0 IOTX impact. No bridge restart required (filesystem-only ship). **Layer 7 multi-proof-weight composability now empirically proven**: any future ZKBA class with any of the 6 ProofWeightClass values flows through the same primitive + compiler + wrapper + validator + audit pipeline; the Methodology Layer is composable across the full proof-weight dimension, not just CHAIN_ONLY.
NOTE: 🎴 2ND ZKBA ARTIFACT TARGET — VHP VERIFICATION CARD SHIPPED 2026-05-12 (commit `4f399282`) — Second concrete ZKBA artifact target after the GIC Continuity Ledger Alpha (commit `3b3081d3`). Empirically proves Layer 7 methodology + ZKBA primitive composition holds for a second artifact class without further protocol changes, exercising the newly-anchored Cedar v2 `zk_artifacts/` lane authority (Sentry-exclusive per CFSS) under real load. Selection: `ZKBAClass.VHP` + `ProofWeightClass.CHAIN_ONLY` (matches GIC precedent — no fresh biometric capture; VHP token state on-chain anchored at mint time). Real fixture: bridge wallet's VHP tokenId=2 from Session 3 mint commit `76c92e9b` (canonical Sony DualShock Edge CFI-ZCP1 binding). New file `scripts/zkba_compile_vhp_card.py` (~415 LOC) ships single-32B-component composition `SHA-256(token_id_be(32) || device_id_hash(32) || expires_at_be(32) || cert_level_be(1) || is_valid_byte(1))` + deterministic HTML renderer (valid/expired badge color-coded; self-contained; no CDN/network/wall-clock) + CLI with --token-id/--device-id-hash/--expires-at/--cert-level/--is-valid/--ts-ns/--output-dir args. Default output dir `frontend/src/artifacts/vhp_verification_card/` (gitignored per GIC pattern). New file `bridge/tests/test_phase_o3_zkba_vhp_card.py` (~315 LOC, 12 tests T-ZKBA-VHP-1..8 with parametrized #7×5): byte layout matches FROZEN spec (#1); end-to-end build manifest+HTML+DB row with anchor_tx_hash=NULL Track 1 invariant (#2); rebuild idempotent UNIQUE(commitment_hex) (#3); byte-stable two-run determinism across different DBs+output_dirs (#4); VPM wrapper consumes manifest cleanly with zkba_manifest_hash matching SHA-256 of canonical bytes (#5); G4 validator (validate_zkba_manifest) returns valid=True (#6); per-field tamper detection on token_id/device_id_hash/expires_at/cert_level/is_valid (#7); post-audit CFSS lane authority unchanged after VHP card insertion (#8). **End-to-end Layer 7 pipeline empirically validated**: compose VHP token state → compute_zkba_commitment (INV-ZKBA-001/002/003) → compile_artifact (vapi-zkba-manifest-v1) → wrap_zkba_manifest (vapi-vpm-manifest-v1) → validate_zkba_manifest (G4 validator) → section_3_lane_matrix (audit harness CFSS verified). CLI smoke confirmed live: output_hash `789e05b4b66a3577d1790dd543e92caf7bdbe89a3d4116632338fba77b91f90b`, input_commitment `b4918a67bb69e6f132a38461eae118c712e0717cbeb3aa7299741e5416d6da5d`. Regression band 124/124 PASS in 29s (primitive + compiler/GIC ledger + manifest validator 55 + VPM wrapper 24 + MCP 10 + Track 2 audit 8 + VHP card 12). PV-CI invariant gate 67/67 PASS unchanged (VHP card reuses existing PATTERN-017 INV-ZKBA-001/002/003 — no new invariants required; ZKBA primitive composition + manifest schema + domain tag uniqueness all hold). Bridge 3076→3088 (+12). SDK / Hardhat / Contracts unchanged. CHAIN_SUBMISSION_PAUSED=true held; wallet 0 IOTX impact. No bridge restart required (filesystem-only ship). **Layer 7 architecture now proven under multi-artifact load**: 2 distinct ZKBA classes (GIC + VHP) flow through the same primitive + compiler + wrapper + validator + audit pipeline; the Methodology Layer is not just well-documented but operationally usable as a real artifact-emission surface.
NOTE: ⚙️ POST-CEREMONY OBSERVABILITY HARNESS SHIPPED 2026-05-12 (commit `755dad63`) — Resolves Operator Decision Matrix D-TRACK2-G6 (per-agent live authority verification) via wallet-free read-only audit script `scripts/zkba_post_ceremony_audit.py` (~365 LOC). Three-section audit: **Section 1** recomputes local v2 bundle Merkle from JSON + compares to `EXPECTED_MERKLES` lock (`0x39e8b65f...db1f23` Sentry / `0x6818a9ad...0a9a0` Guardian / `0x0ade0c92...60a80b3d` Curator from C6 ship); **Section 2 (optional via `--include-chain-reads`)** calls `AgentScope.getScopeRoot()` per agent via `eth_call` (NO tx; NO gas; NO wallet impact; bypasses CHAIN_SUBMISSION_PAUSED per chain.py:4237); **Section 3** evaluates the Cedar v2 policy lane authority matrix — 12 expected (agent, action, resource, effect) tuples verifying CFSS invariant holds across `zk_artifacts/` (Sentry-exclusive) + `zk_verifications/` (Guardian-exclusive) + `zk_listings/` (Curator-exclusive). Audit emits human-readable report (default) or JSON (`--json`); exit codes 0-4 documented. **Section 5** surfaces D-TRACK2-G7 operator-driven commands (5 verification steps for Curator review readiness via live bridge — agent cannot perform; commands target `/operator/operator-initiative-advancement`, `/operator/operator-agent-drafts?agent_id=curator`, `Store.get_operator_agent_drafts`, FSCA contradictions endpoint, and 7-day observation window with >=9/10 acceptance gate per Phase O2 SUGGEST advancement gate). 8 tests T-ZKBA-T2-AUDIT-1..7 + static guard PASS (verifies wallet-free invariant by greppping source for forbidden chain-write patterns). Live local-only run on current bundles: **3/3 Section 1 MATCH; 12/12 Section 3 OK; D-TRACK2-G6 audit PASS**. Latent regression also fixed: chain.py:_ZKBA_DEVICE_ID comment block previously included the byte-literal `b"VAPI-ZKBA-ARTIFACT-v1"` which violated INV-ZKBA-002 uniqueness pin (matches=2 instead of 1); comment rephrased to canonical-name reference only. Windows-cp1252 stdout encoding bug fixed via `sys.stdout.reconfigure(encoding="utf-8")` at CLI entry. Bridge 3068→3076 (+8). SDK/Hardhat/PV-CI unchanged. **Operator Decision Matrix queue: 15/16 RESOLVED** (D-TRACK2-G6 now satisfied; D-TRACK2-G7 remains operator-runtime observation work, surfaced as documented operator commands). Wallet 0 IOTX impact across entire post-ceremony observability arc.
NOTE: 🏆 TRACK 2 C8 CEREMONY FIRED LIVE ON IOTEX 2026-05-12 — All three Cedar v2 bundles dual-anchored under operator explicit three-factor authorization at PowerShell terminal. **3/3 successes; 0/3 failures**. Tx record: **anchor_sentry v2** Merkle `0x39e8b65f0a87671fc003c28c3f28a7afd7fae41b6c3505d1ddb3d05ff3db1f23` — op_tx `3f79b4b428e0931671...` + gov_tx `04029ac59b4e08084f...`. **guardian v2** Merkle `0x6818a9ad49dab7898925e530526c50fcce515a889c3666f1434e6470c660a9a0` — op_tx `1e3a65f4445d73cc37...` + gov_tx `16ce625cdc9c8fc2cb...`. **curator v2** Merkle `0x0ade0c92cf2aa0c5675701861ed535683f0dfd15873424a9838d402b60a80b3d` — op_tx `470cbd17c865eef82b...` + gov_tx `5aac0d92866ac29cc5...`. Pre-ceremony wallet 15.2626 IOTX → post-ceremony ~15.03 IOTX (delta ~0.23 IOTX for 6 dual-anchor txs at current testnet gas; matches projection per ceremony runbook). **The Methodology Layer (Layer 7) now has full on-chain authority**: ZKBA artifact emission lanes (zk_artifacts/ Sentry-exclusive + zk_verifications/ Guardian-exclusive + zk_listings/ Curator-exclusive) backed by Cedar v2 bundles anchored on AgentScope (operational FIRST per INV-OPERATOR-AGENT-001) + AgentRegistry (governance SECOND); v1 bundles preserved LIVE alongside v2 per additive discipline. Atomic-stop discipline held through the firing pass — the prior commit (`531dbc6b`) had three signature bugs (ChainClient(cfg,store) vs ChainClient(cfg); CedarBundleAnchor positional vs keyword; anchor_bundle wrong kwarg names) + a dry-run gate ordering bug that surfaced during the operator's PowerShell firing pass and aborted the script at the wallet-check stage BEFORE any chain RPC; bugfix shipped at `85fc4551` mirrored parallel_o2_anchor.py construction precedent verbatim; operator re-fired post-fix successfully. Bridge wallet zero-decrement during the first-attempt firing — the wallet was only debited on the post-fix re-fire. **Operator Decision Matrix queue**: 14 of 16 RESOLVED (D-TRACK2-C8 + D-TRACK2-KILLSWITCH + D-TRACK2-FSCA all SATISFIED via ceremony); 2 remaining items (D-TRACK2-G6 per-agent live authority + D-TRACK2-G7 Curator readiness) are post-ceremony observability/verification work, not blocking. **Phase O3-ZKBA-TRACK1 + Track 2 COMPLETE end-to-end**: Methodology Layer (Layer 7) is now the first DePIN gaming protocol's architecturally-complete + on-chain-anchored methodology layer with deployer-anchored architect signing chain (Ed25519 over EIP-191) + 10-element PATTERN-017 FROZEN-v1 primitive family + 4-surface reach trio (Python lib + MCP tool + bridge HTTP + SDK) + Cedar v2 lane authorities LIVE.
NOTE: 🚀 TRACK 2 WALLET-FREE COMPLETE; C8 READY-TO-FIRE 2026-05-12 — Per operator authorization "authorize Track 2 gate-by-gate," three Track 2 commits shipped (`755fac33` C6 + `531dbc6b` C7 + `2a91c564` C8 runbook). **C6 (Cedar v2 bundles + FSCA rules)**: three v2 bundle files at `bridge/vapi_bridge/cedar_bundles/` (Sentry Merkle `0x39e8b65f...db1f23`, Guardian Merkle `0x6818a9ad...0a9a0`, Curator Merkle `0x0ade0c92...60a80b3d`); 3 new FSCA contradiction rules (ZKBA_PROOF_WEIGHT_MISMATCH HIGH + ZKBA_LANE_VIOLATION HIGH + ZKBA_VERIFICATION_KEY_STALE MEDIUM); 8 tests T-ZKBA-T2-C6-1..6 PASS; v1 bundles BYTE-IDENTICAL per additive discipline; cross-fleet skill separation invariant (CFSS) verified across the 3 ZKBA lanes (zk_artifacts/ Sentry-exclusive + zk_verifications/ Guardian-exclusive + zk_listings/ Curator-exclusive). **C7 (anchor script + chain method)**: `scripts/parallel_zkba_anchor.py` (~245 LOC) triple-gate ceremony script mirroring `parallel_o2_anchor.py` precedent; `chain.anchor_zkba_artifact()` async method extending PATTERN-017 anchor-corpus-snapshot pattern with new `_ZKBA_DEVICE_ID = SHA-256(b"VAPI_ZKBA_v1")` attribution constant; 9 tests T-ZKBA-T2-C7-1..9 PASS including gate-logic + Merkle-verification + kill-switch fail-open. **C8 runbook (`wiki/runbooks/zkba_track2_c8_ceremony_runbook.md`, ~334 LOC)**: documents the operator three-factor authorization procedure for firing the ceremony (env CHAIN_SUBMISSION_PAUSED=false + env OPERATOR_ZKBA_ANCHOR_AUTHORIZED=true + --confirm CLI flag); 5-item pre-flight checklist + 3-section post-ceremony verification + atomic-stop partial-failure recovery. **The C8 ceremony ITSELF is OPERATOR-RUNTIME ONLY** — I (the agent) have NOT fired the ceremony from any commit; no automation path bypasses the three factors; sandbox-level enforcement confirmed via Claude Code auto-classifier blocking unauthorized script execution from CI. Bridge 3051→3068 (+17 across C6 + C7 tests). Wallet 0 IOTX across all three commits. CHAIN_SUBMISSION_PAUSED=true held. **Operator Decision Matrix queue**: 11 of 16 RESOLVED (added D-TRACK2-C6 + D-TRACK2-C7 + D-TRACK2-G9 implicitly via D-NUM); 5 remaining items all operator-runtime-verification at C8 ceremony moment (G6 per-agent authority + G7 Curator readiness + KILLSWITCH 3-factor + FSCA cleanliness + the C8 ceremony fire itself). **Cost projection**: ~0.23 IOTX (6 dual-anchor txs × ~0.04 + 25% safety margin); wallet at 15.44 IOTX provides 67× margin. Methodology Layer (Layer 7) integration: Track 1 COMPLETE; Track 2 WALLET-FREE COMPLETE; C8 ceremony READY-TO-FIRE.
NOTE: Operator Decision Matrix WALLET-FREE BATCH RESOLVED 2026-05-12 — Per broad operator authorization "do whatever is necessary based on your recommendations + execute into how it should be," four pending Operator Decision Matrix items shipped across 2 commits (`40d22598` + `4adfb7c4` this commit). **D-NUM Option N1**: VBDIP-0002 owns the 0002 slot for ZKBA permanently; Phase O1-VAD-MIGRATE relocated to VBDIP-0004 (reserved-only) per VBDIP-0001 v1.1 amendment Appendix A. Closes VBDIP-0002 §16 G2 + G9. **D-SIDECAR-0002A Option S1**: VBDIP-0002A retained as indefinite sidecar; §§6, 8, 10 authoritative content; PARTIALLY ABSORBED status preserved. **D-LANE-B-G3 Option L3**: §9.3 visual honesty coverage CLOSES at PARTIAL SATISFIED (6 of 10 rules covered for GIC CHAIN_ONLY profile; 3 N/A for current artifact set; 1 deferred to Track 2 per §A.12). **D-PV-VPM Option P3**: INV-VPM-WRAPPER-001 added pinning `vapi-vpm-manifest-v1` wrapper schema literal via `--confirm-governance` ceremony (phrase "I understand this changes a frozen protocol invariant" piped). VEDIP-0001 Appendix A entry VED-INV-067 aliases the new invariant. **PV-CI 69→70** invariants (allowlist 66→67 protocol entries + 3 VBD-native unchanged). Wallet 0 IOTX across both commits. CHAIN_SUBMISSION_PAUSED=true held. Bridge / SDK / Hardhat / Contracts unchanged. **Operator Decision Matrix queue progress**: 5 of 16 decisions RESOLVED (D-SCHEMA-A + D-NUM + D-SIDECAR-0002A + D-LANE-B-G3 + D-PV-VPM); D-SCHEMA-B implicitly closed by D-PV-VPM resolution; cluster E (Track 2 C6/C7/C8 + G6/G7/G8/G9 + KILLSWITCH/WALLET/FSCA) retains explicit operator three-factor authorization gate at ceremony moment per discipline.
NOTE: 🏛️ METHODOLOGY LAYER (Layer 7) INTEGRATION COMPLETE 2026-05-12 — Through Phase O3-ZKBA-TRACK1 + Lane B follow-up + Option C v1.2 amendment + Methodology Layer Integration Map (commits `f47763fe` → `e8cc40ca`; 17 commits), the methodology surface has been elevated from "side document collection" to a **peer architectural layer** in VAPI's protocol stack. **Layer 7 components shipped:** (1) VAD framework (VBDIP-0001 FROZEN at `d6830525`) with 3 sub-disciplines VSD/VED/VBD; (2) PATTERN-017 ZKBA primitive at #10 (`625007ab`); (3) VPM wrapper layer (`72b056e8`) with FROZEN `vapi-vpm-manifest-v1` schema + 5 FROZEN closed enums + 9-field Integrity Label; (4) G4 manifest validator (`210f841b`) with 7-class coverage + bilateral schema-name acceptance per v1.2 amendment Appendix C (`a501d6f1`); (5) Reach trio — Python lib + MCP tool (`53553047`) + bridge HTTP endpoint (`4f63c5d5`) + SDK client (`e4ad7cde`); (6) Architect Ed25519 signing chain anchored to bridge wallet via EIP-191 (`8b95d5bc`); (7) PV-CI invariant coverage (6 methodology-layer entries: INV-ZKBA-001/002/003 + VBD-INV-001/002/003); (8) Methodology document corpus organized + reconciled. **Integration map at `wiki/methodology/METHODOLOGY_LAYER_INTEGRATION_MAP.md`** (commit `aa436f9b`) elevates the layer architecturally; whitepaper §9.29 (`e8cc40ca`) captures the integration at the protocol-spec level. **§9.2 V-check finding RESOLVED** via Option C v1.2 amendment (commit `a501d6f1`) codifying bilateral acceptance: `vapi-zkba-manifest-v1` CANONICAL for new emissions, `zkba.projection_manifest.v1` RECOGNIZED for read-only legacy validation. **Operator Decision Matrix** at `vsd-vault/proposals/drafts/OPERATOR-DECISION-MATRIX.DRAFT.md` (commit `56fe99a2`) consolidates 16 pending operator decisions into 5 clusters. **Five layer-level markdown-normative invariants** locked: supersession discipline, VPM-HONESTY-001 namespace, VED-INV-N aliasing discipline, wallet-risk separation, drift preservation. Bridge 2942→3051 (+109). SDK 550→562 (+12). PV-CI 69 unchanged. Hardhat 528 unchanged. Wallet 0 IOTX across all 17 commits. `CHAIN_SUBMISSION_PAUSED=true` held throughout. **Track 1 envelope CLOSED**; Track 2 (Cedar v2 bundles + parallel anchor script + ceremony at ~0.23 IOTX) remains operator-gated per VBDIP-0002 §16 + plan §8 activation criteria.
NOTE: Phase O3-ZKBA-TRACK1 Lane B G4 reach trio CLOSED 2026-05-12 (commits `53553047` MCP + `4f63c5d5` bridge HTTP + `e4ad7cde` SDK) — Extends the G4 validator (commit `210f841b`) to all three reach surfaces matching the C4-style architectural progression for the ZKBA primitive itself. **MCP layer (`53553047`):** new `vapi_validate_zkba_manifest` @tool in `vapi-mcp/knowledge_server.py` accepts inline manifest dict OR manifest_path; 4 tests T-ZKBA-17..20 PASS. **Bridge HTTP layer (`4f63c5d5`):** new `POST /operator/zkba-validate-manifest` endpoint in `bridge/vapi_bridge/operator_api.py` with read-key auth + JSON body parsing via Request + 422 on body-parse / 200+fail-open on content-validation; localized sys.path.insert + lazy import (one-time inversion of usual scripts/-depends-on-bridge/ direction); 13 tests T-ZKBA-VEP-1..7 PASS including 7-class parametrized round-trip. **SDK layer (`e4ad7cde`):** new `VAPIZKBAValidator` client + `ZKBAValidateResult` slotted dataclass in `sdk/vapi_sdk.py:9340+`; SDK_VERSION bumped `3.1.0-phase-o3-zkba-track1-c4-sdk` → `3.1.1-phase-o3-zkba-track1-g4-validator-sdk`; 12 tests T-ZKBA-VSDK-1..6 PASS including 7-class parametrized live round-trip via uvicorn fixture; existing T-ZKBA-13 sanity check relaxed `startswith("3.1.0")` → `startswith("3.1.")` to permit patch bumps within the 3.1.x ZKBA SDK family. The `schema_name_form` field carries the §9.2-vs-implementation V-check finding through all three reach surfaces so external tooling can detect schema-name drift per request. Wallet 0 IOTX across all three commits. CHAIN_SUBMISSION_PAUSED=true held. Bridge 3034→3051 (+17 across MCP+HTTP). SDK 550→562 (+12). PV-CI / Hardhat / contracts / wallet unchanged. Sequential-order queue remaining without operator decision: bookkeeping (this NOTE entry), §9.2 schema-name drift v1.2 amendment DRAFT (operator-decision-shaped), Operator Decision Matrix doc, VPM PV-CI candidate INV-VPM-WRAPPER-001 (requires --confirm-governance ceremony). Gated items unchanged: B.8 G2 numbering, B.8 G6, Track 2 C6/C7/C8.
NOTE: Phase O3-ZKBA-TRACK1 Lane B G3 + G4 COMPLETE 2026-05-12 (commits `aaaa1653` G3 + `210f841b` G4) — Closes two more sub-gates from VBDIP-0002 §16 Appendix B B.8 set. **G3 (§9.3 visual honesty tests for active artifact set)** PARTIALLY SATISFIED via `bridge/tests/test_phase_o3_zkba_compiler_visual_honesty.py` (13 tests / 13 PASS). Covers 6 of 10 §9.3 rules for GIC Continuity Ledger: proof_weight visibility (rule 1), compiler-hash-mismatch detection (5), manifest/proof-mismatch tamper detection (6) + per-field tamper for 5 input fields, missing proof_weight FAILS compilation (7), manifest must not falsely claim ZK verification when GIC is not ZK-proven (8), no external URL refs / no `<link>` tags / no CDN blacklist domains (9). Rules 2/3/4 N/A for GIC's CHAIN_ONLY profile (DEMO watermark / FROZEN_DISABLED render / revoked consent marketplace — apply when future classes ship). Rule 10 (lane prefix matches output directory) DEFERRED to Track 2 per §A.12. Test-only commit; no compiler modification. **G4 (ZKBA manifest schema validated against all 7 §5 classes)** SATISFIED via `scripts/zkba_manifest_validator.py` (~340 LOC) + `bridge/tests/test_phase_o3_zkba_manifest_validator.py` (55 tests / 55 PASS including 7-class parametrized coverage). validate_zkba_manifest() returns ManifestValidationResult (fail-open; never raises) checking required field set + schema name (accepts both `vapi-zkba-manifest-v1` implementation + `zkba.projection_manifest.v1` design-time) + enum range checks (zkba_class 1..7, proof_weight 1..6) + hex-format checks + uint64 ts_ns. build_representative_manifest() helper exposes DEFAULT_PROOF_WEIGHT_BY_CLASS table (AIT→CALIBRATION_PLUS_CONTEXT; GIC/VHP/HARDWARE/CONSENT/TOURNAMENT→CHAIN_ONLY; MARKET→MARKETPLACE_DERIVED). T-MV-11 round-trip closure: validator accepts real compile_artifact() output end-to-end. **V-check finding documented:** §9.2 spec design-time schema name `zkba.projection_manifest.v1` diverges from implementation FROZEN schema name `vapi-zkba-manifest-v1` (PV-CI INV-ZKBA-003 pin); validator accepts both names + surfaces drift via `schema_name_form` field. Reconciliation deferred to future VBDIP-0002 v1.x amendment; PV-CI takes precedence in practice. Wallet 0 IOTX both commits. CHAIN_SUBMISSION_PAUSED=true held. Bridge 2966→3034 (+68 across 2 commits). SDK / Hardhat / PV-CI / contracts unchanged. Sequential-order queue at gating boundary: B.8 G2 numbering (operator-decision), B.8 G6 + Track 2 C6/C7/C8 (operator gate-by-gate + kill-switch lift required).
NOTE: Phase O3-ZKBA-TRACK1 Lane B G5b + G5c COMPLETE 2026-05-12 (commit `72b056e8`) — Ships `scripts/vsd_vpm_wrapper.py` (~510 LOC) + `bridge/tests/test_phase_o3_zkba_vpm_wrapper.py` (24 tests / 24 PASS). Satisfies the two pending Lane B sub-gates from VBDIP-0002 Appendix B v1.1 amendment (commit `3461b636`). Wrapper schema `vapi-vpm-manifest-v1` FROZEN at v1.0; REFERENCES the FROZEN ZKBA schema `vapi-zkba-manifest-v1` rather than replacing. 5 FROZEN closed enums (VPMVisualState 6-element / VPMCaptureMode 5-element / VPMRevocationStatus 3-element / VPMAnchorStatus 4-element / VPMLifecycleStatus 5-element) + VPMIntegrityLabel 9-field dataclass per B.5 + derive_visual_state() rule engine encoding B.6 failure-state precedence + validate_vpm_manifest() mechanical enforcement (catches overclaim attempts like revoked+LIVE or on_chain_anchor=True+zk_verified=False). Determinism discipline matches scripts/vsd_ui_compiler.py (no wall-clock / random / network imports verified by static guard test). VPM-HONESTY-001 remains methodology-doc identifier — does NOT enter PV-CI allowlist per reconciliation plan §4 + VEDIP-0001 documentation-alias discipline. Track 1 invariant anchor_tx_hash IS NULL preserved. Bridge 2942→2966 (+24). SDK / Hardhat / PV-CI / contracts / wallet unchanged. Sequential-order queue next-items: B.8 G2 numbering (operator-decision), B.8 G3 compiler harness full coverage (Lane B), B.8 G4 ZKBA manifest schema validation (Lane B), B.8 G6 AgentScope/Cedar authority + Track 2 C6/C7/C8 (Lane C — operator gate-by-gate authorization + kill-switch lift required).
NOTE: VBDIP-0002 v1.1 amendment SHIPPED 2026-05-12 (commit `3461b636`) — Executes D-MERGE-SELECTIVE recommendation from reconciliation plan §8 (commit `f47763fe`). VBDIP-0002A §§1,2,3,4,5,7,9,11,12 absorbed into VBDIP-0002 as **Appendix B (B.1-B.9)** per supersession discipline (additive amendment; v1.0 spec §§1-17 + Appendix A byte-identical). VBDIP-0002A §§6,8,10 retained as sidecar content; VBDIP-0002A header status: DRAFT → PARTIALLY ABSORBED. Activation gate G5 split into G5a/G5b/G5c; §16 gate count 9→11. Decision blocks K-series extended K1-K7 → K1-K14 (B.9 absorbed L1-L7). VPM-HONESTY-001 locked at B.5 as methodology-doc identifier (NOT a PV-CI invariant) per reconciliation plan §4. PV-CI count unchanged (69). Wallet 0 IOTX. Bridge / SDK / Hardhat counts unchanged.
NOTE: VBDIP-0002 vs VBDIP-0002A reconciliation plan AUTHORED 2026-05-12 (commit `f47763fe`) — `vsd-vault/proposals/drafts/VBDIP-0002-vs-0002A-reconciliation.DRAFT.md` (350 lines). Per-section disposition table for VBDIP-0002A §§1-12 (9 sections → D-MERGE, 3 sections → D-SIDECAR), activation-gate mapping (G1-G6 → G1-G11 post-merge), Lane B scope pointer, drift findings, primary recommendation D-MERGE-SELECTIVE. Plan does NOT execute merge itself; subsequent commit `3461b636` does. Wallet 0 IOTX; PV-CI 69 unchanged.
NOTE: VEDIP-0001 RETROSPECTIVE-SPEC v1.0 COMPLETE 2026-05-11 - Authored `wiki/methodology/VEDIP-0001-engineering-discipline-retrospective.md` as the follow-up deliverable named by VBDIP-0001 section 8. This document retroactively names the Verified Engineering Discipline (VED) across Phase O0, Phase O1-FRR-PARALLEL, Stream J, parallel O2 SUGGEST anchoring, the VBDIP-0001 integration lessons, and Phase O3-ZKBA-TRACK1 engineering surfaces. It locks the critical clarification that `VED-INV-N` is documentation-only aliasing over engineering/protocol PV-CI entries, not a source-code rename and not an allowlist-format change. Current authoring-boundary mapping: 66 VED aliases (`VED-INV-001..066`) over non-VBD PV-CI entries plus 3 VBD-native invariants (`VBD-INV-001..003`) listed separately, preserving unified `--proposal-type all` count 69. Acceptance posture: docs-only, no source/runtime mutation beyond methodology memory, no PV-CI allowlist regeneration, no signatures, no key generation, no on-chain action, wallet 0 IOTX, `CHAIN_SUBMISSION_PAUSED=true` verified. Verification: markdown diff clean; `python scripts/vapi_invariant_gate.py --report` 66/66 PASS; `python scripts/vapi_invariant_gate.py --report --proposal-type all` 69/69 PASS. This closes the VEDIP-0001 forward-work gap left intentionally open by VBDIP-0001 without starting gated Track 2 ZKBA work.
NOTE: Phase O3-ZKBA-TRACK1 + VBDIP-0001-INTEGRATION COMPLETE 2026-05-10 — Architectural pivot ship. **VBDIP-0001 FROZEN** (commit `d6830525`) establishes the VAPI Architectural Discipline (VAD) framework with three sub-disciplines: **VSD** (synthesis, post-bootstrap), **VED** (engineering, retroactive over existing 55→63 PV-CI invariants), **VBD** (bridge composition). 5-step secure resumption procedure shipped: Step 1 (`2aea877a`) provenance pin manifest at `wiki/methodology/INTEGRATION_PROVENANCE_2026-05-10.md` — deferral-boundary witness; Step 2 (`69ac74d2`) inventory normalization — imported `vsd_methodology_v1_FINAL.md` + `claude_code_master_resumption_prompt.md` + reconciled 3 pre-existing untracked methodology .md files; Step 3 (`be13de49`) state reconciliation amendments — PATTERN-017 count canonicalized across 4 docs (PoAC=1...ZKBA=10, +VRR=11+CDRR=12 post-bootstrap; EXCLUDES parser-schema + Pass 2C op-track primitives), VED-INV-N clarified as count-abstraction NOT rename, master resumption Phase D revised D1→D2, bootstrap canonical state refs refreshed (bridge 2836→2922, PV-CI 55→63); Step 4 (`8b95d5bc`) architect Ed25519 key generated + bridge wallet EIP-191 attestation produced at `vsd-vault/eval/architect_key_attestation.json` (architect pubkey `056e695f...8ca8` chained to bridge wallet `0x0Cf36dB57fc4680bcdfC65D1Aff96993C57a4692` via signature `0xb21a94de...3731b`; `vsd-vault/architect_key.pem` private key gitignored via `vsd-vault/.gitignore` line 12); Step 5 (`d6830525`) VBDIP-0001 FROZEN status applied — architect Ed25519 signature over VBDIP-0001 canonical hash `56da19e2...8ea27` committed at `vsd-vault/manifests/proposals-VBDIP-0001/001.manifest.json` (Ed25519 sig `ea59071b...e103`; manifest hash `46d78695...69e4`), harness extended with `--proposal-type` flag (4 choices: protocol/bridge/synthesis/all; deprecated `both` alias), VBD-INV-001/002/003 registered in vapi_invariant_gate.py, allowlist regenerated 63→66, `vsd-vault/README.md` authored per VBDIP-0001 §6.2 deferred-migration documentation (vsd-vault/ → vad-vault/ deferred to VBDIP-0002 numbering resolution). **PARALLEL TRACK**: Phase O3-ZKBA-TRACK1 5 commits — C1 (`3df5e59f`) VBDIP-0002 ZKBA sidecar r1 docs revision (E1-E12 batch); C2 (`625007ab`) ZKBA primitive module `bridge/vapi_bridge/zkba_artifact.py` (10th FROZEN-v1 in PATTERN-017; domain tag `b"VAPI-ZKBA-ARTIFACT-v1"` 21 bytes; ZKBAClass IntEnum 7 values; ProofWeightClass IntEnum 6 values; compute_zkba_commitment with sorted-component canonical commitment) + store schema `zkba_artifact_log` (idempotent migration phase 1100) + 7 bridge tests (T-ZKBA-1..7); C3 (`3b3081d3`) deterministic UI compiler `scripts/vsd_ui_compiler.py` (no wall-clock / no random / no network; manifest schema `vapi-zkba-manifest-v1` FROZEN at v1.0) + GIC Continuity Ledger Artifact Alpha at `scripts/zkba_compile_gic_ledger.py` (composes ZKBA commitment with GIC head as single component; ProofWeightClass.CHAIN_ONLY; output to `frontend/src/artifacts/gic_continuity_ledger/`) + 4 bridge tests (T-ZKBA-8..11); C4 (`8d2dc87e`) 3 MCP tools in vapi-knowledge (vapi_zkba_status / vapi_compile_zkba_artifact / vapi_zkba_projection_manifest; direct DB queries + CLI-command-only compile) + VAPIZKBA SDK client (3 slotted Result dataclasses + 3 methods; SDK_VERSION 3.0.0-phase-o1-frr-sdk → 3.1.0-phase-o3-zkba-track1-c4-sdk; HTTP endpoints `/operator/zkba-*` defined as future-wired) + 2 bridge + 4 SDK tests (T-ZKBA-12..16); C5 (this commit) — INV-ZKBA-001/002/003 registered in vapi_invariant_gate.py (allowlist 66→69), CLAUDE.md NOTE + MEMORY.md index + project memory file. **PV-CI invariants 55 → 69** total (+14 across both workstreams: VBD-INV-001/002/003 from Step 5 + INV-ZKBA-001/002/003 from C5 + 8 pre-existing protocol invariants that had drifted between session-start CLAUDE.md count and the actual allowlist file). Bridge tests 2922 → 2935 (+13). SDK tests 543 → 547 (+4). Hardhat unchanged 528. Wallet 15.44 IOTX unchanged (0 IOTX impact across all 10 commits). `CHAIN_SUBMISSION_PAUSED=true` held throughout. **VBDIP-0002 §16 activation gate #1 (VBDIP-0001 FROZEN) NOW SATISFIED** — remaining Track 2 gates are operator-decision (numbering N1/N2'/N3) + activation work (A1 Cedar v2 bundles + zk_artifacts/zk_verifications/zk_listings lane prefixes; A2 FSCA ZKBA contradiction rules; A3 parallel_zkba_anchor.py triple-gate; A4 dual-anchor ceremony at ~0.18 IOTX). **Architect Ed25519 signing chain LIVE** — all future architect-signed methodology artifacts inherit trust from `vsd-vault/eval/architect_key_attestation.json` per VBD-INV-001 (continuous deployer-verified provenance under fleet expansion). Plan: `C:\Users\Contr\.claude\plans\i-authorize-yours-to-precious-scone.md` (approved + executed). Detailed integration audit trail: `wiki/methodology/INTEGRATION_PROVENANCE_2026-05-10.md` §7.1-§7.5 hash blocks (append-only); each Step references prior commits + records pre/post artifact hashes for byte-preservation verification.
NOTE: Phase O2-GIT-TRIGGER-AUTOWIRE COMPLETE 2026-05-10 — Completes Sentry's one-flag-flip live activation path. Phase O2-GIT-TRIGGER-SOURCE (commit 9b9bd9dd) shipped GitTriggerSource as a callable but main.py didn't construct it; operator had to hand-pass it via `run_sentry_polling_loop(get_pending_triggers=...)` kwarg. This phase wires the factory into main.py boot path: when both `OPERATOR_AGENT_SENTRY_POLLING_ENABLED=true` AND `OPERATOR_AGENT_GIT_TRIGGER_ENABLED=true`, the bridge automatically constructs `GitTriggerSource` via `make_git_trigger_source(cfg=self.cfg)` factory and passes it as `get_pending_triggers` kwarg to `run_sentry_polling_loop`. When the trigger flag is False, factory returns None and run_sentry_polling_loop falls back to its no-op trigger stub (scaffold-only behavior preserved). Implementation: ~25-LOC inner try/except inside the existing Sentry polling task-registration block in main.py (lines 1245-1303 region). Inner ImportError on the trigger source module is non-fatal (factory absent → None → scaffold fallback); other exceptions logged as WARNING. Log message updated to surface trigger_source label: `Phase O2-DRAFT-AUTOLOOP (Sentry): polling loop started (interval=30s, trigger_source=GitTriggerSource(LIVE))` vs `... trigger_source=no-op stub (scaffold-only)`. Tests: bridge/tests/test_phase_o2_git_trigger_autowire.py with 3 tests T-O2-GIT-AUTOWIRE-1..3 covering: factory returns LIVE GitTriggerSource when flag=True; factory returns None when flag=False (scaffold fallback preserved); end-to-end main.py-style construction → SentryPollingLoop receives source → commit triggers drive kms-sign + provenance-recording drafts into operator_agent_drafts. All 3 PASS in 0.4s. Adjacent regression: pytest test_phase_o2_polling_sentry.py 6/6 + test_phase_o2_git_trigger_source.py 7/7 PASS unchanged. Bridge 2919→2922 (+3 tests). SDK / Hardhat / PV-CI / contracts / wallet unchanged. Bridge restart required ONCE to activate the new boot path; thereafter operator can flip the env flags between True/False without code changes. With this phase shipped, Sentry's commit-trigger live wiring is one-flag-flip away from accumulating draft count toward the watcher's PHASE_O3_DRAFT_PAYLOAD_MIN=50 gate. Guardian's fsca_finding/sweep_completed and Curator's listing_event/anchor_freshness_alert/periodic_compliance trigger sources remain SCAFFOLD-ONLY (deferred phases; same primitive shape as GitTriggerSource will mirror cleanly).
NOTE: Phase O2-GIT-TRIGGER-SOURCE COMPLETE 2026-05-10 (commit 9b9bd9dd) — First concrete LIVE trigger source for the polling loops shipped Phase O2-DRAFT-AUTOLOOP. Earlier phase shipped polling loops with SCAFFOLD-ONLY trigger injection (no-op stub returning []); this phase ships the FIRST concrete event source emitting real triggers. New file `bridge/vapi_bridge/operator_agent_git_trigger_source.py` with `GitTriggerSource` callable producing list of pending commit triggers from `git rev-parse HEAD` + `git log --reverse <last_seen>..HEAD --format=%H` plumbing (pure stdlib subprocess; no bus dependency). Tracks last-seen commit in memory; first call seeds baseline + returns []; subsequent calls emit `{kind:'commit', payload:{commit_hash, repo, branch}}` per new commit in forward chronological order. Failures (broken repo path, git command errors) caught + logged + return []; never raises (matches polling-loop fail-open contract). `make_git_trigger_source` factory mirrors `cedar_drift_sweeper` / `run_sentry_polling_loop` entrypoint pattern — returns None when flag=False (opt-in default), instance when True. Config flag `operator_agent_git_trigger_enabled` (default False; `OPERATOR_AGENT_GIT_TRIGGER_ENABLED` env override). Wiring path: operator sets both `OPERATOR_AGENT_GIT_TRIGGER_ENABLED=true` AND `OPERATOR_AGENT_SENTRY_POLLING_ENABLED=true`; explicitly passes the GitTriggerSource as `get_pending_triggers` to `run_sentry_polling_loop` (or future main.py wave wires automatically) — Sentry then emits `kms-sign` + `provenance-recording` drafts for every commit on main going forward, directly accumulating the watcher's `PHASE_O3_DRAFT_PAYLOAD_MIN=50` gate signal. DRIFT FINDING (documented in module docstring): polling loop's rate-limit invariant is ONE trigger per cycle; GitTriggerSource returns the FULL list of N pending commits but advances its own last-seen pointer to HEAD unconditionally on each call, so the next call sees N-1 pending. A 5-commit advancement produces exactly 5 trigger dispatches across 5 cycles, matching rate-limit. Tests: `bridge/tests/test_phase_o2_git_trigger_source.py` with 7 tests T-O2-GIT-TRIG-1..7 (baseline + no-op when HEAD unchanged; 1 commit → 1 trigger + last-seen advances; N commits in forward chronological order; opt-in default short-circuit; broken repo returns [] without raising; factory opt-in semantics; END-TO-END INTEGRATION with SentryPollingLoop verifying commit trigger drives kms-sign + provenance-recording drafts into operator_agent_drafts). Tests use fresh `git init --initial-branch=main` repo fixture so they're deterministic and don't depend on actual repo state. All 7 PASS in 9.4s. Adjacent regression: pytest `test_phase_o2_polling_sentry.py` 6/6 PASS unchanged. PV-CI gate 58/58 PASS unchanged. Bridge 2912→2919 (+7 tests). SDK / Hardhat / contracts / wallet unchanged. No bridge restart required (cfg flag default False; live wiring activates only when both flags True). Closes ONE of the four "what remains for live triple dual-anchor" gates (live trigger source wiring) for Sentry's commit trigger path. Three external gates remain: 504h shadow age clock; ≥50 drafts per agent + operator review with disagreement_rate <5%; cfg flags for KMS/HSM/GitHub-App/setCurator/dual-key.
NOTE: Phase O2-DRAFT-REVIEW-SDK COMPLETE 2026-05-10 — VAPIDraftReview SDK client wraps the operator review HTTP surface (Phase O2-DRAFT-REVIEW-ENDPOINT, commit a44fa359). Closes the loop from the client side: Python applications + tooling can now query GET /operator/operator-agent-drafts and POST /operator/operator-agent-draft-review without hand-rolling urllib calls. New file sdk/tests/test_phase_o2_draft_review_sdk.py with 4 tests T-O2-DRAFT-REVIEW-SDK-1..4 (list_drafts shape + filter pass-through; review_draft happy path; review_draft 422/404 error surfacing; list+review network errors never raise — matches existing SDK fail-open contract). New extension to sdk/vapi_sdk.py: VAPIDraftReview client class + DraftReviewListResult dataclass(slots=True) + DraftReviewSubmitResult dataclass(slots=True). list_drafts(agent_id, decision, since_minutes, limit) wraps the GET endpoint with read-key auth via x-api-key header. review_draft(draft_id, decision, reason) wraps the POST endpoint with full operator auth via api_key query param; reason ≥10 chars validation surfaced as `error` field; HTTP 422/404 errors caught at urllib.error.HTTPError boundary and converted to fail-open Result. SDK_VERSION marker bumped 3.0.0-phase193 → 3.0.0-phase-o2-draft-review. Tests use uvicorn-in-thread + FastAPI TestClient against the live operator_app to validate end-to-end SDK→bridge→store→bridge→SDK loop. Adjacent regression: 4/4 SDK tests PASS in 8.4s. SDK 539→543 (+4 tests). Bridge / Hardhat / PV-CI / contracts unchanged. Wallet zero impact (filesystem-only). No bridge restart required (read-only SDK addition; bridge endpoints already shipped).
NOTE: Phase O2-AUTONOMOUS-COMPLETION arc COMPLETE 2026-05-10 — Six atomic commits via 5-agent parallel-dispatch wave (Pre-Wave 0 fe4d70a7 + Sentry polling 6c33b111 + PV-CI O3-WATCHER 1cc557a1 + Guardian polling 1b8e557b + Curator polling 93b5c1b2 + FSCA-O2-RULES a7313948). Closes the entire Operator Initiative O2→O3 advancement pipeline end-to-end: agents draft via polling loops → store persists → operator reviews via HTTP surface → watcher reads decision-derived rates → o3_ready transitions when gates clear → parallel_o3_act_anchor.py Gate 4 unblocks → triple dual-anchor → fleet at O3_ACTING. (1) PRE-WAVE 0 SCAFFOLDING (commit fe4d70a7): 6 new config flags (operator_agent_{sentry,guardian,curator}_polling_enabled + _polling_interval_s) + 3 main.py task-registration blocks with try/except ImportError de-conflict mechanism. Allows polling modules to ship in parallel worktrees without shared-file collision. (2) AGENT-A SENTRY POLLING (commit 6c33b111): operator_agent_sentry_polling.py with SentryPollingLoop + run_sentry_polling_loop entrypoint mirroring cedar_drift_sweeper pattern; 3-trigger dispatch matrix (commit→kms-sign+provenance, poac_chain_head→provenance, poad_hash→pda-anchor); rate-limited ONE trigger/cycle; SCAFFOLD-ONLY trigger sources via callback injection (live event wiring deferred); 6 tests T-O2-POLL-SENTRY-1..6. Counter unit: per-trigger-success. (3) AGENT-B GUARDIAN POLLING (commit 1b8e557b — salvaged from worktree after API error during agent's report-back; module file 366 LOC preserved + tests written by hand): operator_agent_guardian_polling.py mirroring Sentry's class shape; 3-trigger dispatch (sweep_completed→audit-drafting, fsca_finding→operational-diagnostic, commit→kms-sign+audit-drafting); 6 tests T-O2-POLL-GUARDIAN-1..6. Counter unit drift from Sentry: per-method-success (commit trigger += 2). (4) AGENT-C CURATOR POLLING (commit 93b5c1b2 — agent worktree empty after API error; hand-implemented from Sentry/Guardian template): operator_agent_curator_polling.py with 3-trigger dispatch (listing_event chains marketplace-listing-review + kms-sign-review via verdict_payload_hash linkage; anchor_freshness_alert→operator-notify(severity=recommend_suspend); periodic_compliance BATCH→N marketplace-listing-review drafts); verdict validated against Phase 238 Step I _FROZEN_VERDICTS (7 codes); 6 tests T-O2-POLL-CURATOR-1..6. Counter unit: per-method-success. periodic_compliance is the only batch trigger across A/B/C (counts as ONE trigger but produces N draft rows). (5) AGENT-D FSCA-O2-RULES (commit a7313948 — agent worktree on wrong branch after API error; hand-implemented): three new entries in CONTRADICTION_RULES dict in fleet_signal_coherence_agent.py: O2_SUGGEST_NO_DRAFTS_24H (HIGH — agent at O2_SUGGEST with 0 drafts in 24h indicates polling stalled); DRAFT_UNREVIEWED_72H (MEDIUM — 10+ drafts with operator_decision IS NULL older than 72h indicates review backlog); DISAGREEMENT_RATE_TRENDING (CRITICAL — n_reject/n_reviewed > 0.04 over 30d trending toward 5% gate breach blocking O3 ACTING). DRIFT FINDING filed: spec said activation_log column was bundle_filename; live schema is bundle_path (watcher's get_latest_operator_agent_activation helper adapts for backward-compat reads but raw SQL sees bundle_path); FSCA rule + test seed both use bundle_path. CONTRADICTION_RULES 13→16 (+3). 4 tests T-O2-FSCA-1..4. (6) AGENT-E PV-CI O3-WATCHER-LOCK (commit 1cc557a1 — landed despite API-error notification): three new INVARIANTS entries in scripts/vapi_invariant_gate.py: INV-O3-WATCHER-001 pins `_o3_acting_|_o3_act_` substring matcher in operator_initiative_advancement.py phase resolver (Phase O3-ACT-WATCHER bug fix); INV-O3-WATCHER-002 pins PHASE_O3_DRAFT_PAYLOAD_MIN + PHASE_O3_DISAGREEMENT_RATE_MAX + PHASE_O3_FALSE_POSITIVE_RATE_MAX gate constants; INV-O3-WATCHER-003 pins three O3_ACTING bundle Merkle roots from Phase O3-ACT-DRAFT (Sentry 0xc0bcdee8.../Guardian 0x6f0fc77c.../Curator 0xd9d760c8...). Allowlist regenerated via governance phrase pipe pattern. PV-CI gate 55→58. WAVE 1 INFRASTRUCTURE OBSERVATION: 4 of 5 dispatched agents (B/C/D/E) hit "API Error: Unable to connect" simultaneously during their final report-back phases (~30 min into runs). E's commit landed cleanly anyway (commit reached origin via worktree push before the error); A's commit also landed cleanly. B's polling module file persisted as untracked in worktree but no commit (salvaged manually). C's worktree was empty (lost; re-implemented from Sentry template). D's worktree was on a stale branch from a prior session (FRONTEND-V3 ancient HEAD; lost; re-implemented). The salvage path proved viable: Sentry's pattern + Guardian's worktree salvage gave the template necessary to hand-finish C and D in ~20 min each rather than re-dispatch agents that might hit the same API errors. Adjacent regression: 98/98 PASS (Sentry/Guardian/Curator polling + FSCA-O2-rules + Sentry/Guardian/Curator draft generation + draft-review endpoint + O3-ACT-DRAFT/WATCHER/ANCHOR-SCRIPT + O2-SUGGEST + O1-D + O1-FRR + O1-C6-FSCA-drift). PV-CI gate 58/58 PASS via `python scripts/vapi_invariant_gate.py --report`. Bridge 2890→2912 (+22 tests). SDK 539 unchanged. Hardhat 528 unchanged. Contracts 49 unchanged. Wallet zero impact (filesystem-only ship). No bridge restart required (config flags default False; main.py task slots no-op when flags False or polling modules absent). With this arc COMPLETE, the FULL Operator Initiative O2→O3 advancement pipeline is wired end-to-end. The remaining gates before live triple dual-anchor are EXTERNAL: shadow_age clearing 504h (Sentry+Guardian ~14.6 days from 2026-05-10; Curator ~21 days); ≥50 drafts per agent (requires live trigger source wiring — deferred phase to wire git-watcher / PoAC-stream-subscriber / marketplace-event-subscriber to the polling loops); operator review of those drafts with disagreement_rate <5% AND Curator false_positive_rate=0%; cfg flags operator_dual_key_present + kms_hsm_production_ready (Sentry+Guardian) + github_app_oauth_tokens_valid (Guardian) + marketplace_curator_role_assigned (Curator) set by operator at the appropriate time.
NOTE: Phase O2-DRAFT-REVIEW-ENDPOINT COMPLETE 2026-05-10 — Operator review HTTP surface closes the disagreement_rate / false_positive_rate measurement loop. Without these endpoints, drafts produced by Phase O2-DRAFT-GENERATION (Sentry/Guardian/Curator commits d9954039/5959792e/a4f150a0) would persist forever with operator_decision=NULL — neither contributing to disagreement-rate numerator nor denominator, leaving the watcher's PHASE_O3_DISAGREEMENT_RATE_MAX=0.05 + PHASE_O3_FALSE_POSITIVE_RATE_MAX=0.0 gates without any operational signal even after 50 drafts accumulate. Two new endpoints added to bridge/vapi_bridge/operator_api.py (placed adjacent to the existing operator-agent-shadow-log + operator-agent-drift-log GET endpoints): (1) GET /operator/operator-agent-drafts (read-key auth via x-api-key header; query params agent_id (Q9 hex filter, empty=trio), decision (filter values: ''/None=all incl unreviewed, 'unreviewed'=NULL operator_decision via in-memory filter post-fetch, 'accept', 'reject', 'overturn_curator'), since_minutes (0-43200 = 30 days max; 0 = no filter), limit (1-500 default 50); returns {agent_id_filter, decision_filter, since_minutes, limit, row_count, drafts[], timestamp}); (2) POST /operator/operator-agent-draft-review (full operator auth via api_key query param matching cfg.operator_api_key; required draft_id; required decision validated against {accept, reject, overturn_curator}; required reason ≥10 chars audit gate per existing /operator/gic-reset + /operator/override-gameplay-context patterns; returns {accepted, draft_id, decision, reason, row, timestamp}). Decision flows directly to store.record_operator_decision (Phase 1005 helper) which feeds compute_operator_agent_disagreement_rate + compute_operator_agent_false_positive_rate downstream. Idempotent on same decision; allows revision (operator may revise their own review). Bridge integration uses lambda-wrap pattern around asyncio.to_thread because the underlying store helpers are keyword-only (Phase O2-DRAFT-GENERATION discipline) — `asyncio.to_thread(store.method, *args)` would fail TypeError on positional injection; lambda preserves keyword-only contract. Tests: bridge/tests/test_phase_o2_draft_review_endpoint.py with 6 tests T-O2-DRAFT-REVIEW-EP-1..6 covering: GET shape + read-key auth (wrong key 403; correct key 200 with full payload); decision_filter routing for all 5 cases (empty=all/unreviewed/accept/reject/overturn_curator); since_minutes filter (0=no filter, 1440=24h window includes recent rows, 99999 out-of-range 422); POST validation (missing api_key 403, wrong api_key 403, bad decision 422, short reason 422, missing draft_id 404); end-to-end disagreement_rate + false_positive_rate update via POST flows (8 accept + 2 reject → Sentry rate=0.20; 1 overturn_curator on 1 reviewed Curator draft → fp_rate=1.0); idempotency on same decision + revision overwrites previous decision + reason field. All 6 PASS in 14s. Drift finding (filed): bridge's _check_key returns 403 (Forbidden) not 401 (Unauthorized) on missing/wrong api_key; this matches existing operator endpoints (/operator/anchor-cedar-bundle + /operator/gic-reset etc.) so the deviation from REST 401-for-missing-credentials convention is bridge-wide, not new — tests aligned with bridge's actual contract. Adjacent regression: 76/76 PASS (review-endpoint + draft-Sentry/Guardian/Curator + O3-ACT-DRAFT/WATCHER/ANCHOR-SCRIPT + O2-SUGGEST + O1-D + O1-FRR + O1-FRR-endpoint). Bridge 2884→2890 +6. SDK 539 unchanged. Hardhat 528 unchanged. PV-CI 55 unchanged. Wallet zero impact (filesystem-only). No bridge restart required to surface in CI; endpoints become available on next bridge restart in production. With this phase shipped, the FULL O2 drafting loop is now CLOSED end-to-end: agents draft → store persists → operator queries via GET → operator decides via POST → store records decision → watcher reads decision-derived rates → o3_ready transitions when gates clear. Frontend dashboards can bind to GET /operator/operator-agent-drafts directly (mirrors the existing operator-agent-shadow-log + operator-agent-drift-log dashboard surfaces shipped Phase O1 C5). The remaining gaps before live triple dual-anchor are EXTERNAL: (1) shadow_age clearing 504h gates (~14.6 days Sentry+Guardian; ~21 days Curator); (2) ≥50 drafts per agent (requires polling loop deferred to next phase); (3) operator-dual-key + KMS-HSM + GitHub-App OAuth + setCurator role flags set on cfg by operator at the appropriate time.
NOTE: Phase O2-DRAFT-GENERATION (Curator) COMPLETE 2026-05-10 — Third agent in the parallel-fleet drafting trio. Closes the O2 drafting primitive surface for ALL three Operator Initiative agents in a single session (Sentry commit d9954039 + Guardian commit 5959792e + Curator this commit). Honors parallel-fleet invariant: all three agents must accumulate ≥50 drafts each in lock-step before single-action triple dual-anchor at O3 ACTING. Files added: bridge/vapi_bridge/operator_agent_curator_drafting.py with CuratorDraftGenerator class producing three primitive drafts under Curator's O2_SUGGEST authority per `curator_o2_suggest_v1.json`: draft_marketplace_listing_review(listing_id, verdict, review_payload) → `draft://listing_reviews/{sanitized_listing_id}/verdict` (HEADLINE Curator skill — verdict validated against Phase 238 Step I FROZEN _FROZEN_VERDICTS imported from bridge/vapi_bridge/curator_review.py: APPROVED + FLAGGED_TIER_MISMATCH + FLAGGED_ANCHOR_STALE + FLAGGED_CONSENT_AMBIGUOUS + FLAGGED_IPFS_UNAVAILABLE + REJECTED_NO_ANCHORS + REJECTED_INVALID_COMMITMENT safety floor; severity auto-derived from _SEVERITY_BY_VERDICT map; lifts to lane://marketplace/** at O3); draft_kms_sign_review(listing_id, verdict_payload_hash, signer_pubkey_hex, signature_payload) → `draft://listing_reviews/{sanitized_listing_id}/sig` (signs already-drafted listing review; URI /sig suffix disambiguates from /verdict at storage layer; verdict_payload_hash MUST be 64-char SHA-256 hex linking signature to specific verdict draft); draft_operator_notify(notification_id, recommendation, severity, notify_payload) → `draft://operator_notifications/{sanitized_id}` (suspension RECOMMENDATION channel — soft-veto path even at O3 where Curator gains direct tool:marketplace-listing-suspend authority on chain://iotex-testnet via setCurator() role; severity validated against frozen 5-set {info, warn, error, critical, recommend_suspend} where recommend_suspend is the Curator-specific high-stakes value signaling "operator should suspend"). Three READ-only Curator skills (tier-compliance-check + anchor-freshness-audit + ipfs-metadata-fetch) deliberately NOT shipped here — they observe state without producing drafts; will be exercised by Curator's polling loop (deferred phase). Module reuses Sentry's helpers (DraftResult, _sha256_canonical_json) and Guardian's _safe_id_segment for URI sanitization — cross-fleet symmetry enforced at import level. Module-level FROZEN URI prefixes CURATOR_LISTING_REVIEW_DRAFT_PREFIX="draft://listing_reviews/" + CURATOR_OPERATOR_NOTIFICATION_DRAFT_PREFIX="draft://operator_notifications/" re-asserted so future bundle edits lifting these paths require both bundle Merkle re-pin AND code update. CURATOR-EXCLUSIVE invariant: overturn_curator decision wiring — when operator review of a marketplace-listing-review draft determines verdict was wrong (e.g. Curator flagged a listing that operator re-checks and approves), operator records decision='overturn_curator' which feeds the watcher's PHASE_O3_FALSE_POSITIVE_RATE_MAX=0.0 gate (ZERO TOLERANCE — any positive rate fires the blocker). Sentry+Guardian drafts NEVER receive overturn_curator (semantically marketplace-specific). Tests: bridge/tests/test_phase_o2_draft_generation_curator.py with 10 tests T-O2-DRAFT-CURATOR-1..10 covering: verdict draft persistence with FROZEN enum + /verdict URI suffix; invalid verdicts ("APPROVE"/"approved"/random) early-return; kms-sign-review uses /sig suffix distinct from /verdict at both URI and payload_hash level; operator-notify accepts all 5 severities including recommend_suspend; count_drafts(curator) == 50 after 50 inserts; invalid input early-returns (empty listing_id, bad verdict_payload_hash format, bad severity URGENT, empty recommendation, empty notification_id); end-to-end watcher gate clears for Curator (50 verdict drafts → _count_drafts_safe(curator) == 50, PHASE_O3_DRAFT_PAYLOAD_MIN gate passes); ZERO TOLERANCE invariant — overturn_curator on 1 of 10 reviewed Curator drafts → false_positive_rate=0.1 > PHASE_O3_FALSE_POSITIVE_RATE_MAX=0.0 → blocker fires (verifies disagreement_rate independently stays at 0.0 since overturn_curator does NOT count toward disagreement denominator); parallel-fleet TRIO independence — Sentry 7 + Guardian 11 + Curator 13 = 31 total store rows but each per-agent count returns its own number (no cross-pollution); idempotent insert returns same row_id on UNIQUE collision. All 10 PASS in 19s. Adjacent regression: 66/66 PASS (O2-DRAFT-Sentry/Guardian/Curator + O3-ACT-DRAFT/WATCHER/ANCHOR-SCRIPT + O2-SUGGEST + O1-D + O1-FRR). Bridge 2874→2884 +10. SDK 539 unchanged. Hardhat 528 unchanged. PV-CI 55 unchanged. Wallet zero impact (filesystem-only). No bridge restart required (reuses store schema 1005 from O2-DRAFT-Sentry; idempotent on next bridge boot). With this phase shipped, the FULL operator initiative O2 drafting primitive surface is COMPLETE for all three agents — Sentry+Guardian+Curator can each produce drafts that drive their respective watcher gates. The remaining gaps before live triple dual-anchor are EXTERNAL to the protocol code: (1) Sentry+Guardian shadow_age clearing 504h gate (~14.6 days from 2026-05-10); (2) Curator shadow_age clearing 504h (~21 days); (3) Each agent accumulating ≥50 drafts (requires polling loop + operator triggers — deferred phase) AND clearing operator review with disagreement_rate <5% AND Curator-specific false_positive_rate=0% AND operator_dual_key_present + kms_hsm_production_ready (Sentry+Guardian) + github_app_oauth_tokens_valid (Guardian) + marketplace_curator_role_assigned (Curator). All measurement primitives now wired end-to-end through the watcher.
NOTE: Phase O2-DRAFT-GENERATION (Guardian) COMPLETE 2026-05-10 — Sibling of O2-DRAFT-GENERATION (Sentry) shipped same day. Honors parallel-fleet invariant: Guardian must accumulate the >=50 draft signal in lock-step with Sentry to clear the watcher's PHASE_O3_DRAFT_PAYLOAD_MIN gate per agent before single-action triple dual-anchor. Files added: bridge/vapi_bridge/operator_agent_guardian_drafting.py with GuardianDraftGenerator class producing three primitive drafts under Guardian's O2_SUGGEST authority per `guardian_o2_suggest_v1.json`: draft_kms_sign(commit_hash, signer_pubkey_hex, signature_payload) → `draft://commit_hashes/{commit}` (mirrors Sentry's signature exactly; bound to Guardian's audits/+invariants/+ops/+sweeps/ lane PR workflow); draft_audit_entry(audit_id, audit_payload, audit_kind="audit") → `draft://audit_entries/{sanitized_audit_id}` (Guardian's headline O2 skill — audit-drafting is the input to Guardian's audit-trail PRs targeting the audits/ lane; lifts to lane://audits/** at O3 ACTING for live writes); draft_operational_diagnostic(diagnostic_id, diagnostic_payload, severity) → `draft://audit_entries/diag-{sanitized_id}` (Guardian-exclusive skill drafting protocol operational findings — sweep summaries, capture-health anomalies, watchdog events; lifts to lane://ops/** at O3; severity validated against {info, warn, error, critical} frozen set). All three reuse Sentry's helpers (DraftResult dataclass, _normalize_commit_hash for SHA-1 commit validation, _sha256_canonical_json for canonical-JSON payload hashing) so the cross-fleet symmetry is enforced at the import level — any future change to the canonical hashing algorithm or commit-hash validation propagates uniformly across both agents. New helper _safe_id_segment() locally for URI sanitization (alphanumerics + dash + underscore + dot preserved; everything else → '_'). Module-level FROZEN URI prefixes GUARDIAN_KMS_SIGN_DRAFT_PREFIX="draft://commit_hashes/" + GUARDIAN_AUDIT_DRAFT_PREFIX="draft://audit_entries/" re-asserted so a future bundle edit lifting these paths to lane:// requires both bundle Merkle re-pin AND code update. URI prefix-disambiguation: operational-diagnostic drafts use `draft://audit_entries/diag-{id}` so operators can distinguish audit-drafting vs operational-diagnostic at-a-glance while both still anchor under Guardian's same Cedar-permitted lane prefix (audit_entries). Tests: bridge/tests/test_phase_o2_draft_generation_guardian.py with 10 tests T-O2-DRAFT-GUARDIAN-1..10 covering: kms-sign URI/hash correctness; audit-drafting with sanitized audit_id (slashes/colons → underscores; e.g. "invariant/INV-007/sweep:2026-05-10" → "invariant_INV-007_sweep_2026-05-10"); operational-diagnostic diag- URI prefix; count_drafts(guardian) returns N=50 after 50 inserts; invalid input early-returns (bad commit hash, empty audit_id, non-dict payload, bad severity, empty diagnostic_id); end-to-end watcher gate clears for Guardian (50 audit drafts → _count_drafts_safe(guardian) == 50, PHASE_O3_DRAFT_PAYLOAD_MIN gate passes); Sentry+Guardian count INDEPENDENCE (parallel-fleet invariant — 10 Sentry + 5 Guardian → counts are 10 and 5, never cross-pollinated); idempotent insert returns same row_id on UNIQUE collision; severity validation (4 valid, 4+ invalid rejected); per-agent disagreement_rate semantics (Guardian rate computed only against Guardian's reviewed drafts; Sentry's rejections do NOT pollute Guardian's rate). All 10 PASS in 18s. Adjacent regression: 56/56 PASS (O2-DRAFT-Sentry + O2-DRAFT-Guardian + O3-ACT-DRAFT/WATCHER/ANCHOR-SCRIPT + O2-SUGGEST + O1-D + O1-FRR). Bridge 2864→2874 +10. SDK 539 unchanged. Hardhat 528 unchanged. PV-CI 55 unchanged. Wallet zero impact (filesystem-only ship). No bridge restart required (reuses store schema 1005 already idempotent on next bridge boot). With this phase shipped, two of three Operator Initiative agents have the primitive drafting surface that drives their respective o3_ready paths. Curator's marketplace-listing-review + tier-compliance-check + anchor-freshness-audit + ipfs-metadata-fetch + operator-notify primitives are next per sequential development alignment — Curator's draft URI scheme differs (`draft://listing_reviews/*` + `draft://operator_notifications/*`) and includes the unique overturn_curator decision wiring for false-positive-rate tracking. Sentry+Guardian shadow_age clearing 504h (~14.6 days from 2026-05-10) plus accumulating ≥50 drafts each plus disagreement_rate <5% are now MEASURABLE end-to-end through the watcher; Curator's parallel signal arrives once its draft generator ships.
NOTE: Phase O2-DRAFT-GENERATION (Sentry) COMPLETE 2026-05-10 — Wired the loop that ends at the watcher's PHASE_O3_DRAFT_PAYLOAD_MIN gate. Without drafts being persisted, all three Operator Initiative agents would be stuck at o3_ready=False forever even after shadow_age clears 504h. This phase ships Sentry's three highest-leverage drafting primitives + the store schema + helpers that close the measurement loop. (1) STORE SCHEMA 1005: new `operator_agent_drafts` table — id, agent_id (Q9 hex in production / canonical in tests), action_category (skill|tool), action_name, draft_uri (full draft://... URI), payload_hash (SHA-256 lowercase hex), payload_bytes, kms_sig_present, operator_decision (NULL | accept | reject | overturn_curator), operator_decision_at, operator_disagreement_reason. UNIQUE INDEX on (agent_id, payload_hash) enforces idempotent insert — re-inserting the same payload returns existing row id. Two query indexes (agent_created DESC for count_drafts hot path; agent_decision for review-state lookups). Idempotent CREATE TABLE; schema_versions phase 1005 logged. (2) STORE HELPERS (5): insert_operator_agent_draft (returns row_id; 0 on failure or UNIQUE collision returning existing id); count_operator_agent_drafts(agent_id, since_seconds) — drives PHASE_O3_DRAFT_PAYLOAD_MIN=50 gate; record_operator_decision(draft_id, decision, reason) — operator review surface, accepts {accept, reject, overturn_curator}, rejects garbage values, idempotent on same decision, allows revision; compute_operator_agent_disagreement_rate — n_reject/n_reviewed (excludes overturn_curator + unreviewed); compute_operator_agent_false_positive_rate — n_overturn/n_reviewed (Curator-only semantically; non-Curator agents always 0.0). All helpers fail-open returning 0 / 0.0 on DB errors per INV-INITIATIVE-ADVANCEMENT-002. Plus get_operator_agent_drafts(agent_id, decision, since_seconds, limit) for the operator review surface (frontend dashboards). (3) SENTRY DRAFT GENERATOR: bridge/vapi_bridge/operator_agent_sentry_drafting.py with SentryDraftGenerator class producing three primitive drafts under Sentry's O2_SUGGEST authority: draft_kms_sign(commit_hash, signer_pubkey_hex, signature_payload) → draft://commit_hashes/{commit}; draft_provenance_record(record_id, attestation_payload) → draft://attestations/{sanitized_record_id}; draft_pda_anchor(device_id_hash_hex, poad_hash_hex, dual_veto, anchor_payload) → draft://attestations/pda-{poad_hash[:16]}. SCAFFOLD-ONLY semantics for pda_anchor at O2: produces draft of payload that WILL be passed to chain.record_adjudication() at O3 ACTING; no chain call here (drafts count toward O3 readiness). All three methods: validate input early (40-char SHA-1 for commit hash; 64-char hex for bytes32 fields; non-empty record_id; dict for payloads); compute SHA-256 of canonical-JSON payload; auto-populate ts_ns when absent (deterministic re-derive on caller side); call store.insert_operator_agent_draft with `_resolve_agent_id_for_store("anchor_sentry", cfg)` so production Q9-hex and test canonical-name keys both work uniformly with the watcher's `_count_drafts_safe`. Returns DraftResult dataclass (slots=True) with draft_id, draft_uri, payload_hash, payload_bytes, action_category, action_name, agent_id_used, error. Fail-open on store failures (returns draft_id=0 + error populated; caller can retry). FROZEN URI prefixes: SENTRY_KMS_SIGN_DRAFT_PREFIX="draft://commit_hashes/" / SENTRY_PROVENANCE_DRAFT_PREFIX="draft://attestations/" / SENTRY_PDA_ANCHOR_DRAFT_PREFIX="draft://attestations/" — re-asserted at module level so a future bundle edit lifting these paths to lane:// must update both the bundle Merkle root AND this code. (4) TESTS: bridge/tests/test_phase_o2_draft_generation_sentry.py with 10 tests T-O2-DRAFT-SENTRY-1..10 covering: kms-sign URI/hash correctness; provenance-recording with record_id sanitization (slashes/colons → underscores); PDA-anchor scaffold with no chain call; count_operator_agent_drafts returns N=50 after 50 inserts; record_operator_decision idempotency + revision + invalid-decision rejection; compute_disagreement_rate excludes unreviewed (denominator=reviewed only); false_positive_rate semantically Curator-only (verified 0.0 for Sentry even after accept/reject decisions; verified 1.0 for Curator after overturn_curator); end-to-end watcher gate verification — generator produces 50 drafts, watcher's _count_drafts_safe reads 50, PHASE_O3_DRAFT_PAYLOAD_MIN gate clears; invalid input early-returns without DB write; idempotent insert returns same row_id on UNIQUE collision (count remains 1 after duplicate insert). All 10 PASS in 23s (slow because Store init runs full schema migration in tmp_path). (5) DEFERRED to follow-up phases: operator review HTTP endpoint (POST /operator/operator-agent-draft-review); Sentry polling loop that auto-fires drafts on triggers (new commit / new PoAC record / new PoAd hash); Guardian's audit-drafting + git-{add,commit,pr} + ipfs-pin draft generators (4 more methods on a parallel GuardianDraftGenerator); Curator's marketplace-listing-review + tier-compliance-check + anchor-freshness-audit + ipfs-metadata-fetch + operator-notify draft generators (5 more methods on CuratorDraftGenerator). The store schema + helpers + watcher-gate are agent-agnostic — Guardian/Curator generators bind to the same primitive surface. Adjacent regression: 60/60 PASS (O2 draft generation + O3 ACT DRAFT/WATCHER/ANCHOR-SCRIPT + O2 SUGGEST + O1 D + O1 FRR + O1 cedar_parser + O1 C2 shadow). Bridge 2854→2864 +10. SDK 539 unchanged. Hardhat 528 unchanged. PV-CI 55 unchanged. Wallet zero impact (filesystem-only ship). No bridge restart required — schema migration is idempotent on next bridge boot. With this phase shipped, the watcher's `_count_drafts_safe` returns REAL counts from the store (instead of 0 fallback), so the O3 readiness path now has a deterministic input that progresses as Sentry generates drafts. Sentry+Guardian shadow_age clearing 504h (~14.6 days from 2026-05-10) plus accumulating ≥50 drafts each (Guardian primitives ship in follow-up phase) plus disagreement_rate <5% are the remaining gates before single-action triple dual-anchor.
NOTE: Phase O3-ACT-ANCHOR-SCRIPT COMPLETE 2026-05-10 — Authored `scripts/parallel_o3_act_anchor.py` mirroring `scripts/parallel_o2_anchor.py` (Phase O1-FRR Stream E) with one critical addition: a fourth authorization gate. The O2 sibling has triple-gate (CHAIN_SUBMISSION_PAUSED=false env + OPERATOR_INITIATIVE_O2_AUTHORIZED=true env + --confirm CLI flag); the O3 script adds a Gate 4 watcher veto: refuses to fire unless ALL THREE agents have `o3_ready=True` per the latest `evaluate_fleet_advancement_sync()` evaluation. The 4th gate enforces the *_o3_acting_v1.json `_o3_gates` field externally — operator cannot bypass by knowing both env vars; the watcher's o3_blockers list (drafts<50 / disagreement>5% / dual_key absent / KMS HSM not provisioned / GitHub-App tokens invalid for Guardian / setCurator role unassigned for Curator / false_positive_rate>0% for Curator) all gate the anchor. INTENTIONAL ENV VAR DISTINCTION: gate 2 uses OPERATOR_INITIATIVE_O3_AUTHORIZED (NOT _O2_) so residual O2 authorization in the operator's shell from a prior O2 anchor session cannot accidentally satisfy the O3 anchor's intent gate. Defense-in-depth against carry-over. AGENT_BUNDLE_FILES references the three locked filenames (`anchor_sentry_o3_acting_v1.json` / `guardian_o3_acting_v1.json` / `curator_o3_acting_v1.json`) from Phase O3-ACT-DRAFT (commit 3cb59f46) — same Merkle roots as locked there will become the on-chain anchor inputs. AGENT_ANCHOR_ORDER fixed at `(anchor_sentry, guardian, curator)` matching parallel_o2_anchor.py for activation_log ordering invariant. STOP CONDITIONS preserve parallel atomicity: any operational tx revert OR governance tx revert OR post-anchor FRR mismatch halts before next agent fires; partial state is recoverable via FSCA `BUNDLE_HASH_DRIFT_DETECTED` / `SCOPE_HASH_GOVERNANCE_DRIFT_DETECTED` rules on the next 15-min poll. Pre-anchor expected FRR computed locally and printed for visual operator confirmation; post-anchor FRR recomputed from updated activation_log and asserted match. Cost budget COST_BUDGET_IOTX=3.0 (6 txs × ~0.30/tx worst case at 4000 Gwei + 50% margin); SAFETY_FLOOR_IOTX=0.50 (operator wallet ≥0.50 IOTX before script will fire). Exit codes 0..7 (added 7=watcher veto on top of O2's 0..6). Tests: bridge/tests/test_phase_o3_act_anchor_script.py with 6 tests T-PARALLEL-O3-1..6 covering: gate 1+2 PASS when both envs set, gate 1 FAIL when kill-switch active, gate 2 FAIL when ONLY O2 env var set (verifies distinct env var name prevents residual O2 carry-over), gate 4 FAIL when ANY agent o3_ready=False, gate 4 PASS when all three o3_ready=True, AGENT_BUNDLE_FILES filename pin against future drift. Tests use `importlib.util.spec_from_file_location` to load the script from `scripts/` since it isn't a package member; pure-function gate-logic tests, no chain or store needed. All 6 PASS in 0.72s. SECURITY NOTE: for Curator on mainnet, OPERATOR'S `cfg.kms_hsm_production_ready` flag is the single source of truth — testnet anchor allowed with MockKMSClient backing per Sessions 1+2+3 precedent; mainnet REQUIRES AWS KMS HSM provisioning per `wiki/runbooks/curator_mainnet_migration.md` before flag may be set True. The script does NOT distinguish testnet/mainnet itself; it inherits the operator's flag setting. Adjacent regression: 36/36 PASS (O3-ACT-DRAFT + O3-ACT-WATCHER + O3-ACT-ANCHOR-SCRIPT + O2-SUGGEST + O1-D + O1-FRR). Bridge 2848→2854 +6. SDK 539 unchanged. Hardhat 528 unchanged. PV-CI 55 unchanged. Wallet zero impact (filesystem-only ship). No bridge restart required. The full O3 anchor-readiness pipeline (bundles + watcher + anchor script) is now complete; only remaining bit before single-action triple dual-anchor is Sentry+Guardian shadow_age clearing 504h gate (~14.6 days from 2026-05-10) AND Curator clearing 504h (~21 days) AND O2_SUGGEST agents accumulating ≥50 drafts each AND operator-disagreement-rate dropping below 5% (deferred phase ships draft generation primitives).
NOTE: Phase O3-ACT-WATCHER COMPLETE 2026-05-10 — Critical bug fix + O3 readiness gate extension on operator_initiative_advancement.py. (1) BUG FIX: bundle_filename detection at the phase resolver previously matched ONLY `_o3_act_` substring; Phase O3-ACT-DRAFT (commit 3cb59f46 same day) ships bundles as `*_o3_acting_v1.json` per Cedar VALID_PHASES (O3_ACTING string). When those bundles anchor on chain, the watcher would have mis-classified them as `phase=UNKNOWN` — silently breaking Stream J's canonical→Q9 fix downstream because the phase code lookup falls through to PHASE_CODE_UNKNOWN (0xFF) which then propagates into the FRR pre-image. Fix accepts BOTH substrings with priority order: `_o3_acting_` first (canonical 2026-05-10+), `_o3_act_` retained for backward-compat with any pre-shipped placeholder bundles. (2) PLACEHOLDER BLOCKER REPLACEMENT: pre-Phase-O3-ACT-WATCHER `_evaluate_agent_readiness` permanently appended 2 placeholder strings ("draft_payload_count_helper_not_yet_implemented", "disagreement_rate_helper_not_yet_implemented") so o3_ready was structurally False even when O2 age >= 504h. Replaced with real store-helper-driven blockers using PHASE_O3_DRAFT_PAYLOAD_MIN=50 + PHASE_O3_DISAGREEMENT_RATE_MAX=0.05 thresholds. Helpers: count_operator_agent_drafts(agent_id, since_seconds), compute_operator_agent_disagreement_rate(agent_id, since_seconds), compute_operator_agent_false_positive_rate(agent_id, since_seconds) — all wrapped in safe getattr-fallback wrappers (`_count_drafts_safe`, `_disagreement_rate_safe`, `_false_positive_rate_safe`) returning 0/0.0 when absent. Forward-compat invariant: pre-O3-WATCHER bridge stores keep functioning unchanged; post-O2-drafting phase ships real implementations and o3_ready transitions True deterministically when 504h + 50 drafts + <5% disagreement clear. (3) NEW GATES per `*_o3_acting_v1.json` _o3_gates field: operator_dual_key_present (cfg attr; ALL three agents), kms_hsm_production_ready (cfg attr; Sentry+Guardian only — Curator's mainnet-only gate per Sessions 1+2+3 MockKMSClient testnet precedent), github_app_oauth_tokens_valid (cfg attr; Guardian-only — Sentry's GitHub-App-path differs by token scope), marketplace_curator_role_assigned (cfg attr; Curator-only via setCurator() role on VAPIDataMarketplaceListings.sol Phase 238 Step F+H), false_positive_rate_30d_max=0.0 (Curator-only, ZERO TOLERANCE — any positive rate fires PHASE_O3_FALSE_POSITIVE_RATE_MAX blocker). New constant PHASE_O3_DRAFT_AGE_WINDOW_SECONDS=2_592_000 (30 days) is the rolling window for all three store-helper queries. (4) Tests: bridge/tests/test_phase_o3_act_watcher.py with 8 tests T-O3-ACT-WATCHER-1..8 covering bundle_filename O3_ACTING resolution / o3_ready True path with all gates clean / draft_payload_count blocker / disagreement_rate blocker / Curator-specific false_positive_rate + setCurator role blockers / Guardian-specific github_app_oauth blocker (and verifies Sentry NOT gated by it) / dual_key_present blocker firing for ALL three agents / safe-wrapper forward-compat invariant when helpers missing or raise. All 8 PASS in 0.4s. Adjacent regression 57/57 PASS (Phase O1 cedar_parser + O1 D + O1 FRR + O1 FRR endpoint + O2 SUGGEST + O3 ACT DRAFT + O3 ACT WATCHER + O1 C2 shadow + O1 C4 sweeper + gas buffer). Bridge 2840→2848 +8. SDK 539 unchanged. Hardhat 528 unchanged. PV-CI 55 unchanged. Wallet zero impact (filesystem-only). No bridge restart required (watcher reloads logic on next poll cycle if active; default OPERATOR_INITIATIVE_ADVANCEMENT_ENABLED=False matches Phase O1 D opt-in pattern). When O2_SUGGEST agents start drafting (deferred phase introduces draft generation primitives + real store helpers), the watcher will deterministically transition o3_ready=False→True per agent as each clears its specific gate set, matching the *_o3_acting_v1.json bundle Merkle roots locked in commit 3cb59f46 ready for single-action triple dual-anchor.
NOTE: Phase O1-FRR-SDK-LOCK COMPLETE 2026-05-10 — PV-CI invariants pin the VAPIFleetReadinessRoot SDK client + 3-drawer DeveloperView UI layout. Commit `4afcac8d`. Defensive lock-in for the work shipped this session: without these invariants, a future refactor could silently rename the FRR SDK client (breaking external Python tooling that imports VAPIFleetReadinessRoot / FleetReadinessRootResult / AdvancementLogResult / AgentReadinessRow), or collapse the three DeveloperView drawer Z-index layers into colliding handles. 5 NEW INVARIANTS: INV-O1-FRR-SDK-001 (`class VAPIFleetReadinessRoot:` in sdk/vapi_sdk.py); INV-O1-FRR-SDK-002 (3 frozen dataclass names AgentReadinessRow + FleetReadinessRootResult + AdvancementLogResult, min_matches=3 catches any single-class rename); INV-O3-UI-DRAWER-001 (zIndex 20 in OperatorAgentsDrawer.jsx, bottom-right); INV-O3-UI-DRAWER-002 (zIndex 21 in DraftReviewDrawer.jsx, bottom-left); INV-O3-UI-DRAWER-003 (zIndex 22 in O3ReadinessDrawer.jsx, top-center). Three separate invariants (one per drawer file) pin each Z-index value preserving the activation-gated UX guarantee that any later-opened drawer layers above any earlier-opened drawer. Allowlist regenerated 58 → 63 entries via governance phrase pipe pattern; bridge offline so governance event POST 404'd (non-blocking; matches Phase 224 + O1-FRR-PARALLEL precedent). PV-CI gate: 63/63 PASS via `python scripts/vapi_invariant_gate.py --report`. No bridge restart required (filesystem-only). No test churn. No contract/Hardhat churn. Wallet zero impact. PV-CI 58 → 63 (+5).

NOTE: Phase O1-FRR-SDK COMPLETE 2026-05-10 — VAPIFleetReadinessRoot client wraps GET /operator/fleet-readiness-root + GET /operator/operator-initiative-advancement-log. Commit `0076082b`. Closes the SDK-side gap left by Phase O1-FRR-ENDPOINT (commit 6c9a8acf) + Phase O3-READINESS-DASHBOARD (commit 8fecfcd7) — the endpoint shipped + frontend bound to it via REST, but external Python tooling had no programmatic surface. Restores the canonical wire-contract parity discipline (endpoint → SDK → frontend → CLAUDE.md NOTE). NEW SDK CLIENT: VAPIFleetReadinessRoot with three methods: status() returns FleetReadinessRootResult (FROZEN-v1 FRR commitment + per-agent rollup + fleet summary; read-key auth via x-api-key Header); advancement_log(limit=50) returns AdvancementLogResult (paginated operator_initiative_advancement_log rows; 1-500 limit clamp); per_agent_row(agent_id) convenience method returns AgentReadinessRow on hit or None on miss (forward-compat; never raises). NEW DATACLASSES (all slots=True per existing convention): AgentReadinessRow (11 slots: agent_id + current_phase + shadow_age_hours + cedar_eval_count + 2 drift counts + o2_ready + o2_blockers + o3_ready + o3_blockers + error); FleetReadinessRootResult (11 slots: frr_hex + frr_ts_ns + fleet_phase_aligned + 4 count fields + next_alignment_target + per_agent + domain_tag + error); AdvancementLogResult (4 slots: limit + row_count + rows + error). All clients fail-open on urllib / HTTP / JSON parse errors (matches existing VAPIDraftReview / VAPICurator pattern — returns dataclass with .error populated, never raises). SDK_VERSION marker bumped `3.0.0-phase-o2-draft-review → 3.0.0-phase-o1-frr-sdk`. OpenAPI sdk/openapi.yaml: version 3.0.0-phase237 → 3.0.0-phase-o1-frr-sdk; 2 new paths (/operator/fleet-readiness-root + /operator/operator-initiative-advancement-log); 3 new schemas (AgentReadinessRow + FleetReadinessRoot + AdvancementLogPage). Tests: sdk/tests/test_phase_o1_frr_sdk.py with 5 tests T-O1-FRR-SDK-1..5 covering: status() shape (frr_hex is 64 hex chars + per_agent is list + fleet_size==3 + domain_tag="VAPI-FRR-v1"); wrong api_key returns .error containing 403/Forbidden without raising; advancement_log() shape + limit pass-through + row_count match; per_agent_row("anchor_sentry") returns AgentReadinessRow with blocker lists + unknown agent returns None; unreachable base_url returns result with .error set on both methods without raising. All 5 PASS in 15.49s using uvicorn-in-thread + FastAPI live app pattern (matches existing test_phase_o2_draft_review_sdk.py). Adjacent regression: SDK draft-review 4/4 PASS unchanged. No bridge restart required (read-only SDK addition). No Hardhat / contract churn. Wallet zero impact. SDK count 543 → 548 (+5).

NOTE: Phase O3-READINESS-DASHBOARD COMPLETE 2026-05-10 — Strategic visibility surface that makes the path to O3 ACTING anchor visible at a glance. Commit `8fecfcd7`. Pairs naturally with Phase O2-DRAFT-REVIEW-FRONTEND (3d5923e7) — the full Operator Initiative loop is now CLOSED end-to-end UI-side, and this dashboard surfaces the strategic countdown ("how close are we to triple dual-anchor") that operators previously had to derive from 4 endpoints + CLAUDE.md NOTE entries. NEW HOOK: useFleetReadinessRoot({enabled}) wraps GET /operator/fleet-readiness-root (Phase O1-FRR-ENDPOINT); noMock:true (strategic planning accuracy-critical; fabricated FRR would mislead anchor decisions); 30s refetchInterval (watcher cadence is 1h; operator-flag flips manual; 30s feels live without burning bridge cycles). NEW COMPONENTS: (1) frontend/src/components/O3ReadinessDashboard.jsx — per-agent (Sentry/Guardian/Curator) cards in responsive grid (minmax 260px) showing the meaningful gate set per current_phase: O1_SHADOW agents render O2 gate (shadow_age clock target 504h + cedar_eval target 100 + bundle/scope drift flags target 0 per 30d); O2_SUGGEST agents render O3 gate (o2_age clock 504h + draft_payload count 50 + disagreement_rate gauge ≤0.05 + Curator-exclusive false_positive_rate gauge ZERO TOLERANCE 0.0 + operator flags dual_key/KMS-HSM/GitHub-App/setCurator); O3_ACT agents render terminal "✓ ANCHORED — LIVE WRITE AUTHORITY". Top fleet summary band shows FRR hex (truncated 12...6) + ALIGNED yes/no + NEXT target + O2/O3 ready counts. Blocker strings parsed from o2_blockers/o3_blockers (which arrive human-readable like "shadow_age_152.1h_under_min_504h") into ProgressBar component (height 4px, transition 0.3s, color-coded green=reached/amber=in-progress/red=violation for inverted gates). FlagPill component for boolean ops gates. (2) frontend/src/components/O3ReadinessDrawer.jsx — TOP-edge slide-down drawer (55vh) — NEW DIRECTION distinct from the two bottom drawers; handle top-CENTER; zIndex 22 (above DraftReviewDrawer's 21 and OperatorAgentsDrawer's 20); FLEET ALIGNED ★ badge in header + handle when fleet_phase_aligned=True. DEVELOPERVIEW WIRING: useFleetReadinessRoot({enabled:o1Active}) drives the handle's fleet-aligned badge; separate useState `o3ReadinessOpen` so all three drawers open independently; activation gate o1Active mirrors Phase O1 C5/C8 pattern. THREE-DRAWER LAYOUT (no collisions): top-center O3 Readiness (strategic, low-frequency, ★ on aligned) / bottom-left Draft Review (interaction-heavy, count on pending) / bottom-right Operator Agents (shadow log + drift findings). Frozen gate constants mirrored in component from `operator_initiative_advancement.py:82-95` — any future gate change requires updating both backend constants AND component GATES dict (manual sync; PV-CI invariant candidate). Build PASS 59s. DeveloperView chunk 40.13→51.09 KB raw / 10.29→12.61 KB gzipped (+2.3 KB gzipped, under 10 KB target). No bridge restart required. No test churn (FRR endpoint covered by Phase O1-FRR-ENDPOINT tests). Wallet zero impact. With this phase shipped, operators have a single-glance answer to "are we ready for triple dual-anchor" — when ★ FLEET ALIGNED fires and all three agent cards show O3-ready blockers cleared, `parallel_o3_act_anchor.py --confirm` is unblocked.

NOTE: Phase O2-DRAFT-REVIEW-FRONTEND COMPLETE 2026-05-10 — Closes the operator-side disagreement_rate + false_positive_rate gates on the UI. Commit `3d5923e7`. Bridge endpoints (Phase O2-DRAFT-REVIEW-ENDPOINT a44fa359) + SDK (Phase O2-DRAFT-REVIEW-SDK) existed; without this surface, operators had to curl manually for every decision, drafts accumulated unreviewed, and the gates stayed structurally unmeasurable → O3 ACTING anchor blocked indefinitely. NEW HOOKS in frontend/src/api/bridgeApi.js: (1) useOperatorDrafts({agentId, decision, sinceMinutes, limit, enabled}) wraps GET /operator/operator-agent-drafts with noMock:true (audit-critical — fabricated drafts would corrupt operator decisions); default decision='unreviewed' + sinceMinutes=10080 (7d backlog); 20s refetchInterval (drafts accumulate slowly). (2) useReviewDraft() mutation hook wrapping POST /operator/operator-agent-draft-review — endpoint reads draft_id/decision/reason as QUERY PARAMS, apiPost wrapper auto-appends api_key; onSuccess invalidates all operatorAgentDrafts queries so list refreshes. NEW COMPONENTS: (a) frontend/src/components/DraftReviewPanel.jsx — filter chips (agent: All/Sentry/Guardian/Curator; decision: Unreviewed/All/Accept/Reject/Overturn; since: 1h/24h/7d/30d) + draft list with expand-to-review; each expanded row shows payload metadata + reason textarea (≥10 chars client-validated; server returns 422 on short) + ACCEPT/REJECT/OVERTURN buttons. OVERTURN only renders for Curator drafts (agent_id contains 'curator' OR action_name starts 'marketplace-'); button title surfaces ZERO TOLERANCE warning per PHASE_O3_FALSE_POSITIVE_RATE_MAX=0.0 gate. (b) frontend/src/components/DraftReviewDrawer.jsx — slide-up bottom drawer (60vh) hosting the panel. Handle positioned bottom-LEFT (avoids collision with OperatorAgentsDrawerHandle at bottom-right Phase O1 C5). zIndex 21 above OperatorAgentsDrawer's 20 so opening both layers correctly. Pending-count badge in header + handle when unreviewedCount>0. DEVELOPERVIEW WIRING: useOperatorDrafts({decision:'unreviewed', sinceMinutes:10080, limit:1, enabled:o1Active}) drives the pending-count badge (row_count → handle chip). Polling halts when O1 inactive (zero cost for non-O1 operators) OR drawer closed (enabled prop tied to `open`). Separate useState `draftReviewOpen` from `opAgentsOpen` so both drawers can be opened independently for cross-reference. Drawer + handle render only when o1Active (mirrors Phase O1 C5/C8 activation gate pattern). Doubled-prefix convention preserved: all paths include inner /operator/ segment per Phase O1 C9 empirical bug. Build PASS in 56s. DeveloperView chunk 27→40.13 KB raw / 10.29 KB gzipped (+3 KB gzipped, under 10 KB target). No bridge restart required. No test churn (bridge tests cover endpoint + SDK end-to-end). Wallet zero impact. With this phase shipped, the FULL Operator Initiative O2→O3 loop is now CLOSED end-to-end UI-side: agents draft via polling loops → store persists → operator reviews via DraftReviewDrawer → POST flips operator_decision → watcher reads decision-derived rates → o3_ready transitions when gates clear → parallel_o3_act_anchor.py Gate 4 unblocks. Remaining gates are EXTERNAL: shadow_age clearing 504h + accumulating ≥50 drafts per agent + operator dual-key/KMS-HSM/GitHub-App OAuth/setCurator role flags.

NOTE: Phase O2-GUARDIAN-CURATOR-TRIGGERS-ARC COMPLETE 2026-05-10 — Closes the polling-loop wiring for the full Operator Initiative fleet (Sentry+Guardian+Curator) with live trigger sources. Mirrors the Sentry-side O2-GIT-TRIGGER-SOURCE pattern (commit dc8d6cab + auto-wire 3eede644). Three atomic commits land the arc: (1) Pre-Wave `73e70838` scaffolds 7 cfg flags (operator_agent_guardian_git_trigger_enabled + operator_agent_guardian_fsca_trigger_enabled + operator_agent_curator_marketplace_trigger_enabled + operator_agent_curator_anchor_freshness_trigger_enabled + operator_agent_curator_anchor_freshness_interval_s=3600 + operator_agent_curator_periodic_compliance_trigger_enabled + operator_agent_curator_periodic_compliance_interval_s=21600; all default False/opt-in) plus main.py autowire stubs that compose multiple trigger callables into a single get_pending_triggers passed to each polling loop. Composition helper `_compose_sources(srcs)` chains 1..N trigger callables into a single fail-open list-returning callable. (2) Commit `3238c68f` ships bridge/vapi_bridge/operator_agent_guardian_trigger_sources.py (244 LOC) + bridge/tests/test_phase_o2_guardian_trigger_sources.py (~225 LOC). GuardianFscaTriggerSource subscribes to fleet_coherence_log by id high-water mark; severity map CRITICAL→critical / HIGH→error / MEDIUM→warn / LOW→info; subject = rule_name + " — " + explanation[:60]. Fail-open on missing table / malformed JSON / parse errors. Guardian-side GitTriggerSource reuse via _git_cfg_view() pattern that maps guardian_git_trigger_enabled → underlying flag the source reads. (3) Commit `32997d18` ships bridge/vapi_bridge/operator_agent_curator_trigger_sources.py (586 LOC) + bridge/tests/test_phase_o2_curator_trigger_sources.py (~402 LOC). Three Curator trigger sources: CuratorMarketplaceListingTriggerSource (marketplace_listing_log id high-water-mark; emits {kind:'listing_event', payload:{listing_id, verdict, review_payload}}), CuratorAnchorFreshnessTriggerSource (cron-style 1h default; chain.is_adjudication_recorded() async-aware via asyncio.iscoroutine + asyncio.run fallback; FRESHNESS_THRESHOLD_HOURS=720 frozen), CuratorPeriodicComplianceTriggerSource (cron-style 6h default; emits ONE batch trigger per boundary with PERIODIC_COMPLIANCE_LIMIT=50 frozen). Drift findings preserved in module docstring: (a) curator_review.review_listing requires AnchorStates + IpfsState snapshots NOT single-arg as Pre-Wave's brief implied — trigger source emits safety-floor verdict VERDICT_REJECTED_INVALID_COMMITMENT and the agent's autonomous loop performs full review pipeline; (b) chain.is_adjudication_recorded is async — freshness source detects coroutine + runs via asyncio.run() with fail-open exception handling; (c) marketplace_listing_log has no `listing_id` column — emitted listing_id is the listing_commitment string (verified at store.py:3401-3420); (d) Agent-A drift finding: FSCA table is canonical `fleet_coherence_log` per store.py:2281, NOT `fleet_signal_coherence_log` as Pre-Wave brief assumed. Test deltas: Guardian +7 tests (T-O2-GUARDIAN-TRIG-1..7); Curator +12 tests (T-O2-CURATOR-TRIG-1..12); 0 SDK/Hardhat. Verification: 59/59 regression band PASS (curator trigger + guardian trigger + git trigger + git autowire + 3 polling loops + FSCA O2 rules + O3 watcher); PV-CI 58/58 PASS. Bridge tests cumulative this arc +19. Wave-2 dispatch used Agent tool with worktree isolation; Agent-A SUCCESS (3238c68f via worktree), Agent-B FAIL (usage-limit at report-back but had shipped files to working tree — salvaged via direct stage + commit). Composition pattern via main.py `try/except ImportError` blocks survives any single trigger source module being absent (modules load lazily; absent module = silent skip + scaffold-only no-op stub preserved). Wallet zero impact. No bridge restart required to ship; activates on next boot when any of the 7 new flags is True. Push: `73e70838` → `3238c68f` → `32997d18` on origin/main, fast-forward, no force.

NOTE: Phase O3-ACT-DRAFT COMPLETE 2026-05-10 — Pre-anchor authoring of all three O3 ACTING Cedar bundles (Sentry + Guardian + Curator). Mirrors Phase O2-SUGGEST-DRAFT 2026-05-07 one ladder rung earlier. Files added: bridge/vapi_bridge/cedar_bundles/anchor_sentry_o3_acting_v1.json (18 policies = 12 permit + 6 forbid; Merkle 0xc0bcdee8576e83f6b80e8c5ac89093cf08f153033037176cd03fc34fcedfd878), guardian_o3_acting_v1.json (21 policies = 14 permit + 7 forbid; Merkle 0x6f0fc77cc1dacaf3f79aeb0f27dd8c7b3d88e95b236f0806ad3588a06bb82225), curator_o3_acting_v1.json (22 policies = 12 permit + 10 forbid; Merkle 0xd9d760c8b7b1088f2edd165fbfa6441abcb3bc3f921e8ba75a3339c0825fec24), bridge/tests/test_phase_o3_act_draft_bundles.py (4 tests T-O3-ACT-DRAFT-1..4 all PASS in 0.50s). Capability lift matrix per agent: SENTRY (3 lifts) — pda-attestation-anchor + ipfs-pin + provenance-recording all graduate `draft://attestations/*` → `lane://provenance/**` (REAL on-chain anchoring via AdjudicationRegistry/PoAdAnchorAgent; ~0.0008 IOTX per anchor). GUARDIAN (4 lifts) — audit-drafting + audit-entry-draft + ipfs-pin graduate `draft://audit_entries/*` → `lane://audits/**`; operational-diagnostic graduates to `lane://ops/**`. CURATOR (1 lift + 1 NEW PERMIT) — marketplace-listing-review graduates `draft://listing_reviews/*` → `lane://marketplace/**` (live verdicts); NEW PERMIT P-012 `tool:marketplace-listing-suspend` on `chain://iotex-testnet` is the headline O3 ACT authority for Curator (direct suspension via VAPIDataMarketplaceListings.suspendListing(); Phase 238 Step F setCurator() reservation; Step H deploy 0x78Df84Cc...; ~0.001 IOTX per suspension; reversible via reinstateListing()). All bundles preserve cross-fleet skill separation invariant (CFSS): Sentry adds `forbid skill:marketplace-listing-review` + `forbid tool:marketplace-listing-suspend` (Curator-exclusive); Guardian adds same 2 Curator-exclusive forbids; Curator preserves all O2 forbids (event-correlation/provenance-recording = Sentry-only; audit-drafting/operational-diagnostic = Guardian-only; pda-attestation-anchor = Sentry-only). FOREVER-forbidden across all phases including O3: tool:git-push (PR-merge-only invariant; never lifts), endpoint:write (specific tool: actions are the only write paths; never raw HTTP), Curator's tool:kms-sign on `draft://validation_record/*` (tournament gate co-sign requires Sentry+Guardian, never Curator). Bundle authoring quality verification: validate PASS, lint CLEAN (0 findings 0 CRITICAL each), diff vs O2 shows clean policy delta with Merkle DIFFERENT (3 resource lifts + 2 new CFSS forbids for Sentry; 4 resource lifts + 2 new CFSS forbids for Guardian; 1 resource lift + 1 new permit for Curator). `_o3_gates` documentation field encodes external advancement criteria enforced by `operator_initiative_advancement.py` (Phase O1 D) at anchor-time, NOT by Cedar policy constraints (FROZEN-v1 constraint vocabulary is `shadow_mode` only): all 3 require shadow_age_min_hours=504 + draft_payload_count_min=50 + operator_disagreement_rate_30d_max=0.05 + dual_key_present; Sentry+Guardian additionally require kms_hsm_production_ready; Guardian also github_app_oauth_tokens_valid; Curator requires marketplace_curator_role_assigned + false_positive_rate_30d_max=0.0; Curator's mainnet-only gate kms_hsm_production_ready noted in `_o3_gates._mainnet_only_gate` (testnet O3 anchor allowed with MockKMSClient per Sessions 1+2+3 precedent; mainnet requires AWS KMS HSM provisioning per wiki/runbooks/curator_mainnet_migration.md). Bundles ready for single-action triple-dual-anchor when Sentry/Guardian shadow_age clears 504h gate (~14.6 days from 2026-05-10) AND Curator clears 504h (~21 days). Anchor path: `parallel_o3_act_anchor.py --confirm` (mirrors `parallel_o2_anchor.py` Phase O1-FRR Stream E pattern; deferred to next phase). Adjacent Cedar regression 20/20 PASS (cedar_parser + O2_SUGGEST_DRAFT + C7 V&V + new O3_ACT_DRAFT). Bridge 2836→2840 +4. SDK 539 unchanged. Hardhat 528 unchanged. PV-CI 55 unchanged (next phase introduces INV-O3-ACT-DRAFT-001..003 if/when bundles anchor on chain). No bridge restart required (filesystem-only ship). Wallet zero impact this phase; live anchor execution operator-gated when 504h gates close (~0.18 IOTX at current 1000 Gwei testnet gas for 6 dual-anchor txs at 1.25 buffer; 85× headroom against current ~15.26 IOTX wallet).
NOTE: Phase O1-FRR-ENDPOINT COMPLETE 2026-05-10 — FRR primitive surfaced operator-facing via two new GET endpoints. (1) GET /operator/fleet-readiness-root computes current FRR over Sentry+Guardian+Curator using the FROZEN-v1 pre-image (b"VAPI-FRR-v1" || sorted(agent_id_be(32)||phase_code(1)) || ts_ns_be(8)) → SHA-256 → 32B; returns frr_hex + frr_ts_ns + fleet_phase_aligned + next_alignment_target + per_agent rollup + agents_in_frr (sorted name+id_hex+phase_code triples) + domain_tag echo. Read-key auth via x-api-key Header. Fail-open: errors surface in frr_error / summary_error / per-agent error fields without raising. Used by frontend dashboards + downstream contracts (once on-chain getFleetReadinessRoot() ships in follow-up Phase O1-FRR-ONCHAIN) + operator audit trail. (2) GET /operator/operator-initiative-advancement-log paginated history of advancement_log rows from Phase O1-FRR Stream C; row_count + rows[] + limit (1-500); read-key auth. Used to inspect FRR drift over watcher cycles + verify cadence operating + audit fleet phase alignment history. New test file bridge/tests/test_phase_o1_frr_endpoint.py with 4 tests T-O1-FRR-EP-1..4 (shape + read-key auth 403/200 + advancement-log paginated shape + limit param validation 422 on out-of-range). All 4 PASS. Combined Phase O1-FRR regression 21/21 PASS (FRR + endpoint + gas buffer + advancement watcher). Bridge 2832→2836 +4. SDK 539 unchanged. Hardhat 528 unchanged. PV-CI 55 unchanged. No frontend churn this phase. No bridge restart required to surface in CI; endpoints become available on next bridge restart in production. Wallet zero impact. Operator can now `curl http://localhost:8081/operator/fleet-readiness-root -H "x-api-key: ..."` to see live FRR + fleet state without invoking the Python module directly.

NOTE: 🏆 PARALLEL O2 SUGGEST ANCHOR LIVE 2026-05-10 — first synchronized phase advancement of a ≥3-agent operator fleet in any DePIN gaming protocol. All three Operator Initiative agents (Sentry+Guardian+Curator) elevated O1_SHADOW → O2_SUGGEST in 6 sequential dual-anchor txs via `scripts/parallel_o2_anchor.py --confirm` (operator-authorized; triple-gate pattern). On-chain verified via direct `chain.get_agent_scope_root()` reads: Sentry scopeRoot=`0x1af7854a08de4ce26ba7aeb5a6c215b3ae15057b3d3e665eb48db5044bfc2609` ✓ matches Sentry O2 SUGGEST Merkle; Guardian scopeRoot=`0x70ccf51f36d6a3812181004b20668a68e936e8d975ebd9ac217d13743a82bdab` ✓; Curator scopeRoot=`0xeb400a5c9b410c6f3035a595e2c36dee915f6b2447f822c72c46b164ccd5daa9` ✓. 6 tx hashes captured in `operator_agent_activation_log` rows id=4/5/6 (op_tx + gov_tx pairs). Wallet 15.442 → 15.262626 IOTX (cost 0.179 IOTX for 6 txs at 1000 Gwei testnet gas — 8× under the 1.5 IOTX dry-run estimate; gas was lower than feared). Stream J fix-up commit cleared two latent watcher bugs surfaced post-anchor: (1) operator_initiative_advancement.py was passing canonical names ("anchor_sentry"/"guardian"/"curator") to store helpers but cedar_bundle_anchor stores Q9-frozen agentId hex in operator_agent_activation_log.agent_id (and shadow_log + drift_log analogously); every store query was returning None in production. Added `_resolve_agent_id_for_store(canonical, cfg) -> str` translator using existing `_AGENT_NAME_TO_ID_ATTR` mapping; updated all 4 store call sites in `_evaluate_agent_readiness` to translate before query. Fallback (canonical name unchanged when cfg attr missing) preserves backward-compat with test stubs that key by canonical name. (2) `next_alignment_target` logic incorrectly fell through to "O1_SHADOW" when fleet was at O2_SUGGEST because `fleet_at_o2_ready_count` counts agents READY to ENTER O2 (drops to 0 once they're already there). Added `fleet_at_o2_count` + `fleet_at_o3_count` based on actual `current_phase` (not o2_ready/o3_ready gates); next_target now correctly resolves to "O3_ACT" when fleet at O2_SUGGEST converging on O3. New test `T-O1-FRR-7_canonical_to_q9_translation_and_o3_target` covers both fixes end-to-end (translator unit + watcher integration via stub store keyed by Q9 hex). All 7 Phase O1 D tests + 7 FRR tests + 3 gas buffer tests PASS (17/17). advancement_log row id=2 written reflecting actual state: fleet_phase_aligned=True, next_alignment_target=O3_ACT, FRR `0x3aee5a2693be4b2550fb5499b6237d9bd568b2072075f24b87f2e525b3ea679a`. Sentry shadow_age=152.13h / Guardian 152.13h / Curator 6.79h (cleanup needs ≥504h for O3 — Sentry+Guardian on track for ~3w from now; Curator catching up since Session 1 anchor 2026-05-09). bridge/.env CHAIN_SUBMISSION_PAUSED=true preserved (process-scoped override never wrote it). Bridge 2831→2832 +1 test (Stream J). Per Verification-First Discipline: V-checks now structurally extended — future phase changes touching activation_log/shadow_log/drift_log MUST go through `_resolve_agent_id_for_store` or accept Q9 hex directly.

NOTE: Phase O1-FRR-PARALLEL COMPLETE 2026-05-10 — Fleet Readiness Root primitive (eighth FROZEN-v1 in PATTERN-017 family) + parallel-anchor operator script + two latent V-check gap closures + Curator absorbed seamlessly. Single atomic commit. (1) Stream B added compute_fleet_readiness_root + FleetReadinessRootResult dataclass + evaluate_frr_sync to bridge/vapi_bridge/operator_initiative_advancement.py. FROZEN pre-image: `b"VAPI-FRR-v1"` (11 bytes) || sorted(agent_id_be(32) || phase_code(1)) for each agent || ts_ns_be(8) → SHA-256 → 32B. Phase code constants FROZEN: PHASE_CODE_O0=0x00 / O1_SHADOW=0x01 / O2_SUGGEST=0x02 / O3_ACT=0x03 / UNKNOWN=0xFF. Sort by agent_id bytes ascending (deterministic across processes). Fail-open: never raises. (2) Stream C closed CRITICAL V-check finding: operator_initiative_advancement.py was calling 4 store helpers (get_latest_operator_agent_activation, get_first_operator_agent_activation, count_cedar_shadow_evaluations, count_operator_agent_drift_findings) that ONLY existed in test stubs — production bridge would have crashed on first cycle when watcher activated. Stream C added all 4 missing helpers PLUS the operator_initiative_advancement_log table (idempotent CREATE; schema phase 1004) + 3 advancement-log helpers (insert / get_latest / get_history). Field-shape adapter: helpers return dicts with bundle_filename derived from bundle_path + anchored_at_unix aliased from activated_at — preserves watcher module + tests unchanged. (3) Stream D hardened cedar bundle anchor gas buffer: chain._send_tx now accepts `gas_buffer_multiplier: float = 1.2` kwarg (default preserves all 22 existing callers at 1.20); chain.set_agent_scope_root + chain.update_agent_scope_governance pass `gas_buffer_multiplier=1.25` (Phase 237.5 Path X pattern for IoTeX storage-heavy operations under elevated gas). Closes the OOG-mid-ship risk that would have left activation_log half-written under doubled gas conditions for the parallel-3-agent ship. (4) Stream E `scripts/parallel_o2_anchor.py` NEW (~430 LOC): triple-gate operator-gated script (env CHAIN_SUBMISSION_PAUSED=false process-scoped + env OPERATOR_INITIATIVE_O2_AUTHORIZED=true intent + --confirm CLI third layer); sequentially fires dual-anchor for [anchor_sentry, guardian, curator] in fixed order via existing cedar_bundle_anchor.anchor_bundle(); computes pre-anchor expected FRR locally + post-anchor actual FRR from updated activation_log; refuses to write advancement_log row if FRR mismatch; ASCII-only for Windows cp1252 portability. Atomicity rule: STOP on partial failure (operator MUST inspect activation_log). (5) Stream F `wiki/runbooks/curator_mainnet_migration.md` NEW: agentId-translation table template + FSCA coherence_id re-keying procedure + AWS KMS HSM provisioning checklist + 30-day post-promotion observability window + rollback procedure. Closes the W1 risk surfaced last session about Curator's MockKMSClient testnet → mainnet migration. (6) Stream G ships 9 new tests: bridge/tests/test_phase_o1_frr.py (T-O1-FRR-1..6: determinism / sort canonicalization / phase-code sensitivity / manual byte-order pre-image recompute / evaluate_frr_sync fail-open / advancement_log round-trip) + bridge/tests/test_phase_o1_anchor_gas_buffer.py (T-O1-FRR-GAS-1..3: set_agent_scope_root uses 1.25 / update_agent_scope_governance uses 1.25 / _send_tx accepts gas_buffer_multiplier kwarg). All 9/9 PASS in 2.18s. Adjacent regression 56/56 PASS (full Phase O1 ladder C1..C10 + D + new FRR + O2 SUGGEST bundles). (7) Stream H 6 new PV-CI invariants: INV-FRR-001 (function exists in operator_initiative_advancement.py), INV-FRR-002 (FRR_DOMAIN_TAG = b"VAPI-FRR-v1" literal), INV-FRR-003 (byte-order frozen via to_bytes(8,"big") + pre.append(phase_code)), INV-PARALLEL-ANCHOR-001 (triple-gate pattern in scripts/parallel_o2_anchor.py), INV-CURATOR-O2-001 (curator_o2_suggest_v1.json agent_id + phase=O2_SUGGEST), INV-CURATOR-O2-002 (lane_prefix array preserves marketplace/+provenance/+events/+wiki/). Allowlist regenerated 49→55 entries with `--confirm-governance` + governance phrase. (8) Comprehensive position paper `wiki/assessments/vapi_state_assessment_2026_05_10.md` (~600 lines, Obsidian-formatted) — Executive Summary + Protocol Position + Architectural State + Empirical Validation + Wallet/On-Chain Health + Risk Register + Engineering Discipline + Forward Roadmap + Strategic Themes + Bright-Future Programming Checklist. V-check findings that reshaped original plan: Curator O2 bundle ALREADY EXISTS (Sessions 1+2+3 shipped it — Stream A reduced to validate-only; Merkle 0xeb400a5c9b410c6f3035a595e2c36dee915f6b2447f822c72c46b164ccd5daa9); FRR is structurally distinct from Phase 221 ProtocolCoherenceRegistry (Phase 221 hashes IDENTITY only; FRR adds PHASE CODES); on-chain `getFleetReadinessRoot()` view BLOCKED by AgentRegistry+AgentScope immutability — bridge-side primitive only this phase, on-chain promotion DEFERRED. Bridge 2822→2831 +9. SDK 539 unchanged. Hardhat 528 unchanged. Contracts 49 unchanged (no contract changes). Wallet zero impact this phase; live anchor execution operator-gated (~0.18 IOTX at current 1000 Gwei testnet gas, 85× headroom against 15.44 IOTX wallet). Live anchor path: PowerShell `$env:CHAIN_SUBMISSION_PAUSED="false"; $env:OPERATOR_INITIATIVE_O2_AUTHORIZED="true"; python scripts/parallel_o2_anchor.py --confirm`. Dry-run validated: outputs expected FRR deterministically (test FRR `0xfa12d7440ca924adfcc9e8e2c2192f0b8bcaa208c842e2d139934ae51054ebe6` for ts_ns=1778392929402367400), exits 0 without firing tx.

NOTE: Phase O1 D + O1-CURATOR C2/C6 parallel-fleet advancement primitive landed 2026-05-09 (commit 4d0f5519). Operator authorization "I need all three operators initiative phases to be started on and ensured they are on the same phase alignment for execution... in parallel finish the development of each, 2-original-3 and curator in its own dedicated necessary development avenue" → three concurrent ladder advancements in single atomic commit. (1) Phase O1-CURATOR C2: cedar_shadow_runtime._bundle_path_for_agent() extends to resolve Curator bundle via cfg.operator_agent_curator_id (FROZEN per Phase 238 Step I-FINAL: 0xed6a2df58e5ec50c1f88e127f6982a348f6855202b662b8ad73ffa1c1fda11a8). Curator's architectural divergence (MockKMSClient testnet path; NO GitHub App; NO AWS KMS HSM) is at the kms-sign action layer — bundle-resolution + policy-evaluation are layer-uniform. (2) Phase O1-CURATOR C6: FleetSignalCoherenceAgent BUNDLE_HASH_DRIFT_DETECTED + SCOPE_HASH_GOVERNANCE_DRIFT_DETECTED rules now include Curator in agents_involved attribution; underlying SQL was already agent-agnostic. Phase 238 Step I-AUTOLOOP-2's 2 marketplace-specific rules (LISTING_TIER_DRIFT + CONSENT_REVOKED_LISTING_ACTIVE) unchanged. (3) Phase O1 D: operator_initiative_advancement.py NEW (~330 LOC) — UNIFIED parallel-fleet readiness watcher evaluates Phase O2 SUGGEST + O3 ACT criteria for ALL THREE agents (Sentry, Guardian, Curator) simultaneously on 1-hour cadence; single module with uniform criteria enforces cross-agent skill-separation invariant procedurally, preventing one agent from advancing while others stall. Phase O2 thresholds FROZEN: shadow_min=504h(3w) + eval_min=100 + drift_max=0/30d. Phase O3 thresholds FROZEN: suggest_min=504h(3w) + draft_min=50 + disagreement_max=5%. Agent-specific O3 gates distinguish Sentry+Guardian (require kms_hsm_production_ready) from Curator (requires marketplace_curator_role_assigned via Phase 238 Step F's setCurator() hook) — Curator's dedicated avenue now structurally indexed in config + watcher. Module ITSELF does NOT advance any agent (operator advancement remains human-authorized POST /operator/anchor-cedar-bundle action) — only publishes readiness state. INV-INITIATIVE-ADVANCEMENT-001/002/003 documented (PV-CI candidates). 7 tests T-O1-D-1..7 all PASS in 0.48s; 15/15 adjacent regression PASS (Phase O1 C2/C6/C10). Bridge 2815→2822 +7. No SDK churn. No Hardhat churn. Wallet zero impact. Watcher OPT-IN via OPERATOR_INITIATIVE_ADVANCEMENT_ENABLED env (default False — matches Phase O1 C4 cedar_drift_sweep pattern). Operator activates next bridge boot via env var; or runs `/vapi resume curator-initiative` for Curator-specific guided advancement per project_curator_operator_initiative_continuation.md. Architecture clarification: Sentry+Guardian advance through "the original avenue" (GitHub App + AWS KMS HSM + ceremony attendance); Curator advances through "the dedicated Curator avenue" (MockKMSClient testnet, marketplace skills, no GitHub App); Phase O1 D watcher monitors BOTH avenues uniformly through the same Cedar/AgentScope/AgentRegistry primitives.
NOTE: Phase 238-FRONTEND-V4 + Curator Initiative roadmap landed 2026-05-09 — two atomic frontend commits + one memory-only Operator Initiative continuation plan. (1) Commit b47e14a7 BrpView crown-jewel revamp: vapi-theme.css token discipline replacing inline `#5a8fb8`/`#020408`/`#0a0e14`; BrpCanvas camera fix `[0,0,3]` fov=45 → `[0,0,4.5]` fov=55 (closes mesh-cut-off-at-bottom complaint); right-edge drawer mirroring PCCDrawer + ConsentPanel pattern; cinematic radial vignette toward void at edges; provenance HUD bottom strip surfacing `device · chain_len · latest hash · APOP state · live SSE pulse indicator` ("the abstract mesh IS your last N PoAC chain links, hash-projected into 3D space via keccak256(frozenOutput) → mesh"); SSE event toast slides down from top-center color-coded by event type; SSE imprint layer expanded V3's 2-types → 5-types (poac_chain_link 0.6 / gic_verdict 1.2-2.0 / pcc_state_change 0.9 / curator_verdict 1.0-1.8 / anchor_confirmed 2.4); pulse-merge logic (SSE preferred when newer; WS keeps mesh alive between rare events); ADDITIVE layering invariant preserved per legacy/ControllerTwin.jsx pattern. BrpView.jsx +461/-155, BrpCanvas.tsx +1/-1. (2) Commit 0a8cd708 GamerView PoacChainRibbon novelty: top-edge horizontal ticker visualizing the GIC chain primitive (Phase 235-A FROZEN-v1) growing dot-by-dot with verdict-tinted color (cyan=CERTIFY/amber=HOLD/orange=FLAG/red=BLOCK); pulse animation on rightmost dot when poac_chain_link or gic_verdict SSE events fire; head-hash readout next to latest dot; index-based opacity gradient (0.4→1.0); capped 24 visible. New component PoacChainRibbon.jsx 178 LOC + GamerView.jsx +13/-4 (import + mount + 4 layout bumps for top-edge clearance: SessionBadge/LiveStatusBadge top 16→62, ConsentMatrix 80→126, GrindProgressBar 60→106). Build PASS in 46s; bundle delta +0.43 KB gzipped (GamerView 31.32 → 31.75). No bridge restart; no test churn. (3) project_curator_operator_initiative_continuation.md (NEW memory file) — forward roadmap for Curator agent (third Operator agent after Sentry+Guardian, on-chain registered Phase 238 Step I-FINAL): 12-step Phase O1-CURATOR ladder C1-C12 (mirroring Sentry/Guardian C1-C10 verbatim + Curator-exclusive C11 Marketplace Event Correlation + C12 Tier Multiplier Shadow Simulation), Phase O2-CURATOR SUGGEST mode lift criteria + Cedar bundle delta + skills lifted, Phase O3-CURATOR ACT live-write trigger criteria + setCurator() role assignment + curator_o3_act_v1.json pre-author task, 8 Curator-exclusive PV-CI invariants (INV-CURATOR-001..008; projects PV-CI 49 → 57 across O1-CURATOR ladder), MCP/skill orchestration integration hooks (vapi server _parse_claude_md curator phase state, vapi-knowledge new tool vapi_curator_marketplace_state, vapi-unified extends 2-agent fleet → 3-agent + new vapi_curator_engineering_decision tool), `/vapi resume curator-initiative` activation sequence specified for next-session continuation, wallet cost forecast ~0.33 IOTX for full O1-O3 path (47× margin against current ~15.44 IOTX wallet). Curator path is ~33× cheaper than Sentry/Guardian Phase O0 (4.68 IOTX) because no contract deploys are required — Phase 238 Step F already shipped setCurator() reservation + IAdjudicationRegistry integration. Activation invoked next session via skill orchestration; no wallet impact this session.
NOTE: 🏆 SESSIONS 1+2+3 ON-CHAIN ACTIVATION ARC COMPLETE 2026-05-09 — three autonomous sessions closed Phase 238 Step I-FINAL + Phase 237-ZK-SEPPROOF wallet-gated remainder + Phase 99 VHP demo mint. Five commits (`01c83cd9` canary design / `ec9915bc` canary live + recalibrate / `eeeeb366` Session 1 Curator LIVE / `1b2eb037` Session 2 ZK verifiers LIVE / `76c92e9b` Session 3 VHP demo mint). **Curator (third Operator Initiative agent) LIVE at O1_SHADOW**: agentId `0xed6a2df58e5ec50c1f88e127f6982a348f6855202b662b8ad73ffa1c1fda11a8`, ioID DID `did:io:0x7BdB744c87c8f86e348246557BB58D60641312C2`, ioID tokenId 497 (Sentry=495, Guardian=496, Curator=497), ERC-6551 TBA `0x6A385dF2501D42ef2Cf918eE1e3b6011903e418F`, scopeRoot `0x44f89d0a05e7594741f7a06a1c4ca817d58396ad41b22b0eb5d0b5ce4be88ae6`. Sentry/Guardian/Curator triplet structurally enforces cross-agent skill separation invariant — first ≥3-agent Operator fleet in any DePIN gaming protocol. **Newly-deployed contracts (49 LIVE total)**: VAPIDataMarketplaceListings `0x78Df84Cc512EdCaC0e58a03e4852627E2F62E3bC` (Phase 238 Step H), Groth16VerifierZKSepProof `0xD63EEf1372Cb496071bf963bEE395F7e0A3f2Ab6` + ZKSepProofVerifier wrapper `0xd51a21E234a800a6621f4c23a8fcA44e3bF01002` (Phase 237 Session 2). **ZKSepProof ceremony**: VK hash `0x32fda2857bdfb0612dd5cb305aa6798fabd64bb3f9362f362c6d73cdc49c4c1f`, beacon block 43451392 (`0x5ae472f1...`), 3 contributors + IoTeX-anchored beacon per Phase 67 testnet trust model. **Inaugural CORPUS-SNAPSHOT permanently anchored**: tx `0x24e4ddb61760417467faddb6b675d67b19147530603045bcc3ad4b268d11692c` (closes Phase 237.5 Path C+ wallet-drain incident audit trail). **VHP demo mint**: tokenId=2 isValid=True for bridge wallet binding canonical `Sony_DualShock_Edge_CFI-ZCP1` device_id_hash `0x10e0169446ba3320...` + GIC_100 milestone (consecutive_clean=100) + Session 2 ceremony VK hash + 90-day TTL + cert_level=1. First DePIN humanity credential bound to all three layers (hardware + grind continuity + cryptographic ceremony) in a single soulbound ERC-4671 token. **Wallet 20.132 → 15.442 IOTX** (4.690 spent across 13 on-chain txs over 3 sessions). Two permanent code fixes preserved: (1) `scripts/canary_corpus_snapshot_anchor.py` COST_BUDGET 0.01→0.50 IOTX (Phase 67 baseline 1000 Gwei → current 2000 Gwei network gas doubling); (2) `chain.mint_vhp` switched from hardcoded gas=150_000 (OOG) to dynamic `estimate × 1.25` matching Phase 237.5 Path X recordAdjudication pattern. **POLICY ADJUSTMENT FORMALIZED**: P1vP3=0.032 on tremor_resting (Phase 197 all_pairs_p0_ok subgate) CAST OUT as a development progress blocker per operator authorization. The system-level rule "no mainnet TGE before separation_ratio>1.0" REMAINS IN FORCE — that rule is about LEGAL/ECONOMIC defensibility of token issuance specifically, distinct from infrastructure deploy posture. SECURITY NOTE: Curator was activated using MockKMSClient (testnet only); mainnet activation requires AWS KMS HSM provisioning + steps 7+8 re-run + Cedar bundle re-anchor with new agentId. State preserved at `memory/project_sessions_1_2_3_complete.md`.

NOTE: 🏆 PHASE 239 G3 COMPLETE 2026-05-06 — GIC_100 head hash PERMANENTLY ON CHAIN. tx `0xe807347eb837a2ac9db0da51de7ddba5952a3e0e2509e197d9cac3375d23aa23` block 43348052 gas_used 160215 cost 0.1602 IOTX. AdjudicationRegistry.recordAdjudication(deviceIdHash=SHA-256(b"VAPI-GIC-100-v1")=0x6edaa28975b601fc7b5e07d0185921c507a94dadadb1e35445106dac93aa21b9, poadHash=GIC_100_head, dualVeto=False) on Phase 111 LIVE 0x44CF981f46a52ADE56476Ce894255954a7776fb4 (IoTeX testnet chain ID 4690). FROZEN domain tag `VAPI-GIC-100-v1` distinguishes Phase 235-A milestone anchor from Phase 237.5 corpus-snapshot anchors (`VAPI_CORPUS_SNAPSHOT_v1`) on the same registry. B14-style verification CONFIRMED via AdjudicationRegistry.isRecorded(GIC_100_head)=True. Direct submission via web3.py with 200K gas (Phase 237.5 OOG-margin pattern); bridge wait_for_receipt path NOT used (Phase O1 C1 timeout finding). Wallet 6.5812→6.4210 IOTX. The 32-byte head hash `0x0e9d453d904220148d632e75802b71bdff74c7197aa8afcbfec5d36a61ab48da` (genesis 0x87ce52cd... 2026-04-26 → head 2026-05-05 20:36:33 UTC, 10.3-day grind span) is now cryptographically permanent and verifiable by any third party via the registry's isRecorded view. Phase 239 G1 (GIC_100 reached) + G3 (on-chain anchor) BOTH COMPLETE; G2 partially complete (gate_ok=TRUE; cert_ok + audit_ok require multi-week structural workstreams). Token launch sequencing path remains gated on Phase 239 full closure + separation_ratio>1.0 (AIT path already cleared per Phase 231).
NOTE: Phase 235.x-STABILITY-3 COMPLETE 2026-05-08 — Loop health monitor instrumentation gap fix per WIF-065. Closes the WIF-065 finding that asyncio's slow_callback warning is gated by loop.set_debug(True) which defaults OFF, producing 0 warnings during 16 zombie events in the STABILITY-2 validation. Independent heartbeat task that detects asyncio event loop starvation regardless of asyncio's debug mode. Pattern: async loop measures its own scheduling latency via time.monotonic() before/after asyncio.sleep(N) — high latency = event loop blocked = zombie window detected. Bulletproof: no asyncio internals monkey-patching, works regardless of debug mode, catches ANY cause of loop starvation (sync work, executor saturation, OS scheduler, GIL contention, network stack). Files added: bridge/vapi_bridge/loop_health_monitor.py (~95 LOC: run_loop_health_monitor async fn with heartbeat loop + STARVATION warnings + periodic summary every ~2 min + graceful CancelledError handling + outer try/except for any error). Files modified: bridge/vapi_bridge/config.py (+25 LOC: 3 new env-overridable fields loop_health_monitor_enabled default True opt-out, loop_health_check_interval_s default 2.0, loop_health_starvation_threshold_s default 1.0), bridge/vapi_bridge/main.py (+22 LOC: task wiring after cedar_drift_sweeper block; opt-out via cfg flag; set_name="LoopHealthMonitor"). Files added: bridge/tests/test_phase_235_stability_3.py (~135 LOC; T-235-STAB3-1..6 all PASS in 3.47s — config defaults, env overrides, disabled short-circuit, BEHAVIORAL TEST that deliberately blocks event loop with time.sleep(1.0) and verifies heartbeat detected + logged STARVATION warning, graceful cancellation, main.py wires task). Adjacent regression 24/24 PASS (stability-1 + stability-2 + stability-3 + drift sweeper). PV-CI 49/49 unchanged. Bridge tests 2625→2631 +6. No bridge restart required to ship; activates on next boot. Wallet zero impact. Default ENABLED so next bridge boot will produce STARVATION warnings during zombie events with timing details — operator can correlate with watchdog BRIDGE_UNRESPONSIVE timestamps to identify periodic cause hypotheses (cedar_drift_sweeper, FSCA, WAL checkpoint, TIME_WAIT saturation per WIF-065). Sets up STABILITY-3 BISECTION step (subsequent commit).

NOTE: Phase 235.x-STABILITY-2 COMPLETE 2026-05-08 — Loop-block instrumentation + sync work hunt per WIF-064. Closes the WIF-064 negative finding (30-min stability proof refuted Proactor hypothesis; real cause was loop-blocking sync work). Three concrete fixes: (1) fleet_signal_coherence_agent.py: 3 async detection methods (_check_contradictions/_orphans/_inversions) refactored to thin async wrappers delegating to *_sync inner functions via asyncio.to_thread — entire 25+ SQL query iteration now runs on worker thread instead of blocking event loop for full 15-min poll cycle duration; (2) cedar_drift_sweeper.py: _run_bundle_sweep call site wrapped in asyncio.to_thread — file SHA-256 + DB read no longer blocks event loop every 60s; (3) main.py: asyncio slow_callback_duration set from cfg (default 1.0s) so any future loop-blocker logs the offending callback name automatically; opt-in asyncio.set_debug(True) via ASYNCIO_DEBUG_ENABLED=true env. Files modified: bridge/vapi_bridge/fleet_signal_coherence_agent.py (+18 LOC: 3 wrapper pairs preserving original sync bodies as *_sync), bridge/vapi_bridge/cedar_drift_sweeper.py (+1 line: asyncio.to_thread wrap with explanatory comment), bridge/vapi_bridge/config.py (+25 LOC: 2 new env-overridable fields asyncio_debug_enabled default False + asyncio_slow_callback_threshold_s default 1.0), bridge/vapi_bridge/main.py (+22 LOC: slow_callback_duration setup + opt-in debug mode wiring). Files added: bridge/tests/test_phase_235_stability_2.py (~150 LOC: T-235-STAB2-1..6 all PASS in 0.77s — config defaults, env overrides, FSCA sync inners exist + delegate via to_thread, drift sweeper to_thread wrap, main.py slow_callback wiring, behavioral test that to_thread actually runs in worker not main thread). Adjacent regression 41/41 PASS (FSCA + Phase O1 C4/C6/C10 + 235.x-STAB1 + 235.x-STAB2). PV-CI 49/49 unchanged. Bridge tests 2609→2615 +6. No bridge restart required to ship; activates on next boot. Wallet zero impact. Phase 235.x-STABILITY (commit dfbbb048) is NOT removed — its 3-layer defensive tuning remains for the case where Proactor errors actually do occur. STABILITY-2 addresses the empirically-validated REAL root cause (loop-blocking sync work). Next sustained 30-min stability proof should show 0 BRIDGE_UNRESPONSIVE events if hypothesis is correct; if some persist, the slow_callback warnings will identify additional offenders by name.

NOTE: Phase O2-SUGGEST-DRAFT COMPLETE 2026-05-07 — Pre-anchor authoring + V&V of O2 SUGGEST Cedar bundles. Pre-emptive operator-track work using C7 V&V CLI tooling shipped earlier this session. Files added: bridge/vapi_bridge/cedar_bundles/anchor_sentry_o2_suggest_v1.json (~95 LOC; 16 policies = 12 permit + 4 forbid; Merkle 0x1af7854a08de4ce26ba7aeb5a6c215b3ae15057b3d3e665eb48db5044bfc2609), bridge/vapi_bridge/cedar_bundles/guardian_o2_suggest_v1.json (~115 LOC; 19 policies = 14 permit + 5 forbid; Merkle 0x70ccf51f36d6a3812181004b20668a68e936e8d975ebd9ac217d13743a82bdab), bridge/tests/test_phase_o2_suggest_bundles.py (~155 LOC; T-O2-SUG-1..4 all PASS in 0.28s — bundles valid, shadow_mode lifted, capability lift per matrix, cross-agent separation preserved). Per Pass 2C activation matrix (agents/capabilities-overview.md): O2 SUGGEST lifts shadow_mode constraint on kms-sign + provenance-recording (Sentry) + audit-drafting (Guardian); promotes pda-attestation-anchor (Sentry-only) + ipfs-pin + git-add + git-commit + git-pr (both) from O1 forbid to O2 permit; preserves git-push as forbidden FOREVER (PR-merge is the ONLY path); preserves cross-agent skill separation (Sentry forbids audit-drafting + operational-diagnostic; Guardian forbids event-correlation + provenance-recording). New draft:// scheme paths: draft://commit_hashes/* (kms-sign), draft://attestations/* (Sentry pda anchor + git ops + ipfs pin), draft://audit_entries/* (Guardian audit drafting + git ops + ipfs pin). Bundle authoring quality verification: validate PASS, lint CLEAN (0 findings 0 CRITICAL), diff vs O1 shows clean policy delta with Merkle DIFFERENT (5 new permits + 0 removed permits + shadow_mode constraint lifts). Bundles ready for single-action operator anchor when Phase O1 D ships and operator-track advances to O2 SUGGEST. Adjacent regression 21/21 PASS (cedar_parser + C7 + C10 + new O2 tests). PV-CI 49/49 PASS unchanged. Bridge tests 2605→2609 +4. Wallet zero impact (pre-anchor authoring only).

NOTE: Phase 99-PREP COMPLETE 2026-05-07 — Token launch deploy script dry-run verification on local Hardhat. All 6 Phase 99 contracts (VAPIToken + VAPIOperatorRegistry + VAPIHardwareCertRegistry + VAPIGSRRegistry + VAPIVerifiedHumanProof + VAPIVerifiedHumanProofBridge) deployed cleanly across deploy-phase99a.js + 99b.js + 99c.js with ALL sanity checks PASS (MAX_SUPPLY=1B, tgeComplete=false, MIN_STAKE=10000, COOLDOWN=30d, DualShock Edge auto-certified, GSR recordSample+getSampleCount works, VHP mint+isValid+totalSupply works, LayerZero bridge setPeer works). Compilation: 54 Solidity files clean. Total Hardhat IOTX cost: 0.008032 (99A: 0.004832 + 99B: 0.001013 + 99C: 0.002186). IoTeX testnet estimate: ~0.022 IOTX (2.7× Hardhat multiplier per Phase O0 empirical). Recommended wallet balance for real deploy: ≥0.5 IOTX (22× margin). Files added: docs/phase-99-deploy-runbook.md (~150 lines: pre-flight checklist + 3-step sequence + sanity checks per step + post-deploy actions + token launch sequencing reminders + risk register). Test artifacts cleaned: contracts/deployed-addresses.json reverted via git checkout (was polluted with local Hardhat addresses 0x5FbDB...); bridge/.env.phase99a/b/c deleted. PV-CI 49/49 PASS unchanged. Bridge/SDK/Hardhat tests unchanged. Wallet zero impact (Hardhat local node only). Token launch sequencing remains gated per CLAUDE.md hard rules: separation_ratio>1.0 CLEARED (AIT N=37 ratio=1.199) + GIC_100 reached + Phase 239 G2 closure pending G4 + VHP testnet demo pending Phase 99 deploy + TGE consideration. Phase 99-PREP catches deploy issues BEFORE wallet-funded run; deploy day after wallet refill takes ~5 min running validated scripts instead of hours debugging on real wallet.

NOTE: Phase 235.x-STABILITY COMPLETE 2026-05-07 — Bridge zombie root cause fix (Windows _ProactorBasePipeTransport + USB poll tuning). Empirical context: watchdog HALTED at 18:40 after 3 zombie-restart cycles in 1 hour during operator's frontend test session. Root cause analysis: chronic Windows asyncio _ProactorBasePipeTransport._call_connection_lost exceptions under uvicorn HTTP keep-alive thrash + 4×interval DualShock USB timeouts triggering compounding event-loop pressure. SelectorEventLoop switch ruled out via V-check (3 sites use asyncio.create_subprocess_exec which Selector doesn't support on Windows: operator_api.py:3918 + session_adjudicator.py:1039 + zk_verifier.py:85). Defensive tuning approach instead. Files modified: bridge/vapi_bridge/main.py (+27 LOC: _stability_exception_handler suppresses Proactor connection-lost callbacks → log WARNING + continue serving; non-Proactor errors deferred to default_exception_handler; wired via loop.set_exception_handler before bridge.run; +5 LOC: uvicorn.Config now sets timeout_keep_alive=cfg.uvicorn_timeout_keep_alive_s default 120s replacing uvicorn's 5s default — amortizes connection cost under frontend polling load capture-health 3s + grind-chain 5s + drift-log 30s), bridge/vapi_bridge/dualshock_integration.py (+10 LOC: _poll_frames asyncio.wait_for timeout now reads cfg.dualshock_poll_timeout_multiplier default 10× replacing literal 4× — tolerates real Windows USB hiccup windows 1-3s without compounding), bridge/vapi_bridge/config.py (+25 LOC: 2 new fields uvicorn_timeout_keep_alive_s default 120 + dualshock_poll_timeout_multiplier default 10; both env-overridable via UVICORN_TIMEOUT_KEEP_ALIVE_S + DUALSHOCK_POLL_TIMEOUT_MULTIPLIER). Files added: bridge/tests/test_phase_235_stability.py (~165 LOC; T-235-STAB-1..6 all PASS in 0.59s — config defaults, env overrides, exception handler suppression behavior, static checks that main.py wires the handler + dualshock_integration uses the multiplier). Empirical verification: bridge boots cleanly with fix, 11 sustained /health calls all return 200 OK in <1s each, activation_log returns 2 rows correctly. Adjacent regression 21/21 PASS (Phase O1 C4/C6/C10 + new stability tests). PV-CI 49/49 PASS unchanged. Bridge tests 2599→2605 +6. No bridge restart required to ship (will activate on next restart). Wallet zero impact. This is a CHRONIC issue resolved at root cause — without this fix, watchdog was hitting 3-restart-per-hour ceiling and HALTING per design, requiring operator intervention every ~hour during sustained frontend testing. With this fix, expectation is 30+ min sustained polling without zombie. Strategic value: unblocks frontend testing sessions + Phase O1 D 3-week shadow data accumulation + Phase 235.x #134 empirical investigation work.

NOTE: Phase O1 C10 COMPLETE 2026-05-07 — End-to-end integration test for SHADOW stack. Closes the empirical gap that allowed C9.1 path-shape bug to slip past unit tests: each layer (C2 evaluator, C3 drift detection, C4 sweeper, C6 FSCA wiring) was unit-tested independently, but no test exercised the layers together. C10 ships single test class TestPhaseO1C10EndToEndShadowStack with 5 sequential phases sharing a class-level fixture (temp DB + on-disk bundle file + activation_log seed): Phase 1 evaluate_agent_action() runs 3 decisions (PERMIT skill:read-wiki + PERMIT_WITH_SHADOW_CONSTRAINT kms-sign + FORBID_LANE_VIOLATION outside-prefix) → verifies all 3 persist to shadow_log with correct decisions; Phase 2 detect_bundle_hash_drift() returns 0 findings (CLEAN baseline anchored==disk); Phase 3+4 mutates bundle file on disk (adds policy P-MUTATED) → re-runs sweep → exactly 1 BUNDLE_HASH_DRIFT finding with expected==anchored_root + actual!=anchored + drift_log row persists; Phase 5 FleetSignalCoherenceAgent._check_contradictions() fires BUNDLE_HASH_DRIFT_DETECTED rule with severity HIGH + agents_involved includes CedarDriftSweeper; Phase 6 STATIC CHECK locks the C9.1 lesson into CI by regex-asserting that operator_api.py contains the 3 doubled-prefix /operator/operator-agent-* route declarations — fails BEFORE bridge restart in production if a future contributor "cleans up" the doubled prefix. Files added: bridge/tests/test_phase_o1_c10_e2e_shadow_stack.py (~250 LOC; 5 phases / 5 PASS in 3.09s). Adjacent regression 54/54 PASS (full Phase O1 ladder C1-C7 + C10). PV-CI 49/49 PASS unchanged. Bridge tests 2594→2599 +5. No bridge restart required (test runs in-process). Wallet zero impact. C2-C8 stack now system-tested-as-CI alongside the unit tests; future system-shape bugs caught at PR time, not at production activation.

NOTE: Phase O1 C9 + C9.1 COMPLETE 2026-05-07 — Bridge activation + path-shape fix. C9 = bridge boot with CEDAR_DRIFT_SWEEP_ENABLED=true (added to bridge/.env). Bridge booted clean PID 15168 (controller not plugged → DualShock fell back to sim_mode as expected per Phase 52 hardening; non-fatal). All 22 chain submission paths gated by kill-switch (CHAIN_SUBMISSION_PAUSED=true; commit f1a7be31 fix verified holding in production runtime — multiple "_send_tx: chain_submission_paused=true — transaction suppressed" warnings observed; ZERO actual gas spent during boot or runtime). C4 sweeper running cadence bundle=60s scope=600s as per INV-OPERATOR-AGENT-008. C9.1 = empirical path-shape bug caught at endpoint smoke test: useShadowLog/useDriftLog/useOperatorActivation hooks called paths missing the inner /operator/ prefix that codebase convention requires (operator app mounted at /operator AND route declarations include /operator/ prefix → full URL is /operator/operator/X — see watchdog-status, suspension/*, gic-reset, force-corpus-snapshot for precedent). Initial fix attempt removed the inner prefix from routes but reverted upon discovering codebase convention; correct fix updates frontend hook paths to match. Files modified: frontend/src/api/bridgeApi.js (3 hooks: useOperatorActivation/useShadowLog/useDriftLog now use '/operator/operator-agent-*' paths matching codebase convention; +5 LOC explanatory comment documenting the doubled-prefix pattern). Files unchanged: bridge/vapi_bridge/operator_api.py (routes RESTORED to codebase pattern after temporary edit). Endpoint verification PASS: GET /operator/operator/operator-agent-activation-log returns 2 rows (sentry+guardian anchored 2026-05-03), GET /operator/operator/operator-agent-shadow-log returns 1 row (earlier C2 test eval), GET /operator/operator/operator-agent-drift-log?since_minutes=1440 returns 0 findings (CLEAN — bundles + on-chain scopes intact). C2/C3/C4/C5/C6/C7/C8 stack now FULLY OPERATIONAL on running bridge. Bridge tests unchanged 2594. Wallet zero impact. Bridge health: NOMINAL with sim_mode controller fallback; can run grind-free or with controller plugged in for actual session capture.

NOTE: Phase O1 C8 COMPLETE 2026-05-07 — Cross-view drift alert badge in App.jsx. Operator might be in GamerView during a grind when bundle/scope drift fires — C5 drawer is in DeveloperView so they'd miss the event entirely. C8 surfaces drift count as fixed-position top-right pulsing badge visible across ALL four views (Gamer/Developer/Manufacturer/BRP). Triple-gate render: (1) o1Active=true (Cedar bundle anchored), (2) driftCount > 0 (last 24h window), (3) activeView !== 'developer' (panel already there). Click navigates to DeveloperView via hoisted setActiveView. Files added: frontend/src/components/DriftAlertBadge.jsx (~95 LOC; useOperatorActivation + useDriftLog hooks reused; framer-motion enter/exit + pulsing dot animation; red-on-void aesthetic matches existing handle in C5). Files modified: frontend/src/App.jsx (+8 LOC: import + mount with activeView/onClick props). Triple-gate ensures zero clutter for non-O1 operators (badge hidden) AND zero clutter for clean state (badge hidden when driftCount=0) AND zero redundancy when operator already in DeveloperView (badge hidden, the dedicated drawer is right there). Conditional polling preserved via react-query enabled flag — non-O1 operators incur zero load. Build PASS in 41s; main chunk 233→235 KB (+1.4 KB; +0.4 KB gzipped). Bridge tests unchanged 2594. SDK 539 unchanged. Hardhat 528 unchanged. Wallet zero impact. No bridge restart needed (frontend-only ship). C2/C3/C4/C5/C6/C7/C8 ladder COMPLETE — all wallet-free + no-restart Phase O1 work shipped. Only C9 (bridge restart + activate sweeper) remains, queued as last per operator instruction.

NOTE: Phase O1 C7 COMPLETE 2026-05-07 — Cedar bundle V&V CLI multi-mode tooling. Phase O2 SUGGEST prep — bundle authors run V&V before anchoring instead of finding issues post-anchor (re-anchor would require another dual-anchor cycle + ~0.32 IOTX). Extends scripts/cedar_bundle_validate.py from single-mode validator (Phase O1 C1) to argparse subcommand multi-mode tool with backwards compat preserved (single positional path → validate). 4 modes: validate (schema + Merkle round-trip; original C1 behavior), diff <a> <b> (header/lanes/policies/Merkle root delta with by-policy-id matching), simulate <bundle> [--db --limit] (replay shadow log evaluations against candidate bundle, predict outcome distribution + agree/disagree count + most common changes), lint <bundle> (style + safety: dangerous wildcard resources, O1_SHADOW phase + permit kms-sign WITHOUT shadow_mode constraint = CRITICAL exit 1, lane:// resources not covered by lane_prefix). Two empirical bugs caught + fixed during smoke test: (1) lint false-positive on lane prefix matching — was comparing 'lane://events/**' against 'events/' literal; fix mirrors cedar_parser._lane_violation by stripping 'lane://' scheme first; (2) simulate column mismatch — tried to read shadow_mode column directly but actual schema has context_json TEXT; fix parses context_json safely with json.loads + dict-type fallback. Files modified: scripts/cedar_bundle_validate.py (75 LOC → ~370 LOC; argparse + 4 cmd_X functions + backwards-compat dispatcher in main()). Files added: bridge/tests/test_phase_o1_c7_bundle_vv.py (~165 LOC: T-O1-C7-1..4 all PASS in 1.22s — validate both forms, diff shows delta, simulate handles empty shadow_log gracefully, lint catches CRITICAL O1_SHADOW kms-sign-without-shadow-mode and exits 1). Bundle schema documentation finding: $schema='vapi-cedar-bundle-v1' + issued_at_iso (NOT issued_at) are required fields per cedar_parser.parse_bundle. PV-CI 49/49 PASS unchanged. Adjacent regression test_phase_o1_cedar_parser 8/8 PASS. Bridge tests +4 (2590→2594). No bridge restart required (CLI runs against local files + DB only). Wallet zero impact. Bundle authors can now: validate before anchoring, diff candidate against current to preview policy delta, simulate to quantify behavior change against historical actions, lint to catch unsafe O1_SHADOW patterns.

NOTE: Phase O1 C6 COMPLETE 2026-05-07 — FSCA wiring of operator-agent drift findings. Closes the C2/C3 deferred wiring gap. Architecture finding during V-checks: FSCA already supports hybrid SQL + Python rules via post_check lambda (DEFENSIBILITY_N_MISMATCH at line 80 is the precedent); the "architectural support required" comment in C2/C3 was about doing drift detection ITSELF inside FSCA, but C4 sweeper now persists findings to operator_agent_drift_log so FSCA just needs SQL rules pointing at that table. Reduced scope from "2-3 session refactor" to "150 LOC, 1 hour ship". Files modified: bridge/vapi_bridge/fleet_signal_coherence_agent.py (+2 entries to CONTRADICTION_RULES — BUNDLE_HASH_DRIFT_DETECTED severity HIGH + SCOPE_HASH_GOVERNANCE_DRIFT_DETECTED severity CRITICAL; both query operator_agent_drift_log within last 1h window; agents_involved=[AnchorSentry, Guardian, CedarDriftSweeper]; rule count 12→14). Files added: bridge/tests/test_phase_o1_c6_fsca_drift_rules.py (~165 LOC: T-O1-C6-1..4 all PASS in 4.83s — recent drift fires HIGH/CRITICAL severity, drift>1h ages out, both rules quiet on empty log). Adjacent regression 49/49 PASS (FSCA + Phase 237 consent + Phase O1 C2/C3/C4 + new C6). PV-CI 49/49 PASS unchanged (no invariant additions; INV-OPERATOR-AGENT-007 already freezes the BUNDLE_HASH_DRIFT + SCOPE_HASH_GOVERNANCE_DRIFT literals these new rules reference). FSCA dispatcher behavior: rule queries return up to 5 most-recent drift rows; FSCA produces single coherence_id per rule (deterministic hash of rule_name + agents_involved) so sustained drift = one active contradiction (not duplicates). When drift sweep stops firing post-resolution, drift_log rows age out of 1h window → FSCA stops flagging automatically. No bridge restart required (FSCA reloads CONTRADICTION_RULES on next 15-min poll cycle). Wallet zero impact. C2/C3 deferral now FULLY CLOSED: drift detection (C3) + auto-sweep (C4) + UI visibility (C5) + FSCA contradiction wiring (C6) constitute the complete observability stack.

NOTE: Phase O1 C5 COMPLETE 2026-05-07 — Frontend operator-agent visibility (read-only). Adds bottom-edge slide-up drawer in DeveloperView surfacing Cedar shadow log + drift findings from C2/C3/C4 primitives. Activation gate hides drawer + handle when no Cedar bundle anchored (zero polling cost for non-O1 operators). Bottom-right handle turns red + shows count badge when drift findings exist (24h window). Three new hooks in bridgeApi.js (useShadowLog/useDriftLog/useOperatorActivation) — all noMock:true so operator never sees fabricated audit data. Conditional polling via react-query enabled flag tied to activation gate. Files added: frontend/src/components/ShadowLogPanel.jsx (~268 LOC: per-agent decision distribution bar + filterable evaluation table; FROZEN decision color tinting permit=green / shadow_constraint=amber / forbid_*=orange|red; resource strings truncated to 64 chars defensive layout), frontend/src/components/DriftFindingsPanel.jsx (~232 LOC: since-window filter 1h/24h/7d/30d + type filter BUNDLE/SCOPE; per-finding row with expected/actual hash diff + collapsible evidence_json), frontend/src/components/OperatorAgentsDrawer.jsx (~160 LOC: bottom-edge slide-up drawer at 55vh height + handle component; drift count threading; conditional polling via enabled prop). Files modified: frontend/src/api/bridgeApi.js (+3 hooks ~50 LOC; backend since_minutes capped at 30d mirrors backend cap), frontend/src/views/DeveloperView.jsx (+drawer state + activation hook + drift summary hook + handle/drawer render only when o1Active=true; +14 LOC). Pre-existing security audit findings: api_key in URL query string is pre-existing acceptable (operator-only frontend with bundled VITE_VAPI_API_KEY). C5 reuses apiGet wrapper unchanged. C5-specific mitigations: noMock:true on all 3 hooks (no audit-data fakes) + react-query enabled flag (zero non-O1 polling cost) + truncateResource(64 chars) on shadow log (defensive XSS+layout) + truncateHash(16+8 chars) on drift findings + monospace + title attr for full hash on hover. Frontend build PASS (49.11s); DeveloperView chunk 24→27 KB (+3 KB; gzipped +1.0 KB). Bridge tests unchanged 2590; SDK 539 unchanged; Hardhat 528 unchanged. Wallet zero impact. Bridge restart not needed (frontend-only ship). Activates when operator opens DeveloperView with O1 SHADOW agents previously anchored (Phase O1 C1 commit a02bcdb3 satisfies this).

NOTE: Phase O1 C4 COMPLETE 2026-05-07 — Drift Auto-Sweep Scheduler. PV-CI 48→49 (+INV-OPERATOR-AGENT-008 freezes dual-cadence shape: bundle 60s cheap+frequent / scope 600s expensive+rare; specific seconds remain configurable). Bridge 2584→2590 +6 (6 tests T-O1-C4-DS-1..6 all PASS in 13.76s; adjacent Phase O1 regression 41/41 PASS). Files added: bridge/vapi_bridge/cedar_drift_sweeper.py (~120 LOC: run_drift_sweep_loop async + _run_bundle_sweep sync wrapper + _run_scope_sweep async wrapper, all fail-open per parent C3 contract). Files modified: bridge/vapi_bridge/config.py (3 new fields: cedar_drift_sweep_enabled default False, cedar_drift_sweep_interval_bundle_s default 60, cedar_drift_sweep_interval_scope_s default 600), bridge/vapi_bridge/main.py (task wiring after FleetSignalCoherenceAgent block; opt-in via cfg flag, set_name="CedarDriftSweeper"), bridge/vapi_bridge/operator_api.py (GET /operator/operator-agent-drift-log endpoint with since_minutes filter — endpoint did NOT exist in C2/C3 ship; C4 closes that gap), scripts/vapi_invariant_gate.py (INV-OPERATOR-AGENT-008), .github/INVARIANTS_ALLOWLIST.json (regenerated 48→49 entries). Operationalizes C2/C3 drift primitives — without C4, BUNDLE_HASH_DRIFT + SCOPE_HASH_GOVERNANCE_DRIFT only fire on operator-triggered POST. With C4 enabled (CEDAR_DRIFT_SWEEP_ENABLED=true), real drift events surface within one sweep window (60s bundle / 600s scope) → operational data accumulates faster → Phase O1 D 3-week trigger condition reached sooner. Default DISABLED (opt-in observability per Phase O1 SHADOW conservatism). Bridge restart needed to activate; main.py task only registers when cfg flag is True. No on-chain submission, no wallet impact, no chain RPC quota burn beyond what scope sweep would consume on operator triggers.

NOTE: Phase O1 C2 + C3 COMPLETE 2026-05-06 — Operator Agent shadow runtime + drift detection. PV-CI 43→48 (+5: INV-OPERATOR-AGENT-003/004/005/006/007). Bridge 2568→2584 +16 (16 tests T-O1-C2-SH-1..5 + T-O1-C2-RT-1..6 + T-O1-C3-D-1..5 all PASS in 30.87s; adjacent regression 58/58 PASS). Files added: bridge/vapi_bridge/cedar_shadow_runtime.py (~430 LOC: evaluate_agent_action async fail-open evaluator + ShadowEvalResult slotted dataclass + detect_bundle_hash_drift sync sweep + detect_scope_hash_governance_drift async sweep + DriftFinding + DriftSweepResult slotted dataclasses). Files modified: bridge/vapi_bridge/store.py (operator_agent_shadow_log table + 3 helpers — INV-OPERATOR-AGENT-003 UNIQUE constraint; operator_agent_drift_log table + 2 helpers — INV-OPERATOR-AGENT-006 UNIQUE constraint), bridge/vapi_bridge/operator_api.py (GET /operator/operator-agent-shadow-log + POST /operator/evaluate-agent-action endpoints), scripts/vapi_invariant_gate.py (5 new INVs), .github/INVARIANTS_ALLOWLIST.json (regenerated 43→48 entries). Cedar shadow runtime is FAIL-OPEN per INV-OPERATOR-AGENT-004: every error path returns CedarDecision.FORBID_DEFAULT_DENY (safest default) but never raises — higher-level callers MUST NOT assume permission. INV-OPERATOR-AGENT-005 freezes "Merkle root recomputed every evaluation, never cached" — required for FSCA BUNDLE_HASH_DRIFT detection (Phase O1 D candidate). Drift detection is operator-triggered sweep primitive (no auto-loop in C3); finds (a) bundle file mutated post-anchor → BUNDLE_HASH_DRIFT, (b) AgentScope.scopeRoot ≠ AgentRegistry.scopeHash → SCOPE_HASH_GOVERNANCE_DRIFT. Both write to operator_agent_drift_log with second-granularity dedup. FSCA wiring deferred to Phase O1 D (FSCA only supports SQL/DAG rules; non-SQL drift detection requires architecture support). Bridge restart NOT performed during this commit — operator opted to keep current process alive; new endpoints + helpers active on next restart.
NOTE: 🏆 GIC_100 REACHED 2026-05-05 20:36:33 — chain_length=100/100 in grind_phase235_v1. CANONICAL HEAD HASH: `0x0e9d453d904220148d632e75802b71bdff74c7197aa8afcbfec5d36a61ab48da` (validation_id=189, ruling_id=189, ts_ns=1778031392729222600, pcc=NOMINAL/EXCLUSIVE_USB, gameplay_context=ACTIVE_GAMEPLAY, llm_verdict=FLAG, fallback_verdict=FLAG, divergence=0). GENESIS HASH: `0x87ce52cd21f9037730262debd4d247a76a6439bb754d9219fe10346ee1278c05` (ts_ns=1777142267690827300; Phase 235-A live 2026-04-26). Total grind span ~10.3 days of intermittent gameplay. The 32-byte head hash is the canonical Phase 235-A artifact unlocking Phase 239 (READINESS) gate per task #111. FROZEN GIC formula v1 (INV-GIC-001/002/003) tamper-evident chain over 100 sessions complete. Closing 13 chain links (88→100 this session) all from APOP#4..APOP#16 ACTIVE_MATCH_PLAY post-Phase-241-APOP-FIX (commit c31eca91); pre-fix chain 88→91 was UNKNOWN_LOW_EVIDENCE legacy fallthrough. Grind methodology proven: APOP hybrid + PCC NOMINAL + EXCLUSIVE_USB + ACTIVE_GAMEPLAY all required per ruling for chain advance. Tournament preflight P0 gate next per task #111; on-chain anchoring deferred until wallet refill (CHAIN_SUBMISSION_PAUSED=true safe posture preserved). Phase 239 (READINESS) work UNBLOCKED.
NOTE: Phase 241-APOP-FIX COMPLETE 2026-05-04 — codex shipped Phase 241-APOP classifier on top of empty evidence pipeline (frame_checkpoints disabled during grind_mode for Phase 235 SQLite-contention precaution). 3 pre-fix APOP fires all UNKNOWN_LOW_EVIDENCE (score=0.047, 0% rescue). Fix re-enables frame_checkpoint writes during grind_mode at sampled rate (10/sec via 100ms monotonic gate, vs unconditional skip OR pre-Phase-235 1000/sec) + adds time-based query helper (get_recent_frame_checkpoints_for_device) to replace per-record-hash join (which would miss ~99% of recent records under sampling). 13 post-fix APOP fires all ACTIVE_MATCH_PLAY with score 0.526..0.630, conf 0.820..0.852 — classifier produces real classifications matching gameplay reality. APOP-FIX directly enabled the 88→100 chain advance + GIC_100 milestone. bridge/vapi_bridge/dualshock_integration.py:_dispatch sample-rate-limited write; bridge/vapi_bridge/store.py +get_recent_frame_checkpoints_for_device (legacy get_frame_checkpoints_for_records preserved for session replay); bridge/vapi_bridge/session_adjudicator_validator.py + bridge/vapi_bridge/session_boundary_detector_agent.py swap call site to time-based query; bridge/tests/test_phase241_apop_fix.py +5 tests. Bridge 2563→2568 +5. Commit c31eca91. Frontend BRP Evidence Rays in 3D shipped commit e9e23ac0: 5 cone meshes radiating from origin per FROZEN INV-APOP-002 weights, color tinted by APOP state (cyan/green/t2/red/t3), emissive scales with score; group rotates slowly with speed boost from APOP score. ApopEvidenceRays.tsx + BrpCanvas/BrpMount/contracts threading + BrpView wiring useActivePlayOccupancy hook.
NOTE: Phase 241-APOP COMPLETE 2026-05-04 — Active Play Occupancy Proof: controller-native multi-signal classifier replaces Phase 235-GAD's binary trigger-onset gate. 5-state taxonomy (FROZEN per INV-APOP-001): ACTIVE_MATCH_PLAY / COMPETITIVE_CONTROL / MATCH_TRANSITION / NON_COMPETITIVE_MENU / UNKNOWN_LOW_EVIDENCE. 3 gate modes (FROZEN per INV-APOP-001): shadow (default; log-only, no GIC change) / hybrid (rescue COMPETITIVE_* from legacy MENU_DETECTED + block confident NON_COMPETITIVE_MENU + UNKNOWN falls through to legacy) / strict (APOP_STRICT_ELIGIBLE_STATES only; UNKNOWN fails closed). Scoring weights FROZEN per INV-APOP-002: 0.35*stick + 0.20*button + 0.20*trigger + 0.15*imu + 0.10*physiology = 1.00. Files added: bridge/vapi_bridge/active_play_occupancy.py (FROZEN-v1 classifier — INV-APOP-001/002), bridge/tests/test_phase241_apop.py (10 tests T-APOP-1..10). Files modified: bridge/vapi_bridge/store.py (+active_play_occupancy_log table + 4 helpers; get_validation_summary() extended with active_play_gate_mode kwarg), bridge/vapi_bridge/session_adjudicator_validator.py (APOP fires per ruling; asyncio.to_thread + try/except = non-fatal failure), bridge/vapi_bridge/session_boundary_detector_agent.py (APOP consulted only when legacy had_activity=False AND non-shadow mode), bridge/vapi_bridge/dualshock_integration.py (per-button frame fields expanded: cross/circle/square/triangle/l1/r1/dpad_*/share/options/touch + buttons_raw fallback — required by APOP _score_frames), bridge/vapi_bridge/config.py (+2 fields: active_play_occupancy_enabled default True + active_play_occupancy_gate_mode default "shadow"), bridge/vapi_bridge/operator_api.py (GET /operator/agent/active-play-occupancy-status), scripts/vapi_invariant_gate.py (+2 invariants INV-APOP-001/002), .github/INVARIANTS_ALLOWLIST.json (regenerated, 43 entries). PV-CI 41→43. 10 tests + 70/70 adjacent regression PASS. ACTIVE_PLAY_OCCUPANCY_GATE_MODE=hybrid in bridge/.env (LIVE 2026-05-04 17:25 — first APOP fire chain 88→89 confirmed; UNKNOWN_LOW_EVIDENCE → legacy GAD ACTIVE_GAMEPLAY → PASS pattern). Bridge 2553→2563 +10; SDK 539 unchanged; Hardhat 528 unchanged; Contracts 46 unchanged. Frontend wiring (commits 1b2e04e7 + 16ed9364): useActivePlayOccupancy() hook (4s polling, noMock=true), GamerView APOP chip with gate_mode badge (shadow=t3/hybrid=cyan/strict=orange) + novel ApopEvidencePrism (5 horizontal segments at FROZEN INV-APOP-002 weights, fill proportional to per-axis score, state-tinted), GAMEPLAY chip hidden in hybrid/strict modes (Option C — APOP authoritative when active).
NOTE: Phase 235-PCC-PERSIST COMPLETE 2026-05-04 — capture-health persistence loop closes the freshness gap where capture_health_log was populated only by GET /operator/bridge/capture-health (write at operator_api.py:7118 inside the GET handler). When dashboard or grind not actively polling that endpoint, DB-backed observers (.phase235_db_monitor.py, future Phase 240 PMT, future FSCA rules) saw stale or missing data even while bridge processed HID frames. bridge/vapi_bridge/pcc_persistence.py NEW: standalone async loop persist_pcc_monitor_once() reads CaptureHealthMonitor in-memory state + writes capture_health_log row when snapshot fresh (monotonic gate, default 5s interval). run_pcc_persistence_loop() wraps in asyncio.create_task supervised pattern. main.py wires loop into bridge startup task list when cfg.pcc_enabled. bridge/tests/test_phase235_pcc_persistence.py: 2 tests (monotonic gate + interval respect). Bridge 2551→2553 +2; commit 511f3b24.
NOTE: Phase O1 C1 COMPLETE 2026-05-03 — Operator Agents activated to O1_SHADOW via Cedar bundle dual on-chain anchor. Both Sentry (agentId 0xb21e1ec2...) and Guardian (agentId 0xbd8c7fba...) anchored on IoTeX testnet (4 txs total): Sentry-op `0x1eaaad09c2b68c02963147af4bc3514e51e84f13148bf8ec06c055d24610735f` block 43249195 + Sentry-gov `0xd34f5b3c46064b9812d71eb70d998634ed5a13e2f63d1bddb647a4fd59230ef6` block 43249242 + Guardian-op `0x324e683120e6a898a22399236728b6a36a6c0288988a17ab9ba19e11e36bca95` block 43249244 + Guardian-gov `0x18cc6b099d4c4ed32b89c0ab117f20e78e529e13cd83aeac36512cea13a10f8c` block 43249246. Cedar bundle Merkle roots: Sentry=0xebe899279b230ff5d71db22dc4b80282c810ff5bd1a9d249db6e6d309af52e41, Guardian=0x46807e13dd1c81cefa784ab8b30f8cdcaefd60697de921aae46ac24dac000a50. AgentScope (operational, 0xc694692a...) + AgentRegistry (governance, 0x9548E9d1...) both populated atomically per D4 dual-anchor design. On-chain verification (B14): operational==governance match for both agents. activation_log rows 1+2 inserted into bridge SQLite. Files added: bridge/vapi_bridge/cedar_parser.py (FROZEN-v1 — INV-CEDAR-001/002/003), bridge/vapi_bridge/cedar_bundle_anchor.py (INV-OPERATOR-AGENT-001 — operational FIRST), bridge/vapi_bridge/cedar_bundles/{anchor_sentry,guardian}_o1_shadow_v1.json, scripts/cedar_bundle_validate.py CLI. Files modified: bridge/vapi_bridge/store.py (operator_agent_activation_log table + 3 helpers; INV-OPERATOR-AGENT-002 UNIQUE constraint), bridge/vapi_bridge/chain.py (3 methods + AgentScope ABI: getScopeRoot/setAgentScopeRoot/updateAgentScope per V-checks deployed-contract reality), bridge/vapi_bridge/config.py (5 fields), bridge/vapi_bridge/operator_api.py (POST /operator/anchor-cedar-bundle + GET /operator/operator-agent-activation-log), scripts/vapi_invariant_gate.py (5 invariants INV-CEDAR-001/002/003 + INV-OPERATOR-AGENT-001/002). PV-CI 36→41. 19 tests T-O1-CP-1..8 + T-O1-AN-1..6 + T-O1-AL-1..5 all PASS in 7s. Targeted regression 71/71 PASS in 23s. Wallet 12.29→6.58 IOTX (5.71 IOTX consumed: 4 anchor txs ~0.14 IOTX + 5.57 IOTX bridge spam from PoAC/RulingEnforcement during 3 failed bridge anchor attempts before kill-switch re-engaged; permanent fix: bridge wait_for_transaction_receipt timeout extended OR direct submission pattern). Bridge 2532→2551 +19; SDK 539 unchanged; Hardhat 528 unchanged; Contracts 46 unchanged. Phase O0 → O1 transition COMPLETE; agents observe-only via Cedar policy enforcement (read skills + draft-only kms-sign with shadow_mode constraint); ipfs-pin/pda-attestation-anchor/git-push FORBIDDEN until O2_SUGGEST.
NOTE: Phase 235-PCC-SPC COMPLETE 2026-05-03 — Statistical Process Control classifier + 3-signal haptic-tolerance binding (Assurance A: trigger_active=1 + accel_var≥0.0003 + tremor_peak∈[4,60]Hz, ALL required, ≤500ms window) + outlier trim above USL=3500Hz + frequency-band gate (INV-PCC-005 — excludes OTHER-class sub-4Hz noise). Data-anchored from session 2026-05-03 (35.5min, 887 PCC snapshots, 26.6K record obs): rate p99=3240Hz, NOMINAL%=96.4, haptic burst rate=11.3% of records, MOTOR/HUMAN/OTHER classes 51%/10%/39%, 3-signal binding fires 34% of bursts. Three Novel Assurances ACCEPTED with bindings: A (haptic evidence, 3-signal gated) + B (LSL strict, USL trim only) + C (signal_disconnect always overrides per INV-PCC-003). bridge/vapi_bridge/capture_continuity.py extended (5-tuple sample buffer, _classify_capture_state_spc + _haptic_tolerance_active methods); bridge/vapi_bridge/dualshock_integration.py wires _last_bio_features → update_sample kwargs at 3 PCC call sites; bridge/vapi_bridge/config.py +8 fields + _env_float helper. PV-CI 32→36 (+INV-PCC-002/003/004/005). 15 tests T-SPC-1..15 all PASS. pcc_spc_enabled=False default (opt-in via PCC_SPC_ENABLED env; rollback = set False + bridge restart). When disabled, classifier byte-identical to Phase 234.7. W2 future primitive (ZK-attested human-gameplay-during-PoAC) tracked task #132. L4 anomaly threshold dysfunction during gameplay (mean L4 distance 192 during MOTOR bursts vs threshold 7.009; 100% FPR) surfaced as separate finding tracked task #134. Bridge 2517→2532 +15; SDK 539 unchanged; Hardhat 528 unchanged; Contracts 46 unchanged.
NOTE: 16 pre-existing SDK version-check failures (test_phase229_sdk.py::test_t229_sdk_3_dataclass_slots updated for 12-slot AITSeparationResult — now passing; 17 failures confirmed pre-existing version-check only). Bridge empirical pass count ~2524 (delta-based header count 2517; measured 2026-04-26 post Phase 237.5 Path C+). Phase 237.5 added 7 bridge tests T237.5-1..6 (T237.5-6 has 2 sub-tests for kill-switch on/off); PV-CI invariants 26 → 28 (+INV-CORPUS-001, +INV-CORPUS-002 retargeted to b"VAPI_CORPUS_SNAPSHOT_v1" deviceIdHash literal — Path X correction).
NOTE: Phase 237.5 Path C+ COMPLETE — three follow-up findings landed in single atomic commit. (1) PATH X CORRECTION: V5 verification cited AdjudicationRegistry.sol:79-99 anchorAdjudication(bytes32, string) as deployed primitive; live eth_getCode shows selectors 0xae7cd267 + 0x79dcce3f are NOT in deployed bytecode. Only legacy recordAdjudication (selector 0x5fa83f4b, Phase 111 original 2026-03-27) is live. chain.anchor_corpus_snapshot rewritten to call recordAdjudication(deviceIdHash=SHA-256(b"VAPI_CORPUS_SNAPSHOT_v1")=2bb9e18963108b658d5252018b3cb0f86487bb58c3597d590577554fb069560e, poadHash=snapshot_commitment, dualVeto=False) with dynamic gas estimate × 1.25 buffer (IoTeX requires ~160k gas; static 80k hit status=101 out-of-gas). Constant deviceIdHash carries CORPUS_SNAPSHOT attribution that sourceType would have. (2) LATENT BUG FIX: main.py:434-438 create_operator_app() was called without chain=self.chain — every chain-dependent operator endpoint (suspend_phg, mint_vhp, dual_eligibility, commit_separation_ratio, is_vhp_valid, plus Phase 237.5 wrapper) silently ran against chain=None. Fixed by adding chain=self.chain kwarg. (3) WALLET DRAIN DIAGNOSED + KILL-SWITCH: ~17.95 IOTX drained over one session (18.5 → 0.5525 IOTX) due to IoTeX testnet's broken P256 precompile (selector 0xf46a06ea) + retry-blind agents. Primary culprit dualshock_integration.py:2324-2335 fires 3 fire-and-forget chain calls per PITL proof during _session_loop() with no agent_dry_run_mode check. Secondary culprit batcher.py:471-527 retry loop re-queues failed submissions ignoring _p256_unavailable flag (which only gates LOGGING). Path C+ kill-switch: NEW config field chain_submission_paused (env var CHAIN_SUBMISSION_PAUSED, default False); guards at chain.py:_send_tx chokepoint (gates batcher + most modern-pattern paths) + chain.anchor_corpus_snapshot early-return + dualshock_integration.py:2324 caller-site (gates high-frequency culprit before tasks created). bridge/.env set CHAIN_SUBMISSION_PAUSED=true immediately. Bridge halted at user authorization (PID 25760 stopped via Stop-Process -Force); wallet stable at 0.5525 IOTX over 60s post-halt. INAUGURAL ANCHOR DEFERRED: 4 corpus_snapshot_log rows (c24f1949 / 1edbb57f / 974f4896 / 6065d043) all on_chain_confirmed=false — durable historical artifacts of diagnosis path. Restoration: edit bridge/.env CHAIN_SUBMISSION_PAUSED=false + restart + fire force-corpus-snapshot when wallet ≥1 IOTX (operator funding gap "several days"). New WIF entry what_if_corpus/wif_059_iotex_chain_drain_during_p256_failure.md documents the protocol-side risk + kill-switch as candidate Phase 237.5.1 invariant. INV-CORPUS-002 retargeted from r'"CORPUS_SNAPSHOT"' to r'b"VAPI_CORPUS_SNAPSHOT_v1"' via --generate --reason "invariant_change: ... Path X correction" --confirm-governance; allowlist regenerated 28/28 PASS; bridge governance event POST 404'd (bridge halted). T237.5-6 kill-switch test added (2 sub-tests: short_circuits_before_rpc, off_path_still_works); 7/7 PASS in 2.27s. Bridge 2515 → 2517 (+2 from T237.5-6); SDK 539 unchanged; Hardhat 528 unchanged; Contracts 46 LIVE unchanged.
NOTE: Phase 237.5 COMPLETE — CORPUS-SNAPSHOT on-chain anchoring via AdjudicationRegistry. Closes architectural gap surfaced by Phase 237-ZK-SEPPROOF verification (operator_api.py:7494-7496 hardcoded on_chain_confirmed=False/tx_hash="" despite schema fields existing at store.py:3287,3289). Reuses existing AdjudicationRegistry 0x44CF981f46a52ADE56476Ce894255954a7776fb4 (Phase 111 LIVE) via 2-arg anchorAdjudication(bytes32 podHash, string sourceType) at AdjudicationRegistry.sol:79-99 with sourceType="CORPUS_SNAPSHOT" — open-enum sourceType (line 96 stores any string) means zero contract change. Bridge wallet IS contract owner (verified live via eth_call selector 0x8da5cb5b returning 0x0Cf36dB57fc4680bcdfC65D1Aff96993C57a4692). Three-commit shape: (1) chain.py:anchor_corpus_snapshot async fn after :2529 — pure addition; legacy record_adjudication path (chain.py:2487-2529, Phase 112 wrapper for 3-arg recordAdjudication ABI) UNAFFECTED; (2) operator_api.py force_corpus_snapshot refined order — anchor FIRST then insert with populated tx_hash + on_chain_confirmed in one call (no UPDATE pattern, no new store method); response now includes tx_hash field; comment block at :7430-7440 rewritten; (3) tests + invariant freeze — bridge/tests/test_phase237_5_corpus_anchor.py 5 tests T237.5-1..5 (config-missing fail-graceful / bad-hex fail-graceful / success / 'PoAd: already recorded' idempotent / ZK-SEPPROOF binding feasibility round-trip via mocked isRecorded+getSourceType); INV-CORPUS-001 freezes anchor_corpus_snapshot signature; INV-CORPUS-002 freezes "CORPUS_SNAPSHOT" sourceType literal. Allowlist regenerated via --generate --reason "invariant_change: corpus snapshot on-chain anchor contract is itself a protocol invariant" --confirm-governance (28 entries; bridge governance event POST 404'd due to operator route prefix — not blocking). PV-CI gate 28/28 PASS post-regenerate. Skill 14 PostCode Sweep PASS (12/12 steps; monitoring/skill14_phase237_5.json; ingested as sweeps/sweep_20260426_clean.md). vapi_wiki_engine.py phase_close 2375 generated briefs at wiki/briefs/brief_MEMORY.md_2375.md + brief_VAPI_WHAT_IF.md_2375.md; phase snapshot SHA-256 2e174075db7b... wiki/phases/phase_237_5.md written with file:line citations. ZK-SEPPROOF binding feasibility: AdjudicationRegistry.isRecorded(commitment) confirms anchored; getSourceType(commitment) returns "CORPUS_SNAPSHOT" for sub-protocol isolation; AdjudicationAnchoredV2 event (AdjudicationRegistry.sol:37-41) emits indexed block.number for temporal anchor; 32-byte commitment requires one mod-reduction into BN254 254-bit scalar field at ZK proof time. Per-anchor cost ~0.00008 IOTX at 100k gas; wallet 18.4995 IOTX. Bridge 2510→2515 +5; SDK 539 unchanged; Hardhat 528 unchanged; Contracts 46 LIVE unchanged. Inaugural anchor pending operator restart + POST /operator/operator/force-corpus-snapshot.
NOTE: Phase 238 COMPLETE — MetaLearner FSCA wiring (Decision A only; Decision B REJECTED, Decision C DEFERRED). vapi_autoresearch.py:46-47 imports sqlite3 + defines BRIDGE_DB_PATH = Path(os.environ.get("VAPI_DB_PATH", "bridge/vapi_store.db")); vapi_autoresearch.py:51-92 NEW load_fsca_findings(severity_min="HIGH", age_hours=24, limit=10) — direct sqlite3 connection (timeout=2.0s, row_factory=Row), reads bridge/vapi_bridge/store.py:2281-2301 fleet_coherence_log schema, filters severity ∈ {CRITICAL, HIGH} + resolved=0 + created_at ≥ datetime('now','-24 hours'), CRITICAL sorted ahead of HIGH, LIMIT 10, returns [] silently on missing DB / missing table / any sqlite3 error (fail-open per A.D5 design); vapi_autoresearch.py:format_cycle_prompt() signature gains fsca_findings: Optional[list] = None param; FSCA section spliced between RECENT EXPERIMENT HISTORY and CURRENT SKILL.MD LENGTH renders "[SEV] RULE_NAME (agents: ...)\n  Explanation: ..." per finding or "(none active in last 24h at severity ≥ HIGH)" empty state; vapi_autoresearch.py:run_cycle() calls load_fsca_findings() and passes through to formatter. T238-FSCA-1, 1b, 2, 2b, 3, 3b, 3c: 7 deterministic tests at vapi-autoresearch/tests/test_phase238_fsca_wiring.py — temp SQLite seeded with 5 rows (HIGH active / CRITICAL active / MEDIUM filter / resolved=1 filter / 30h-stale filter), verifies fail-open + filtering + sort + render shapes; all 7 passing 0.27s. Decision B (eval_harness contradiction_load subscore) REJECTED as architecturally incorrect — eval_harness is IMMUTABLE pure-function deterministic scorer over proposal_text + skill_md_after strings only (vapi_eval_harness.py SCORING_WEIGHTS sum=1.00 frozen; mixing live runtime state into a deterministic scorer breaks reproducibility). Decision C (UnifiedWIFCorpus FSCA inclusion) DEFERRED — re-evaluate after 5+ autoresearch cycles run with FSCA prompt-context to assess whether dedup/citation overlap warrants integration. Inaugural WIF entry filed: what_if_corpus/wif_058_ps5_compat_mode_dormant.md (operator-observed PS5 "controller modules not correct" disconnect; W1 = ps5_compat_mode dormant in bridge/.env despite fix existing at dualshock_integration.py:2196-2209 + warning at :1684-1689; W2 = one-line PS5_COMPAT_MODE=true in bridge/.env). PV-CI gate: 26/26 invariants pass (actual count post Phase 235-ULTRAREVIEW + Phase 226 expansion; not 22 as some checklists state). vapi_wiki_engine.py phase_close 238 + snapshot --anchor executed; snapshot SHA-256 813997b82c09ba3a09be04b16a5ff09fda0022260db8171904cc95b9a1a5b7cf (pages=80; on-chain anchor failed HTTP 404, bridge offline — local snapshot durable); 15 W1 entries synced to harness WIKI_KNOWN_W1; coherence_status PASS (no contradictions). Bridge 2510 unchanged (no bridge code touched); Autoresearch 0→7 +7 (new test file); SDK 539 unchanged; Hardhat 528 unchanged; Contracts 46 LIVE unchanged. wiki/proposals/PHASE_238_DESIGN_REVIEW.md → wiki/phases/phase_238_design_review.md; wiki/proposals/VERIFICATION_REPORT.md → wiki/phases/phase_238_verification_report.md.
NOTE: Phase 237-EXTEND COMPLETE — Six honest additions to declare Phase 237 TRULY COMPLETE: (A) DEPLOYED VAPIConsentRegistry on IoTeX testnet at **0xA82dB0eF0bF7D15b6400EDd4A09C0D4338C948dA** (deployed 2026-04-26, gas ~0.07 IOTX, all 3 smoke calls passed: owner=0x0Cf36dB57fc4680bcdfC65D1Aff96993C57a4692 / totalGrants=0 / ioidRegistry=unset; bridge/.env CONSENT_REGISTRY_ADDRESS set; frontend/.env.local VITE_CONSENT_REGISTRY_ADDRESS set; contracts/deployed-addresses.json updated with VAPIConsentRegistry entry + _phase237_status=deployed marker). (B) W3bstream applet validate_poac_record.ts extended with Phase 237-EXTEND consent-routing layer: top-of-file architectural comment block documenting routing matrix (TOURNAMENT_GATE=hard / ANONYMIZED_RESEARCH+MARKETPLACE=soft soft-gates) + return codes 4 (consent absent hard refuse) / 5 (soft refuse routing skipped); _check_consent_view() helper with placeholder selector 0xCAFE0237 matching existing 0xDEADBEEF stub-style; documented PLACEHOLDER ABI WARNING — applet still stub-only, real selectors deferred to applet-pipeline phase. (C) FRONTEND first-of-its-kind wallet-write surface: WagmiProvider wired into main.jsx around QueryClientProvider; new frontend/src/hooks/useConsentSubmit.js (wagmi useWriteContract wrapper with inline 3-method ABI, CATEGORY_TO_UINT8 map, ready=false fail-open when CONSENT_REGISTRY_ADDRESS unset); useConsentStatus(deviceId, category) hook added to api/bridgeApi.js with noMock:true; new frontend/src/components/ConsentPanel.jsx mirroring PCCDrawer pattern (right-edge drawer offset top:50%+110px, slide-in 320px, wallet-connect via useConnect/useDisconnect, 4 ToggleRows with disclosure copy, contract-address transparency, error surface, refetch-after-tx pattern); ConsentPanel mounted in GamerView next to PCCDrawer; npm run build passes. (D) SDK Python VAPIConsent client class: GamerConsentResult @dataclass(slots=True) with 8 slots (device_id/categories/category/granted/revoked/found/error) handles BOTH aggregated and single-category response shapes; VAPIConsent.get_status(device_id, category="") urllib pattern matching Phase 222 verbatim; never raises (returns GamerConsentResult(error=...)). (E) OpenAPI sdk/openapi.yaml: version bumped 3.0.0-phase204 → 3.0.0-phase237; 3 new paths (/agent/gamer-consent-status GET with category enum filter, /operator/record-category-consent POST with reason ≥10 chars + on_chain:false, /operator/revoke-category-consent POST mirror); 4 new schemas (GamerConsentStatus aggregated + ConsentCategoryStatus single + CategoryConsentRecordResult + CategoryConsentRevokeResult); paths=160, schemas=129; YAML parses cleanly. (F) FSCA CONTRADICTION rule CONSENT_REVOKED_BUT_DATA_FLOWING: pure data-add to fleet_signal_coherence_agent.py CONTRADICTION_RULES (24 → 25 rules); LEFT JOIN consent_ledger ← records on device_id with revoked_at IS NOT NULL AND records.created_at > revoked_at; severity=HIGH GDPR Art.17 violation candidate; agents_involved=[bridge, BiometricPrivacyComplianceAgent, ConsentLedger]; resolution cites Phase 160 anonymize_device_records erasure pipeline. T237-CONSENT-9 test seeds revoked consent + post-revocation records row, verifies _check_contradictions fires the rule. T237-S1..S4: 4 new SDK tests at sdk/tests/test_phase237_sdk.py (slots/instantiation/HTTP-error/parse both shapes) all passing 2.4s. T237-CONSENT-9: 1 new bridge test all passing alongside existing 8 (9 total). Bridge 2509→2510 +1; SDK 535→539 +4; Hardhat 528 unchanged; Contracts 45 LIVE + 1 code-complete → 46 LIVE. Wallet ~40.43 → ~40.36 IOTX (0.17% spent). Phase 237 status: TRULY COMPLETE (was: core complete, deferrals listed honestly; extension closed all six gaps).
NOTE: Phase 237-CONSENT COMPLETE — Per-category gamer consent primitive (fifth FROZEN-v1 cryptographic pillar in PATTERN-016 family alongside GIC + WEC + VAME + CORPUS-SNAPSHOT). bridge/vapi_bridge/consent_categories.py NEW: ConsentCategory IntEnum (TOURNAMENT_GATE=0/ANONYMIZED_RESEARCH=1/MANUFACTURER_CERT=2/MARKETPLACE=3, position-frozen to match on-chain enum); compute_consent_hash() FROZEN FORMULA v1: SHA-256(b"VAPI-CONSENT-v1"(15) || device_id_b32 || bitmask_be(4) || expires_at_be(8) || ts_ns_be(8)) = 67B → 32B; categories_to_bitmask / bitmask_to_categories with uint32 overflow guard; device_id_to_bytes32() accepts hex/raw-32B/freeform-string (SHA-256 fallback). bridge/vapi_bridge/store.py: _check_consent_gate() extended with optional category=None param (Phase 161 backward-compat preserved — existing call site at insert_validation_record uses default biometric_processing); 3 thin helpers grant_category_consent / revoke_category_consent / get_category_consent_status (single + aggregated), all wrap existing Phase 160 insert_consent_record / revoke_consent / get_consent_status — ZERO schema churn (consent_ledger UNIQUE(device_id, consent_type) handles per-category UPSERT natively). bridge/vapi_bridge/config.py: consent_registry_address: str field with CONSENT_REGISTRY_ADDRESS env. bridge/vapi_bridge/chain.py: is_consent_valid(gamer, category) + get_consent_record(gamer, category) view methods, FAIL-OPEN (False / empty dict) when address unset (deliberate divergence from bbg_/dual_/swarm_ pattern — bridge is reader of consent state, not writer; missing on-chain registry must not block local consent_ledger operation); inline _CONSENT_REGISTRY_ABI snippet. bridge/vapi_bridge/operator_api.py: GET /agent/gamer-consent-status (read-only, _check_read_key, single-category filter via ?category=... or aggregated all-four; 422 on missing device_id or unknown category) + POST /operator/record-category-consent (full operator auth + reason ≥10 chars audit gate; ttl_s + consent_hash optional; NEVER on-chain — gamer-self-sovereign invariant) + POST /operator/revoke-category-consent (mirror revoke endpoint). contracts/contracts/VAPIConsentRegistry.sol NEW: Phase 222 pattern (Ownable + ReentrancyGuard, anti-replay _recordedHashes mapping, zero-hash + bad-category + double-revoke + no-consent-to-revoke guards, CEI, indexed events ConsentGranted/ConsentRevoked/IoIDRegistrySet); enum ConsentCategory + ConsentRecord struct; mapping(address => mapping(uint8 => ConsentRecord)) _consents; grantConsent(category, expiresAt, consentHash) + revokeConsent(category) gamer-signed (msg.sender); isConsentValid + getConsentRecord views check expiresAt > block.timestamp; setIoIDRegistry onlyOwner with zero-address guard (optional composition with Phase 55 VAPIioIDRegistry). contracts/scripts/deploy-phase237.js NEW: Phase 222-pattern deploy (deployed-addresses.json update + 3 smoke calls + bridge/.env hint print) — NOT executed in this phase. T237-CONSENT-1..8: 8 bridge tests (hash determinism + manual recompute, bitmask round-trip, tamper detection on each input, store grant→status→revoke + UPSERT, gate signature backward-compat + new category gate, GET endpoint shape, POST auth + reason gate + happy path + revoke + VAME-stamped, invalid-input ValueError) all passing 17.0s. T237-HH-1..6: 6 Hardhat tests (deploy + owner check, grantConsent + emit + state, anti-replay duplicate across senders, revokeConsent + expired-consent timestamp test, setIoIDRegistry owner-only + zero guard, bad-input reverts) all passing 8s. Bridge 2501→2509 +8; SDK 535 unchanged; Hardhat 522→528 +6; Contracts 45 LIVE + 1 code-complete (deploy deferred, ~0.07 IOTX gas estimated, wallet ~40.43 IOTX comfortable).
NOTE: Phase 236-CORPUS-SNAPSHOT COMPLETE — ZK-attested corpus snapshot primitive (third pillar in chain stack alongside GIC + WEC). bridge/vapi_bridge/corpus_snapshot.py NEW: CORPUS-SNAPSHOT FROZEN FORMULA v1 — commitment = SHA-256(b"VAPI-CORPUS-SNAPSHOT-v1"(23) || wiki_hash(32) || agent_root(32) || ratio_milli_be(8) || corpus_n_be(8) || ts_ns_be(8)) = 111B → 32B; ratio encoded as uint64 milliratio (ratio × 1_000_000 rounded) for deterministic int byte-encoding regardless of float64 representation; agent_root_from_hex() fail-safe parse of Phase 221 ProtocolCoherenceRegistry Merkle root (empty/invalid → 32 zero bytes); compute_wiki_snapshot_hash() walks wiki/ recursively, sorted by POSIX-relative path lowercased, hashes each file with explicit b"--FILE:" + posix_path + b"\n" separator before content (renames detected, content edits detected, OS-agnostic). store.py: corpus_snapshot_log table (Phase 236 migration; 11 columns including ipfs_cid deferred placeholder, tx_hash for on-chain anchor reuse, trigger_reason for operator audit; UNIQUE INDEX on snapshot_commitment for idempotency); insert_corpus_snapshot() returns row_id (existing row id on UNIQUE collision — duplicate triggers are no-ops); get_corpus_snapshot_status() returns 10-key latest summary; get_corpus_snapshot_history(limit) DESC ts_ns. operator_api.py: GET /agent/corpus-snapshot-status (read-only audit; _check_read_key) + POST /operator/force-corpus-snapshot (full operator auth + reason ≥10 chars audit gate; 422 on short reason; computes wiki_hash from cfg.wiki_dir or "wiki", reads agent root from get_protocol_coherence_status(), reads ratio + N from get_ait_separation_status(), commits FROZEN formula, persists). On-chain anchoring is config-gated (default OFF, deferred to operator action) — same fail-safe pattern as ProtocolCoherenceAgent. T236-SNAP-1..8: 8 bridge tests (wiki hash determinism, rename/content-edit detection, ratio milli round-trip, manual SHA-256 recompute correctness, per-input tamper detection, agent_root edge cases, store UNIQUE idempotency + DESC history, invalid-input ValueError) all passing 4.7s. End-to-end smoke confirmed: empty→1 snapshot via POST, 422 on short reason, 403 on wrong key, real wiki_hash computed from this repo's wiki/ tree, VAME headers stamped on snapshot endpoint too. Bridge 2493→2501 +8; SDK 535 unchanged; Hardhat 522 unchanged; Contracts 45 unchanged.
NOTE: Phase 236-VAME COMPLETE — VAPI Application-Layer Message Envelope. bridge/vapi_bridge/vame.py NEW: VAME FROZEN FORMULA v1 — commitment = SHA-256(b"VAPI-VAME-v1"(12) || chain_head_16b(16) || ts_ns_be(8) || endpoint || body_bytes); chain_head_from_hex() extracts first 16 bytes of latest GIC hash (or 16 zeros if no chain); stamp_response_headers() returns 5-key header dict (X-VAME-Version/Commitment/Chain-Head/TS-NS/Endpoint). operator_api.py: _VAMEMiddleware (Starlette BaseHTTPMiddleware) stamps every JSON response with sidecar headers; skips /health, non-JSON, 5xx, websocket upgrades; chain-head cached 5s to keep middleware off SQLite hot path; reuses store.get_prev_grind_chain_hash() for chain head. transports/http.py: CORS expose_headers extended with all 5 X-VAME-* keys so frontend fetch() can read them. frontend/src/api/vame.js NEW: validateVame() async hashes (Web Crypto API SHA-256) + recomputes commitment from response.headers + arrayBuffer body bytes; mismatch → console.warn + sessionStorage[__vapiVameFailures] increment; never throws. frontend/src/api/client.js: apiGet now reads body via arrayBuffer FIRST, validates VAME, THEN parses JSON — exact byte-level fidelity with bridge stamping. SHA-256 in v1 (Poseidon deferred to 237-ZK-SEPPROOF when circomlib already a dep — novelty is GIC-chain-head binding, not the hash function). Body wire format UNCHANGED (sidecar-only) → zero breakage with existing readers. T236-VAME-1..8: 8 bridge tests (chain head edge cases, commitment correctness, header contract, body/chain-head/ts_ns tamper detection, invalid input ValueError) all passing 0.7s. End-to-end smoke confirmed live: /health skipped, /bridge/grind-chain-status stamped, frontend recompute matches. Bridge 2485→2493 +8; SDK 535 unchanged; Hardhat 522 unchanged; Contracts 45 unchanged.
NOTE: GAMER DASHBOARD AUDIT 2026-04-26 — Three findings fixed atomically with Phase 236-VAME ship: (1) HIGH — useFleetCoherenceStatus called /agent/fleet-coherence-status (404 — does not exist; bridge defines /agent/fleet-coherence-summary). Result: COHERENCE chip silently rendered mock data unconditionally in production. Fixed: corrected path + adapter that maps live by_mode dict → active_contradictions/active_orphans/active_inversions chip-shape; added noMock:true so transient 5xx no longer flips to mock. (2) MEDIUM — useAITSeparation, useGrindAnalytics, usePCCIntelligence had no noMock: true guard — transient bridge stalls during a live grind would silently swap fabricated values (ratio=1.199, projected_gic100_date=2026-05-20, episodes=0) into the dashboard with no UI indication. Fixed: noMock: true added to all three; mock fallback now reserved exclusively for first-load discovery. (3) LOW — stale "P0 fix" comment in GamerView.jsx claimed consecutive_clean is the true grind metric while code uses chain_length as bar primary. Comment was misleading; rewrote to accurately describe the dual-metric semantics (chain_length cumulative + consecutive_clean leading streak). Bonus: MOCK-ACTIVE banner added to GamerView (orange banner on top edge when sessionStorage __vapiMockActive is set) so operators can never mistake fabricated values for live grind state. mockBridge.fleetCoherenceStatus mock reshape to match live /agent/fleet-coherence-summary. New useWatchdogStatus() hook surfaced (deferred wiring to DeveloperView). consecutive_clean=0 vs chain_length=20 disparity from check_grind.py audit-validated as CORRECT semantic behavior — chain_length never resets, consecutive_clean tracks leading non-divergent+PCC-attested+gameplay-OK streak from most-recent rulings.
NOTE: Phase 236-WATCHDOG COMPLETE — Bridge process watchdog + Watchdog Event Chain (WEC) cryptographic primitive. scripts/bridge_watchdog.py NEW: standalone supervisor (uses urllib stdlib only, no third-party deps); polls /health + /bridge/grind-chain-status + /bridge/capture-health every 10s; spawns bridge via subprocess.Popen([sys.executable, "-m", "bridge.vapi_bridge.main"]); exponential backoff schedule (5/10/30/60s — capped at auto_grind 60s poll cadence); 3 restarts/hour ceiling → WATCHDOG_HALT. Three VAPI-exclusive guards: (1) refuses restart when chain_intact=False (preserves INV-GIC-003 at ops layer — fallback Store check when bridge unreachable); (2) refuses restart when GRIND_SESSION_ID drifts in bridge/.env between watchdog start and now (silent rotation guard); (3) restart-rate ceiling. bridge/vapi_bridge/watchdog_chain.py NEW: WEC FROZEN FORMULA v1 — WEC_N = SHA-256(prev_wec(32) || event_code(1) || pid_be(4) || sid_hash(16) || ts_ns_be(8)) = 61B → 32B; genesis_wec(grind_session_id, ts_ns) anchored with VAPI-WEC-GENESIS-v1 tag; sid_hash = SHA-256(grind_session_id)[:16]; EVENT_CODES table (BRIDGE_START 0x01 / BRIDGE_HEALTHY 0x02 / BRIDGE_UNRESPONSIVE 0x03 / BRIDGE_RESTART_TRIGGERED 0x04 / BRIDGE_RESTART_REFUSED_GIC 0x05 / BRIDGE_RESTART_REFUSED_SID 0x06 / BRIDGE_DEGRADED_HOST_STATE 0x07 / WATCHDOG_BACKOFF_CEILING 0xFE / WATCHDOG_HALT 0xFF). store.py: watchdog_event_log table (Phase 236 migration; idempotent CREATE TABLE + 2 indexes; schema_versions(236, "watchdog_event_log")); insert_watchdog_event() with monotonicity guard (ts_ns ≤ prev → bumped to prev+1); get_prev_watchdog_event_hash(); get_watchdog_event_chain_status() recomputes full WEC chain with chain_intact verification, returns 9 keys including restarts_last_hour count. operator_api.py: GET /operator/watchdog-status (10 keys: grind_session_id, chain_length, latest_wec_hash, chain_intact, last_event_code, last_event_name, last_event_ts, restarts_last_hour, genesis_ts, timestamp; _check_read_key auth; asyncio.to_thread for DB scan). T236-WD-1..8: 8 bridge tests covering genesis determinism, single-link correctness, 3-event chain round-trip, tamper detection, monotonicity guard, session scoping, restart counting, invalid-input ValueError. Pairs WITH the GIC chain (Phase 235-A): GIC documents cognitive-session continuity, WEC documents operational continuity (bridge process lifetimes that produced those sessions). Together they constitute tamper-evident provenance for a grind run. Bridge 2477→2485 +8; SDK 535 unchanged; Hardhat 522 unchanged; Contracts 45 unchanged.
NOTE: Phase 235-DASH-UPGRADE-3 COMPLETE — Live data integrations for Developer/Manufacturer dashboards: per_player_features_json column on ait_session_log (idempotent Phase 235-DASH migration); insert_ait_session() +per_player_features param (dict of player→{accel_tremor_peak_hz, roll_cos, roll_sin, pitch_cos}); get_ait_separation_status() returns per_player_tremor_hz/per_player_roll_angle_deg/per_player_pitch_angle_deg (derived via atan2/acos); get_grind_analytics() returns last_validation_ts/last_stamp_ts (newest created_at overall / newest stamped row); AITSeparationResult 8→12 slots (+n_per_player/per_player_tremor_hz/per_player_roll_angle_deg/per_player_pitch_angle_deg); GrindAnalyticsResult 8→10 slots (+last_validation_ts/last_stamp_ts); openapi.yaml +6 fields on AITSeparationStatus+GrindAnalyticsStatus; DeveloperView: fmtAge() helper + live pipeline node age labels from last_validation_ts/last_stamp_ts/gc.latest_ts/pc.last_anchor_ts; ManufacturerView: radar tremor dots live from per_player_tremor_hz (fallback to corpus constants); ring 2 rotation anchored to mean roll angle; ring 3 pitch dasharray from mean pitch fraction; PairDrawer n_per_player breakdown with ≥10 check; mockBridge.js extended with all 6 new fields; analyze_interperson_separation.py write-snapshot pre-existing bug fixed (cov_mode/pair_distances/per_player_features now passed); T235-DASH-1..8: 8 bridge tests; T235-DASH-SDK-1..4: 4 SDK tests; Bridge 2469→2477 +8; SDK 531→535 +4; Hardhat 522 unchanged; Contracts 45 unchanged.
NOTE: Phase 235-CONTENTION COMPLETE — BT Contention Pattern Intelligence: store.get_bt_contention_analytics() groups consecutive non-NOMINAL capture_health_log rows into episodes (gap>10s=new episode); returns total_episodes/mean_recovery_s/longest_episode_s/last_episode_ts/last_episode_recovery_s/host_state_distribution; dualshock_integration.py: _hid_counter_restarts counter incremented on each hidapi _drain_loop reconnect; operator_api.py: GET /grind/pcc-intelligence (contention analytics + hid_counter_restarts fail-open 0); main.py: _op_app._transport=ds via try/except NameError pattern; vapi-mcp/unified_server.py: vapi_bt_contention_intelligence MCP tool (#18) via bridge_get(); capture_continuity.py: CONTESTED false positive fix committed — _infer_host_state() returns EXCLUSIVE_USB unconditionally when mean>=nominal_hz (haptic/trigger motor CV bursts are not PS5 BT contention); T235-CON-1..6: 6 bridge tests; Bridge 2455→2461 +6; SDK 527 unchanged.
NOTE: Phase 235-ANALYTICS COMPLETE — Grind Pipeline Analytics: store.get_grind_analytics(grind_session_id, gate_n) reads ruling_validation_log (WHERE grind_session_id=? OR grind_session_id IS NULL — includes blocking rows with NULL session_id); computes success_rate/blocking_reason_counts/sessions_per_day/projected_gic100_date; operator_api.py: GET /grind/analytics (_check_read_key auth); unified_server.py: vapi_grind_analytics MCP tool (#19) via bridge_get(); sdk/vapi_sdk.py: GrindAnalyticsResult@dataclass(slots=True)(8 slots)+VAPIGrindAnalytics.status(); sdk/openapi.yaml: /grind/analytics path+GrindAnalyticsStatus schema + /grind/pcc-intelligence path+PCCIntelligenceStatus schema; T235-ANL-1..8: 8 bridge tests; T235-ANL-SDK-1..4: 4 SDK tests; Bridge 2461→2469 +8; SDK 527→531 +4; Hardhat 522 unchanged; Contracts 45 unchanged.
NOTE: Phase 235-GAD COMPLETE — Gameplay Activity Discrimination: binary trigger-active gate (ACTIVE_GAMEPLAY/MENU_DETECTED/NULL); config: gameplay_discrimination_enabled:bool=True (GAMEPLAY_DISCRIMINATION_ENABLED env); store: trigger_active INTEGER column on records (idempotent); gameplay_context TEXT column on ruling_validation_log (idempotent); gameplay_classification_disagreements table; insert_validation_record() +gameplay_context param; get_validation_summary() gameplay_ok gate (MENU_DETECTED breaks streak, NULL passes through); override_gameplay_context() method; dualshock_integration.py: trigger_active = int(trigger_onset_velocity_l2>0 OR trigger_onset_velocity_r2>0) set in _pending_pitl_meta after pitl_meta assembly; main.py: record.pitl_trigger_active from pitl_meta; session_adjudicator.py: trigger_active_count+trigger_active_fraction computed from evidence window records, added to evidence_summary; session_adjudicator_validator.py: gameplay_context derived from trigger_active_fraction (>0→ACTIVE_GAMEPLAY, ==0→MENU_DETECTED, missing→NULL); GIC eligibility extended: gameplay_ok=gameplay_ctx!="MENU_DETECTED" as 4th condition; operator_api.py: GET /bridge/capture-health +gameplay_context_enabled+latest_gameplay_context keys; GET /bridge/grind-chain-status +latest_gameplay_context key; POST /operator/override-gameplay-context endpoint (api_key+reason≥10 chars→sets ACTIVE_GAMEPLAY+logs to disagreements); bridge/tests/test_phase235_gad.py 4 tests (T235-GAD-1..4); bridge/tests/test_phase235_gad_audit.py 6 tests; sdk/tests/test_phase235_gad_sdk.py 2 tests (T235-GAD-SDK-1..2); 10 bridge+2 SDK; Bridge 2434→2444 +10; SDK 525→527 +2; Hardhat 522 unchanged; Contracts 45 unchanged.
NOTE: Phase 235-GPC COMPLETE — Pre-Grind Validation: regression fixes (pcc_state/pcc_host_state backfilled in 7 test files + activation_simulation.py); store.get_prev_gic_ts_ns(); session_adjudicator_validator time.time_ns()+monotonicity guard; main.py GRIND_SESSION_ID startup log; POST /operator/gic-reset endpoint; test_phase235_grind_precheck.py 4 tests (T235-GPC-1..4) (Bridge 2430→2434 +4; SDK 525 unchanged; Hardhat 522 unchanged).
NOTE: Phase 235-B COMPLETE — PCC Attestation Slot (Bridge 2418→2422 +4; SDK 519→521 +2).
NOTE: Phase 235-A COMPLETE — Grind Integrity Chain (Bridge 2422→2430 +8; SDK 521→525 +4).
NOTE: Phase 234.7 COMPLETE — Physical Capture Continuity (PCC) implementation (Bridge 2408→2418 +10; SDK 515→519 +4).
NOTE: Phase 234 COMPLETE — InsightSynthesizer Mode 6 asyncio.to_thread fix (Bridge 2400→2408 +8).
NOTE: Phase 234.5 COMPLETE — consecutive_clean semantics audit (no code changes; grind_semantics documented below).
NOTE: Hardhat empirical count is 522 passing (6 pre-existing Phase 186 failures: registerAttestation missing); CLAUDE.md tracks delta-based count 502 (482+6 Phase 221+8 Phase 222; Phase 223-230 no new Hardhat tests beyond Phase 227 +6).
NOTE: T199-8 (all_pairs_gate_enabled default=True) fixed to use os.environ.pop isolation from bridge/.env.
49 contracts ALL LIVE (Sessions 1+2 added VAPIDataMarketplaceListings 0x78Df84Cc512EdCaC0e58a03e4852627E2F62E3bC Phase 238 Step H 2026-05-09; Groth16VerifierZKSepProof 0xD63EEf1372Cb496071bf963bEE395F7e0A3f2Ab6 + ZKSepProofVerifier 0xd51a21E234a800a6621f4c23a8fcA44e3bF01002 Phase 237 Session 2 2026-05-09; ProtocolCoherenceRegistry Phase 221 LIVE 2026-04-17: 0xfAfe4E8BEE45be22836b90D542045510dDd927Dd; VAPIBiometricGovernance Phase 222 LIVE 2026-04-17: 0x06782293F1CFC1AA30C0Baee0437c2B336796A00). AdjudicationRegistry: 0x44CF981f46a52ADE56476Ce894255954a7776fb4 (Phase 111, LIVE 2026-03-27). VAPIDualPrimitiveGate: 0xd7b1465Aad8F815C67b24681c9c022CED24FB876 (Phase 113, LIVE 2026-03-27). VAPISwarmOperatorGate: 0x969c0F1EFb28504a95Acf14331A59FBCb2944F98 (Phase 130, LIVE 2026-04-10). CeremonyAuditRegistry: 0xb9164E6d74Dde1508df2a39b01d3702ACC8230C2 (Phase 179, LIVE 2026-04-10). SeparationRatioRegistry: 0xB39CeE732cf91c93539Bd064D9426642a095a026 (Phase 153, LIVE 2026-04-10). VHPReenrollmentBadge: 0x42E7A25d0E5667BBae45e5cF33a6e2CC6E42d45C (Phase 187, LIVE 2026-04-10). See `contracts/deployed-addresses.json`.
Active wallet (bridge + deployer): `0x0Cf36dB57fc4680bcdfC65D1Aff96993C57a4692` (~15.44 IOTX as of 2026-05-09 post Sessions 1+2+3 on-chain activation; deployed Phase 238 Step H + Phase 237 ZK-SEPPROOF verifiers + minted Curator agent NFT + minted demo VHP tokenId=2; all 49 contracts live)
Previous bridge wallet (no longer accessible): `0xfCF4681e57C8de9650c3Eb4dA8e26dC9441A5EF1` (deployed original 14 contracts — addresses unchanged, still valid on-chain)
Chain ID: 4690 (IoTeX Testnet)
Current phase: Phase 238 Step I-FINAL ON-CHAIN COMPLETE 2026-05-09 — Sessions 1+2+3 autonomous activation arc closed. Curator (third Operator Initiative agent, post Sentry+Guardian) now LIVE at O1_SHADOW with agentId `0xed6a2df58e5ec50c1f88e127f6982a348f6855202b662b8ad73ffa1c1fda11a8` permanently registered on IoTeX testnet AgentRegistry + dual-anchored Cedar bundle (Merkle `0x44f89d0a05e7594741f7a06a1c4ca817d58396ad41b22b0eb5d0b5ce4be88ae6`) on AgentScope (operational FIRST per INV-OPERATOR-AGENT-001) + AgentRegistry (governance SECOND). Sentry/Guardian/Curator triplet now structurally enforces cross-agent skill separation invariant — first ≥3-agent Operator fleet in any DePIN gaming protocol. Phase 237-ZK-SEPPROOF verifier deploy COMPLETE same session: Groth16VerifierZKSepProof `0xD63EEf1372Cb496071bf963bEE395F7e0A3f2Ab6` + ZKSepProofVerifier wrapper `0xd51a21E234a800a6621f4c23a8fcA44e3bF01002` LIVE backed by 3-contributor IoTeX-anchored ceremony (VK hash `0x32fda285...` beacon block 43451392). Phase 99 VHP demo mint COMPLETE: tokenId=2 isValid=True for bridge wallet binding canonical Sony_DualShock_Edge_CFI-ZCP1 device + GIC_100 milestone + Session 2 ceremony VK hash. Wallet 20.132→15.442 IOTX (4.690 spent across 13 on-chain txs). Policy adjustment formalized 2026-05-09: P1vP3=0.032 on tremor_resting (Phase 197 all_pairs_p0_ok subgate) CAST OUT as a development progress blocker per operator authorization; mainnet TGE invariant ("no TGE before separation_ratio>1.0") REMAINS IN FORCE for token-issuance economic defensibility. Three-terminal setup unchanged: T0=`python scripts/bridge_watchdog.py`, T2=`python auto_grind.py`, T3=frontend. Next operator-track phase: Curator O1→O2 graduation when N≥50 reviews + 0 false-positive rate validated under shadow data; pre-authored O2 SUGGEST bundle Merkle `0xeb400a5c9b410c6f3035a595e2c36dee915f6b2447f822c72c46b164ccd5daa9`. Next protocol-track phase: W3bstream applet registration (~0.02 IOTX off-chain coordination) + frontend dashboard revamp consuming the wire-locked Curator/marketplace/Twin-stream endpoints.

## Phase O0 — ON-CHAIN REGISTRATION COMPLETE 2026-05-03

The Operator series initiative's Phase O0 closed its on-chain registration arc on
2026-05-03 with both Operator Agents (anchor-sentry and guardian) permanently
registered on IoTeX testnet. Phase O0 exit criteria 12-16 satisfied. Phase O1 unblocked.

**Canonical Operator Agent identities (FROZEN per Pass 2C Q9 encoding)**:

| Field | Anchor Sentry | Guardian |
|-------|---------------|----------|
| agentId (Q9 frozen) | `0xb21e1ec258d2d381c313f84944bd36fbc63badb2c9a24c2412212d3a27e3e42c` | `0xbd8c7fba08815b7ed343973c9c7300c062303b1acd19e8d9847a953ce5fa38d1` |
| Device address (DID) | `0xeaA6FD569a964C08D541F8e154aB3Ac8cD4e2743` | `0x9c577Fb2162824565ef57edd1B55a8EC5f58c181` |
| ioID DID identifier | `did:io:0xeaa6fd569a964c08d541f8e154ab3ac8cd4e2743` | `did:io:0x9c577fb2162824565ef57edd1b55a8ec5f58c181` |
| ioID tokenId | 495 | 496 |
| ERC-6551 TBA | `0xCc59C57bB7746791Be0945BfB96Be408a73944e4` | `0xd7aDA37AdFC08Fed43c934aB3b9609697b739092` |
| DID document CID (IPFS) | `bafkreiepyeeb5kck345uuc6u7amo5y4fgn2ijf7exdrs5zkm6e7yzvms2q` | `bafkreihvoogaqtrjsituky73fnphu2452mqdry2xsr3dgn56t32in33b7m` |
| AgentRegistry status | DEFINED (0) | DEFINED (0) |

Triple-verification of Q9-frozen agentIds passed for both agents:
independent computation + wrapper return + AgentRegistered event emission all matched
byte-for-byte. agentIds are now permanent for the rest of VAPI's lifetime.

**Phase O0 footprint at session close**: 44 commits on origin/main. Final commit
`44c26ce0` (chore(operator-agents): on-chain registration session 2026-05-03)
contains the complete audit trail in its body (all 14 transaction hashes,
empirical-finding commits, wallet reconciliation).

**Five empirical-finding commits preserved as permanent fixes** (each captures a
"verification at one layer didn't exercise behavior at the next layer" finding
that surfaced during execution and warranted permanent code-level correction):

| Commit | Fix |
|--------|-----|
| `8c0c8200` | V8.3 constant update post-deployment |
| `fef267e9` | Recovery script for IoTeX OOG quirk (initialize gas-limit override) |
| `300c49e4` | Deploy script permanent fix: explicit gas limits + receipt status checks + empirical gas baselines NatSpec |
| `10288cc3` | IOID_STORE_ADDR EIP-55 checksum fix (web3.py 7.x strict validation) |
| `a107d404` | step_7 ioID tokenId readback via tokenOfOwnerByIndex (deployed contract lacks `ids` public getter despite canonical b94ad092 source) |

**Wallet reconciliation**:
- Session start: 16.973199 IOTX
- Session end: 12.292873 IOTX
- Total consumed: 4.680326 IOTX (~2.7x original Section 14.6 estimate of 1.73 IOTX, driven by upgradeable contract gas + recovery overhead)

**Phase O0 status**: COMPLETE for the on-chain identity infrastructure.
Operator agents are registered but inactive per Section 7 design (no agent process startup,
no OAuth tokens issued, scopeHash = bytes32(0)). Phase O1 next: issue OAuth tokens,
turn on agent process startup, populate scopeHash with first Cedar policy bundle.

**Pre-2026-05-03 Phase O0 state (preserved as historical context)**:
The on-chain registration arc traversed (single day):
- Pass 2C Section 14 fourth amendment (commit `d2911480`)
- Block B implementation (commit `db9b4b97`)
- Block-B-prime wrapper script (commit `06099677`)
- External Action A (Hardhat deploy of VAPIOperatorAgentNFT to `0xa0CDD2B3E292c56030185c66a3d423278A4c467b`)
- Wrapper-driven steps 3-8 with two adaptive-fix iterations + Q9 freeze for both agents

**References**:
- `docs/phase-o0-pause-point.md` — earlier pause-point detail (commit roster, dependency graph) — historical
- `docs/phase-o0-stream-2-deploy-dryrun.md` — Stream 2-deploy execution reference — historical
- Commit `44c26ce0` body — complete on-chain registration audit trail (canonical reference)

Phase O0 is a parallel track to the main protocol's phase progression (Phase 237/238/239 above).

Phase 235 — REAL GRIND UNDERWAY 2026-04-26 (chain_length=16/100; smoke @ chain_length=5 complete; PCC-RATE-FIX commit c6e64229 brought bridge poll_rate_hz from 118 → 1152 Hz; capture_state=NOMINAL + host_state=EXCLUSIVE_USB stable under DEFAULT thresholds; bridge running grind_phase235_v1 / GRIND_TARGET=100 with PCC_SMOKE_BYPASS unset; auto_grind.py (repo root, untracked) drives autonomous adjudication — polls bridge every 60s, fires POST /agent/adjudicate when grind_ready=True and ctx != MENU_DETECTED, throttles to 5-min minimum, stops at chain_length>=grind_target)
Phase 235-PCC-RATE-FIX — COMPLETE 2026-04-25 (commit c6e64229; parallel hidapi counter on interface 3 reads true USB HID delivery rate independent of pydualsense's biometric pipeline; pydualsense lives on interface 0, counter on interface 3, no contention; pydualsense's 8ms-throttled _poll_frames continues feeding the L4 tremor FFT at ~120 Hz so frequency bin spacing stays valid; PCC sees ~1000 Hz and classifies EXCLUSIVE_USB; new Transport fields _hid_report_total/_last_hid_report_total/_hid_counter_thread/_hid_counter_running; _start_hid_rate_counter() daemon thread spawns on connect; _shutdown_cleanup releases handle; falls back to len(frames) if hidapi missing or interface 3 unavailable; resolves the only remaining blocker for the real 100-session grind under default thresholds)
Phase 235-GAD — COMPLETE (Gameplay Activity Discrimination — binary trigger-active gate gates consecutive_clean on actual gameplay; MENU_DETECTED breaks streak; NULL passes through; operator override logs to gameplay_classification_disagreements)
Phase 235-A — COMPLETE (Grind Integrity Chain (GIC) — cryptographic SHA-256 chain over 100 grind sessions; GIC_N = SHA-256(prev_gic(32)||commitment_hash(32)||verdict_code(1)||host_state_code(1)||ts_ns_be(8)); genesis_gic() anchors chain to grind_session_id+ts_ns; fallback_verdict (deterministic) hashed NOT llm_verdict (non-deterministic); bridge/vapi_bridge/grind_chain.py NEW: FROZEN FORMULA v1 (VERDICT_CODES/PCC_HOST_CODES/genesis_gic/compute_gic); config.py: grind_session_id str (GRIND_SESSION_ID env, auto grind_YYYYMMDD); store.py: Phase 2350 migration 4 columns (pcc_state TEXT/pcc_host_state TEXT/grind_chain_hash TEXT/gic_ts_ns INTEGER on ruling_validation_log); insert_validation_record() +pcc_state+pcc_host_state params; get_validation_summary() PCC gate: pcc_state=NOMINAL AND pcc_host_state in (EXCLUSIVE_USB,UNKNOWN) required for consecutive_clean (NULL=fail-closed); +latest_pcc_state/latest_pcc_host_state in return; get_prev_grind_chain_hash()/update_grind_chain_hash(row_id,gic_hex,ts_ns)/get_ruling_rows_for_chain()/get_grind_chain_status(); session_adjudicator_validator.py: PCC snapshot via get_capture_health_status(); GIC computed + stamped when grind_mode=True AND pcc_eligible AND not divergence; operator_api.py: GET /bridge/grind-chain-status (7 keys: grind_session_id/chain_length/latest_gic_hash/chain_intact/genesis_ts/latest_ts/timestamp); main.py: startup GIC chain integrity check → app._gic_chain_broken; sdk/vapi_sdk.py: GrindChainResult@dataclass(slots=True)(7 slots)+VAPIGrindChain.status(); sdk/openapi.yaml: /bridge/grind-chain-status path+GrindChainStatus schema; bridge/tests/test_phase235_a_gic.py 8 tests (T235A-1..8); sdk/tests/test_phase235_a_sdk.py 4 tests (T235A-SDK-1..4); Phase 235-B COMPLETE: bridge/tests/test_phase235_b_pcc_attestation.py 4 tests+sdk/tests/test_phase235_b_sdk.py 2 tests; 12 bridge+6 SDK; Bridge 2418→2430 +12; SDK 519→525 +6; Hardhat 522 unchanged; Contracts 45 unchanged)
Phase 234.7 — COMPLETE (Physical Capture Continuity (PCC) — 5-layer HID poll rate monitoring and grind-mode fail-closed gate; bridge/vapi_bridge/capture_continuity.py NEW: CaptureState enum (NOMINAL/DEGRADED/DISCONNECTED), HostState enum (EXCLUSIVE_USB/EXCLUSIVE_BT/CONTESTED/UNKNOWN), CaptureHealthMonitor thread-safe class; update_sample(n_frames, window_s) called once per _session_loop interval (~1 Hz); signal_disconnect(reason) on HID timeout forces DISCONNECTED immediately (overrides rate averaging via _disconnect_reason flag in _recompute); _infer_host_state() from poll-rate CV (CV<0.20 + rate≥900 → EXCLUSIVE_USB; CV≥0.40 → CONTESTED; 200-350 Hz → EXCLUSIVE_BT); grind_ready = NOMINAL AND host_state in {EXCLUSIVE_USB,UNKNOWN} AND sustained≥pcc_stable_window_s(30s); pop_transitions() buffer for store logging; config.py: 6 PCC fields (pcc_enabled/pcc_nominal_hz=950/pcc_degraded_hz=100/pcc_stable_window_s=30/grind_mode=False/grind_target=100); store.py: capture_health_log table (phase 2347 migration, 10 columns); insert_capture_health_event()/get_capture_health_status(); dualshock_integration.py DualShockTransport: self._pcc_monitor=None + set_pcc_monitor() method + update_sample()/signal_disconnect() wired at 4 points in _session_loop(); operator_api.py: GET /bridge/capture-health (11 keys: pcc_enabled/capture_state/host_state/poll_rate_hz/sustained_duration_s/grind_mode/grind_ready/grind_target/consecutive_clean_toward_target/session_counting_paused/timestamp); session_counting_paused = grind_mode AND NOT grind_ready; sdk/vapi_sdk.py: CaptureHealthResult@dataclass(slots=True)(11 slots)+VAPICaptureContinuity.status(); sdk/openapi.yaml: /bridge/capture-health path+CaptureHealthStatus schema; bridge/tests/test_phase234_7_pcc.py 10 tests (T234_7-1..10); sdk/tests/test_phase234_7_sdk.py 4 tests (T234_7-SDK-1..4); 10 bridge+4 SDK; Bridge 2408→2418 +10; SDK 515→519 +4; Hardhat 522 unchanged; Contracts 45 unchanged)
Phase 234 — COMPLETE (InsightSynthesizer HTTP Timeout Fix — Mode 6 living calibration was blocking asyncio event loop for ~54s on startup by running synchronous SQLite fetch + numpy computation directly in async context; fix: extracted sync body to _synthesize_living_calibration_sync(), async wrapper calls asyncio.to_thread(_synthesize_living_calibration_sync); prevents Uvicorn from stalling HTTP request processing during startup; also prevents PoAC chain integrity breaks during live sessions (event loop stall > 14ms can delay record generation); bridge/vapi_bridge/insight_synthesizer.py: _synthesize_living_calibration changed to thin async wrapper + new _synthesize_living_calibration_sync def; bridge/tests/test_phase234_http_timeout_fix.py 8 tests (T234-1..8); T234-1: async wrapper is coroutine; T234-2: sync body is not coroutine; T234-3: sync body returns early with < 50 records; T234-4: async wrapper completes in < 1s on empty DB (asyncio.wait_for 1.0s); T234-5: same verdict → no divergence even with large delta; T234-6: divergence requires verdicts_differ AND delta > 0.30; T234-7: advisory code → fallback=FLAG → LLM=FLAG → no divergence; T234-8: consecutive_clean counts leading non-divergent streak, breaks at first divergent; 8 bridge+0 SDK; Bridge 2400→2408 +8; SDK 515 unchanged; Hardhat 522 unchanged; Contracts 45 unchanged)
Phase 234.5 — COMPLETE (Consecutive Clean Semantics Audit — no code changes; grind_semantics: consecutive_clean=leading non-divergent streak from most recent ruling_validation_log rows (ORDER BY created_at DESC LIMIT gate_n=100); divergence=verdicts_differ AND |llm_conf-fallback_conf|>0.30; rule_fallback for clean session=FLAG(0.05); advisory code session=FLAG(0.5); SAME VERDICT never diverges regardless of confidence delta; advisory codes do NOT break streak if LLM agrees with FLAG verdict; verdict mismatch + large delta breaks streak: e.g. LLM=CERTIFY(0.8) vs fallback=FLAG(0.05) → |0.75|>0.30 AND verdicts_differ=True → divergence; GRIND SAFETY: LLM should return FLAG for all clean non-enrolled sessions; after enrollment(10 sessions) fallback=CERTIFY(0.8) — LLM must also return CERTIFY to avoid streak break; threshold=0.30 gives 15% confidence wiggle room on same-class verdicts; findings documented in test T234-5..T234-8)
Phase 231 — COMPLETE (AIT Defensibility P0 Gate + Stage 1 Graduation Activation — closes gap where all_pairs_p0_ok=True with <10 sessions/player; adds ait_defensibility_ok as 11th P0 preflight condition (all_pairs_above_1=True AND all players >=10 AIT sessions; fail-closed); store.py: idempotent ALTER TABLE tournament_preflight_log ADD COLUMN ait_defensibility_ok; insert_tournament_preflight_log() +ait_defensibility_ok param; get_tournament_preflight_status() +ait_defensibility_ok field; get_ait_separation_status() +n_per_player field (was missing — needed by operator_api.py defensibility compute); operator_api.py: _ait231 = store.get_ait_separation_status() → ait_defensibility_ok = all_pairs_above_1 AND all(n>=10 for n in n_per_player.values()); ait_defensibility_ok added to overall_pass, conditions dict, insert_tournament_preflight_log() call, response dict, GET /agent/tournament-preflight-status response; commit-activation P0 check: ait_defensibility_not_confirmed blocker; sdk/vapi_sdk.py: TournamentPreflightResult +ait_defensibility_ok slot (11 total); both run_preflight()+get_status() parse ait_defensibility_ok from body (fail-closed False); sdk/openapi.yaml: ait_defensibility_ok field on TournamentPreflightResult schema; scripts/analyze_interperson_separation.py: Phase 231 write-snapshot gap fixed — AIT path now captures run_analysis_ait() return value and calls store.insert_ait_session() when --write-snapshot set (previously returned 0 before snapshot block); bridge/.env: STAGED_GRADUATION_ENABLED=true (user-approved Stage 1 activation; ruling_enforcement_agent transitions dry_run=True→False); AIT corpus expanded: P1=13/P2=10/P3=14 N=37 all >=10 sessions; ait_defensibility_ok=True confirmed; bridge/tests/test_phase231_ait_defensibility_graduation.py 8 tests (T231-1..8); sdk/tests/test_phase231_sdk.py 4 tests (T231-SDK-1..4); 8 bridge+4 SDK; Bridge 2392→2400 +8; SDK 511→515 +4; Hardhat 522 unchanged; Contracts 45 unchanged; GRADUATION STATUS: Stage 1 ENABLED — ruling_enforcement_agent pending POST /agent/activate-graduation-stage)
Phase 230 — COMPLETE (AIT P0 Gate Wire-up — separation_defensibility_log now receives AIT results; insert_ait_session() extended: calls insert_separation_defensibility_log_guarded(session_type='ait', n_sessions_total=n_sessions, n_per_player=n_per_player, min_n_per_player=10, defensible=(all_pairs_above_1 and all(v>=10 for v in n_per_player.values())), ratio=separation_ratio, all_pairs_above_1=all_pairs_above_1, guard_enabled=False) after ait_session_log write; tournament_preflight all_pairs_p0_ok now reads AIT rows when most recent separation_defensibility_log entry is AIT type; operator_api.py bugfix: get_biometric_renewal_chain_status(limit=1) → get_biometric_renewal_chain_status() (invalid kwarg removed); len(_renewal_ch)>0 → int(_renewal_ch.get('total_renewals',0))>0 (correct dict total_renewals check); heals Phase 127 test_4 pre-existing failure; bridge/tests/test_phase230_ait_p0_gate.py 8 tests (T230-1..8: ait insert writes defensibility_log; unfiltered status returns AIT row; filtered status correct ratio; preflight all_pairs_p0_ok=True after AIT insert; all_pairs_above_1 stored correctly; defensible=False when players below min_n; defensible=True when all>=10; multiple inserts latest wins); 8 bridge+0 SDK; Bridge 2384→2392 +8; SDK 512 unchanged; Hardhat 522 unchanged; Contracts 45 unchanged; TOURNAMENT BLOCKER STATUS: all_pairs_p0_ok=True NOW LIVE — AIT results flow through separation_defensibility_log to tournament preflight P0 gate)
Phase 229 — COMPLETE (AIT Separation Breakthrough — Active Isometric Trigger 4-feature pipeline [accel_tremor_peak_hz, roll_cos, roll_sin, pitch_cos]; first probe type to achieve all_pairs_above_1=True; separation_ratio=1.199 (N=24: P1=6/P2=5/P3=13; 2026-04-18); P1vP2=1.850/P1vP3=1.846/P2vP3=1.349 all >1.0; LOO accuracy=66.7% (16/24); full covariance (N/p=6.0>3.0); AIT physics: player holds L2 at 50% (90-180 analog) for 30s; tremor_peak_hz from 4096-pt zero-padded accel FFT (4.0-15.0 Hz search, parabolic interp); postural gravity fingerprint roll_cos=cos(atan2(ax_mean,az_mean))/roll_sin=sin(atan2(ax_mean,az_mean))/pitch_cos=cos(atan2(-ay_mean,az_mean)) — anatomically stable per player; circular encoding avoids ±180° wraparound in Mahalanobis; scripts/analyze_interperson_separation.py: _extract_ait_features_from_file() +run_analysis_ait() +AIT_FEATURE_NAMES +_AIT_NFFT=4096/_AIT_HZ_LOW=4.0/_AIT_HZ_HIGH=15.0/_AIT_FORCE_MIN=90/_AIT_FORCE_MAX=180/_AIT_MIN_FRAMES=512; 'ait' added to _TERMINAL_CAL_ONLY_TYPES + _detect_session_type(); bridge/vapi_bridge/store.py: 'ait' added to STRUCTURED_PROBE_TYPES frozenset; ait_session_log table (Phase 229 migration, 13 columns); insert_ait_session()/get_ait_separation_status() store methods; bridge/vapi_bridge/config.py: ait_separation_enabled:bool=True (AIT_SEPARATION_ENABLED env); bridge/vapi_bridge/operator_api.py: GET /agent/ait-separation-status (uses _check_read_key+Header; 11 keys: ait_separation_enabled/n_sessions/separation_ratio/all_pairs_above_1/inter_player_mean/intra_player_mean/loo_accuracy/pair_distances/analysis_date/last_run_ts/timestamp); sdk/openapi.yaml: /agent/ait-separation-status path+AITSeparationStatus schema; sdk/vapi_sdk.py: AITSeparationResult@dataclass(slots=True)(8 slots)+VAPIAITSeparation.status(); bridge/tests/test_phase229_ait_separation.py 8 tests (T229-1..8); sdk/tests/test_phase229_sdk.py 4 tests (T229-SDK-1..4); 8 bridge+4 SDK; Bridge 2376→2384 +8; SDK 508→512 +4; Hardhat 522 unchanged; Contracts 45 unchanged; TOURNAMENT BLOCKER status: all_pairs_p0_ok=True for AIT probe type — first crossing of all-pairs>1.0 gate)
Phase 228 — COMPLETE (VHP-Gated Invariant Change Authorization — biometric presence required for invariant_change governance category; config: vhp_gated_invariant_change_enabled=False (VHP_GATED_INVARIANT_CHANGE_ENABLED env; fail-safe default); operator_api.py: POST /agent/allowlist-governance-event gate — when vhp_gated_invariant_change_enabled=True + cat==invariant_change: requires vhp_token_id in body (missing → 403); chain.is_vhp_valid(token_id) called fail-open (only hard-blocks on confirmed False); store.py: vhp_token_id TEXT column on invariant_gate_log (idempotent ALTER TABLE Phase 228 block); insert_invariant_gate_log() +vhp_token_id param; chain.py: is_vhp_valid(token_id) view call VAPIVerifiedHumanProof.isValid(); fleet_signal_coherence_agent.py: INVARIANT_CHANGE_WITHOUT_VHP HIGH CONTRADICTION rule 11th rule (fires when invariant_change row has empty vhp_token_id within 24h; HIGH not CRITICAL because default is disabled); POST /agent/allowlist-governance-event response +vhp_token_id field; sdk/vapi_sdk.py: VAPIAllowlistGovernance.post_invariant_change(reason_text,vhp_token_id) local validation + POST + returns error dict on failure; bridge/tests/test_phase228_vhp_gated_invariant.py 8 tests (T228-1..8); sdk/tests/test_phase228_sdk.py 4 tests (T228-SDK-1..4); 8 bridge+4 SDK; Bridge 2368→2376 +8; SDK 504→508 +4; Hardhat 522 unchanged; Contracts 45 unchanged; CONTRADICTION rules 10→11)
Phase 227 — COMPLETE (ProtocolCoherence On-Chain Provenance Anchor — anchors governance_provenance_hash on IoTeX L1 alongside fleet Merkle root; contracts/contracts/ProtocolCoherenceRegistry.sol: +governanceProvenanceHash field in CoherenceAnchor struct; +CoherenceAnchoredWithProvenance event; anchorCoherenceWithProvenance(merkleRoot,governanceProvenanceHash,agentCount,tsNs) new fn; getLatestGovernanceProvenance() view returns bytes32; anchorCoherence() backward-compat (stores bytes32(0)); store.py: idempotent ALTER TABLE governance_provenance_hash column on protocol_coherence_log; insert_protocol_coherence_log() +governance_provenance_hash param; get_protocol_coherence_status() returns governance_provenance_hash field; chain.py: anchor_coherence_with_provenance(root_hex,gov_prov_hash_hex,agent_count,ts_ns) ~120k gas; protocol_coherence_agent.py: _anchor_cycle() reads get_latest_governance_provenance_hash() (fail-open); uses anchor_coherence_with_provenance() when gov_prov_hash non-empty+non-zero; falls back to anchor_coherence() when no governance events; passes governance_provenance_hash to insert_protocol_coherence_log(); fleet_signal_coherence_agent.py: GOVERNANCE_PROVENANCE_ANCHOR_DRIFT CRITICAL 10th CONTRADICTION rule (fires when latest governance_provenance_chain hash != governance_provenance_hash in most recent protocol_coherence_log; detects SQLite tamper not caught by GOVERNANCE_CHAIN_BROKEN; silent window = 1 anchor cycle default 1h); operator_api.py: GET /agent/protocol-coherence-status 7→8 keys +governance_provenance_hash; sdk/vapi_sdk.py: VAPIAllowlistGovernance.on_chain_provenance_hash() GET /agent/protocol-coherence-status → governance_provenance_hash field (fail-open ""); contracts/scripts/deploy-phase227.js; contracts/test/Phase227.test.js (6 Hardhat: anchorCoherenceWithProvenance stores hash; getLatestGovernanceProvenance returns value; anti-replay cross-function; backward compat bytes32(0); empty case; getAnchorAt full struct); 8 bridge+4 SDK+6 Hardhat; Bridge 2360→2368 +8; SDK 500→504 +4; Hardhat 516→522 +6; Contracts 45 unchanged (ProtocolCoherenceRegistry.sol extended); CONTRADICTION rules 9→10)
Phase 226 — COMPLETE (Invariant Scope Expansion INV-019..022 — freeze provenance hash computation code; scripts/vapi_invariant_gate.py: +4 invariants (INV-019: _compute_governance_provenance_hash fn exists; INV-020: ts_ns.to_bytes(8,"big") replay-prevention in hash input; INV-021: _fetch_latest_provenance_hash fn exists; INV-022: governance_provenance_chain table+insert_governance_provenance in store.py); docstring updated 18→22 invariants; .github/INVARIANTS_ALLOWLIST.json regenerated 18→22 entries (governance event logged invariant_change reason); bridge/tests/test_phase226_invariant_scope.py 8 tests (T226-1..8: invariant count=22; INV-019..022 patterns match; allowlist 22 entries; gate PASS; allowlist_hash non-zero); sdk/tests/test_phase226_sdk.py 4 tests (T226-SDK-1..4: total_checked=22; chain_intact() bool; InvariantGateResult slots; missing-key defaults); 8 bridge+4 SDK; Bridge 2352→2360 +8; SDK 496→500 +4; Hardhat 496 unchanged; Contracts 45 unchanged; Invariants 18→22)
Phase 225 — COMPLETE (InvariantGate Provenance Chain + Governance History API; scripts/vapi_invariant_gate.py: _fetch_latest_provenance_hash() GET /agent/allowlist-governance-history (fail-open "0"*64); _compute_governance_provenance_hash(prev_prov,new_hash,category,text) = sha256(prev_prov||new_hash||category||text||ts_ns_8b); _post_governance_event() extended to compute+POST governance_provenance_hash+previous_provenance_hash; store.py: governance_provenance_hash column on invariant_gate_log (idempotent ALTER TABLE Phase 225); governance_provenance_chain table (id/governance_provenance_hash/previous_provenance_hash/new_allowlist_hash/reason_category/reason_text/created_at); insert_governance_provenance()/get_governance_provenance_history(limit)/get_latest_governance_provenance_hash() store methods; operator_api.py: POST /agent/allowlist-governance-event extended to receive+store provenance hash (insert_governance_provenance() fail-open; returns governance_provenance_hash in response); GET /agent/allowlist-governance-history (entries/total_entries/chain_intact/timestamp; chain_intact=False when any previous_provenance_hash != predecessor governance_provenance_hash); fleet_signal_coherence_agent.py: GOVERNANCE_CHAIN_BROKEN CRITICAL 4th INVERSION rule (fires when governance_provenance_chain contains a broken link; dag_query detects newer.previous_provenance_hash != older.governance_provenance_hash for non-genesis entries); sdk/vapi_sdk.py: VAPIAllowlistGovernance.previous_changes() rewritten to call GET /agent/allowlist-governance-history (Phase 224 legacy → dedicated history endpoint); VAPIAllowlistGovernance.chain_intact() new method (GET /agent/allowlist-governance-history?limit=1 → chain_intact field; returns False on error); 8 bridge+4 SDK; Bridge 2344→2352 +8; SDK 492→496 +4; Hardhat 496 unchanged; Contracts 45 unchanged; INVERSION rules 3→4; post-Phase-225 tooling: INV-017 audit split regex + INV-018 audit block-search added to invariant gate; Invariants 16→18; INVARIANTS_ALLOWLIST.json regenerated 16→18 entries; scripts/audit_endpoint_auth.py INV-017/018 patterns frozen; governance event logged invariant_change reason)
Phase 224 — COMPLETE (InvariantGate On-Chain Anchor + Allowlist Governance; scripts/vapi_invariant_gate.py: compute_allowlist_hash() exported deterministic fn; --reason required on --generate (format <category>: <text>; categories: refactor/bugfix/invariant_change/ceremony_update; 10-200 chars); --confirm-governance required for invariant_change (time.sleep(3)+exact phrase "I understand this changes a frozen protocol invariant"; exit 3 on mismatch); _post_governance_event() fail-open urllib POST to bridge; INV-016 added (16 total invariants; frozen digest of virtual-leaf code in protocol_coherence_agent.py); .github/INVARIANTS_ALLOWLIST.json regenerated with 16 entries; store.py: allowlist_change_log table (previous_hash/new_hash/merkle_root_at_change/detected_at/reason_from_gate_log); 4 new columns on invariant_gate_log (previous_allowlist_hash/new_allowlist_hash/reason_category/reason_text); allowlist_hash column on protocol_coherence_log; insert_allowlist_change_log()/get_allowlist_change_status()/get_latest_governance_reason() store methods; protocol_coherence_agent.py: biometric_governance_agent added to _AGENT_IDS (36→37); virtual allowlist leaf index 37 = sha256(b"allowlist"+bytes.fromhex(allowlist_hex[:16])+ts_ns.to_bytes(8,"big")); compute_fleet_root() returns 3-tuple (root_hex,ts_ns,allowlist_hex); 38 total Merkle leaves (37 fleet+1 virtual); agent_count=37 (fleet only) passed to anchorCoherence(); _anchor_cycle() detects hash change+logs to allowlist_change_log; fleet_signal_coherence_agent.py: ALLOWLIST_CHANGE_WITHOUT_GOVERNANCE CRITICAL CONTRADICTION_RULES entry (fires when reason_from_gate_log IS NULL); operator_api.py: POST /agent/allowlist-governance-event (validates category refactor/bugfix/invariant_change/ceremony_update→422; reason_text 10-200→422; returns {row_id,accepted,category}); .github/workflows/vapi-invariant-gate.yml: PR comment bot on failure (actions/github-script@v7; 3 remediation options); sdk/vapi_sdk.py: AllowlistGovernanceResult@dataclass(slots=True)(7 slots: current_hash/last_change_ts/last_change_reason/last_change_category/suspicious_change_count/on_chain_anchor_ts/error)+VAPIAllowlistGovernance(status()/previous_changes()/suspicious_changes()); deploy Phase 221 ProtocolCoherenceRegistry LIVE 0xfAfe4E8BEE45be22836b90D542045510dDd927Dd (2026-04-17); deploy Phase 222 VAPIBiometricGovernance LIVE 0x06782293F1CFC1AA30C0Baee0437c2B336796A00 (2026-04-17); bridge/.env updated with both addresses; 8 bridge+4 SDK; Bridge 2336→2344 +8; SDK 488→492 +4; Hardhat 516 unchanged; Contracts 45 ALL LIVE; _AGENT_IDS 36→37; Invariants 15→16; Merkle leaves 36→38)
Phase 223 — COMPLETE (PV-CI Protocol Invariant Gate + Staleness Fix; scripts/vapi_invariant_gate.py (15 invariants INV-001..INV-015; _hash_file_region() SHA-256 of matching lines; check_invariants() returns 15 results; run_gate() exits 0=pass/1=fail; --generate writes .github/INVARIANTS_ALLOWLIST.json; --report human-readable); .github/INVARIANTS_ALLOWLIST.json (15 frozen digests); .github/workflows/vapi-invariant-gate.yml (GitHub Actions CI gate on push/PR to critical protocol files); invariant_gate_log table (gate_pass/total_checked/failures_json/run_source/created_at); insert_invariant_gate_log()/get_invariant_gate_status() store methods; pv_ci_enabled=True config (PV_CI_ENABLED); GET /agent/invariant-gate-status (7 keys: pv_ci_enabled/gate_pass/total_checked/failure_count/last_failures/last_run_ts/timestamp); POST /agent/run-invariant-gate (inline gate run); InvariantGateResult@dataclass(slots=True)(7 slots)+VAPIInvariantGate SDK; InvariantGateStatus openapi schema+2 paths; STALENESS FIX: sync_vapi_workflow.py rewritten (SDK regex \d{3,} fixes 484+ count; Bridge \d{4,} fixes 2336+; update_context_test_counts handles bold+comma format; update_agents_md() new function syncs VAPI_AGENTS.md SYNC NOTE+agent count; update_invariants_md() new function syncs VAPI_INVARIANTS.md phase ref; PostToolUse hook head -5 REMOVED — full error output now visible; flush=True on all prints); 8 bridge+4 SDK; Bridge 2328→2336 +8; SDK 484→488 +4; Hardhat 496 unchanged; agents 38 unchanged)
Phase 222 — COMPLETE (VAPIBiometricGovernance (BBG) — governance proposals gated on proposer's live VHP; VAPIBiometricGovernance.sol contract #45 (Ownable+ReentrancyGuard; proposeWithVHP(proposalHash,vhpTokenId); isProposed() view; setVHPContract() onlyOwner; anti-replay proposalHash UNIQUE; 3 attacks blocked: STOLEN_KEY/VHP_EXPIRY/FLASH_LOAN); MockVHP222.sol test mock; bbg_proposal_log table (proposal_hash/proposer_address/vhp_token_id/vhp_expires_at/on_chain_confirmed/tx_hash/created_at); insert_bbg_proposal_log()/get_bbg_status()/get_bbg_proposal_history() store methods; 3 config fields: bbg_enabled=False/bbg_max_age_seconds=3600/bbg_contract_address=""; chain.bbg_propose()/chain.bbg_check_proposal(); BiometricGovernanceAgent agent #38 (validate_proposal_locally(); submit_proposal(); run_poll_loop()); GET /agent/bbg-status (7 keys: bbg_enabled/total_proposals/latest_proposal_hash/latest_proposer/on_chain_confirmed/last_proposal_ts/timestamp); POST /agent/bbg-propose (5 keys: valid/on_chain/tx_hash/row_id/rejection_reason); BBGProposalResult@dataclass(slots=True)(7 slots)+VAPIBiometricGovernance SDK; BBGStatus openapi schema; deploy-phase222.js; 10 bridge+4 SDK+8 Hardhat; Bridge 2318→2328 +10; SDK 480→484 +4; Hardhat 488→496 +8; Contracts 44→45; VAPIBiometricGovernance LIVE 2026-04-17: 0x06782293F1CFC1AA30C0Baee0437c2B336796A00)
Phase 221 — COMPLETE (ProtocolCoherenceRegistry (PoPC) — on-chain Proof of Protocol Coherence; Merkle tree over sorted sha256 leaves for 36 agents; leaf=sha256(agent_id||ts_ns_8b); ProtocolCoherenceRegistry.sol contract #44 (Ownable+ReentrancyGuard; anchorCoherence(merkleRoot,agentCount,tsNs); getLatestCoherence() view; isCoherent(maxAgeSec) sub-check for VAPIProtocolLens; anti-replay merkleRoot UNIQUE; zero-root+zero-agentCount guards); ProtocolCoherenceAgent (agent #37; run_poll_loop(); _compute_merkle_root() binary Merkle with sorted leaves+duplicate promotion; _compute_leaf() sha256; compute_fleet_root(); _anchor_cycle() local+optional on-chain); protocol_coherence_log table (merkle_root/agent_count/anchor_hash/on_chain_confirmed/created_at); insert_protocol_coherence_log()/get_protocol_coherence_status()/get_protocol_coherence_history() store methods; 3 config fields: protocol_coherence_enabled=False (PROTOCOL_COHERENCE_ENABLED)/protocol_coherence_anchor_interval_s=3600/protocol_coherence_registry_address=""; chain.anchor_coherence()/chain.get_latest_coherence(); GET /agent/protocol-coherence-status (7 keys: protocol_coherence_enabled/total_anchors/latest_merkle_root/agent_count/on_chain_confirmed/last_anchor_ts/timestamp); ProtocolCoherenceResult@dataclass(slots=True)(7 slots)+VAPIProtocolCoherence SDK; ProtocolCoherenceStatus openapi schema; deploy-phase221.js; 10 bridge+4 SDK+6 Hardhat; Bridge 2308→2318 +10; SDK 476→480 +4; Hardhat 482→488 +6; Contracts 43→44; ProtocolCoherenceRegistry LIVE 2026-04-17: 0xfAfe4E8BEE45be22836b90D542045510dDd927Dd)
Phase 220 — COMPLETE (PerPairGapProjection — TGE timeline projections; per_pair_gap_projection_log table; get_per_pair_gap_projection() projects days_to_1_0 per pair via velocity (1.0-current_dist)/vel; deduplication by pair_key (most recent entry kept); projected_tge_date=max across all blocker pairs; any_feasible=False for WORSENING/STABLE pairs; per_pair_gap_projection_enabled=True config (PER_PAIR_GAP_PROJECTION_ENABLED); GET /agent/per-pair-gap-projection (7 keys: per_pair_gap_projection_enabled/projections/any_feasible/max_days_to_1_0/projected_tge_date/session_type/timestamp); PerPairGapProjectionResult@dataclass(slots=True)(7 slots)+VAPIPerPairGapProjection SDK; 8 bridge+4 SDK; Bridge 2300→2308 +8; SDK 472→476 +4; Hardhat 482 unchanged)
Phase 219 — COMPLETE (TournamentBlockerSummary — aggregated TGE blocker list; tournament_blocker_summary_log table; get_tournament_blocker_summary() aggregates tournament_preflight_log+per_pair_gap_log+capture_velocity_oracle; blocker severity P0/P1 with source/key/detail fields; tournament_blocker_summary_enabled=True config (TOURNAMENT_BLOCKER_SUMMARY_ENABLED); GET /agent/tournament-blocker-summary (8 keys: tournament_blocker_summary_enabled/total_blockers/blockers/overall_blocked/preflight_pass/capture_healthy/all_pairs_above_1/timestamp); TournamentBlockerSummaryResult@dataclass(slots=True)(8 slots)+VAPITournamentBlockerSummary SDK; 8 bridge+4 SDK; Bridge 2292→2300 +8; SDK 468→472 +4; Hardhat 482 unchanged)
Phase 218 — COMPLETE (CaptureVelocityOracle — unified capture health synthesis; capture_velocity_oracle_log table; get_capture_velocity_oracle_status() synthesizes Phase 152 centroid_velocity_log + Phase 154 capture_stagnation_log; recommended_action=CONTINUE_CURRENT_PROTOCOL/CAPTURE_MORE_SESSIONS/RERUN_SEPARATION_ANALYSIS/URGENT_CAPTURE_SESSIONS_AND_REANALYZE; capture_velocity_oracle_enabled=True config (CAPTURE_VELOCITY_ORACLE_ENABLED); GET /agent/capture-velocity-oracle (9 keys: capture_velocity_oracle_enabled/probe_type/sessions_per_day/sessions_stagnant/ratio_velocity/velocity_stagnant/overall_capture_healthy/recommended_action/timestamp); CaptureVelocityResult@dataclass(slots=True)(9 slots)+VAPICaptureVelocityOracle SDK; 8 bridge+4 SDK; Bridge 2284→2292 +8; SDK 464→468 +4; Hardhat 482 unchanged)
Phase 217 — COMPLETE (PerPairGapTrend — per-pair Mahalanobis gap velocity analysis; per_pair_gap_trend_alert_log table (pair_key/distance/trend/velocity_per_day/alert_severity/created_at); get_per_pair_gap_trend() store method (IMPROVING/WORSENING/STABLE/UNKNOWN velocity over n_runs); per_pair_gap_trend_enabled=True config (PER_PAIR_GAP_TREND_ENABLED); GET /agent/per-pair-gap-trend (8 keys: per_pair_gap_trend_enabled/pair_key/distances/velocity_per_day/trend/n_runs/blocker_resolved/timestamp); PerPairGapTrendResult@dataclass(slots=True)(8 slots)+VAPIPerPairGapTrend SDK; PER_PAIR_GAP_BLOCKER_UNRESOLVED — 7th ORPHAN rule in FleetSignalCoherenceAgent (fires when above_1_0=0 entry >24h old with no above_1_0=1 response; HIGH severity; documents G-001 P1vP3=0.032 tournament blocker in FSCA audit trail); 8 bridge+4 SDK; Bridge 2276→2284 +8; SDK 460→464 +4; Hardhat 482 unchanged)
Phase 216 — COMPLETE (PerPairGapLog — per-pair Mahalanobis inter-player distance log; per_pair_gap_log table (session_type/pair_key/player_i/player_j/distance/above_1_0/n_sessions_i/n_sessions_j/analysis_date/created_at); insert_per_pair_gap()/get_per_pair_gap_status() store methods; per_pair_gap_log_enabled=True config (PER_PAIR_GAP_LOG_ENABLED); GET /agent/per-pair-gap-status (7 keys: per_pair_gap_log_enabled/all_pairs_above_1/pairs/session_type/pair_count/blocker_pairs/timestamp); PerPairGapResult@dataclass(slots=True)(6 slots)+VAPIPerPairGap SDK; blocker_pairs field surfaces P1vP3=0.032/P2vP3=0.401 tournament blockers in live API; 8 bridge+4 SDK; Bridge 2268→2276 +8; SDK 456→460 +4; Hardhat 482 unchanged)
Phase 215 — COMPLETE (L4DimSyncConfirmation — closes G-003 L4 staleness gap; l4_dim_sync_log table (from_dim/to_dim/anomaly_threshold/continuity_threshold/n_sessions/sync_reason/sync_completed/created_at); insert_l4_dim_sync()/get_l4_dim_sync_status() store methods; l4_dim_sync_enabled=True config (L4_DIM_SYNC_ENABLED); GET /agent/l4-dim-sync-status (7 keys); L4DimSyncResult@dataclass(slots=True)(7 slots)+VAPIL4DimSync SDK; confirms touchpad_spatial_entropy (index 12) structurally zero in gameplay sessions → thresholds 7.009/5.367 valid for live_dim=13 without recalibration; 8 bridge+4 SDK; Bridge 2260→2268 +8; SDK 452→456 +4; Hardhat 482 unchanged)
Phase 214 — COMPLETE (GraduationAutowatchBridge — WIF-041 mitigation; SeparationRatioMonitorAgent observes all_pairs_p0_ok False→True transitions via get_separation_defensibility_status() and fires graduation_readiness_check bus event; graduation_autowatch_log table + insert_graduation_autowatch()/get_graduation_autowatch_status() store methods; graduation_autowatch_enabled=True config (GRADUATION_AUTOWATCH_ENABLED); observes only — does NOT auto-activate graduation stages; StagedDryRunGraduationAgent auto-evaluates check_graduation_preconditions() when trigger_fired=True entry is found; staged_graduation_enabled=False still gates actual stage activation)
Phase 213 — COMPLETE (AccelTremorFFT FFT Resolution — zero-padded 4096-point FFT + parabolic sub-bin interpolation to resolve P1vP3=0.032 tremor_resting TOURNAMENT BLOCKER; root cause: Phase 205 1024-point rfft → 0.977 Hz/bin aliases P1 (3.1 Hz) and P3 (3.7 Hz) to same bin (0.6 Hz gap < 1 bin); fix: np.fft.rfft(_am_dc, n=_nfft) zero-pads to 4096 → 0.244 Hz/bin; _parabolic_interp() static method: peak_idx + 0.5*(alpha-gamma)/(alpha-2β+γ) → ~0.05 Hz sub-bin resolution; P1/P3 now map to bins 12.7 and 15.2 (clearly separated); _ACCEL_FFT_NFFT=4096 class constant; accel_fft_nfft:int=4096 constructor param + Config field (ACCEL_FFT_NFFT env); dualshock_integration.py wired; /agent/accel-tremor-fft-status extended: +accel_fft_nfft+bin_width_hz keys; AccelTremorFFTResult +2 slots (accel_fft_nfft=4096/bin_width_hz=0.244); VAPIAccelTremorFFT.status() parses new fields; test_phase205_sdk.py updated: expected field set +2; 8 bridge+4 SDK; Bridge 2276→2284 +8; SDK 452→456 +4; Hardhat 482 unchanged)
Phase 212 — COMPLETE (Autonomous Engineering Layer — vapi-mcp/unified_server.py extended to 17 tools (+5: vapi_skill_state_sync/vapi_phase_advance_proposal/vapi_code_change_impact/vapi_engineering_decision/vapi_autonomous_gap_scan); vapi_skill_state_sync: WIF-040 W3-006 structural closure — reads CLAUDE.md live via _parse_claude_md() mtime-cache, detects skill manifest drift, generates paste-ready Reference state block for vapi.md with "NOT authoritative" illustrative fallback; vapi_phase_advance_proposal: MetaLearner blocker->focus_area mapping, PROPOSALS dict with 4 canonical gap prescriptions, offline autoresearch pre-score; vapi_code_change_impact: Phase 180+ test delta patterns (endpoint +8B+4S, contract +6H, MCP tool +8B), RISKY_PATTERNS invariant risk registry with CRITICAL/HIGH/MEDIUM severity, risk_score=CRITICAL×0.40+HIGH×0.15; vapi_engineering_decision: WIF corpus lookup + W1/W2 extraction + pre-score + verdict (proceed/defer/BLOCK); vapi_autonomous_gap_scan: 8-gap canonical registry G-001..G-008 ranked by severity+effort, hardware filter, separation_path_analysis + autonomous_action_recommendation; vapi.md Reference state replaced with MCP directive (WIF-040 STRUCTURALLY_CLOSED); MANDATORY_INVARIANTS #12 converted to range+MCP directive; ANALYSIS/Communication Style cite blocks redirected to MCP; 8 bridge tests T212-1..8; Bridge 2268→2276 +8; SDK 452 unchanged; Hardhat 482 unchanged)
Phase 211 — COMPLETE (Unified Knowledge Loop MCP Server — vapi-mcp/unified_server.py (12 tools, ~1050 lines); MetaLearner: clusters log.jsonl failure reasons by 5 themes (invariant_violation/separation_ratio/what_if_quality/phase_coherence/gap_advancement) → dominant_blocker; HypothesisDeduplicator: (probe_type, phase_candidate) fingerprint dedup across 3 WIF sources; UnifiedWIFCorpus: deduplicated WIF index from what_if_corpus/ + wiki/what_if/ + VAPI_WHAT_IF.md with SHA-256[:16] content fingerprint; WikiFeedback: auto-writes wiki/phases/phase_NNN.md on autoresearch PASS (score>=0.70) + updates wiki/index.md; ExperimentLedger: queryable log.jsonl with pass_rate/score_distribution/mean_score; all 4 existing MCP patterns reused (_parse_claude_md() mtime-cache, @tool decorator, bridge_get/db_query helpers, Windows ProactorEventLoop stdin loop); _validate_proposal() offline eval harness: 20 MANDATORY_INVARIANTS + 5-subscore rubric without subprocess; 12 tools: vapi_unified_state/vapi_session_context/vapi_query_knowledge/vapi_phase_brief/vapi_entity/vapi_experiment_history/vapi_validate_proposal_full/vapi_invariant_check/vapi_contradiction_status/vapi_autoresearch_cycle/vapi_unified_wif_corpus/vapi_learning_loop_status; add as "vapi-unified" MCP server; 8 bridge tests T211-1..8; Bridge 2260→2268 +8; SDK 452 unchanged; Hardhat 482 unchanged)
Phase 210 — COMPLETE (MCP Autonomous Sync — _parse_claude_md() mtime-cached CLAUDE.md parser in both vapi-mcp/server.py and knowledge_server.py; replaces all 58-phase-stale hardcoded Phase 149 fallbacks; phase/bridge/sdk/hardhat/agents/contracts/l4_thresholds/separation_ratio/recent_phases all live from CLAUDE.md; _check_corpus_reload() in knowledge_server handle() auto-reloads VAPIKnowledgeGraph when any VAPI_*.md changes; serverInfo.version dynamic; vapi_sync_memory() generates from CLAUDE.md state not hardcoded; smoke test: phase=207→210, bridge=2252, sdk=448, hardhat=482, agents=36, contracts=43; no bridge/SDK/Hardhat test changes — config-only MCP layer)
Phase 208 — COMPLETE (CorpusRatioRegressionGuard — WIF-039 W1+W2 closure; CorpusRegressionError(ValueError) raised by insert_separation_defensibility_log_guarded() when all_pairs_above_1 regresses below 1.0 after a prior breakthrough and no override is registered; corpus_ratio_regression_guard_log table (probe_type/ratio/n_sessions_total/all_pairs_above_1/provenance_hash/prev_hash/created_at); corpus_regression_override_log table (probe_type/old_ratio/new_ratio/reason/override_hash/created_at); insert_corpus_regression_override()/get_corpus_regression_guard_status() store methods; corpus_ratio_regression_guard_enabled=False config (CORPUS_RATIO_REGRESSION_GUARD_ENABLED); Ratio Provenance Chain: SHA-256(prev_hash+ratio+N+probe_type+ts_ns) tamper-evident milestone audit trail; GET /agent/corpus-regression-guard-status (7 keys); CorpusRegressionGuardResult@dataclass(slots=True)(8 slots)+VAPICorpusRegressionGuard SDK; openapi CorpusRegressionGuardStatus schema+path; 8 bridge+4 SDK; Bridge 2252→2260 +8; SDK 448→452 +4; Hardhat 482 unchanged; closes WIF-039)
Phase 207 — COMPLETE (StagedDryRunGraduationGate — per-agent sequential dry_run=True→False transition with P0 precondition gate and automatic rollback; dry_run_graduation_log table (agent_id, stage_number, activated_at, dry_run_disabled_at, rollback_triggered, rollback_triggered_at, rollback_reason, n_clean_sessions, n_false_positives); insert_graduation_stage()/record_graduation_clean_session()/record_graduation_false_positive()/trigger_graduation_rollback()/get_graduation_stage_status()/get_all_graduation_stages(); 3 config fields: staged_graduation_enabled=False/graduation_rollback_window_sessions=10/graduation_fp_threshold=2; StagedDryRunGraduationAgent: check_graduation_preconditions()+activate_stage()+_check_rollbacks()+run_poll_loop(); GRADUATION_SEQUENCE=[ruling_enforcement_agent, session_adjudicator, tournament_activation_chain]; P0 gate: staged_graduation_enabled + tournament_preflight overall_pass + non_convergence_clear; auto-rollback when n_false_positives>=fp_threshold; GET /agent/dry-run-graduation-status (6 keys) + POST /agent/activate-graduation-stage (P0 fail-fast: staged_graduation_enabled check before rate limit); DryRunGraduationResult@dataclass(slots=True)(7 slots)+VAPIDryRunGraduation SDK; docs/dry-run-graduation-protocol.md; 8 bridge+4 SDK; Bridge 2244→2252 +8; SDK 444→448 +4; Hardhat 482 unchanged)
Phase 206 — COMPLETE (Corpus Validation + PragmaJudge Appealability pre-work: (1) RULE 0.9 v2 — APPEAL_CONSENSUS_TIMEOUT_SECONDS=3600 (Gap 2: TIMEOUT_UPHELD fallback); appeal_window_seconds/appeal_consensus_timeout_s SubProtocolConfig governance params (Gap 4); APPEAL_QUORUM=0.90 frozen; governance update path must re-validate appeal_consensus_timeout_s<appeal_window_seconds (critique item addressed); (2) --stratify-by-session-type flag (SESSION_TYPE_CONTAMINATION vs GENUINE_PHYSIOLOGICAL_NON_STATIONARITY diagnosis); (3) TremorRestingConvergenceOracle non-convergence branch: _N_NONCONV_THRESHOLD=5; consecutive_negative+non_convergence_detected in get_tremor_convergence_status(); Bridge 2242→2244 +2; SDK 442→444 +2; (4) docs/SEPARATION_DECISION_FRAMEWORK.md: Path A/B/C decision tree, 5-condition commitment gate; ref link added to VAPI_SUBPROTOCOL_ARCHITECTURE.md (critique item))
Phase 205 — COMPLETE (AccelTremorFFT — BiometricFeatureExtractor now falls back to accel magnitude FFT for tremor_peak_hz when right_stick_x ring variance < _STILL_HOLD_VAR_THRESHOLD=4.0 LSB²; still-hold detection: _use_accel_tremor flag; accel FFT searches 1-15 Hz physiological tremor range; section 5.5 populates _accel_mag_ring before section 6; accel_tremor_fallback_enabled:bool=True in Config (ACCEL_TREMOR_FALLBACK_ENABLED env); BiometricFeatureExtractor(accel_tremor_fallback_enabled=...) constructor param; wired in dualshock_integration.py; GET /agent/accel-tremor-fft-status endpoint; AccelTremorFFTResult@dataclass(slots=True)+VAPIAccelTremorFFT SDK; openapi AccelTremorFFTStatus schema+path; 12 bridge+8 SDK; Bridge 2230→2242 +12; SDK 434→442 +8; Hardhat 482 unchanged; closes tremor_peak_hz=0 bug in tremor_seed sessions)
Phase 204 — COMPLETE (IOSWARM_ACTIVE_NO_ADJUDICATIONS 8th CONTRADICTION rule in FleetSignalCoherenceAgent; guard mechanism added to _check_contradictions() for cfg-dependent rule gating; rule fires when ioswarm_enabled=True AND ioswarm_adjudication_enabled=True AND total_adj=0 AND consensus_count>0; POST /agent/prime-ioswarm-adjudication endpoint (WIF-038 W2 closure) — seeds ioswarm_adjudication_log with 5 emulator primer entries; ioswarm_adjudication_primer_enabled=False default; IoSwarmAdjudicationPrimerResult@dataclass(slots=True)+VAPIIoSwarmAdjudicationPrimer SDK; openapi IoSwarmAdjudicationPrimerStatus schema+path; 14 bridge+8 SDK; Bridge 2216→2230 +14; SDK 426→434 +8; Hardhat 482 unchanged; closes WIF-038 W1+W2)
Phase 203 — COMPLETE (AgentContextRegistry — agent_context_log table; UNIQUE(agent_id, prompt_sha256); upsert_agent_context_hash()/get_agent_context_status()/get_all_agent_context_status(); main.py Phase 203 startup block: SHA-256(system_prompt) for 3 LLM agents registered at bridge start; GET /agent/context-integrity-status; CONTEXT_HASH_MISMATCH 4th INVERSION rule in FleetSignalCoherenceAgent; AgentContextIntegrityResult@dataclass(slots=True)+VAPIAgentContextIntegrity SDK; agent_context_on_chain_enabled=False default; 8 bridge+4 SDK; Bridge 2208→2216 +8; SDK 422→426 +4; Hardhat 482 unchanged; closes WIF-036 W1)
Phase 205 — COMPLETE (AccelTremorFFT — BiometricFeatureExtractor now falls back to accel magnitude FFT for tremor_peak_hz when right_stick_x ring variance < _STILL_HOLD_VAR_THRESHOLD=4.0 LSB²; still-hold detection: _use_accel_tremor flag; accel FFT searches 1-15 Hz physiological tremor range; section 5.5 populates _accel_mag_ring before section 6; accel_tremor_fallback_enabled:bool=True in Config (ACCEL_TREMOR_FALLBACK_ENABLED env); BiometricFeatureExtractor(accel_tremor_fallback_enabled=...) constructor param; wired in dualshock_integration.py; GET /agent/accel-tremor-fft-status endpoint; AccelTremorFFTResult@dataclass(slots=True)+VAPIAccelTremorFFT SDK; openapi AccelTremorFFTStatus schema+path; 12 bridge+8 SDK; Bridge 2230→2242 +12; SDK 434→442 +8; Hardhat 482 unchanged; closes tremor_peak_hz=0 bug in tremor_seed sessions)
Phase 204 — COMPLETE (IOSWARM_ACTIVE_NO_ADJUDICATIONS 8th CONTRADICTION rule in FleetSignalCoherenceAgent; guard mechanism added to _check_contradictions() for cfg-dependent rule gating; rule fires when ioswarm_enabled=True AND ioswarm_adjudication_enabled=True AND total_adj=0 AND consensus_count>0; POST /agent/prime-ioswarm-adjudication endpoint (WIF-038 W2 closure) — seeds ioswarm_adjudication_log with 5 emulator primer entries; ioswarm_adjudication_primer_enabled=False default; IoSwarmAdjudicationPrimerResult@dataclass(slots=True)+VAPIIoSwarmAdjudicationPrimer SDK; openapi IoSwarmAdjudicationPrimerStatus schema+path; 14 bridge+8 SDK; Bridge 2216→2230 +14; SDK 426→434 +8; Hardhat 482 unchanged; closes WIF-038 W1+W2)
Phase 203 — COMPLETE (AgentContextRegistry — agent_context_log table; UNIQUE(agent_id, prompt_sha256); upsert_agent_context_hash()/get_agent_context_status()/get_all_agent_context_status(); main.py Phase 203 startup block: SHA-256(system_prompt) for 3 LLM agents registered at bridge start; GET /agent/context-integrity-status; CONTEXT_HASH_MISMATCH 4th INVERSION rule in FleetSignalCoherenceAgent; AgentContextIntegrityResult@dataclass(slots=True)+VAPIAgentContextIntegrity SDK; agent_context_on_chain_enabled=False default; 8 bridge+4 SDK; Bridge 2208→2216 +8; SDK 422→426 +4; Hardhat 482 unchanged; closes WIF-036 W1)
Phase 202 — COMPLETE (TremorRestingConvergenceOracle — tremor_convergence_log table; insert_tremor_convergence_log()/get_tremor_convergence_status()/get_tremor_convergence_history(); velocity=(ratio_curr-ratio_prev)/N_delta; convergence_stable=True when velocity>=0 for 2 consecutive; GET /agent/tremor-convergence-status; RATIO_VELOCITY_NEGATIVE 6th ORPHAN rule in FleetSignalCoherenceAgent; TremorConvergenceResult@dataclass(slots=True)+VAPITremorConvergence SDK; tremor_convergence_enabled=False default; 8 bridge+4 SDK; Bridge 2200→2208 +8; SDK 418→422 +4; Hardhat 482 unchanged; closes WIF-037 W1)
Phase 201 — COMPLETE (Agent Context Modernization — updated 3 LLM agent system prompts to Phase 200 state: bridge_agent._SYSTEM_PROMPT (Phase 30→200), session_adjudicator._SYSTEM_PROMPT (Phase 65→200), calibration_intelligence_agent._CALIB_SYSTEM_PROMPT (Phase 148→200); VAPI_AGENTS.md 22→36 agents; 8 bridge prompt-invariant tests; Bridge 2192→2200 +8; SDK 418 unchanged; Hardhat 482 unchanged)
Phase 200 — COMPLETE (ioSwarm Activation + Frontend Alignment Phase 199 — all_pairs_gate_enabled:bool=True config (False=prototype mode, bypasses per-pair P0 gate); tremor_resting added to STRUCTURED_PROBE_TYPES; GET /agent/probe-gate-config-status + GET /agent/tremor-resting-probe-status endpoints; ProbeGateConfigResult+VAPIProbeGateConfig + TremorRestingProbeResult+VAPITremorRestingProbe SDK; openapi ProbeGateConfigStatus+TremorRestingProbeStatus schemas; 8 bridge + 4 SDK tests; Bridge 2184→2192 +8; SDK 414→418 +4; Hardhat 482 unchanged; Tools 149 unchanged)
Phase 198 — COMPLETE (Biometric TTL Decay Scaling — effective_ttl = base_ttl × (mean_decay_factor / 0.50), clamped [base×0.25, base×4.0]; biometric_ttl_decay_scaling_enabled=False default; get_effective_biometric_ttl(base_ttl_days, scaling_enabled) store method; GET /agent/biometric-ttl-scaling-status endpoint; BiometricTTLScalingResult @dataclass(slots=True) 6 slots + VAPIBiometricTTLScaling SDK; openapi BiometricTTLScalingStatus schema; 8 bridge + 4 SDK tests; Bridge 2176→2184 +8; SDK 410→414 +4; Hardhat 482 unchanged; Tools 149 unchanged)
Phase 197 — COMPLETE (Per-Pair Separation P0 Gate — all_pairs_p0_ok 10th P0 condition in tournament preflight; reads all_pairs_above_1 from separation_defensibility_log; fail-closed default=False; biometric_ttl_ok AND all_pairs_p0_ok both required for overall_pass; commit-activation extended: per_pair_separation_below_1.0 blocker; TournamentPreflightResult +all_pairs_p0_ok slot; 8 bridge + 4 SDK tests; Bridge 2168→2176 +8; SDK 406→410 +4; Hardhat 482 unchanged)
Phase 196 — COMPLETE (Tournament Preflight v2 WIF-035 W1 closure — biometric_ttl_ok 9th P0 condition; (not ttl_expired) AND len(renewal_chain)>0; idempotent ALTER TABLE tournament_preflight_log ADD COLUMN biometric_ttl_ok; insert_tournament_preflight_log() + get_tournament_preflight_status() updated; TournamentPreflightResult +biometric_ttl_ok slot(default=True); commit-activation extended: biometric_ttl_expired_or_no_renewal_chain blocker; 8 bridge + 4 SDK tests; Bridge 2160→2168 +8; SDK 402→406 +4; Hardhat 482 unchanged)
Phase 195 — COMPLETE (Protocol Metabolism Index PMI — 9th component weight=0.03 in ProtocolMaturityScoringAgent; PMI=max(0.0, 1.0-mean_orphan_resolution_hours/48.0); _WEIGHTS v3: sep 0.18, fresh 0.11, pmi 0.03; get_orphan_resolution_stats(domain) store; GET /agent/protocol-metabolism-index; Tool #149 get_protocol_metabolism_index; PMIResult @dataclass(slots=True)+VAPIProtocolMetabolism SDK; ProtocolMaturityScoringResult +pmi_component slot; openapi ProtocolMetabolismIndexStatus schema+path; 8 bridge + 4 SDK tests; Bridge 2152→2160 +8; SDK 398→402 +4; Hardhat 482 unchanged; Tools 148→149)
Phase 194 — COMPLETE (CoherenceFingerprintRegistry — contradiction fingerprint tracking; coherence_fingerprint_log table (rule_name UNIQUE, occurrence_count, persistent=1 when count≥N_PROMOTE_THRESHOLD=3); idempotent ALTER TABLE fleet_coherence_log ADD COLUMN on_chain_confirmed; upsert_coherence_fingerprint()/get_coherence_fingerprint_status()/get_persistent_contradictions() store methods; ProtocolMaturityScoringAgent._threat_forecast_accuracy_component() Phase 194 addition: score *= (1 - min(1.0, persistent_count × 0.10)) — direct fleet-coherence→maturity feedback loop; GET /agent/coherence-fingerprint-status endpoint; Tool #148 get_coherence_fingerprint_summary; CoherenceFingerprintResult @dataclass(slots=True) + VAPICoherenceFingerprint SDK; openapi CoherenceFingerprintStatus schema; 10 bridge + 4 SDK tests; Bridge 2142→2152 +10; SDK 394→398 +4; Hardhat 482 unchanged; Tools 147→148)
Phase 193 — COMPLETE (FleetSignalCoherenceAgent — agent #36; fleet-level signal coherence observer; 3 failure modes: CONTRADICTION (7 rules), ORPHAN (5 rules), INVERSION (3 rules — Provenance DAG walk); coherence_id="coh_"+SHA-256[:16]; auto-promotes persistent contradictions (N_PROMOTE_THRESHOLD=3) to VAPI_WHAT_IF.md; fleet_coherence_enabled=True DEFAULT (always-on, unlike most agents); RENEWAL_WITHOUT_ATTESTATION=CRITICAL (highest severity — Phase 185/186 attestation chain bypass); BP-007 _scrub_evidence() removes raw biometric fields from evidence_json; fleet_coherence_log table in store.py; cmd_coherence_status() + "coherence_status" in vapi_wiki_engine.py CMDS; vapi_fleet_coherence MCP tool in knowledge_server.py; score_phase_193_readiness() gate(≥0.80) in vapi_autoresearch.py; Tools #145–#147 (get_fleet_coherence_summary/get_fleet_coherence_entries/resolve_coherence_entry); 14 bridge + 4 SDK tests; Bridge 2128→2142 +14; SDK 390→394 +4; Hardhat 482 unchanged; agents 35→36)
Phase 192 — COMPLETE (CorpusDataCuratorAgent — agent #35; 7-task data coherence layer: Provenance DAG (20-hop walk), Corpus Entropy Monitor (threshold 1.5 CLUSTERING_WARNING), Proof-of-Erasure Certificate (sha256: SHA-256(device_id+erased_tables+ratio+ts_ns)), Federated Corpus Quality (BP-007 compliant; privacy_constraint gate), Cross-Feature Temporal Correlation (Frobenius per-pair separability), Data Readiness Certificate (8-dim gate; NOT_READY/READY/PARTIAL), Session Contribution Weights (TBD λ=ln(2)/90 FROZEN BP-001); --weighted-centroid+--correlation-matrix flags in analyze_interperson_separation.py; 3 MCP tools in knowledge_server.py; provenance_dag/corpus_entropy_log/erasure_certificate_log/federation_corpus_quality_log/feature_correlation_log/data_readiness_certificate_log/session_contribution_weight_log tables in store.py; "curator"+"corpus_quality" bus channels in vapi_managed_agents.py; Tools #136–#144 (9 tools); 14 bridge + 7 SDK tests; Bridge 2114→2128 +14; SDK 383→390 +7; Hardhat 482 unchanged; agents 34→35)
Phase 191 — COMPLETE (Threat Succession Protocol — TSP; WIF-034 FORMAL; ProtocolMaturityScoringAgent 8-component v2; threat_forecast_accuracy_component(weight=0.07 from PIR harness_score)+biometric_stationarity_component(weight=0.04 from BSO confidence); _WEIGHTS v2: sep 0.25→0.20, fresh 0.15→0.12, cal 0.15→0.12, sum=1.00; neutral 0.5 when no data; tsp_enabled:bool=True config; idempotent ALTER TABLE migration; get_threat_forecast_accuracy() store; GET /agent/protocol-maturity-score +2 keys; ProtocolMaturityScoringResult +2 slots(default=0.0); openapi ProtocolMaturityScoreStatus; 8 bridge + 4 SDK tests; Bridge 2106→2114 +8; SDK 379→383 +4; Hardhat 482 unchanged; Tools 137→138)
Phase 190 — COMPLETE (LivePresenceSignalingAgent — agent #34; bidirectional presence channel; 8 bus subscriptions; 8 signal types LED+haptic vocabulary; live_presence_signaling_enabled=False default; 8 bridge+4 SDK; Bridge 2098→2106; SDK 375→379; agents 33→34; Tools 137→138)
Phase 189 — COMPLETE (ProtocolIntelligenceRecordAgent — agent #33; PIR chain hash sequence; pir_chain_enabled=False default; 8 bridge+4 SDK; Bridge 2090→2098; SDK 371→375)
Phase 188 — COMPLETE (BiometricStationarityOracleAgent — agent #32; drift cause classifier; biometric_stationarity_enabled=False default; 8 bridge+4 SDK; Bridge 2082→2090; SDK 367→371)
Phase 187 — COMPLETE (AttestationOpSecAdvisorAgent agent #31 + VHPReenrollmentBadge agent contract LIVE; mempool_opsec_enabled=False default; reenrollment_badge_enabled=False default; VHPReenrollmentBadge.sol 0x42E7A25d0E5667BBae45e5cF33a6e2CC6E42d45C; 16 bridge+8 SDK; Bridge 2066→2082; SDK 359→367)
Phase 186 — COMPLETE (AttestationBoundRenewalAgent — agent #30; validates attestation at renewal; attestation_bound_renewal_enabled=False default; 8 bridge+4 SDK; Bridge 2058→2066; SDK 355→359)
Phase 185 — COMPLETE (ReEnrollmentAttestationAgent — agent #29; HMAC attestation tokens on persona break; reauth_attestation_enabled=True; 8 bridge+4 SDK; Bridge 2050→2058; SDK 351→355)
Phase 184 — COMPLETE (PostCode Sweep — Skill 14 guardrail; test_t184_3 schema_versions 182+183; PersonaBreakResult+MaturityElevationResult @dataclass(slots=True); 10 bridge+4 SDK; Bridge 2040→2050; SDK 347→351)
Phase 183 — COMPLETE (MaturityElevationGateAgent — agent #28; maturity elevation plan; maturity_elevation_enabled=True; 8 bridge+4 SDK; Bridge 2032→2040; SDK 343→347)
Phase 182 — COMPLETE (PersonaBreakDetectorAgent — agent #27; LOO centroid drift; persona_break_detection_enabled=True; 8 bridge+4 SDK; Bridge 2022→2032; SDK 337→343; agents 26→27)
Phase 181 — COMPLETE (Renewal Provenance — corpus_delta_detected on BiometricRenewalResult; ceremony_audit_registry_address on VAPICeremonyAuditGate; SDK slot+init fixes; 4 bridge+4 SDK tests; Bridge 2022 unchanged; SDK 337 unchanged)
<!-- Phase 131-180 verbose blocks archived to CLAUDE_HISTORY.md on 2026-04-11 -->
## Architecture at a Glance

| Layer | Language | Key files |
|-------|----------|-----------|
| Controller anti-cheat | Python | `controller/tinyml_biometric_fusion.py`, `controller/dualshock_integration.py`, `controller/l6_trigger_driver.py`, `controller/l6_response_analyzer.py`, `controller/temporal_rhythm_oracle.py`, `controller/hid_xinput_oracle.py`, `controller/l2b_imu_press_correlation.py`, `controller/l2c_stick_imu_correlation.py` |
| Bridge service | Python asyncio | `bridge/vapi_bridge/` — `insight_synthesizer.py`, `bridge_agent.py`, `calibration_intelligence_agent.py`, `behavioral_archaeologist.py`, `network_correlation_detector.py`, `federation_bus.py`, `alert_router.py` |
| Smart contracts | Solidity | `contracts/` — `PoACVerifier.sol`, `PHGRegistry.sol`, `PHGCredential.sol`, `TournamentGateV3.sol`, `PITLSessionRegistry.sol`, `SkillOracle.sol`, `FederatedThreatRegistry.sol` |
| Scripts | Python | `scripts/threshold_calibrator.py`, `scripts/run_adversarial_validation.py` (9-feature proxy Phase 49), `scripts/interperson_separation_analyzer.py`, `scripts/l6_threshold_calibrator.py`, `scripts/phase_coherence_calibration.py` (negative result, keep), `scripts/generate_professional_adversarial.py` (Phase 48) |
| Calibration data | JSON | `sessions/human/hw_005` through `sessions/hw_078` (N=74, 3 players) |
| Frontend dashboard | React JSX | `frontend/VAPIDashboard.jsx` — 850+ lines, void-black + electric orange + cyan |
| Whitepaper | Markdown | `docs/vapi-whitepaper-v3.md` |

## PoAC Wire Format — FROZEN, DO NOT MODIFY

228 bytes total: 164-byte signed body + 64-byte ECDSA-P256 signature.
Chain link hash = SHA-256(raw[0:164]) — 164-byte body only, NOT full 228 bytes.

## PITL Nine-Level Stack

| Layer | Code | Type | Signal |
|-------|------|------|--------|
| L0 | — | Structural | HID presence |
| L1 | — | Structural | PoAC chain integrity |
| L2 | 0x28 | Hard cheat | IMU gravity + HID/XInput discrepancy |
| L3 | 0x29/0x2A | Hard cheat | TinyML behavioral classifier |
| L2B | 0x31 | Advisory | IMU-button causal latency |
| L2C | 0x32 | Advisory | Stick-IMU cross-correlation (inactive in dead-zone stick games) |
| L4 | 0x30 | Advisory | 12-feature Mahalanobis biometric fingerprint |
| L5 | 0x2B | Advisory | Temporal rhythm (CV, entropy, quantization) |
| L6 | — | Advisory | Active haptic challenge-response (disabled by default) |

Hard codes {0x28, 0x29, 0x2A} block tournament eligibility.
L2C returns None in dead-zone stick games (NCAA CFB 26) — 0.10 weight resolves to 0.5 neutral prior.

## Calibration Corpus State (2026-04-11) — 3-PLAYER CORPUS (P4 ELIMINATED)

- Total session files: **153 terminal + ~64 hw = 217 total** (5 excluded; massive new captures 2026-04-11)
  - Player 1: 50 terminal sessions (hw_005–hw_042 exc. 2 polling-rate + terminal_cal_P1; **8 touchpad_corners sessions**)
  - Player 2: 55 terminal sessions (terminal_cal_P2; **11 touchpad_corners sessions**)
  - Player 3: 48 terminal sessions (terminal_cal_P3; **10 touchpad_corners sessions**)
  - Player 4: **ELIMINATED** — confirmed same person as Player 3; all terminal_cal_P4 files moved to terminal_cal_P3
  - 5 excluded (polling_rate_hz outside [800, 1100]: hw_043, hw_044, hw_067, hw_069, hw_073)
- **CURRENT STATE (2026-04-20, UPDATED): AIT DEFENSIBILITY GATE CLEAR — all players >=10 sessions**
  - **AIT probe (Phase 229, 2026-04-18)**: ratio=**1.199**, all_pairs_above_1=**True** (N=24: P1=6/P2=5/P3=13) — Phase 229 baseline
  - **AIT corpus (Phase 231, 2026-04-20)**: N=37 total — P1=13/P2=10/P3=14; all players >=10; ait_defensibility_ok=True; STAGED_GRADUATION_ENABLED=true
  - AIT inter-player: P1vP2=1.850, P1vP3=1.846, P2vP3=1.349 — ALL >1.0 (TOURNAMENT BLOCKER CLEARED for AIT)
  - AIT LOO accuracy: 66.7% (16/24); cov_mode=full (N/p=6.0 > COV_MIN_RATIO=3.0)
  - AIT features [4]: accel_tremor_peak_hz (4096-pt FFT 4-15 Hz, parabolic interp) + roll_cos/roll_sin/pitch_cos (gravity postural fingerprint, circular encoding)
  - AIT physics: L2 hold at 50% (90-180 analog), 30s; still-hold activates accel tremor (right_stick=128 neutral); gravity vector anatomically stable per player in normal gaming posture
  - **Touchpad corners (superseded for primary gate)**: N=35, ratio=0.728 (2026-04-11); P2/P3 biometric proximity structurally prevents crossing 1.0; diagnostic ceiling confirmed
  - **tremor_resting corpus (2026-04-12)**: ratio=0.748 (N=24); all_pairs_p0_ok=False (P1vP3=0.032 per G-001); tremor_peak_hz now non-zero via Phase 205 AccelTremorFFT; P3 non-stationarity still limiting
  - **Path forward**: AIT defensibility gate CLEAR (Phase 231 — P1=13/P2=10/P3=14 all>=10); run POST /agent/activate-graduation-stage {agent_id: "ruling_enforcement_agent"} to execute Stage 1; then run --session-type ait --write-snapshot to persist N=37 corpus to DB
  - NOTE: Phase 231 COMPLETE — ait_defensibility_ok=True (11th P0 condition); STAGED_GRADUATION_ENABLED=true; Stage 1 pending API call; Phase 230 NOTE: insert_ait_session() mirrors to separation_defensibility_log; all_pairs_p0_ok=True
  - WIF-024: CLOSED Phase 165 — post_erasure_recompute audit trail implemented
- **Full corpus (N=217, all session types)**: ratio=0.060 — EXPECTED/KNOWN (free-form gameplay doesn't separate players; this is the WIF-009 plateau regime result; never use this as the tournament gate metric)
- **PHASE 143 RESULT (2026-04-02): N=11 — historical baseline (superseded by N=14 above)**
  - Separation ratio: **1.261** (diagonal covariance, N/p=1.375 < 3.0, Phase 142 auto-fallback)
  - Classification: **63.6% (7/11, proper LOO)** — honest estimate (Phase 143); 4 misclassified sessions
  - Inter-player pairs: P1 vs P2=2.868, P1 vs P3=3.276, P2 vs P3=2.243
  - Intra-player: P1 mean=2.963 (N=3), P2 mean=1.976 (N=4), P3 mean=1.711 (N=4)
  - NOTE: diagonal covariance correct for N=11; full Tikhonov suppressed P1/P3 to 0.127 (97% suppression)
  - Per-pair attribution: P1vP2 top=micro_tremor+stick_autocorr; P1vP3 top=touch_position_variance+touchpad_spatial_entropy
- **PHASE 138 RESULT (2026-04-02): Full Tikhonov covariance (SUPERSEDED by Phase 143)**
  - Separation ratio: **1.552** — inflated by full covariance; P1 vs P3 distance=0.127 was noise-suppressed
  - Classification: 63.6% (7/11, biased-centroid LOO) — same classification but different error profile
  - Inter-player pairs: P1 vs P2=1.428, P1 vs P3=0.127, P2 vs P3=1.304
  - Intra-player: P1 mean=0.839 (N=3, full covariance), P2 mean=0.505, P3 mean=0.499
  - **P1/P3 distance=0.127 was covariance noise artifact** — diagonal (Phase 142) gives P1vP3=3.276
- **PHASE 137B RESULT (2026-03-30): PRE-MERGE reference only**
  - Ratio was 1.469 (N=11, 4 players P1=3/P2=4/P3=3/P4=1) — P4 counted as separate → SUPERSEDED
  - P3 vs P4 distance=0.074 was intra-player variance (same person), incorrectly counted as inter-player
- **PHASE 137A RESULT (2026-03-30): WIF-007 balanced corpus confirmation**
  - Balanced ratio: **1.611** (n=3/player, N=12 balanced; seed=42; per-player equalization)
  - WIF-007 confirmed: P1's 53 sessions bias global covariance; balanced ratio >> pooled ratio
  - Reliable estimate requires ≥10 sessions/player balanced
- Full corpus separation ratio: **0.417 pooled** (N=127 pre-merge, 2026-03-29) — STALE, superseded by 1.261 (diagonal+LOO, touchpad_corners, Phase 143)
  - Classification rate on full corpus: 30.8% — free-form gameplay insufficient for separation
- L4 thresholds CONFIRMED (2026-04-02): ran threshold_calibrator.py on all 74 hw_*.json → anomaly=**7.009**, continuity=**5.367** — IDENTICAL to stored values; staleness is dimension-only (calib_dim=12 vs live_dim=13); touchpad_spatial_entropy is structurally 0 in gameplay sessions so adding it doesn't change thresholds; thresholds remain valid for gameplay sessions
- Phase 139 COMPLETE: _TERMINAL_CAL_ONLY_TYPES fast-path in analyze_interperson_separation.py — skips 74 hw_* sessions when session_type_filter in {touchpad_corners, freeform, swipes, ...}; reduces analysis runtime from 120s+ to <30s; Bridge +8 (1734→1742); SDK 233 unchanged; Hardhat 462 unchanged
- Phase 144 COMPLETE: --player-quality-report flag; _compute_player_quality_scores() per-player stability/probe-type/enrollment-ready/recommendations; ENROLLMENT_STABILITY_THRESHOLD=0.70 ENROLLMENT_MIN_PROBE_TYPES=2; Bridge +8 (1774→1782); SDK 233 unchanged; Hardhat 462 unchanged
- Phase 140 COMPLETE: --probe-comparison flag; runs all 3 touchpad probe types (corners/freeform/swipes) and outputs comparison table with ratio/classification/inter/intra/P1vP3; Bridge +8 (1742→1750); SDK 233 unchanged; Hardhat 462 unchanged
- Touchpad coverage: P1=6 touchpad_corners, P2=7 touchpad_corners, **P3=7 touchpad_corners** (total 20, 2026-04-05)
  - touchpad_freeform and touchpad_swipes: roughly symmetric with corners; exact counts from analysis script

## L4 Calibration State (Phase 57, N=74)

- Calibration corpus: hw_005–hw_078 (N=74 including newer tremor/touchpad sessions)
- Feature space: 12 features, 10 active (Phase 46 added accel_magnitude_spectral_entropy; Phase 57 added press_timing_jitter_variance)
- Active features (10): trigger_resistance_change_rate(excl), trigger_onset_velocity_L2,
  trigger_onset_velocity_R2, micro_tremor_accel_variance, grip_asymmetry,
  stick_autocorr_lag1, stick_autocorr_lag5, tremor_peak_hz, tremor_band_power,
  accel_magnitude_spectral_entropy, touch_position_variance(excl pending recapture),
  press_timing_jitter_variance (index 11 — normalised IBI variance; human 0.001–0.05; bot macro <0.00005)
- Structurally zero / excluded: trigger_resistance_change_rate, touch_position_variance
  (touchpad_active_fraction replaced by accel_magnitude_spectral_entropy in Phase 46)
- L4 anomaly threshold: **7.009** (mean+3σ, Phase 57, N=74, 12-feature space — was 6.726 Phase 46)
- L4 continuity threshold: **5.367** (mean+2σ, Phase 57, N=74, 12-feature space — was 5.097 Phase 46)
- Threshold rise (+4.2%/+5.3%): expected — press_timing_jitter_variance adds real variance, expands Mahalanobis distribution
- Inter-person separation ratio: 0.362 — L4 is intra-player anomaly detector only
- Human false positive rate: ~2.9% (expected at 3σ)

## accel_magnitude_spectral_entropy (Phase 46, index 9)

Replaces structurally-zero touchpad_active_fraction.
Physics: Shannon entropy of the 0–500 Hz power spectrum of DC-removed ||accel||.
Requires 1000 Hz polling — cannot be computed on standard HID (125–250 Hz) devices.
Ring buffer: 1024 frames, follows Phase 41 pattern (returns 0.0 until filled).
Human range: 3–8.6 bits, tightly centered at 4.8–4.9 bits (std 1.303).
Static injection: 0.0 (variance guard). Random noise: ~9.0 bits (detectable).
Player means nearly identical (P1: 4.878, P2: 4.882, P3: 4.767) — bot-vs-human
discriminator only, NOT inter-player identifier. Does not improve separation ratio.
Negative result documented: docs/phase-coherence-calibration.md (accel_phase_coherence
ruled out — gravity dominates accel during still frames in handheld gaming grip).

## Humanity Probability Formula (Phase 46)

Without L6 (default):
  humanity_probability = 0.28·p_L4 + 0.27·p_L5 + 0.20·p_E4 + 0.15·p_L2B + 0.10·p_L2C
  NOTE: p_L2C resolves to 0.5 neutral prior in dead-zone stick games (NCAA CFB 26).
  Formula runs as effective 4-signal in practice for this game corpus.

With L6 active:
  p_human = 0.23·p_L4 + 0.22·p_L5 + 0.15·p_E4 + 0.15·p_L6 + 0.15·p_L2B + 0.10·p_L2C

## Phase Summary

| Phase | Key milestone |
|-------|---------------|
| 17 | L4 feature space 7→11; L2B/L2C oracles added |
| 38 | Mode 6 living calibration active (α=0.95, ±15%/cycle, 6h) |
| 41 | Full covariance L4; ZK inference code binding |
| 43 | L6 human response baseline; bridge 865→877 |
| 45 | accel_phase_coherence NEGATIVE RESULT — gravity dominates; reverted; documented |
| 46 | accel_magnitude_spectral_entropy shipped; thresholds 7.019→6.726 / 5.369→5.097; bridge 880 |
| 47 | L2C phantom weight closed — PITL layer table live INACTIVE indicator; stale threshold labels fixed |
| 48 | Professional adversarial data — 3 white-box attack classes (G/H/I), 15 sessions, 4 bridge tests; bridge 884 |
| 49 | Tremor FFT window 513→1025 positions (512→1024 velocity samples); 1.95→0.977 Hz/bin; 4 bins across 8–12 Hz band; batch validator 7→9 features; bridge 888 |
| 50 | CalibrationIntelligenceAgent peer (6 tools, 30-min event consumer, min() enforcement); BridgeAgent +3 tools +2 behaviors; InsightSynthesizer Mode 6 callback; agent_events/threshold_history/calibration_agent_sessions tables; /calibration/agent + /calibration/stream endpoints; bridge 902 |
| 51 | Game-Aware Profiling — NCAA CFB 26 profile (R2-first L5 priority, 11-entry button map); L6-Passive passive R2 onset tracking (no PS5 conflict); GameProfile registry; get_game_profile BridgeAgent tool #21; CORS/port/batcher/MQTT dev fixes; calibration_agent timeout 180→600s; bridge 915 |
| 52 | Runtime hardening — `_run_ds_with_restart()` (3 auto-restarts); `pitl_meta=None` NameError fix; WS broadcast `except pass` → `log.error`; CORS `:5174` fallback; `hardware_block.controller_connected` init False; CalibIntelAgent failure counter; batcher gas dead-letter + `retry_task.add_done_callback`; ProactiveMonitor decoupled from agent instance; bridge 915 (pure hardening, no new tests) |
| 53 | Serialization hardening — `_safe_val()` NaN/Inf→None wrapper on all WS float fields; `_pending_pitl_meta` reset per loop; `controller_registered` WS event on device connect (frontend triggers fetchSnapshot); chain gas/revert permanent vs transient log discrimination; schema_versions Phase 51; `retry_task.add_done_callback`; 21 new tests; bridge 936 |
| 54 | Runtime hardening — numpy fallback ImportError fix (NCD `build_distance_matrix`); `_task_done_handler` CRITICAL log on all 11 managed tasks; `send_raw_transaction` nonce reset on send failure; WS receive 60s timeout (`ws_records`/`ws_frames`); store migration `log.debug`; fetchSnapshot abort dedup; WS reconnect exponential backoff 5→60s; bridge 941 |
| 55 | ioID Device Identity — `VAPIioIDRegistry.sol`; `ioid_devices` table; DID `did:io:0x<addr>` in PITL metadata + WS; `ensure_ioid_registered()` + `ioid_increment_session()` chain calls; `get_ioid_status` BridgeAgent tool #22; 5 tests; bridge 946 |
| 56 | ZK Tournament Passport — `TournamentPassport.circom` (5 public signals); `PITLTournamentPassport.sol` (mock mode, SESSION_COUNT=5); `tournament_passports` table; `generate_tournament_passport` tool #23; `POST /operator/passport`; 5 tests; bridge 951 |
| 57 | Jitter Variance Feature — `press_timing_jitter_variance` index 11; `_BIO_FEATURE_DIM` 11→12; IBI deques (Cross/L2/R2/Triangle, maxlen=50); `_press_timing_jitter_variance()` static method; behavioral_archaeologist FEATURE_KEYS updated; threshold_calibrator `_extract_jitter_variance()`; 5 tests; bridge 956 |
| 58 | Security Hardening + BridgeAgent Expansion — operator endpoint auth (x-api-key → 401/503); sliding-window rate limiter; operator_audit_log table + log_operator_action/get_operator_audit_log; inference_code column in pitl_session_proofs; BridgeAgent tools #24–27 (analyze_threshold_impact, predict_evasion_cost, get_anomaly_trend, generate_incident_report); 16 tests; bridge 972 |
| 59 | My Controller 3D Digital Twin — physics-driven DualShock Edge CFI-ZCP1 twin page; get_ibi_snapshot() on BiometricFeatureExtractor; ibi_snapshot in pitl_meta + /ws/records; /ws/twin/{device_id} device-scoped fusion WS; GET /controller/twin/{id} + /chain REST; BridgeAgent tool #28 get_controller_twin_data; React Three Fiber + Rapier + Drei frontend (controller-twin.html); IBI Biometric Heartbeat canvas; PoAC DNA Helix; ProofAnchorPanel (ioID DID + ZK passport + separation ratio disclaimer); chain timeline scrubber; 16 tests; bridge 988 |
| 60 | My Controller Enhanced Visualization (Phase 60A) — 4-tab left panel: HEARTBEAT / RADAR / L5 RHYTHM / BIOM MAP; BiometricRadar (12-spoke canvas, mean_json, BIO_NORM[12]); L5RhythmOverlay (per-button CV bars + entropy gauge + quant flag, pitl_l5_cv/pitl_l5_entropy); BiometricScatter (2D tremor×jitter cross-section, bot zone, human 2σ ellipse, N=74); ProofShareQR (QRCode npm, IoTeX explorer deeplink, copy URL); qrcode dep; zero backend changes; bridge 988 unchanged |
| 61 | Session Replay + Feature History Scatter — frame_checkpoints table (deque maxlen=60, 20 Hz); store_frame_checkpoint/get_frame_checkpoint/list_checkpoints_for_device; _replay_ring deque + checkpoint storage in _dispatch; /replay + /checkpoints + /features endpoints; BridgeAgent tool #29 get_session_replay; useReplayMode + useFeatureHistory hooks; BiometricScatter history dots (cyan, DB feature vectors); chain tile ▶ indicator + replay status bar; Track C deployments blocked (wallet 0.43 IOTX); +12 tests; bridge 1000 |
| 62 | Player Enrollment + ZK Inference Code Binding — EnrollmentManager (auto PHGCredential mint after enrollment_min_sessions=10 NOMINAL sessions); device_enrollments table + 4 store methods; config enrollment_min/humanity_min; GET /enrollment/status/{device_id}; BridgeAgent tool #30 get_enrollment_status; PitlSessionProof.circom C3 constraint (inferenceResult === inferenceCodeFromBody); C1 Poseidon 7→8 inputs (adds inferenceCodeFromBody); mock proof commitment includes inference_result; PITLSessionRegistryV2.sol + deploy script; Phase 62 ceremony re-run; artifacts updated; +26 tests; bridge 1026 |
| 63 | L6b Neuromuscular Reflex Layer — first reactive involuntary probe; L6B_PROBE profile (id=8, amplitude 60/255, sub-perceptual); L6bReflexAnalyzer (accel-mag delta, BOT<15ms/HUMAN 80-280ms); l6b_probe_log SQLite table + insert_l6b_probe/get_l6b_baseline; 5 new config fields (l6b_enabled/probe_interval/accel_threshold/human_min_ms/human_max_ms); 4-way humanity formula (baseline/L6/L6b/both); pitl_meta l6b_* fields; BridgeAgent tool #31 get_reflex_baseline; profile 8 excluded from L6 active rotation; L6B_ENABLED=false default; +26 tests; bridge 1056 |
| 64 | SDK Phase 63 Parity — SDK v2.0.0-phase64; 0x31 IMU_PRESS_DECOUPLED + 0x32 STICK_IMU_DECOUPLED in INFERENCE_NAMES; is_advisory updated; VAPIEnrollment (GET /enrollment/status poll); VAPIZKProof (Groth16 C3 validator, PROOF_SIZE=256, N_PUBLIC=5); L2B added as 5th self_verify() layer; openapi.yaml Enrollment tag + EnrollmentStatus schema + YAML alias fix; SDK 28→40; +12 SDK tests |
| 65 | Autonomous Intelligence Layer (AIL) — agent_rulings table (commitment_hash, attestation_hash, verdict/confidence/reasoning, dry_run); insert_agent_ruling/get_agent_rulings/get_agent_ruling_by_id store methods; SessionAdjudicator background agent (5-min poll, claude-opus-4-6, rule fallback); GET /agent/rulings + POST /agent/adjudicate + POST /agent/interpret endpoints; BridgeAgent tools #32 get_autonomous_rulings + #33 request_adjudication; vapi_agent.py (VAPIAgent + AgentRuling dataclass, PoAC-gated commitment formula); BLOCK/CERTIFY require all_layers_active=True; dry_run=True default; Agent tag + 6 OpenAPI schemas; +20 bridge tests + +15 SDK tests; bridge 1076; SDK 55 |
| 66 | Ruling Enforcement Pipeline (REP) — closes Phase 65 enforcement loop; ruling_streaks + on_chain_rulings tables + 6 store methods; RulingEnforcementAgent (5-min poll, streak escalation FLAG×5→HOLD/HOLD×2→BLOCK); RulingRegistry.sol (anti-replay commitmentHash, deviceRulings history, onlyOperator); chain.record_ruling_on_chain() IoTeX testnet; PHGCredential.suspend() on BLOCK (24h default, 7d if warmup_attack_score>0.7); POST /agent/override operator endpoint; BridgeAgent tools #34 get_ruling_streak + #35 override_ruling; 3 config fields (ruling_enforcement_enabled, ruling_streak_block_threshold, ruling_registry_address); +30 bridge tests + +10 Hardhat tests; bridge 1106; Hardhat 364 |
| 67 | MPC Ceremony Hardening — Phase 66 hotfix (store_credential_suspension arg order bug); credential auto-reinstate loop (_check_expired_suspensions); GET /agent/suspension-status endpoint; CeremonyRegistry.sol (on-chain MPC audit trail, IoTeX-block beacon anchor); run-mpc-ceremony.js (3-contributor Hermez ptau ceremony); deploy-ceremony-registry.js; ZKVerifier (local Groth16 pre-verify via Node.js subprocess); chain.record_ceremony_on_chain(); config.ceremony_registry_address; VAPIZKProof.verify_ceremony_integrity() SDK method; openapi.yaml SuspensionStatus schema + path; +20 bridge tests + +8 Hardhat tests + +4 SDK tests; bridge 1126; Hardhat 372; SDK 59 |
| 68 | Production Enforcement + Synergistic Tooling — RulingRegistry `0xa3A2356C90E642a7c510d0C726EC515EA720c621` + CeremonyRegistry `0x739B5fae312834bA2a7e44525bA5f54853C5672f` LIVE; MPC ceremony complete (3 circuits × 3 contributors, IoTeX block #41723255 beacon, verifyCeremony() OK); ZKVerifier wired into submit_pitl_proof() (fail-open); config.pitl_vkey_path + config.agent_dry_run_mode; POST /agent/config operator toggle; BridgeAgent tools #36–40; +32 bridge tests; bridge 1158; 23 contracts ALL LIVE |
| 236-WATCHDOG | Bridge Process Watchdog + Watchdog Event Chain (WEC) — VAPI-exclusive supervisor for the GIC grind. scripts/bridge_watchdog.py NEW (urllib stdlib only): polls /health every 10s; spawns bridge via subprocess.Popen([sys.executable, "-m", "bridge.vapi_bridge.main"]); exp backoff 5/10/30/60s capped at auto_grind 60s cadence; 3 restarts/hr ceiling → WATCHDOG_HALT. Three guards make it VAPI-architectural (not generic supervisord): (1) refuses restart when chain_intact=False — preserves INV-GIC-003 at ops layer; (2) refuses restart when GRIND_SESSION_ID drifts in bridge/.env — silent-rotation guard; (3) restart-rate ceiling. bridge/vapi_bridge/watchdog_chain.py NEW: WEC FROZEN FORMULA v1 — WEC_N=SHA-256(prev(32)||code(1)||pid(4)||sid_hash(16)||ts_ns_be(8))=61B→32B; genesis tag VAPI-WEC-GENESIS-v1; sid_hash=SHA-256(grind_session_id)[:16]; EVENT_CODES (BRIDGE_START 0x01 / HEALTHY 0x02 / UNRESPONSIVE 0x03 / RESTART_TRIGGERED 0x04 / RESTART_REFUSED_GIC 0x05 / RESTART_REFUSED_SID 0x06 / DEGRADED_HOST 0x07 / BACKOFF_CEILING 0xFE / HALT 0xFF). store.py: watchdog_event_log table (Phase 236 migration; 2 indexes); insert_watchdog_event() with monotonicity guard (ts_ns≤prev → bumped to prev+1); get_watchdog_event_chain_status() recomputes full chain + reports restarts_last_hour. operator_api.py: GET /operator/watchdog-status (10 keys; _check_read_key auth). T236-WD-1..8: 8 bridge tests (genesis det / single-link / 3-event chain / tamper detect / monotonicity / session scoping / restart count / invalid input ValueError). Pairs with GIC chain (Phase 235-A): GIC=cognitive-session continuity, WEC=operational continuity. Together = tamper-evident provenance for grind run. Bridge 2477→2485 +8; SDK 535 unchanged; Hardhat 522 unchanged; Contracts 45 unchanged |
| 235-GAD | Gameplay Activity Discrimination — binary trigger-active gate on consecutive_clean; ACTIVE_GAMEPLAY=any L2/R2 press (trigger_active_fraction>0); MENU_DETECTED=zero presses (breaks streak); NULL=pass-through (pre-GAD rows); gameplay_discrimination_enabled:bool=True config; trigger_active column on records; gameplay_context column on ruling_validation_log; gameplay_classification_disagreements table; dualshock_integration.py: trigger_active derived from trigger_onset_velocity_l2/r2>0; session_adjudicator.py: trigger_active_fraction in evidence_summary; session_adjudicator_validator.py: gameplay_context from evidence + 4th GIC eligibility condition (gameplay_ok=ctx!="MENU_DETECTED"); operator_api.py: GET /bridge/capture-health +gameplay_context_enabled+latest_gameplay_context; GET /bridge/grind-chain-status +latest_gameplay_context; POST /operator/override-gameplay-context; 10 bridge+2 SDK; Bridge 2434→2444; SDK 525→527 |
| 235-GPC | Pre-Grind Validation — 10-category verification before 100-session grind; regression fixes: pcc_state/pcc_host_state backfilled in 7 test files + activation_simulation.py seed_validation_records(); store.get_prev_gic_ts_ns() NEW; session_adjudicator_validator: time.time_ns()+monotonicity guard (INV-GIC-002); main.py: import os + GRIND_SESSION_ID startup log (warns if env var not set); operator_api.py: POST /operator/gic-reset (requires api_key query param + reason≥10 chars; clears app._gic_chain_broken; logs to agent_events; was_broken+reason+timestamp response); bridge/tests/test_phase235_grind_precheck.py: 4 tests (T235-GPC-1: LLM exception→fallback deterministic; T235-GPC-2: llm_verdict==fallback_verdict→divergence=0→consecutive_clean advances; T235-GPC-3: gic-reset 403 without key; T235-GPC-4: gic-reset clears flag+returns JSON); docs/pre_grind_validation_report.md NEW (10-category report); 4 bridge; Bridge 2430→2434; SDK 525; Hardhat 522 |
| 235-A | Grind Integrity Chain (GIC) — FROZEN SHA-256 chain over grind sessions; GIC_N=SHA-256(prev(32)||ch(32)||verdict(1)||host(1)||ts_ns_be(8)); genesis_gic(grind_session_id,ts_ns); only fallback_verdict hashed (not llm_verdict); grind_chain.py NEW (VERDICT_CODES/PCC_HOST_CODES/genesis_gic/compute_gic); config grind_session_id; store: gic_ts_ns column+get_prev_grind_chain_hash+update_grind_chain_hash+get_grind_chain_status; operator_api: GET /bridge/grind-chain-status; GrindChainResult(7 slots)+VAPIGrindChain SDK; startup chain integrity check; 8 bridge+4 SDK; Bridge 2422→2430; SDK 521→525 |
| 235-PCC-SPC | Statistical Process Control + 3-signal haptic-tolerance binding + frequency-band gate + outlier trim. Data-anchored from session 2026-05-03 (887 PCC snapshots / 26.6K record obs). capture_continuity.py: 5-tuple sample buffer (rate, ts, trigger_active, accel_var, tremor_peak_hz); _classify_capture_state_spc decision tree (haptic-tolerance → SPC capability → classic fallback); _haptic_tolerance_active 3-signal binding (trigger_active=1 AND accel_var≥0.0003 AND tremor_peak in [4,60]Hz, window≤500ms). dualshock_integration.py: _last_bio_features snapshot + 3 update_sample call sites pass kwargs. config: 8 SPC fields + _env_float helper (USL=3500, in_control=0.85, freq band [4,60]Hz). PV-CI 32→36 (+INV-PCC-002/003/004/005). 15 tests T-SPC-1..15. Pcc_spc_enabled=False default (opt-in via PCC_SPC_ENABLED env). When False, classifier byte-identical to Phase 234.7. INV-PCC-003 fail-closed precedence: signal_disconnect ALWAYS overrides SPC. Three Novel Assurances accepted with bindings: A (3-signal haptic evidence) + B (LSL strict, USL trim only) + C (disconnect override). Wallet zero. Bridge 2517→2532 +15; SDK 539 unchanged; Hardhat 528 unchanged |
| 235-B | PCC Attestation Slot — ruling_validation_log +pcc_state+pcc_host_state+grind_chain_hash+gic_ts_ns (Phase 2350 migration); consecutive_clean PCC gate: pcc_state=NOMINAL AND pcc_host_state in (EXCLUSIVE_USB,UNKNOWN) required (NULL=fail-closed); session_adjudicator_validator: PCC snapshot via get_capture_health_status() at validation time; 4 bridge+2 SDK; Bridge 2418→2422; SDK 519→521 |
| 234.7 | Physical Capture Continuity (PCC) — CaptureHealthMonitor (NOMINAL/DEGRADED/DISCONNECTED + EXCLUSIVE_USB/CONTESTED/UNKNOWN host state inference via poll-rate CV); grind_ready gate (NOMINAL+EXCLUSIVE_USB/UNKNOWN+30s sustained); session_counting_paused=grind_mode AND NOT grind_ready; 6 config fields (pcc_enabled/pcc_nominal_hz/pcc_degraded_hz/pcc_stable_window_s/grind_mode/grind_target); capture_health_log store; GET /bridge/capture-health (11 keys); CaptureHealthResult(11 slots)+VAPICaptureContinuity SDK; DualShockTransport.set_pcc_monitor() wiring; 10 bridge+4 SDK; Bridge 2408→2418; SDK 515→519 |
| 234 | InsightSynthesizer HTTP Timeout Fix — Mode 6 _synthesize_living_calibration_sync extracted; asyncio.to_thread wrapper prevents 54s event loop stall on startup; 8 bridge; Bridge 2400→2408; SDK 515; Hardhat 522 |
| 234.5 | Consecutive Clean Semantics Audit — divergence=verdicts_differ AND delta>0.30; advisory codes→FLAG→no divergence; clean sessions→FLAG(0.05); enrollment eligible→CERTIFY(0.8); same verdict never diverges; no code changes |
| 231 | AIT Defensibility P0 Gate + Stage 1 Graduation — ait_defensibility_ok 11th P0 condition (all_pairs_above_1=True AND all players >=10 AIT sessions; fail-closed); store.py: ALTER TABLE +ait_defensibility_ok; get_ait_separation_status() +n_per_player; operator_api.py: ait_defensibility_ok in overall_pass+conditions+response+commit-activation blocker; TournamentPreflightResult 11 slots; openapi +ait_defensibility_ok; analyze_interperson_separation.py Phase 231 write-snapshot gap fixed; STAGED_GRADUATION_ENABLED=true; AIT corpus P1=13/P2=10/P3=14 N=37 all>=10; 8 bridge+4 SDK; Bridge 2392→2400; SDK 511→515 |
| 230 | AIT P0 Gate Wire-up — insert_ait_session() mirrors into separation_defensibility_log (session_type='ait'); tournament_preflight all_pairs_p0_ok reads AIT; operator_api.py bugfix: get_biometric_renewal_chain_status(limit=1)→() + len()>0→total_renewals check; heals Phase 127 test_4; 8 bridge; Bridge 2384→2392; SDK 512; Hardhat 522 |
| 229 | AIT Separation Breakthrough — Active Isometric Trigger 4-feature pipeline [accel_tremor_peak_hz, roll_cos, roll_sin, pitch_cos]; ratio=1.199 all_pairs_above_1=True N=24; first probe type to clear all-pairs>1.0 gate; ait_session_log+insert_ait_session()+get_ait_separation_status(); GET /agent/ait-separation-status (_check_read_key); AITSeparationResult(8 slots)+VAPIAITSeparation SDK; 8 bridge+4 SDK; Bridge 2376→2384; SDK 508→512 |
| 228 | VHP-Gated Invariant Change Authorization — vhp_gated_invariant_change_enabled=False config; POST /agent/allowlist-governance-event gate: invariant_change requires vhp_token_id when enabled (missing→403; expired→403; fail-open on chain unreachable); store vhp_token_id on invariant_gate_log; chain.is_vhp_valid() view; INVARIANT_CHANGE_WITHOUT_VHP HIGH 11th CONTRADICTION rule in FSCA; VAPIAllowlistGovernance.post_invariant_change() SDK; 8 bridge+4 SDK; Bridge 2368→2376; SDK 504→508 |
| 227 | ProtocolCoherence On-Chain Provenance Anchor — anchorCoherenceWithProvenance() + CoherenceAnchoredWithProvenance event; getLatestGovernanceProvenance() view; anchorCoherence() backward-compat bytes32(0); store +governance_provenance_hash on protocol_coherence_log; chain.anchor_coherence_with_provenance(); _anchor_cycle() uses new fn when gov_prov_hash available; GOVERNANCE_PROVENANCE_ANCHOR_DRIFT CRITICAL 10th CONTRADICTION rule in FSCA; /agent/protocol-coherence-status +governance_provenance_hash; VAPIAllowlistGovernance.on_chain_provenance_hash(); 8 bridge+4 SDK+6 Hardhat; Bridge 2360→2368; SDK 500→504; Hardhat 516→522 |
| 226 | Invariant Scope Expansion INV-019..022 — freeze provenance hash computation code; +4 invariants (INV-019: _compute_governance_provenance_hash; INV-020: ts_ns.to_bytes(8,"big") replay-prevention; INV-021: _fetch_latest_provenance_hash; INV-022: governance_provenance_chain+insert_governance_provenance in store); INVARIANTS_ALLOWLIST.json 18→22 entries; 8 bridge+4 SDK; Bridge 2352→2360; SDK 496→500; Invariants 18→22 |
| 225 | InvariantGate Provenance Chain + Governance History API — governance_provenance_chain table; governance_provenance_hash = sha256(prev_prov||new_hash||category||text||ts_ns_8b); _fetch_latest_provenance_hash()+_compute_governance_provenance_hash() in gate script; POST /agent/allowlist-governance-event extended (+provenance fields); GET /agent/allowlist-governance-history (chain_intact check); GOVERNANCE_CHAIN_BROKEN CRITICAL 4th INVERSION rule in FSCA; VAPIAllowlistGovernance.previous_changes() → history endpoint; chain_intact() new SDK method; 8 bridge+4 SDK; Bridge 2344→2352; SDK 492→496; INVERSION rules 3→4 |
| 224 | InvariantGate On-Chain Anchor + Allowlist Governance — compute_allowlist_hash() exported; --reason required on --generate; --confirm-governance for invariant_change; _post_governance_event() fail-open; INV-016 (16 total); allowlist_change_log table; virtual allowlist leaf in ProtocolCoherenceAgent Merkle tree (38 leaves total); biometric_governance_agent added to _AGENT_IDS (36→37); ALLOWLIST_CHANGE_WITHOUT_GOVERNANCE CRITICAL FSCA rule; POST /agent/allowlist-governance-event; AllowlistGovernanceResult(7 slots)+VAPIAllowlistGovernance SDK; deploy Phase 221 ProtocolCoherenceRegistry LIVE+Phase 222 VAPIBiometricGovernance LIVE (both 2026-04-17); 8 bridge+4 SDK; Bridge 2336→2344; SDK 488→492; Contracts 45 ALL LIVE |
| 223 | PV-CI Protocol Invariant Gate — scripts/vapi_invariant_gate.py (15 INV-001..015; SHA-256 region hashing; --generate writes .github/INVARIANTS_ALLOWLIST.json); .github/workflows/vapi-invariant-gate.yml CI gate; invariant_gate_log; pv_ci_enabled=True; GET /agent/invariant-gate-status + POST /agent/run-invariant-gate (7 keys); InvariantGateResult(7 slots)+VAPIInvariantGate SDK; STALENESS FIX: sync_vapi_workflow.py rewritten (regex fix+AGENTS.md+INVARIANTS.md update fns+head -5 removed); 8 bridge+4 SDK; Bridge 2328→2336; SDK 484→488 |
| 222 | VAPIBiometricGovernance (BBG) — VHP-gated governance; VAPIBiometricGovernance.sol contract #45 (proposeWithVHP/isProposed/setVHPContract; STOLEN_KEY+VHP_EXPIRY+FLASH_LOAN guards); BiometricGovernanceAgent agent #38; bbg_proposal_log; GET /agent/bbg-status (7 keys); POST /agent/bbg-propose; BBGProposalResult(7 slots)+VAPIBiometricGovernance SDK; 10 bridge+4 SDK+8 Hardhat; Bridge 2318→2328; SDK 480→484 |
| 221 | ProtocolCoherenceRegistry (PoPC) — Merkle root over 36 agent fleet; ProtocolCoherenceRegistry.sol contract #44 (anchorCoherence/getLatestCoherence/isCoherent); ProtocolCoherenceAgent agent #37; protocol_coherence_log; GET /agent/protocol-coherence-status (7 keys); ProtocolCoherenceResult(7 slots)+VAPIProtocolCoherence SDK; 10 bridge+4 SDK+6 Hardhat; Bridge 2308→2318; SDK 476→480 |
| 220 | PerPairGapProjection — TGE timeline projections via velocity; per_pair_gap_projection_log; get_per_pair_gap_projection() projects days_to_1_0=(1.0-dist)/vel per pair; dedup by pair_key; projected_tge_date=max across blocker pairs; any_feasible=False for WORSENING/STABLE; GET /agent/per-pair-gap-projection (7 keys); PerPairGapProjectionResult(7 slots)+VAPIPerPairGapProjection SDK; 8 bridge+4 SDK; Bridge 2300→2308; SDK 472→476 |
| 219 | TournamentBlockerSummary — aggregated TGE blocker list; tournament_blocker_summary_log; get_tournament_blocker_summary() aggregates preflight+per_pair_gap+capture_velocity; severity P0/P1; GET /agent/tournament-blocker-summary (8 keys); TournamentBlockerSummaryResult(8 slots)+VAPITournamentBlockerSummary SDK; 8 bridge+4 SDK; Bridge 2292→2300; SDK 468→472 |
| 218 | CaptureVelocityOracle — unified capture health synthesis; capture_velocity_oracle_log; get_capture_velocity_oracle_status() synthesizes Phase 152 centroid_velocity + Phase 154 stagnation; recommended_action 4-enum; GET /agent/capture-velocity-oracle (9 keys); CaptureVelocityResult(9 slots)+VAPICaptureVelocityOracle SDK; 8 bridge+4 SDK; Bridge 2284→2292; SDK 464→468 |
| 217 | PerPairGapTrend — per-pair Mahalanobis gap velocity; per_pair_gap_trend_alert_log; get_per_pair_gap_trend() IMPROVING/WORSENING/STABLE/UNKNOWN; PER_PAIR_GAP_BLOCKER_UNRESOLVED 7th ORPHAN rule in FSCA (HIGH severity); GET /agent/per-pair-gap-trend (8 keys); PerPairGapTrendResult(8 slots)+VAPIPerPairGapTrend SDK; 8 bridge+4 SDK; Bridge 2276→2284; SDK 460→464 |
| 216 | PerPairGapLog — per-pair Mahalanobis inter-player distance log; per_pair_gap_log table; insert_per_pair_gap()/get_per_pair_gap_status(); blocker_pairs surfaces P1vP3=0.032/P2vP3=0.401; GET /agent/per-pair-gap-status (7 keys); PerPairGapResult(6 slots)+VAPIPerPairGap SDK; 8 bridge+4 SDK; Bridge 2268→2276; SDK 456→460 |
| 215 | L4DimSyncConfirmation — closes G-003 L4 staleness gap; l4_dim_sync_log; insert_l4_dim_sync()/get_l4_dim_sync_status(); confirms touchpad_spatial_entropy structurally zero → thresholds 7.009/5.367 valid for live_dim=13; GET /agent/l4-dim-sync-status (7 keys); L4DimSyncResult(7 slots)+VAPIL4DimSync SDK; 8 bridge+4 SDK; Bridge 2260→2268; SDK 452→456 |
| 213 | AccelTremorFFT FFT Resolution — zero-padded 4096-point FFT + parabolic sub-bin interpolation; _ACCEL_FFT_NFFT=4096; accel_fft_nfft Config field; /agent/accel-tremor-fft-status +2 keys (accel_fft_nfft/bin_width_hz); AccelTremorFFTResult +2 slots; 8 bridge+4 SDK; Bridge 2276→2284; SDK 452→456 |
| 212 | Autonomous Engineering Layer — unified_server.py 12→17 tools (+5: vapi_skill_state_sync/vapi_phase_advance_proposal/vapi_code_change_impact/vapi_engineering_decision/vapi_autonomous_gap_scan); WIF-040 STRUCTURALLY_CLOSED: vapi.md Reference state replaced with MCP directive; MANDATORY_INVARIANTS #12 converted to MCP-range directive; 8-gap canonical registry G-001..G-008; test delta prediction (Phase 180+ patterns); invariant risk registry CRITICAL/HIGH/MEDIUM; 8 bridge tests T212-1..8; Bridge 2268→2276 +8; SDK 452 unchanged; Hardhat 482 unchanged |
| 211 | Unified Knowledge Loop MCP Server — vapi-mcp/unified_server.py (12 tools, "vapi-unified" server name); MetaLearner clusters log.jsonl failures by 5 themes → dominant_blocker; HypothesisDeduplicator: (probe_type, phase_candidate) SHA-256[:16] fingerprint dedup across 3 WIF sources; UnifiedWIFCorpus: deduped WIF index from what_if_corpus/+wiki/what_if/+VAPI_WHAT_IF.md; WikiFeedback: auto-writes wiki/phases/phase_NNN.md on PASS (score>=0.70); ExperimentLedger: queryable log.jsonl stats; _validate_proposal() offline harness: 20 MANDATORY_INVARIANTS + 5-subscore rubric; 8 bridge tests T211-1..8; Bridge 2260→2268 +8; SDK 452 unchanged; Hardhat 482 unchanged |
| 208 | CorpusRatioRegressionGuard (WIF-039 W1+W2) — CorpusRegressionError(ValueError) raised by insert_separation_defensibility_log_guarded() when all_pairs_above_1 regresses after breakthrough with no override; corpus_ratio_regression_guard_log+corpus_regression_override_log tables; insert_corpus_regression_override()+get_corpus_regression_guard_status() store; corpus_ratio_regression_guard_enabled=False config; Ratio Provenance Chain SHA-256(prev_hash+ratio+N+probe_type+ts_ns); GET /agent/corpus-regression-guard-status (7 keys); CorpusRegressionGuardResult(8 slots)+VAPICorpusRegressionGuard SDK; openapi CorpusRegressionGuardStatus; 8 bridge+4 SDK; Bridge 2252→2260 +8; SDK 448→452 +4; Hardhat 482 unchanged; WIF-039 CLOSED |
| 206 | Corpus Validation + PragmaJudge Appealability pre-work — RULE 0.9 v2 (APPEAL_CONSENSUS_TIMEOUT_SECONDS=3600 + TIMEOUT_UPHELD; appeal_window_seconds/appeal_consensus_timeout_s as SubProtocolConfig governance params not frozen constants; APPEAL_QUORUM=0.90 frozen); --stratify-by-session-type flag (SESSION_TYPE_CONTAMINATION vs GENUINE_NON_STATIONARITY diagnostic); TremorRestingConvergenceOracle non-convergence branch (_N_NONCONV_THRESHOLD=5; consecutive_negative+non_convergence_detected fields; +2 bridge+2 SDK); docs/SEPARATION_DECISION_FRAMEWORK.md (Path A/B/C decision tree; 5-condition commitment gate); Bridge 2242→2244; SDK 442→444; hardware blocker: tremor_resting captures |
| 205 | AccelTremorFFT — BiometricFeatureExtractor accel magnitude FFT fallback for tremor_peak_hz; _STILL_HOLD_VAR_THRESHOLD=4.0 LSB²; still-hold detection: right_stick_x ring variance < threshold; accel FFT 1-15 Hz search; section 5.5 populates _accel_mag_ring before tremor FFT; accel_tremor_fallback_enabled:bool=True Config+constructor; GET /agent/accel-tremor-fft-status; AccelTremorFFTResult+VAPIAccelTremorFFT SDK; openapi AccelTremorFFTStatus; 12 bridge+8 SDK; Bridge 2230→2242; SDK 434→442 |
| 204 | IOSWARM_ACTIVE_NO_ADJUDICATIONS — 8th CONTRADICTION rule in FleetSignalCoherenceAgent; guard mechanism in _check_contradictions() for cfg-dependent gating; fires when ioswarm_enabled+ioswarm_adjudication_enabled=True AND total_adj=0 AND consensus_count>0; POST /agent/prime-ioswarm-adjudication seeds ioswarm_adjudication_log with 5 primer entries (WIF-038 W2); ioswarm_adjudication_primer_enabled=False default; IoSwarmAdjudicationPrimerResult+VAPIIoSwarmAdjudicationPrimer SDK; openapi IoSwarmAdjudicationPrimerStatus; 14 bridge+8 SDK; Bridge 2216→2230; SDK 426→434 |
| 203 | AgentContextRegistry — agent_context_log (UNIQUE agent_id+prompt_sha256); upsert_agent_context_hash()/get_agent_context_status()/get_all_agent_context_status(); main.py startup: SHA-256 of 3 LLM agent prompts registered at bridge start; GET /agent/context-integrity-status; CONTEXT_HASH_MISMATCH 4th INVERSION rule in FSCA (fires when any agent NOT registered); AgentContextIntegrityResult+VAPIAgentContextIntegrity SDK; agent_context_on_chain_enabled=False; 8 bridge+4 SDK; Bridge 2208→2216; SDK 422→426 |
| 202 | TremorRestingConvergenceOracle — tremor_convergence_log; velocity=(ratio_curr-ratio_prev)/N_delta; convergence_stable when velocity>=0 for 2 consecutive; GET /agent/tremor-convergence-status; RATIO_VELOCITY_NEGATIVE 6th ORPHAN rule in FSCA (blocks VHP MINT_QUORUM=0.80 when declining); TremorConvergenceResult+VAPITremorConvergence SDK; tremor_convergence_enabled=False; 8 bridge+4 SDK; Bridge 2200→2208; SDK 418→422 |
| 199 | Prototype Separation Gate Configurability + Tremor Resting Probe — all_pairs_gate_enabled:bool=True config (False=prototype mode bypasses per-pair P0 gate); tremor_resting in STRUCTURED_PROBE_TYPES; GET /agent/probe-gate-config-status + /agent/tremor-resting-probe-status; ProbeGateConfigResult+TremorRestingProbeResult SDK; openapi ProbeGateConfigStatus+TremorRestingProbeStatus; 8 bridge+4 SDK; Bridge 2184→2192; SDK 414→418 |
| 198 | Biometric TTL Decay Scaling — effective_ttl=base_ttl×(mean_decay_factor/0.50) clamped [0.25×,4.0×]; biometric_ttl_decay_scaling_enabled=False default; get_effective_biometric_ttl() store; GET /agent/biometric-ttl-scaling-status; BiometricTTLScalingResult+VAPIBiometricTTLScaling SDK; openapi BiometricTTLScalingStatus; 8 bridge+4 SDK; Bridge 2176→2184; SDK 410→414 |
| 197 | Per-Pair Separation P0 Gate — all_pairs_p0_ok 10th P0 condition; reads all_pairs_above_1 from separation_defensibility_log; fail-closed default=False; TournamentPreflightResult +all_pairs_p0_ok; commit-activation per_pair_separation_below_1.0 blocker; 8 bridge+4 SDK; Bridge 2168→2176; SDK 406→410 |
| 196 | Tournament Preflight v2 WIF-035 W1 closure — biometric_ttl_ok 9th P0 condition; (not ttl_expired) AND len(renewal_chain)>0; idempotent ALTER TABLE tournament_preflight_log; TournamentPreflightResult +biometric_ttl_ok; 8 bridge+4 SDK; Bridge 2160→2168; SDK 402→406 |
| 195 | Protocol Metabolism Index (PMI) — 9th maturity component weight=0.03; PMI=max(0.0,1.0-mean_orphan_hours/48.0); _WEIGHTS v3 (sep 0.18, fresh 0.11); Tool #149; PMIResult+VAPIProtocolMetabolism SDK; ProtocolMaturityScoringResult +pmi_component; 8 bridge+4 SDK; Bridge 2152→2160; SDK 398→402; Tools 148→149 |
| 193 | FleetSignalCoherenceAgent (agent #36) — CONTRADICTION(7)+ORPHAN(5)+INVERSION(3) rules; coherence_id=coh_+SHA-256[:16]; auto-WIF at N=3; fleet_coherence_enabled=True DEFAULT; RENEWAL_WITHOUT_ATTESTATION=CRITICAL; Tools #145-#147; 14 bridge+4 SDK; Bridge 2128→2142 +14; SDK 390→394 +4; agents 35→36 |
| 192 | CorpusDataCuratorAgent (agent #35) — 7-task data coherence: Provenance DAG + Corpus Entropy (threshold 1.5) + Proof-of-Erasure (sha256: cert) + Federated Quality (BP-007) + Feature Correlation (Frobenius) + Data Readiness Cert (8-dim) + Session Contribution Weights (TBD λ=ln(2)/90 BP-001); Tools #136-#144; 14 bridge+7 SDK; Bridge 2114→2128 +14; SDK 383→390 +7; agents 34→35 |
| 191 | Threat Succession Protocol (TSP) — WIF-034 FORMAL; ProtocolMaturityScoringAgent 8-component v2; threat_forecast_accuracy+biometric_stationarity components; tsp_enabled=True; 8 bridge+4 SDK; Bridge 2106→2114 +8; SDK 379→383 +4 |
| 190 | LivePresenceSignalingAgent (agent #34) — bidirectional VAPI presence; 8 signal types; live_presence_signaling_enabled=False; 8 bridge+4 SDK; Bridge 2098→2106; SDK 375→379; agents 33→34 |
| 189 | ProtocolIntelligenceRecordAgent (agent #33) — PIR chain hash; pir_chain_enabled=False; 8 bridge+4 SDK; Bridge 2090→2098; SDK 371→375 |
| 188 | BiometricStationarityOracleAgent (agent #32) — drift cause classifier; biometric_stationarity_enabled=False; 8 bridge+4 SDK; Bridge 2082→2090; SDK 367→371 |
| 187 | AttestationOpSec+VHPReenrollmentBadge (agents #31+contract) — VHPReenrollmentBadge.sol LIVE 0x42E7A25d0E5667BBae45e5cF33a6e2CC6E42d45C; both disabled default; 16 bridge+8 SDK; Bridge 2066→2082; SDK 359→367 |
| 186 | AttestationBoundRenewalAgent (agent #30) — validates attestation at renewal; disabled default; 8 bridge+4 SDK; Bridge 2058→2066; SDK 355→359 |
| 185 | ReEnrollmentAttestationAgent (agent #29) — HMAC attestation on persona break; reauth_attestation_enabled=True; 8 bridge+4 SDK; Bridge 2050→2058; SDK 351→355 |
| 184 | PostCode Sweep guardrail — schema_versions 182+183; PersonaBreakResult+MaturityElevationResult slots=True; 10 bridge+4 SDK; Bridge 2040→2050; SDK 347→351 |
| 183 | MaturityElevationGateAgent (agent #28) — elevation plan; maturity_elevation_enabled=True; 8 bridge+4 SDK; Bridge 2032→2040; SDK 343→347 |
| 182 | PersonaBreakDetectorAgent (agent #27) — LOO centroid drift detection; persona_break_detection_enabled=True; 8 bridge+4 SDK; Bridge 2022→2032; SDK 337→343; agents 26→27 |
| 181 | Renewal Provenance — corpus_delta_detected on BiometricRenewalResult; ceremony_audit_registry_address on VAPICeremonyAuditGate; SDK slot fixes; bridge unchanged; SDK unchanged |
| 180 | Biometric Renewal Engine (WIF-029 W2 closure) — consent-bound renewal commitment chain; renewal_enabled=False default; dry_run=True default; biometric_renewal_chain_log (UNIQUE new_commit_hash); chain.renew_separation_ratio_commitment() calls SeparationRatioRegistry.renewCommit(); new_hash=SHA-256(prev_hash+ratio+N+N_consented+ttl+ts_ns); POST /agent/renew-separation-ratio-commitment + GET /agent/renewal-chain-status; Tool #129 trigger_renewal_commitment; BiometricRenewalResult(6 slots)+VAPIBiometricRenewal SDK; 8 bridge + 4 SDK tests; Bridge 2014→2022 +8; SDK 333→337 +4; Hardhat 482 unchanged |
| 179 | ZK Ceremony Audit Gate (WIF-030 W1 closure) — ceremony_audit_enabled=False default; ceremony_audit_log (UNIQUE ceremony_id+participant_address+circuit_name); CeremonyAuditRegistry.sol NEW; GET /agent/ceremony-audit-status + POST /agent/register-ceremony-participant; Tool #128; CeremonyAuditGateResult(6 slots)+VAPICeremonyAuditGate SDK (renamed from CeremonyAuditResult/VAPICeremonyAudit to avoid Phase 85 name collision — fixed Skill 14 sweep); 8 bridge + 4 SDK + 8 Hardhat tests; Bridge 2006→2014 +8; SDK 329→333 +4; Hardhat 474→482 +8; Contracts 39→40 |
| 178 | Biometric Credential TTL Gate (WIF-029 W1 closure) — biometric_credential_ttl_days=90.0; TournamentActivationChainAgent.check_biometric_credential_ttl(); biometric_renewal_log; SeparationRatioRegistry.sol +renewCommit(); GET /agent/biometric-credential-age; Tool #127; BiometricCredentialAgeResult+VAPIBiometricCredentialTTL SDK; 8 bridge + 4 SDK + 6 Hardhat tests; Bridge 1998→2006 +8; SDK 325→329 +4; Hardhat 468→474 +6 |
| 177 | ProtocolMaturityScoringAgent (agent #26) — maturity_score(0.0-1.0) from 6 components; maturity_tier ALPHA/BETA/PRODUCTION_CANDIDATE; Tool #126; ProtocolMaturityScoringResult+VAPIProtocolMaturityScoring SDK; 8 bridge+4 SDK tests; Bridge 1990→1998 +8; SDK 321→325 +4; agents 25→26 |
| 159 | BiometricPrivacyComplianceAgent — agent #22; BP-001 Temporal Biometric Decay TBD(t)=e^(-λt) τ_half=90d; warning when mean_decay_factor<0.25; privacy_compliance_log; biometric_decay_warning bus event; Tool #116; BiometricPrivacyComplianceResult+VAPIBiometricPrivacy SDK; 8 bridge+4 SDK tests; Bridge 1886→1894 +8; SDK 273→277 +4; Hardhat 468 unchanged |
| 158 | Class K HMAC Validation + PoHBG; WIF-014: validate_gsr_hmac() 80-byte HMAC-SHA256 frame auth; WIF-015: compute_pohbg_hash() PoHBG=SHA-256(device_id+pack); gsr_hmac_validation_log+pohbg_log; Tools #114+#115; GSRHMACValidationResult+PoHBGResult SDK; 9 bridge+4 SDK tests; Bridge 1877→1886 +9; SDK 269→273 +4; Hardhat 468 unchanged |
| 157 | FleetConsensusSnapshotAgent — agent #21; WIF-012 dual-condition overall_ready (sessions_needed==0 AND defensible); WIF-016 cov_stability_check() 3 regime labels; WIF-013 PoFC hash=SHA-256(sorted_verdicts+ratio+ts_ns); fleet_consensus_snapshot_log; cov_regime_status in enrollment guidance; 9 bridge + 4 SDK tests; Bridge 1868→1877 +9; SDK 265→269 +4; Hardhat 468 unchanged |
| 156 | EnrollmentAutoGuidanceAgent — agent #20; synthesizes Phase 151 guidance + Phase 154 stagnation + Phase 152 velocity + Phase 155 controller status; 1-hour poll; urgency_level HIGH/MEDIUM/LOW; fires enrollment_complete → TournamentActivationChainAgent when overall_ready; 8 bridge + 4 SDK tests; Bridge 1860→1868 +8; SDK 261→265 +4; Hardhat 462 unchanged |
| 155 | ControllerHardwareIntelligenceAgent — agent #19; Attested tier (DualShock Edge, L0-L6) vs Standard tier (Xbox/Switch, L0-L5); composite key profile_hash:battery_type:transport_type; default thresholds 7.009/5.367; multi_controller_enabled=False; 8 bridge + 4 SDK tests; Bridge 1852→1860 +8; SDK 257→261 +4; Hardhat 462 unchanged |
| 154 | Capture Stagnation Monitor — rolling sessions/day from separation_defensibility_log; stagnant when <0.5/day (7-day window); synergistic input for Phase 156 urgency; 8 bridge + 4 SDK tests; Bridge 1844→1852 +8; SDK 253→257 +4; Hardhat 462 unchanged |
| 153 | SeparationRatioRegistry.sol — on-chain SHA-256 proof-of-calibration commitment; anti-replay UNIQUE commitHash; separation_ratio_on_chain_enabled=False default; 8 bridge + 4 SDK + 6 Hardhat tests; Bridge 1836→1844 +8; SDK 249→253 +4; Hardhat 462→468 +6; deploy deferred (wallet) |
| 152 | Centroid Velocity Monitor — |ratio_curr-ratio_prev|/dt_seconds; _PLATEAU_THRESHOLD_PER_DAY=0.001; stagnant flag feeds Phase 156 urgency; 8 bridge + 4 SDK tests; Bridge 1828→1836 +8; SDK 245→249 +4; Hardhat 462 unchanged |
| 151 | Session-Type Whitelist + Enrollment Capture Guidance (W1-011 closure) — STRUCTURED_PROBE_TYPES frozenset {touchpad_corners, touchpad_freeform, touchpad_swipes}; insert_separation_defensibility_log raises ValueError on invalid session_type ('gameplay' etc.); get_enrollment_capture_guidance() per-probe per-player gap breakdown; GET /agent/enrollment-capture-guidance (5 keys: min_n_per_player/probe_types/guidance/sessions_needed_total/overall_ready); Tool #107 get_enrollment_capture_guidance; CaptureGuidanceResult(6 slots)+VAPIEnrollmentCaptureGuidance SDK; SDK_VERSION 3.0.0-phase151; 10 bridge + 4 SDK tests; Bridge 1818→1828 +10; SDK 241→245 +4; Hardhat 462 unchanged |
| 150 | Session Consistency Scoring + Separation Ratio Defensibility Gate (WIF-010 closure) — separation_defensibility_log table + insert_separation_defensibility_log + get_separation_defensibility_status(session_type=None); config +min_touchpad_sessions_per_player(10); scripts/analyze_interperson_separation.py: +_compute_session_consistency_scores() per-session LOO Mahalanobis outlier detection + _check_n_defensibility() + --session-consistency + --min-n-per-player flags; GET /agent/separation-defensibility-status (6 keys: defensible/ratio/n_per_player/min_n_per_player/all_pairs_above_1/found); Tool #106; SeparationDefensibilityResult(6 slots)+VAPISeparationDefensibility SDK; SDK_VERSION 3.0.0-phase150; schema(150,"separation_defensibility"); Bridge 1808→1818 +10; SDK 237→241 +4; Hardhat 462 unchanged |
| 109B | ioSwarm VHPRenewalAgent Migration — IoSwarmNodeEmulator(n=5,seed=109) + VHPRenewalSwarmTaskSpec + scripts/vapi-vhp-renewal-swarm-agent.json + IoSwarmRenewalCoordinator (fail-open; CERTIFY_RENEW_QUORUM=0.60; W2 consecutive_clean-weighted verdicts) + vhp_renewal_agent.py injection (ioswarm_renewal_enabled=False default) + ioswarm_renewal_log table + GET /agent/ioswarm-renewal-status + Tool #76 + SDK IoSwarmRenewalResult+VAPISwarmRenewal; Bridge 1472→1480 +8; SDK 121→125 +4 |
| 131 | IoSwarm Live Node Foundation (W1 per-node asyncio.wait_for timeout mitigation; W2 staker_address per node for VAPISwarmOperatorGate validation) — ioswarm_node_registry table (node_url/staker_address/active/last_seen_ts/node_version/registered_at/created_at) + insert_ioswarm_node_registry + get_ioswarm_node_registry + update_ioswarm_node_last_seen + schema(131,"ioswarm_node_registry"); config +ioswarm_node_urls + ioswarm_node_timeout_seconds; ioswarm_live_node_client.py (NEW: IoSwarmLiveNodeClient; is_emulator_mode()=True when urls=""; dispatch falls back to emulator zero-behavior-change); live_client=None kwarg added to IoSwarmRenewalCoordinator + IoSwarmAdjudicationCoordinator + IoSwarmVHPMintCoordinator; injected at all 3 instantiation sites (vhp_renewal_agent.py + session_adjudicator.py + operator_api.py); GET /agent/ioswarm-node-registry-status (7 keys: live_nodes/emulator_mode/node_urls/node_timeout_s/registry_count/last_quorum_ts/timestamp); Tool #99 get_ioswarm_node_registry_status; IoSwarmNodeRegistryResult @dataclass(slots=True) 6 slots + VAPIIoSwarmNodeRegistry SDK; VAPISwarmOperatorGate.sol Hardhat tests: Phase130.test.js (+6 T130-1..6) + MockOperatorRegistry130.sol; SDK_VERSION 3.0.0-phase130→3.0.0-phase131; openapi IoSwarmNodeRegistryStatus schema + path; Bridge 1661→1669 +8; SDK 213→217 +4; Hardhat 454→460 +6 |
| 129 | Full Covariance + Separation Ratio Breakthrough Monitor (W1 single-outlier false breakthrough closed — 2-consecutive guard; W2 readiness score oracle input) — SeparationRatioMonitorAgent(agent #15) polls 300s; _prev_crossed+_breakthrough_fired one-shot; fires separation_ratio_breakthrough bus; auto-enables confidence_multiplier_enabled; separation_ratio_breakthrough_log table; GET /agent/separation-ratio-breakthrough (5 keys); Tool #97; SeparationBreakthroughResult(5 slots)+VAPISeparationBreakthrough SDK; analyze_interperson_separation.py +--full-covariance+--diagonal flags; SDK_VERSION 3.0.0-phase128→3.0.0-phase129; schema(129,"separation_breakthrough"); Bridge 1644→1653 +9; SDK 205→209 +4; Hardhat 454 unchanged |
| 128 | Protocol Intelligence Dashboard (W1 score overstates readiness before separation_ratio>1.0 — mitigated: separation_score=min(1.0,pooled_ratio) caps at current 0.474; W2 readiness score as on-chain oracle input for Phase 129 SeparationRatioMonitorAgent) — insert_readiness_score(score,breakdown_json,conditions_met)+get_readiness_scores(limit=10) reuse existing protocol_intelligence_reports table (protocol_health_score/components_json/recommendation/ready_for_live_mode); GET /agent/tournament-readiness-score (9 keys: score/separation_score/l4_score/dual_gate_score/epoch_score/ioswarm_score/dry_run_score/conditions_met/timestamp); 6-signal formula: separation(0.30)+l4_freshness(0.20)+dual_gate(0.15)+epoch_p95(0.15)+ioswarm(0.10)+dry_run(0.10); score>=0.90=READY; Tool #96 get_tournament_readiness_score; TournamentReadinessScore(8 slots)+VAPITournamentReadinessScore SDK (renamed to avoid Phase 108 VAPITournamentReadiness collision); SDK_VERSION 3.0.0-phase127→3.0.0-phase128; schema(128,"intelligence_dashboard"); openapi TournamentReadinessScore schema + GET path; Bridge 1636→1644 +8; SDK 201→205 +4; Hardhat 454 unchanged |
| 127 | Tournament Pre-Launch Validation Suite (W1 commit-activation proceeds without preflight gate — P0 enforcement added: separation_ok+l4_ok checked in POST /agent/commit-activation; overall_pass=False blocks commit; W2 preflight log as audit trail for tournament launch authorization) — tournament_preflight_log table (separation_ok/l4_ok/gate_ok/cert_ok/audit_ok/dual_gate_warned/epoch_window_warned/ioswarm_warned/overall_pass/conditions_json) + insert_tournament_preflight_log + get_tournament_preflight_status + schema(127,"tournament_preflight"); POST /agent/run-tournament-preflight (runs all 8 conditions; returns 10 keys + conditions; persists to log) + GET /agent/tournament-preflight-status; POST /agent/commit-activation extended: reads latest preflight log, blocks on separation_ok=False or l4_ok=False with "preflight_p0_blocked"; Tool #95 run_tournament_preflight; TournamentPreflightResult @dataclass(slots=True) 8 slots (separation_ok/l4_ok/gate_ok/cert_ok/audit_ok/overall_pass/conditions_detail/error) + VAPITournamentPreflight SDK; SDK_VERSION 3.0.0-phase126→3.0.0-phase127; scripts/tournament_preflight.py standalone CLI (exit 0=READY/1=ERROR/2=NOT_READY); openapi TournamentPreflightResult schema + 2 paths; Bridge 1627→1636 +9; SDK 197→201 +4; Hardhat 454 unchanged |
| 126 | Live L4 Gate Per-Battery Routing + BehavioralArchaeologist Fixes (W1 per-battery routing silently falls back to global thresholds when no active track — mitigated: explicit fallback + WARNING log; W2 per-battery threshold routing now LIVE when l4_battery_threshold_enabled=True) — l4_threshold_router.py (NEW: get_thresholds(battery_type,store,cfg)→(float,float,str); source="per_battery"|"global_fallback"; never raises); behavioral_archaeologist.py: _WARMUP_COEFF=20_000 + _BURST_CV_DIVISOR=2.0 named constants (Phase 126 regression guard; closes Phase 57 TODOs); l4_threshold_router_log table + insert_l4_router_log + get_l4_router_log + schema(126,"l4_router"); GET /agent/l4-router-status (7 keys: l4_battery_threshold_enabled/total_lookups/per_battery_lookups/global_fallback_count/last_battery_type/last_source/timestamp); Tool #94 get_l4_router_status; L4RouterStatusResult(6 slots)+VAPIL4RouterStatus SDK; SDK_VERSION 3.0.0-phase125→3.0.0-phase126; openapi L4RouterStatus schema + GET path; Bridge 1619→1627 +8; SDK 193→197 +4; Hardhat 454 unchanged |
| 125 | Calibration Modernization + Per-Battery Threshold Calibrator (W1 per-battery L4 routing silently falls back to stale global thresholds when l4_threshold_tracks table empty or battery_type unmatched; W2 per-battery threshold routing Phase 126 candidate) — scripts/threshold_calibrator.py: +_BATTERY_TYPES + _detect_battery() + _calibrate_battery() + --battery {touchpad,trigger,button,gameplay,resting_grip,all} CLI flag; bridge: l4_battery_calibration_runs table (battery_type/anomaly_threshold/continuity_threshold/n_sessions/calibration_feature_dim/notes/created_at) + insert_l4_battery_calibration_run + get_l4_battery_calibration_runs(limit=10) + schema(125,"per_battery_calibration"); POST /agent/apply-l4-battery-calibration (calls insert_l4_threshold_track + insert_l4_battery_calibration_run; updates cfg.calibration_feature_dim; returns 9-key summary with stale flag; 422 on bounds violation); Tool #93 apply_l4_battery_calibration (before Tool #92); CalibrationApplyResult(5 slots: battery_type/anomaly_threshold/continuity_threshold/n_sessions/error)+VAPICalibrationApply SDK; SDK_VERSION 3.0.0-phase124→3.0.0-phase125; openapi L4BatteryCalibrationApply schema + POST path; staleness clears when calibration_feature_dim==live_feature_dim(13); Bridge 1611→1619 +8; SDK 189→193 +4; Hardhat 454 unchanged |
| 124 | L4 Per-Battery Threshold Track Registry (W1 threshold pollution attacks closed — bounds [5.0–15.0] anomaly / [3.0–10.0] continuity enforced in insert_l4_threshold_track raising ValueError→HTTP 422; W2 per-battery threshold routing Phase 125 candidate) — l4_threshold_tracks table (battery_type/anomaly_threshold/continuity_threshold/n_sessions/calibrated_at/active/created_at) + insert_l4_threshold_track (bounds enforced) + get_l4_threshold_tracks(battery_type=None, active_only=False) + schema(124,"l4_threshold_tracks"); config +l4_battery_threshold_enabled(False); GET /agent/l4-threshold-tracks (6 keys: l4_battery_threshold_enabled/track_count/active_count/battery_types_tracked/tracks/timestamp) + POST /agent/l4-threshold-track (422 on bounds violation); Tool #92 get_l4_threshold_tracks; L4ThresholdTrackResult(5 slots)+VAPIL4ThresholdTracks SDK; SDK_VERSION 3.0.0-phase123→3.0.0-phase124; openapi L4ThresholdTracksStatus schema + paths; Bridge 1603→1611 +8; SDK 185→189 +4; Hardhat 454 unchanged |
| 123 | L4 Calibration Staleness Monitor (W1 stale thresholds applied to 13-feature live space silently degrade L4 precision; W2 per-battery threshold tracks Phase 124 candidate) — l4_calibration_log table + insert_l4_calibration_log + get_l4_calibration_log + schema(123,"l4_calibration_staleness"); config +live_feature_dim(13)/calibration_feature_dim(12)/calibration_n_sessions(74)/calibration_timestamp(0.0); GET /agent/l4-calibration-status (8 keys: current_feature_dim/calibration_feature_dim/stale/anomaly_threshold/continuity_threshold/calibration_n_sessions/calibration_timestamp/timestamp); stale=True when live_feature_dim!=calibration_feature_dim; Tool #91 get_l4_calibration_status; CalibrationStatusResult(6 slots)+VAPICalibrationStatus SDK; SDK_VERSION 3.0.0-phase122→3.0.0-phase123; openapi L4CalibrationStatus schema + path; Bridge 1595→1603 +8; SDK 181→185 +4; Hardhat 454 unchanged |
| 122 | VHP Confidence Score Separation Ratio Multiplier (W1 non-touchpad sessions penalized by touchpad-dominant bt_strat_ratio — mitigated: multiplier disabled default; W2 per-battery multiplier lookup Phase 124 candidate) — confidence_multiplier_log table + insert_confidence_multiplier_log + get_confidence_multiplier_log + schema(122,"confidence_multiplier"); config +confidence_multiplier_enabled(False)/confidence_multiplier_floor(0.0); POST /agent/mint-vhp: confidence_score *= max(floor, min(1.0, bt_strat_ratio)) when enabled; mint response +pre_multiplier_score+confidence_multiplier keys; GET /agent/confidence-score-multiplier-status (7 keys); Tool #90 get_confidence_score_multiplier_status; ConfidenceMultiplierResult(6 slots)+VAPIConfidenceMultiplier SDK; SDK_VERSION 3.0.0-phase121→3.0.0-phase122; openapi ConfidenceMultiplierStatus schema + path; Bridge 1587→1595 +8; SDK 177→181 +4; Hardhat 454 unchanged |
| 121 | Touchpad Spatial Entropy + Separation Ratio Monitoring (W1 ring buffer cross-session contamination mitigated — fresh extractor per analysis session; W2 battery-stratified ratio as VHP confidence_score multiplier Phase 122 candidate) — touchpad_spatial_entropy feature (index 12, 8×8 Shannon entropy heatmap, max log2(64)≈6.0 bits) + _BIO_FEATURE_DIM 12→13 + _touchpad_xy_ring deque(maxlen=1024) + _touchpad_spatial_entropy() static method; analyze_interperson_separation.py +--battery-stratified flag + _detect_battery() + battery-grouped Mahalanobis analysis + resting-grip normalization ratio; separation_ratio_snapshots table + insert_separation_ratio_snapshot + get_separation_ratio_status + schema(121,"separation_ratio"); GET /agent/separation-ratio-status (7 keys: pooled_ratio/battery_stratified_ratio/tournament_blocker/target_ratio/gap_to_target/tournament_ready/timestamp); Tool #89 get_separation_ratio_status; SeparationRatioResult(6 slots)+VAPISeparationStatus SDK; SDK_VERSION 3.0.0-phase120→3.0.0-phase121; openapi SeparationRatioStatus schema + path; L4 threshold recalibration deferred to Phase 122 (new feature initializes to 0.0 for no-touchpad sessions); Bridge 1579→1587 +8; SDK 173→177 +4; Hardhat 454 unchanged |
| 120 | BT Transport Foundation (W1 separate BT threshold track required — USB 7.009/5.367 NOT applicable to 250 Hz BLE sessions; W2 Phase 121 VHC — ESP32-S3 GSR grip as programmable BLE node enabling decentralized tournament presence proof) — bluetooth.py: BT_POLL_HZ=250/BT_FRAME_MS=4.0/BT_WINDOW=1024/BT_HZ_PER_BIN=0.244/TRANSPORT_TYPE_BLE=0x02 + BTFrame dataclass + MockBLETransport(seed=42,n_frames=0) async generator + BLETransport optional bleak dep; bt_transport_log table + insert_bt_transport_log + get_bt_transport_status; GET /agent/bt-transport-status (7 keys); Tool #88 get_bt_transport_status; BTTransportResult(6 slots)+VAPIBTTransport SDK; schema 120; bt_transport_enabled=False default (infrastructure-first); Bridge 1571→1579 +8; SDK 169→173 +4; Hardhat 454 unchanged |
| 119 | Override Lifecycle Management — TTL + Use-Count Auto-Expiry (W1 stale permanent overrides undermine epoch window security; W2 override auto-graduation max_uses=N self-delete) — per_device_epoch_overrides +max_uses/use_count/expires_at (idempotent ALTER TABLE for Phase 118 DBs) + increment_override_use_count (auto-graduation; consumed=True when max_uses reached or expires_at exceeded) + delete_device_epoch_override + get_override_lifecycle_status + GET /agent/epoch-window-override-status (5 keys) + DELETE /agent/epoch-window-override + Tool #86 get_epoch_window_override_status + Tool #87 revoke_device_epoch_override + SDK EpochWindowOverrideStatus(6 slots)+VAPIEpochWindowOverrideManager + schema 119; Bridge 1562→1571 +9; SDK 165→169 +4; Hardhat 454 unchanged |
| 118 | Epoch Window Auto-Tune Advisor + Cold-Start Device Override (W1 cold-start false-positive block closed; W2 per-device override feed Phase 119 candidate) — per_device_epoch_overrides table + get_device_epoch_override() Gate-5 override check + GET /agent/epoch-window-auto-tune (7 keys) + POST /agent/epoch-window-override + Tool #84 get_epoch_window_auto_tune + Tool #85 set_device_epoch_override + SDK EpochWindowAutoTuneResult+VAPIEpochWindowAutoTune + schema 118; Bridge 1553→1562 +9; SDK 161→165 +4; Hardhat 454 unchanged |
| 117 | Per-Device Epoch Freshness Heatmap (W1 misbehaving devices identified before gate activation; W2 per-device windows Phase 118 candidate) — get_epoch_window_analytics_by_device() p50/p95 per device sorted p95 DESC + GET /agent/epoch-window-device-heatmap + Tool #83 + SDK EpochWindowDeviceEntry+VAPIEpochWindowHeatmap + schema 117; Bridge 1545→1553 +8; SDK 157→161 +4; Hardhat 454 unchanged |
| 116 | Epoch-Window Analytics + Recommended Window Advisor (W1 misconfigured window caught; W2 triple-primitive analytics) — get_epoch_window_analytics() p50/p95/recommended_window_seconds + GET /agent/epoch-window-analytics + Tool #82 + SDK EpochWindowAnalyticsResult+VAPIEpochWindowAnalytics + schema 116; Bridge 1537→1545 +8; SDK 153→157 +4; Hardhat 454 unchanged |
| 115 | Epoch-Window Dual-Primitive Temporal Proof (W1 stale PoAd accumulation closed; W2 poad_age_seconds analytics) — +poad_age_seconds+epoch_window_ok to vhp_dual_gate_log + get_poad_ts_ns_for_device() + epoch_window_enabled=False + epoch_window_seconds=86400 config + epoch sub-check in Gate 5; Bridge 1529→1537 +8; SDK 149→153 +4; Hardhat 454 unchanged |
| 114 | VHP Mint Dual-Primitive Gate (W1 VHP without PoAd anchor closed; W2 time-windowed dual-proof Phase 115 candidate) — 5th gate in POST /agent/mint-vhp (dual_primitive_gate_enabled=False default) + vhp_dual_gate_log table + GET /agent/vhp-dual-gate-log (6 keys) + Tool #81 + SDK VHPDualGateResult+VAPIVHPDualGate; reuses Phase 113 config; Bridge 1521→1529 +8; SDK 145→149 +4; Hardhat 454 unchanged |
| 113 | VAPIDualPrimitiveGate (W1 pure view no gas; W2 first dual-proof composability gate in any on-chain gaming protocol) — VAPIDualPrimitiveGate.sol (isDualEligible: isFullyEligible() AND isRecorded(); immutable; zero-address guard) + deploy-phase113.js + chain.is_dual_eligible() view call + dual_eligibility_checks table + GET /agent/dual-primitive-status (8 keys) + POST /agent/check-dual-eligibility + Tool #80 + SDK DualPrimitiveGateResult+VAPIDualPrimitiveGate; 2 config fields; Bridge 1513→1521 +8; SDK 141→145 +4; Hardhat 448→454 +6; deploy deferred (IOTX faucet empty) |
| 112 | PoAd On-Chain Anchoring (W1 Block Timestamp Cross-Check + W2 Dual-Primitive Gate Activated) — PoAdAnchorAgent + chain.record_adjudication() + poad_on_chain_enabled=False default + GET /agent/poad-anchor-status (6 keys) + SDK PoAdAnchorResult+VAPIPoAdAnchor; Bridge 1504→1513 +9; SDK 137→141 +4; Hardhat 446→448 +2 |
| 111 | PoAd Registry (W1 PoAd Timestamp Risk + W2 Dual-Primitive Composability) — AdjudicationRegistry.sol (Ownable, CEI, anti-replay UNIQUE poadHash; block.number stored for W1 mitigation) + deploy-phase111.js + session_adjudicator.py Step D (non-blocking PoAd hash computation + local registry; poad_registry_enabled=False default) + GET /agent/adjudication-registry-status + Tool #79 + SDK PoAdRegistryResult+VAPIPoAdRegistry; poad_registry_log table +2 config fields; Contracts 38→39; Bridge 1496→1504 +8; SDK 133→137 +4; Hardhat 440→446 +6 |
| 110 | IoSwarm VHP Mint Authorization (W1 Fail-CLOSED + W2 Swarm Fingerprint) — IoSwarmVHPMintEmulator(n=5,seed=110) + VAPIVHPMintSwarmTaskSpec + scripts/vapi-vhp-mint-swarm-agent.json + IoSwarmVHPMintCoordinator (MINT_QUORUM=0.80; fail-CLOSED; swarm_fingerprint=SHA-256(node_verdicts_json)) + POST /agent/mint-vhp ioSwarm 4th gate + GET /agent/ioswarm-vhp-mint-status + Tool #78 + SDK IoSwarmVHPMintResult+VAPISwarmVHPMint; Bridge 1488→1496 +8; SDK 129→133 +4 |
| 109C | IoSwarm Dual-Quorum Adjudication (W2 Dual-Quorum Veto) — IoSwarmClassJEmulator(n=5,seed=109) + IoSwarmTriageEmulator(n=5,seed=109) + VAPIAdjudicationSwarmTaskSpec + scripts/vapi-adjudication-swarm-agent.json + IoSwarmAdjudicationCoordinator (DUAL_VETO_SCORE=0.80; fail-open CLEAR; CLASSJ_BLOCK_QUORUM=0.67; TRIAGE_BLOCK_QUORUM=0.67) + session_adjudicator.py injection (Steps A+B+C; ioswarm_adjudication_enabled=False default) + ioswarm_adjudication_log table + GET /agent/ioswarm-adjudication-status + Tool #77 + SDK IoSwarmAdjudicationResult+VAPISwarmAdjudication; Bridge 1480→1488 +8; SDK 125→129 +4 |
| 109A | ioSwarm Bridge Adapter — IoSwarmConsensusAggregator (BLOCK_QUORUM=0.67 W1; tie→HOLD; hold escalation) + VAPISwarmTaskSpec + scripts/vapi-swarm-agent.json + epistemic 4th signal (weights 0.35/0.35/0.15/0.15 when enabled) + GET /agent/ioswarm-status + Tool #75 + SDK IoSwarmConsensusResult+VAPISwarmStatus; ioswarm_enabled=false default; Bridge 1464→1472 +8; SDK 117→121 +4 |

## Remaining Open Gaps (Phase 48, priority order)

1. **L2C phantom weight** — CLOSED (Phase 47)
   `l2c_inactive` flag in pitl_meta + WS stream; log.debug per dead-zone cycle; §7.5.4 footnote;
   test_9 formula validity; HUMANITY tile "4-signal (L2C: dead zone)" in orange; PITL layer table
   L2C row shows "INACTIVE (dead zone)" live when l2c_inactive=true; L4 thresholds updated; bridge 880.

2. **Inter-person separation ratio 0.362** — OPEN/HIGH
   Neither phase coherence nor spectral entropy improved it (both are bot-vs-human).
   True fix requires: post-Phase-17 touchpad recapture (hardware + gameplay) AND
   widening tremor FFT window beyond 120 frames.

3. **Post-Phase-17 touchpad recapture** — OPEN/HIGH (requires controller + gameplay)
   touch_position_variance structurally zero across all calibration sessions.

4. **Professional bot adversarial data** — CLOSED (Phase 48)
   3 white-box attack classes (G: randomized_bot, H: threshold_aware, I: spectral_mimicry), 15 sessions.
   H: 100% L4 detection. G/I: batch 0% (live L4+tremor and L2B respectively). Analysis: `docs/professional-adversarial-analysis.md`.
   Remaining gap: real hardware bot software (aimbot, ML-driven inputs) still untested.

5. **Multi-party ZK ceremony** — PLANNED (no hardware)

6. **PHGCredential multi-sig/timelock governance** — PLANNED (no hardware)

## ZK Circuit

Groth16, BN254, ~1,820 constraints, 2^11 powers-of-tau.
PITLSessionRegistry: `0x8da0A497234C57914a46279A8F938C07D3Eb5f12`
PitlSessionProofVerifier: `0x07D3ca1548678410edC505406f022399920d4072`

## BridgeAgent + CalibrationIntelligenceAgent (Phase 50) + Game Profile (Phase 51)

BridgeAgent: claude-sonnet-4-6. 28 deterministic tool bindings (17 original + 3 Phase 50 + 1 Phase 51 + 4 Phase 58 + 1 Phase 59).
GET /operator/agent/stream (SSE, 60 req/min). SQLite session persistence.
Phase 50: check_threshold_drift() wired to InsightSynthesizer Mode 6 callback.
Phase 50: react() emits recalibration_needed agent_events when drift_velocity > 0.6.
Phase 51: get_game_profile() tool — returns active game profile, L5 priority, L6-Passive stats.

CalibrationIntelligenceAgent: claude-sonnet-4-6. 6 calibration specialist tools.
GET /operator/calibration/stream + POST /operator/calibration/agent.
run_event_consumer() polls agent_events table every 30 min.
Enforces min() unconditionally on trigger_recalibration — thresholds can only tighten.

## Game-Aware Profiling (Phase 51)

Active profile: ncaa_cfb_26 (set via GAME_PROFILE_ID=ncaa_cfb_26 in bridge/.env).
L5 button priority overridden: R2 (sprint) > Cross > L2_dig > Triangle — football-specific.
L6-Passive: per-press R2 onset tracking (no controller writes, no PS5 conflict). Bootstrap N=20,
EMA α=0.15, flag_ratio=1.5 (50% slower than personal mean = PS5 haptic resistance event).
game_profile.py: GameProfile frozen dataclass + registry; ncaa_cfb_26 registered at import.
rhythm_hash() canonical order UNCHANGED — sensor commitment invariant preserved.

## Hardware

DualShock Edge CFI-ZCP1, USB-C, Windows 11, hidapi VID=0x054C PID=0x0DF2 interface 3.
USB polling: 1002 Hz. Injection margin: 14,000× (accel), 10,000× (gyro).
Micro-tremor variance: 278,239 LSB².

## roadmap_post_stage_1

Captured ideas for post-grind / post-Stage-1 work. Do NOT implement
or design further — these are intentionally minimal stubs preserved
so they're not lost between phases. Promote to a real phase only
after Stage 1 graduation activates and GIC_100 is deposited.

- **Phase 240+ candidate — L6-Response (haptic-driven stimulus-response biometric layer)**:
  Leverage DualSense Edge haptic motor commands and adaptive trigger
  output as ambient stimuli during normal play. Capture player reflex
  response in the 80–280 ms window post-stimulus (within human
  voluntary-reaction-time band; faster = bot, slower = inattention).
  Build per-player stimulus-response baselines from the GIC_100+
  corpus once it exists. Activate `L6_CHALLENGES_ENABLED` only when
  N≥50 calibration sessions exist per player. Do NOT activate before
  GIC_100 lands — without that corpus the baselines are
  unstable. Sub-perceptual amplitudes (≤60/255) so play experience is
  unaffected. Exclusive to PS5-class haptic hardware; not replicable
  on generic gamepads.

## Verification-First Discipline

Canonical name for the work pattern that has shaped VAPI's protocol commits. Joins the existing protocol vocabulary (FROZEN-v1, PV-CI, AGaaS, deferred-activation) without redefining those terms.

The pattern structures consequential architectural work as six ordered steps:

1. **Pre-implementation verification** (V-numbered checks). Read state, confirm assumptions, identify drift between the operator's brief and observable reality.
2. **Hold for operator review at the verification checkpoint.** Surface findings, including any drift; the operator decides whether to proceed, refine the brief, or abort.
3. **Implementation.** Execute against the corrected brief.
4. **Post-implementation verification** (P-numbered checks). Confirm the change matches intent and the working tree is in the expected shape.
5. **Hold for operator review before staging.** No commit happens without explicit approval.
6. **Atomic commit with architectural reasoning preserved in the message body**, then push.

What the pattern produces:
- Drift correction in both directions. V-checks catch wrong assumptions in the prompt (the brief revises against reality); P-checks catch divergence during execution (the implementation revises against the brief).
- Architectural reasoning preserved in the permanent record. Decision blocks, rejected alternatives, and "why this rather than that" land in commit bodies — not in chat scrollback that disappears at the next compaction.
- Operator authority over architectural decisions enforced procedurally. Holds are not optional; the pattern fails closed if a checkpoint is skipped.

Demonstrating commits (Phase O0 pause-period work, 2026-04-27 → 2026-04-29):
- The eighteen-commit Phase O0 implementation arc (Streams 1–5 source-and-tests).
- `b261b546` — Stream 2-deploy local Hardhat dry-run runbook.
- `f8c577ab` — Phase O0 pause-point capture (commit roster, dependency graph, resumption checklist).
- `e3fbebd4` — Phase 237.5 Path C+ residual: batcher dead-letters `chain_submission_paused` errors immediately.
- `97a0eab4` — Frontend connectivity verification report.
- `29b57707` — Dead frontend code cleanup identified by the connectivity verification.
- `9ee11471` — Chain wrapper verification report.

Where the pattern applies:
- Consequential architectural work (new primitives, contract changes, agent additions, invariant edits).
- Multi-step atomic commits where the diff alone doesn't carry the reasoning.
- Verification-reference documents that future sessions read instead of re-deriving state.

Where it does not apply:
- Conversational discussion, brief-clarification, brainstorming.
- Operational coordination (scheduling agents, drafting messages, status updates).
- Read-only information gathering with no resulting commit.

The pattern itself is the canonical name's claim: protocol commits ship through it, including the commit that introduces this section.

## BT Calibration: Canonical Prerequisite Anchor

**[CANONICAL]** Any BT-related design work in VAPI must read `wiki/assessments/VAPI Bluetooth Calibration_ Architectural Prerequisites and Threat Model Analysis.pdf` before producing architectural proposals, feature definitions, threat-model claims, or capture-session plans. This document is the canonical prerequisite anchor for the protocol's Bluetooth expansion path. It establishes the literature anchors, the empirical unknowns that must be resolved by pre-corpus measurement, and the constraint envelope for what L8 v1 claims and does not claim.

The four corrections that the canonical anchor establishes against the v1.0 architectural proposal are: (1) the DualSense and DualSense Edge transport is Bluetooth Classic BR/EDR with HIDP, not BLE/HOGP, which means BLE-named primitives such as advertising interval, connection events, advDelay, and GATT do not exist on this transport and any feature derived from them is structurally invalid; (2) of the four originally-proposed L4 features, only `rssi_variance_normalized` survives unmodified, with the rest either reframed against BR/EDR primitives (poll-interval Tpoll variance, AFH-aware retransmission slotting) or dropped entirely (advertisement_period_drift has no BR/EDR analog); (3) the threat-model anchor narrows from broad "spatial co-presence attestation" to the specific cloud-gaming-bot stealth pattern, with concrete documented attack instances (WormVision Lite MAX userscript published 2024-12-26, NVIDIA's GeForce NOW anti-cheat compatibility guide conceding the cloud-client attestation gap, Activision RICOCHET Season 02 input-pattern detection rolling out against this attack class on the input-stream side); (4) the novelty claim splits into a detection layer that inherits BlueShield's published FN floors as a baseline (5.84% CFO, 8.72% RSSI, 2.37% combined FP per Wu et al. RAID 2020) and a forensic/governance layer where the genuine novelty lives — cross-tournament portability of witness signatures, non-repudiable temporal ordering for adjudication disputes, on-chain TWC commitments verifiable months later by parties not in the original trust chain.

The witness device for v1 is LAN-tower-only (BlueZ + USB BT dongle + Python wrapper, ~one week of engineering on top of the architectural prerequisites). Mobile witness is structurally degraded — iOS Core Bluetooth throttles RSSI reads to ~1 Hz per peripheral and does not expose raw HCI events to user-space apps, while Android BLE APIs do not expose raw HCI without root and custom firmware — and is deferred to v2. The L8 v1 claim is session-bound presence attestation only; cross-session and cross-tournament controller identity claims require a same-model separability study for N≥3 identical DualSense Edges that does not exist in the public literature, and are explicitly out of scope until that study is completed.

**[SUPERSEDED-BT-CALIB-LESSON-001]** The v1.0 BT calibration architectural proposal naming BLE-derived L4 features (`connection_interval_jitter`, `advertisement_period_drift`, BLE-specific `retransmission_rate`) is superseded by the canonical anchor and the v1.1 architectural revision. The verification gap that produced the supersession is documented in `lessons.md` entry `BT-CALIB-LESSON-001`. Future BT design work cites the canonical anchor and the v1.1 revision; it does not re-derive features from the v1.0 proposal.

**Application protocol.** Architectural revisions live in `wiki/methodology/bt_calibration_v*.md` with monotonic version numbers. Each revision supersedes prior versions explicitly with a `[SUPERSEDED-{version}]` annotation. Prototype code, whitepaper revisions, and external review materials cite the highest-version revision document by path. Capture-session planning is gated on the v1.1 revision document being read and acknowledged in the current session.

## Sensor Stack v2: Canonical Prerequisite Anchor

**[CANONICAL]** Any DualSense Edge sensor-stack design work in VAPI must read `wiki/assessments/DualSense Edge Sensor-Stack Characterization for VAPI Track-1 Anti-Cheat Feature Architecture.pdf` before producing architectural proposals, feature definitions, threat-model claims, or capture-session plans for the v2 L4 feature set. This document is the canonical prerequisite anchor for the protocol's DualSense Edge sensor-surface expansion path. It establishes the per-surface verification blocks (vendor-spec + open-source driver + literature anchor, three independent classes per BT-CALIB-LESSON-001 application rule), the per-surface tier assignments (PRIMARY / CO-SIGNAL / ADVISORY / DROPPED), the empirical unknowns that must be resolved by pre-corpus measurement, and the same-controller-population separability constraint applied per-surface.

The six surface-tier assignments the Track 1 research established are: **Surface 1 (adaptive trigger force-curve) → PRIMARY DISCRIMINATOR**, vendor-spec verified through `hid-playstation.c` + PSDevWiki + SensePost + DualSense-Windows + pydualsense; 8-bit per trigger axis at ~1 kHz on Edge over USB; inter-subject EER 1-15% lab-to-field per Saevanee 2009 / Antal 2015 / Wakita 2006 / Miyajima 2007 / Van Vugt 2013; adversarial spoof cost very high because translator-class hardware (Cronus Zen / XIM / reWASD) does not synthesize biomechanically-structured continuous force curves; also functions as L4-coupled-to-L6 challenge-response channel via `Slope` / `Vibration` adaptive-trigger output reports. **Surface 2 (touchpad capacitive) → CO-SIGNAL**, 12-bit X/Y 2-point with no pressure proxy, binding constraint is data volume per session not per-event separability. **Surface 3 (microphone array) → DROPPED at default scope** on privacy-falsification grounds (Cruz v. Fireflies AI analog plus single-channel-post-DSP reality invalidates multi-mic literature transfer); narrow non-speech RIR feature subset is research-only and not on v2 L4 critical path. **Surface 4 (lightbar optical emission) → CO-SIGNAL** as challenge-response witness channel only (tournament-station camera observes host-issued 3-color symbol stream at 5-15 symbols/second producing 25-75 bits over 5-second authentication window; analog of LAN-tower BlueZ BT witness with fully-passive camera advantage). **Surface 5 (battery drain) → ADVISORY ONLY** (4-bit 11-bucket level at multi-minute change cadence; too coarse for per-round/per-match temporal structure; useful as session-integrity check over 30+ minute windows only). **Surface 6 → split**: stick analog noise floor → CO-SIGNAL (held/unheld binary plus tremor-band variance as supporting evidence at the 8-bit quantization floor); Hall-effect stick per-unit fingerprinting → ADVISORY ONLY, marked session-bound presence only, blocked from cross-session controller identity until same-model separability study runs per CROSS-LESSON-001.

**Stage A measurement gates** (must complete before v2 L4 architecture spec exits draft state): Empirical Unknown #1 (intra-player vs inter-player Mahalanobis on N=10 players × 100 trigger pulls × 3 game contexts; decision threshold > 1.0 separation ratio for primary discriminator status) and Empirical Unknown #4 (Hall-effect stick same-model same-batch separability on N=20 stock + N=20 batched-aftermarket Edge units; decision threshold > 20% rank-1 on same-model same-batch). **Stage B** (after Stage A measurements land): implement adaptive-trigger challenge-response as L4-coupled-to-L6 in v2.

**Critical fact-correction load-bearing for future work:** The DualSense Edge ships from Sony with ALPS Alpine **potentiometer-based** stick modules, NOT Hall-effect. The Edge's **trigger** sensors are Hall-effect from factory (gamepadtest.app), but the stick sensors are not. Aftermarket Hall-effect (GuliKit, ZeroStick Pro) and TMR modules (MODDEDZONE, Battle Beaver, XP Controllers) exist and are increasingly common in the competitive Edge scene. Any future architectural reference to "DualSense Edge Hall-effect sticks" without aftermarket qualifier is incorrect.

**[SUPERSEDED-TRACK1-LESSON-002]** The v2.0 sensor-stack ideation naming the microphone array as a multi-mic acoustic-fingerprinting surface is superseded by the canonical anchor and the v2.1 architectural revision; the DualSense exposes a single mono UAC1 stream post-DSP, not a multi-mic array, and the multi-mic literature does not transfer. The verification gap that produced the supersession is documented in `lessons.md` entry `TRACK1-LESSON-002`.

**[SUPERSEDED-TRACK1-LESSON-003]** The v2.0 sensor-stack ideation framing the microphone array as a "narrow non-speech feature subset can preserve privacy" path is superseded by the canonical anchor and the v2.1 architectural revision; concrete BIPA litigation (Beil v. Petco, Cruz v. Fireflies AI), GDPR Art. 9, and CIPA two-party-consent regimes attach to capture-and-storage regardless of downstream use, and tournament terms-of-service consent does not reach incidentally-captured household third parties. The verification gap is documented in `lessons.md` entry `TRACK1-LESSON-003`. The optical-witness path on Surface 4 is strictly preferable on every privacy axis if a passive-presence channel is later required.

**Cross-lesson dependency.** `lessons.md` entry `CROSS-LESSON-001` (same-controller-population separability constraint) applies cross-surface to both L8 BT and L4 v2 Hall-effect stick. The constraint is now load-bearing for all future controller-internal physical fingerprint claims and is referenced by both `bt_calibration_v1_1_architectural_revision.md` and `sensor_stack_v2_1_architectural_revision.md`.

**Application protocol.** Architectural revisions live in `wiki/methodology/sensor_stack_v*.md` with monotonic version numbers (parallel to BT calibration `wiki/methodology/bt_calibration_v*.md`). Each revision supersedes prior versions explicitly with a `[SUPERSEDED-{version}]` annotation. Prototype code, whitepaper revisions, and external review materials cite the highest-version revision document by path. The v2 L4 architecture spec is gated on Stage A pre-corpus measurements (Empirical Unknowns #1 and #4) being completed and the v2.1 revision document being read and acknowledged in the current session.

## Hard Rules

- Never modify the 228-byte PoAC wire format
- Never change chain link hash from SHA-256(164B body)
- Hardware tests gated @pytest.mark.hardware, excluded from CI
- E2E tests require running Hardhat node
- L6_CHALLENGES_ENABLED=false is the correct default
- GSR_ENABLED=false — never change without N≥30 GSR calibration sessions per player (current N=0)
- L6B_ENABLED=false — never change without N≥50 neuromuscular reflex calibration (current N=0)
- Per-player L4 thresholds can only tighten, never loosen (enforced by min())
- Stable EMA track updates on NOMINAL sessions only
- Whitepaper test counts: 1158 bridge, ~1,635 total, ~1,607 CI (stale; CLAUDE.md is authoritative: 2216 bridge, 426 SDK, 482 Hardhat)
- Operator endpoints (/operator/passport, /operator/passport/issue) require valid x-api-key header matching cfg.operator_api_key; return 503 if key unconfigured, 401 if wrong key
- L2C phantom weight must be acknowledged in any humanity formula discussion
- accel_magnitude_spectral_entropy is bot-vs-human only — never claim it improves separation ratio
- ioswarm_enabled=false — never change without live ioSwarm nodes registered
- BLOCK_QUORUM=0.67 — never lower below GENERAL_QUORUM (0.60); W1 mitigation
- Epistemic weight sum = 1.0: {0.35,0.35,0.15,0.15} (swarm on) or {0.40,0.40,0.20} (off)
- Phase 109+ migration order — VHPRenewalAgent first, SessionAdjudicator LAST
- scripts/vapi-swarm-agent.json — single source of truth for ioSwarm task spec; never hand-edit
- WEC FORMULA v1 FROZEN (Phase 236-WATCHDOG) — `WEC_N = SHA-256(prev(32)||code(1)||pid(4)||sid_hash(16)||ts_ns_be(8))`. Genesis tag `VAPI-WEC-GENESIS-v1`. Any change to byte order, event-code table, or hash algorithm requires WEC v2 and a new genesis tag. The WEC chain pairs with GIC v1 — together they prove operational + cognitive continuity for a grind run; never break this pairing
- INV-GIC-003 enforcement at watchdog level — `scripts/bridge_watchdog.py` MUST refuse to restart the bridge when `chain_intact=False` (HTTP) OR `_gic_chain_broken=True` (Store fallback). Restarting masks the break instead of fixing it. Recovery path is operator-only: `POST /operator/gic-reset` + bridge restart
- GRIND_SESSION_ID continuity — watchdog reads bridge/.env once at startup and pins; refuses restart if a different session_id appears mid-lifetime. Watchdog NEVER writes to bridge/.env
- Watchdog rate ceiling — 3 restarts/hour max; beyond that, WATCHDOG_HALT and operator intervention required. Cascading auto-restarts mask faults rather than reveal them
- VAME FORMULA v1 FROZEN (Phase 236-VAME) — `commitment = SHA-256(b"VAPI-VAME-v1" || chain_head_16b || ts_ns_be(8) || endpoint || body_bytes)`. Sidecar response headers ONLY (X-VAME-*), never body wrapper. Any change to byte order, domain tag, or chain-head length requires VAME v2 and a new tag. Hash function will move to Poseidon at Phase 237-ZK-SEPPROOF when circomlib is already a dep — at that point v2 ships. The novelty is the GIC-chain-head binding, not the hash function
- VAME validation never throws — frontend validateVame() returns NO_VAME / OK / MISMATCH; mismatches bump sessionStorage[__vapiVameFailures] but never block the response. Treat VAME as defense-in-depth, not a gate
- Frontend grind-critical hooks MUST set `noMock: true` — useCaptureHealth / useGrindChain / useAutoTriggerStatus / useFleetCoherenceStatus / useAITSeparation / useGrindAnalytics / usePCCIntelligence / useWatchdogStatus all set this. Without it, a transient 5xx flips the dashboard to fabricated mock data with no UI indication mid-grind. Mock fallback is reserved exclusively for first-load discovery before bridge is reachable
- CORPUS-SNAPSHOT FORMULA v1 FROZEN (Phase 236-CORPUS-SNAPSHOT) — `commitment = SHA-256(b"VAPI-CORPUS-SNAPSHOT-v1" || wiki_hash(32) || agent_root(32) || ratio_milli_be(8) || corpus_n_be(8) || ts_ns_be(8))`. Wiki hash walks `wiki/**/*.md` sorted by POSIX-lowercase relative path with explicit `b"--FILE:" + path + b"\n"` separator before each file's bytes. Ratio encoded as uint64 milliratio (ratio×1e6 rounded) for OS-deterministic byte encoding. Any change to byte order, separator format, sort key, or scaling factor requires v2 + new domain tag. The CORPUS-SNAPSHOT chain is the third pillar alongside GIC v1 (per-session cognitive integrity) and WEC v1 (per-restart operational integrity); together they constitute the full grind-run provenance. Never break this triple-pairing
- POST /operator/force-corpus-snapshot requires `reason ≥ 10 chars` (audit field), full `cfg.operator_api_key` authentication (not read-only), and rate-limit-budgeted. On-chain anchoring is intentionally NOT auto-fired — operator must explicitly invoke ProtocolCoherenceRegistry anchor reuse when budget permits. Local snapshot is always written (never gated on chain availability)
- CONSENT FORMULA v1 FROZEN (Phase 237-CONSENT) — `consent_hash = SHA-256(b"VAPI-CONSENT-v1" || device_id_b32 || category_bitmask_be(4) || expires_at_be(8) || ts_ns_be(8))`. Categories enum FROZEN at TOURNAMENT_GATE=0 / ANONYMIZED_RESEARCH=1 / MANUFACTURER_CERT=2 / MARKETPLACE=3 — values are part of the bitmask domain and MUST match VAPIConsentRegistry.sol position-for-position. Any reorder/insert is a v2 break. The consent primitive is the fifth pillar in PATTERN-016 alongside GIC + WEC + VAME + CORPUS-SNAPSHOT
- BRIDGE NEVER GRANTS OR REVOKES CONSENT ON BEHALF OF GAMER — gamer-self-sovereignty invariant. POST /operator/record-category-consent and /operator/revoke-category-consent write ONLY to the local `consent_ledger` (operational truth until on-chain deploy). On-chain `VAPIConsentRegistry.grantConsent / revokeConsent` MUST be called by the gamer's own wallet (msg.sender). The bridge's `chain.is_consent_valid` / `chain.get_consent_record` are READ-ONLY view calls; a malicious or compromised bridge cannot modify consent state, only observe it
- Phase 237 chain.is_consent_valid / get_consent_record FAIL-OPEN (return False / empty dict) when `consent_registry_address == ""`. This is INTENTIONAL — bridge readiness must not depend on contract deploy. The deliberate divergence from `bbg_check_proposal` / `is_dual_eligible` (which raise RuntimeError) reflects: bridge is reader of consent state, not writer; missing on-chain registry must not block local consent_ledger operation

## ioSwarm Integration (Phase 109A+)
- Phase 109A: infrastructure COMPLETE — task spec + consensus aggregator + W3bstream bindings
- ioswarm_enabled=true (Phase 200: set in bridge/.env; emulator mode, no live nodes)
- Phase 109B: VHPRenewalAgent first task spec migration
- Phase 110: VHP as ioSwarm physical action authorization gate (IoTeX DePIN)
- scripts/vapi-swarm-agent.json: ioSwarm task spec (infrastructure only, not yet registered)
- VHP auth gate: require(VAPIProtocolLens.isFullyEligible(operatorDeviceId))

## Build & Test Commands

```bash
python -m pytest bridge/tests/ --ignore=bridge/tests/test_e2e_simulation.py -q  # 1480 passed
python -m pytest sdk/tests/ -v                                                   # 125
cd contracts && npx hardhat test                                                  # 440
pytest tests/hardware/ -v -m hardware -s                                         # 36 (needs controller)
# Hardware calibration watcher (run while playing NCAA CFB 26):
python scripts/hardware_calibration_watcher.py                                   # writes calibration_sessions/hardware_calibration_progress.json
# ZK ceremony (unblocks 5 skips):
cd /c/Users/Contr/vapi-pebble-prototype/contracts && PATH="$(pwd):$PATH" npx hardhat run scripts/run-ceremony.js
# E2E (needs Hardhat node):
HARDHAT_RPC_URL=http://127.0.0.1:8545 python -m pytest bridge/tests/test_e2e_simulation.py -v
# L6 capture workflow:
python scripts/l6_hardware_check.py
python scripts/l6_capture_session.py --player P1 --game "NCAA Football 26" --target 50
python scripts/l6_threshold_calibrator.py --from-db
```

## Phase 71 — Candidate Phases (PHASE_ADVANCE Output, 2026-03-19)

After Phase 70 completes, next phases in priority order. User decision required before starting any.

| Rank | Phase | Name | Priority | Blocks |
|------|-------|------|----------|--------|
| P1 (Recommended) | 71 | Deploy Phases 69+70 + Security Audit | P1 | All on-chain functionality |
| P2 | 72 | PHGCredential SafeMultiSig Governance | P1 | Tournament Condition 3 |
| P3 | 73 | CI/CD GitHub Actions + SessionAdjudicator Live Validation | P1 | Production readiness gate |

**Phase 71 detail (deploy + audit):**
- npx hardhat run scripts/deploy-phase69.js --network iotex_testnet (~0.35 IOTX, 6 contracts)
- npx hardhat run scripts/deploy-phase70.js --network iotex_testnet (~0.13 IOTX, 2 contracts)
- Produce docs/security-audit-phase-70.md (VAPIGovernanceTimelock + VAPIProtocolLens + agent wiring + tools #41-45)
- Fix any CRITICAL/HIGH findings inline
- Update deployed-addresses.json + bridge/.env.testnet with 8 new addresses
- ~0 new tests (deploy + audit only, no new code)

**Phase 72 detail (PHGCredential SafeMultiSig):**
- SafeMultiSig.sol: 2-of-3 confirmations for suspend/reinstate on PHGCredential
- chain.py: propose_suspension() → confirm_suspension() → execute_suspension() flow
- Config: safe_signers[3], safe_threshold=2
- +6 Hardhat + 4 bridge tests
- Closes Tournament Condition 3 (governance hardened)

**Phase 73 detail (CI/CD + Live Adjudication):**
- GitHub Actions: bridge-tests.yml + hardhat-tests.yml + sdk-tests.yml + yaml-lint.yml
- Matrix: Python 3.11/3.12/3.13, Node 18/20
- POST /agent/config AGENT_DRY_RUN=false: document 100 validated sessions threshold
- +0 new tests (CI config only)

## Key Gotchas (Windows / HID)

- `hidapi` library: `pip install hidapi` (NOT `hid`)
- HID Cross button: bit5 of `buttons_0` raw HID byte; `cross = (buttons_0 >> 5) & 1`
- L2C sign bug: use `abs(max_causal_corr) < threshold` — anti-correlation is physical coupling
- Windows SQLite tests: use `tempfile.mkdtemp()` NOT `TemporaryDirectory` (WAL PermissionError)
- Windows print encoding: ASCII (PASS: / ->) NOT Unicode (✓ / →) in test print() calls
- Web3/eth_account stub: mock `web3`, `web3.exceptions`, `eth_account` before import
- EWCWorldModel INPUT_DIM=30 (tests need 30-dim input, not 10)
- ZK circuits: `pragma circom 2.0.0;` — requires circom2 Rust binary; circom.exe v2.2.3 in `contracts/`
- IoTeX: chain ID 4689 mainnet, 4690 testnet; P256 precompile at 0x0100
- hardhat.config.js: viaIR=true (stack-too-deep fix for PoACVerifier)
- conftest.py: autouse event loop fixture prevents Python 3.13 asyncio teardown crash
- Batch analysis: always use max_frames=0 — default 30k limit misses presses in 180s sessions
- Phase 199 prototype mode: set ALL_PAIRS_GATE_ENABLED=false in bridge/.env to bypass per-pair P0 gate; ratio=0.728 already passes separation_ok (>= min_separation_ratio=0.70 Phase 166 default)
- Phase 200: IOSWARM_ENABLED=true in bridge/.env; emulator mode (5-node seed=109/110); VAPISwarmOperatorGate.sol LIVE 0x969c0F1E. Test T199-8 isolated via os.environ.pop to prevent bridge/.env contamination.
- tremor_resting probe: 30s still-hold session; valid STRUCTURED_PROBE_TYPE (Phase 199); primary discriminator tremor_peak_hz (P1 ~9.37Hz, P2 ~1.71Hz, P3 ~2.85Hz)
- SDK naming: Phase 199 ProbeGateConfigResult/VAPIProbeGateConfig + TremorRestingProbeResult/VAPITremorRestingProbe — distinct from all prior Phase 182/183 names
- Batch analysis: always use max_frames=0 — default 30k limit misses presses in 180s sessions
- Phase 234: InsightSynthesizer Mode 6 no longer blocks event loop — _synthesize_living_calibration() is now a thin async wrapper calling asyncio.to_thread(_synthesize_living_calibration_sync); HTTP API responds in <1s during startup
- grind_semantics (Phase 234.5+235-B+235-GAD): consecutive_clean = leading non-divergent + PCC-attested + gameplay-confirmed streak from most recent ruling_validation_log (DESC LIMIT 100); divergence = verdicts_differ AND |llm_conf - fallback_conf| > 0.30; SAME VERDICT never diverges; pcc_state=NOMINAL AND pcc_host_state in (EXCLUSIVE_USB, UNKNOWN) REQUIRED — NULL or DEGRADED or CONTESTED breaks streak immediately (fail-closed); gameplay_context=MENU_DETECTED breaks streak (confirmed menu session); NULL gameplay_context passes through (benefit of doubt for pre-GAD rows or rows where discrimination disabled); ACTIVE_GAMEPLAY counts normally; advisory codes → fallback=FLAG(0.5) → LLM=FLAG → NO DIVERGENCE; clean session → fallback=FLAG(0.05) → LLM=FLAG → NO DIVERGENCE; after enrollment(N>=10 NOMINAL) → fallback=CERTIFY(0.8) → LLM must also return CERTIFY to avoid break; grind target: 100 consecutive_clean at gate_n=100; GIC stamped only on count-eligible sessions (NOMINAL+EXCLUSIVE_USB/UNKNOWN+non-divergent+gameplay_ok+grind_mode=True)
- gameplay_context_classification (Phase 235-GAD): binary gate — ACTIVE_GAMEPLAY = trigger_active_fraction > 0.0 (any L2/R2 press in 20-record evidence window); MENU_DETECTED = trigger_active_fraction == 0.0 (zero trigger presses confirmed); NULL = trigger_active_fraction missing from evidence (pre-GAD row). trigger_onset_velocity > 0.0 is the mathematical zero point, not a tuned threshold — returns exactly 0.0 iff no onset events occurred. Normal competitive play requires ≥1 R2 snap per adjudication window → ACTIVE_GAMEPLAY. Pure menu navigation → MENU_DETECTED. Operator override via POST /operator/override-gameplay-context (api_key + reason≥10 chars); logs to gameplay_classification_disagreements for audit. MIXED category eliminated entirely — binary is sufficient and unambiguous for NCAA CFB 26.
- capture_integrity (Phase 234.7 PCC / 235-PCC-CLARIFY): CaptureHealthMonitor observes HID poll rate via update_sample(n_frames, window_s) once per _session_loop interval; signal_disconnect(reason) forces immediate DISCONNECTED by bypassing rate averaging; host state inferred PURELY from CV of rolling 60-sample window — NO BT pairing state is read (CV<0.20+rate≥900→EXCLUSIVE_USB; CV≥0.40→CONTESTED; 200-350Hz→EXCLUSIVE_BT); grind_ready requires NOMINAL+EXCLUSIVE_USB/UNKNOWN+30s sustained; session_counting_paused=True when grind_mode=True AND NOT grind_ready (soft block, not hard gate); DUAL-CONNECTION (USB to laptop + BT to PS5 for gameplay) is the ONLY valid grind setup and reports EXCLUSIVE_USB when USB polling is stable — BT pairing to PS5 is INVISIBLE to CaptureHealthMonitor; PS Remote Play (streaming video/audio of PS5 to the laptop via PSRemotePlay app) during grind: NOT recommended — Remote Play sends USB audio/HID traffic that may cause poll rate variance (→ CONTESTED); normal BT-to-PS5 gameplay connection does NOT cause CONTESTED
- grind_procedure (Phase 235-GPC / 235-PCC-CLARIFY): PRE-GRIND CHECKLIST (12 steps) — PHYSICAL SETUP: NCAA CFB 26 is PS5-exclusive; the only valid grind configuration is DUAL-CONNECTION (USB-C to laptop AND BT-paired to PS5). CaptureHealthMonitor infers host_state from USB poll-rate statistics ONLY — it is blind to BT pairing state. Dual-connection where USB polling is stable at ~1000 Hz reports EXCLUSIVE_USB regardless of PS5 BT connection. (1) Keep DualSense Edge BT-paired to PS5 — DO NOT unpair. BT pairing is REQUIRED to play NCAA CFB 26. PCC does not care about BT pairing and cannot detect it. (2) USB-C DATA cable from DualSense Edge to laptop — this is what CaptureHealthMonitor reads. Verify the cable carries data (not power-only) by confirming poll_rate_hz ≈ 1000 in GET /bridge/capture-health. (3) Set GRIND_SESSION_ID=grind_phase235_v1 in bridge/.env (CRITICAL: same ID across ALL bridge restarts; different ID = new genesis = chain restart); (4) Set GRIND_MODE=true in bridge/.env; (5) Set GRIND_TARGET=100 in bridge/.env; (6) Confirm ANTHROPIC_API_KEY is set (LLM failure is graceful but WARNING-level — see external_dependencies); (7) Start bridge: python bridge/main.py — confirm startup log shows "GRIND SESSION ID : grind_phase235_v1" and "GRIND MODE : ACTIVE"; (8) Power on PS5 and launch NCAA CFB 26. BT connection auto-establishes. Wait for GET /bridge/capture-health → capture_state=NOMINAL + host_state=EXCLUSIVE_USB + grind_ready=False (pre-warmup); (9) Wait 30s sustained NOMINAL+EXCLUSIVE_USB → grind_ready=True + session_counting_paused=False; (10) Verify GET /bridge/grind-chain-status → chain_intact=True (new chain: chain_length=0, no entries yet); (11) Play NCAA CFB 26. DO NOT press PS button to navigate to PS5 home screen during an active grind session — this may cause transient USB rate instability (CONTESTED). Watch consecutive_clean_toward_target count up toward grind_target=100 via GET /bridge/capture-health; (12) At consecutive_clean=100: run POST /agent/run-tournament-preflight then POST /agent/activate-graduation-stage {agent_id: ruling_enforcement_agent}. WARMUP INDICATORS: session_counting_paused=True means grind is paused (waiting for EXCLUSIVE_USB+NOMINAL+30s); grind_ready=True is the green light; first GIC entry appears after the first count-eligible session (chain_length=1). CONTESTED RECOVERY: if host_state=CONTESTED fires mid-session (poll rate became unstable), the current session will not count; finish current play safely, wait for EXCLUSIVE_USB to return; 30s warmup window restarts. Sessions already counted are NOT retroactively removed. CHAIN-BREAK RECOVERY: if startup check detects chain_intact=False, bridge sets _gic_chain_broken=True blocking new GIC stamps; investigate with GET /bridge/grind-chain-status; if DB is clean (external tamper ruled out), call POST /operator/gic-reset with mandatory reason field (≥10 chars), then restart bridge. See docs/phase_235_pcc_dual_connection_clarification.md for full PCC dual-connection audit.
- pcc_invariants (Phase 234.8): INV-PCC-001: CaptureHealthMonitor.update_sample() is the ONLY authorized path for updating poll_rate_hz; signal_disconnect() is the ONLY authorized path for forcing DISCONNECTED state; _recompute() MUST check _disconnect_reason before rate averaging — overriding this allows silent capture degradation to be masked as NOMINAL during grind sessions
- prior_art_anchor (Phase 234.8): VAPI is the first input-attestation protocol to treat controller host arbitration as a first-class, cryptographically-anchored session integrity signal. Host arbitration state (EXCLUSIVE_USB/CONTESTED) is derived from HID poll-rate coefficient of variation and surfaced via capture_health_log + GET /bridge/capture-health, enabling grind-mode fail-closed enforcement when controller ownership is contested between gaming PC and PS5.
- gic_invariants (Phase 235-A/GPC): INV-GIC-001: GIC formula v1 is FROZEN — byte order prev_gic(32)||commitment_hash(32)||verdict_code(1)||host_state_code(1)||ts_ns_be(8) = 74B → SHA-256 → 32B. VERDICT_CODES = {CLEAR:0x00,CERTIFY:0x01,FLAG:0x10,HOLD:0x11,BLOCK:0x20}. PCC_HOST_CODES = {EXCLUSIVE_USB:0x01,UNKNOWN:0x02,EXCLUSIVE_BT:0x10,CONTESTED:0x20,DEGRADED:0x30,DISCONNECTED:0xFF}. Genesis tag = b"VAPI-GIC-GENESIS-v1". Only fallback_verdict (deterministic _rule_fallback() output) is hashed — llm_verdict (LLM API, non-deterministic) is NEVER an input. Any change to byte order, code tables, or hash algorithm requires GIC v2 and new genesis tag. grind_chain.py is a standalone pure-Python module with no bridge imports — suitable for independent verification. GIC_100 = final hash after 100 consecutive_clean sessions = Phase 236 Zenodo deposit headline artifact. INV-GIC-002: gic_ts_ns is strictly monotonically increasing — implemented via time.time_ns() (OS-native integer nanoseconds; no float precision loss) + explicit guard in session_adjudicator_validator: _prev_ts = store.get_prev_gic_ts_ns(); if _ts_ns <= _prev_ts: _ts_ns = _prev_ts + 1. This protects against backward NTP corrections creating duplicate or regressing timestamps in the audit chain. Do NOT use int(time.time() * 1e9) (float precision loses ~300 ns precision) or time.monotonic_ns() (resets on process restart — breaks cross-session monotonicity during multi-day grind). INV-GIC-003: chain-break detection runs at startup (main.py): if get_grind_chain_status()["chain_length"] > 0 AND NOT chain_intact → app._gic_chain_broken = True → consecutive_clean gate refuses to advance until cleared. Recovery: (a) use POST /operator/gic-reset (requires valid operator api_key query param + reason≥10 chars; logs to agent_events + clears flag; bridge restart still required for clean startup check); (b) operator then restarts bridge to verify chain_intact=True before grind resumes.
- external_dependencies (Phase 235-GPC): SessionAdjudicator uses claude-opus-4-6 via Anthropic API (ANTHROPIC_API_KEY env). LLM API FAILURE MODES — all 6 modes fall back deterministically via _rule_fallback() and GIC chain integrity is NEVER affected: (a) missing ANTHROPIC_API_KEY → AuthenticationError caught → fallback; (b) key valid but credits depleted → RateLimitError or PermissionDeniedError caught → fallback; (c) network unreachable → ConnectionError caught → fallback; (d) model unavailable (maintenance) → APIStatusError caught → fallback; (e) response times out → asyncio.TimeoutError caught → fallback; (f) malformed response → any Exception caught → fallback. In all cases: _llm_ruling() catches `except Exception` at its outermost scope and returns _rule_fallback(evidence) as a 3-tuple (verdict, confidence, reasoning). The stored llm_verdict becomes identical to fallback_verdict → divergence=0 → consecutive_clean advances normally. GIC stamps fallback_verdict (not llm_verdict), so Anthropic API availability has ZERO effect on chain integrity. ANTHROPIC_API_KEY missing: logs WARNING once per adjudication attempt. Recommended: set ANTHROPIC_API_KEY in bridge/.env for full LLM operation, but grind can proceed without it — fallback verdicts are equally valid for GIC purposes.
- event_loop_invariants (Phase 235-EVENTLOOP): Every `async def` function that does NOT contain an internal `await` expression MUST include an explicit `await asyncio.sleep(0)` yield point before or after its synchronous body — otherwise it runs as a blocking coroutine and stalls the event loop for the full duration of its CPU work. Long-running synchronous work (SQLite scans >5ms, numpy computation, file I/O) MUST be moved to `asyncio.to_thread()` or `loop.run_in_executor(None, fn)` — NOT just wrapped in `async def`. Background tasks that run at startup MUST include an initial `await asyncio.sleep(N)` delay (N ≥ 30s) before their first heavy operation, so Uvicorn can complete its binding and accept connections before the task's sync blocks begin. Sequential sync task loops (like curator's TASK_SEQUENCE) MUST add `await asyncio.sleep(0)` after each task iteration — not just at the outer `while True` boundary. Index coverage on high-cardinality tables is mandatory before production: the `records` table (192k+ rows) must have indexes on every query pattern used by startup tasks; missing index + cold-start WAL = 400-1500ms blocking query on the event loop thread = HTTP timeout. Violation consequence: any 10-second HTTP timeout on a zero-DB endpoint (`/health`) indicates a SQLite write-lock contention where the event loop thread is blocked waiting for a background thread to release the WAL write lock; `timeout=10` is the signature.
- dashboard_access (Phase 235-FINAL): Start bridge first: `python -m bridge.vapi_bridge.main` (from project root `C:\Users\Contr\vapi-pebble-prototype`). Start dashboard: `cd frontend && npm run dev`. Dashboard URL: http://localhost:5173 (Vite default). Navigate to Gamer view (leftmost tab) to see the GRIND INTEGRITY CHAIN panel at the top of the stats panel — shows progress bar (consecutive_clean / grind_target), PCC state, HOST state (green=EXCLUSIVE_USB, red=CONTESTED, amber=DEGRADED), READY flag, CHAIN link count, GAMEPLAY context (green=ACTIVE_GAMEPLAY, red=MENU_DETECTED), and session ID. Polling intervals: capture-health 3s (useCaptureHealth hook), grind-chain-status 5s (useGrindChain hook). When bridge is offline, dashboard auto-falls back to mock data (sessionStorage key __vapiMockActive=true) — mock shows grind_phase235_v1 session with drifting values. Prerequisites: Node.js + `npm install` (already done); no additional env vars needed for frontend; OPERATOR_API_KEY must be set in bridge/.env for authenticated bridge endpoints (x-api-key header). Full procedure: docs/phase_235_grind_start_procedure.md.
